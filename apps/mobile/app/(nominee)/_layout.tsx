import { Stack } from 'expo-router'

// Nominee route GROUP (Story 9.1) — a plain Stack owning the Nominee Console screen (`index`). Mirrors the
// (contribution) / (pool-onboarding) group conventions. Calm/neutral register — this is Sunita's
// grief-paced reconciliation surface; the group header is hidden, the screen owns its own dignified frame.
export default function NomineeLayout() {
  return <Stack />
}
