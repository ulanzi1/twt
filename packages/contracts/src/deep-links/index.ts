// Deep-link grammar barrel — Story 5.2 (AC4). Re-exported from the `@twt/contracts` top barrel; no
// `@twt/contracts/deep-links` subpath is wired (mirrors kyc/audit/rbac — see deep-link.ts header).

export {
  DEEP_LINK_SCHEME,
  DeepLinkResource,
  DeepLinkTarget,
  formatDeepLink,
  deepLinkTargetForAlert,
  parseDeepLink,
} from './deep-link.js';
