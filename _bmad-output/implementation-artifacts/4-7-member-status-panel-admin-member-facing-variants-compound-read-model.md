---
baseline_commit: 1d62ed405e5c57bb2d698ce38169425ff605ba39
---

# Story 4.7: `<MemberStatusPanel>` — Admin + Member-Facing Variants + Compound Read Model (~5s Admin Load) `[SURFACE]`

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As an admin doing member-search OR as a member viewing my own status,
I want a `<MemberStatusPanel>` that renders the Story 4.6 Validity Service payload with rule-by-rule provenance and human-readable signals,
so that admins see ~5s load on member-search at 4L scale and members see their own status with the same canonical data.

## Acceptance Criteria

> Verbatim from `epics.md` §Story 4.7 (lines 2009–2028). This is the SECOND `[SURFACE]` of Epic 4 — it CONSUMES the Story 4.6 canonical `MemberValidityPayload` (which was built AC3-explicitly "sufficient to render `<MemberStatusPanel>` without additional queries") and adds the two rendering variants + the AR-65 compound read model that precomputes the admin member-search load. It is NOT a new rule and NOT a new fact producer — it is the read/render surface over 4.6's output.

**AC1 — admin-facing variant: member-search returns the Validity payload + the compound read model's denormalized projection, ~5s p95 @4L, rendered in labeled sections**

**Given** UX-DR24 + AR-65 (compound read models) + Story 4.6 Validity Service
**When** the admin-facing variant is implemented
**Then** admin member-search (by name, mobile, Aadhaar masked, Pariwar ID, etc., scope-respecting) returns the Validity Service payload + a compound read model's denormalized projection (member identity + nominee summary + recent contribution events + recent claim events + state machine status) within p95 ~5s at 4L scale
**And** the panel renders signals in sections: (a) headline status, (b) Vyawastha Shulk status, (c) lock-in (with deep-link to policy `clause_id`), (d) contribution discipline (R7/R8 sub-clause state with provenance), (e) medical disclosure history + concealment flags, (f) retirement coverage if applicable, (g) special flags (`concealment_review_required` highlighted prominently for Epic 6 verifier console)

**AC2 — member-facing variant: same canonical panel, but identity-suppressed, simplified provenance, Hindi-first parity**

**Given** the member-facing variant
**When** rendered on the member's own profile
**Then** the same panel renders but: (a) member identity / Aadhaar / KYC details are not re-displayed; (b) provenance traces are simplified to "what rule applies to you" rather than admin-level audit-trace; (c) Hindi-first parity contract enforced per architectural-freeze row 10

**AC3 — the inherited accessibility gate**

**Given** the inherited accessibility gate (Story 0.10 P0-2c)
**When** the panel renders for assistive-tech users
**Then** the panel is semantically labeled, navigable in logical order, with appropriate ARIA roles and landmarks

## Tasks / Subtasks

- [x] **Task 0 — Decisions CONFIRMED by BigDev 2026-07-04 (settled, taken as written — do NOT re-open).**

  Like Story 4.6 (which flagged D1–D5 at creation, all later confirmed), this `[SURFACE]` is where several load-bearing choices come due: how the AR-65 compound read model is materialized, the contribution/claim producer gap (inherited from 4.6 D2), member-search over encrypted PII, where the panel component lives across two incompatible render stacks, the apps/api contract boundary that 4.6 explicitly left for THIS story, and the member-variant surface/i18n/a11y wiring. **BigDev CONFIRMED every recommended option: D1→A, D2→A, D3→A, D4→A, D5→A, D6→A** — with five refinements now folded in: (i) D3 blind-index search is **exact-match only** (prefix/fuzzy out of scope); (ii) the D1 projection is **write-owned exclusively by the Story 3.1 projector**; (iii) freshness is defined operationally (eventual consistency within the normal projector latency budget); (iv) the D4 presenter is **strictly pure** (no API/DB/permission/i18n side effects); (v) the name/Aadhaar search variance is **recorded against the AC**, and "Aadhaar masked" is interpreted as a **display field, not a v1 search key** (see D3). The recommendations below are the settled spec; the rejected alternatives are retained only so the dev agent understands *why*.

  - [x] **D1 (compound read model — materialization strategy + placement).** AR-65 (architecture §1.18, item 18 at `architecture.md:326-330`) commits a *denormalized read store, no N+1, ~5s@4L*. **This is NOT the Validity cache** — the FR-12A validity payload's cache is the Postgres materialized view built in Story 4.8 (`architecture.md:1044-1048`). This story's compound read model is the admin member-**search result** projection: `member identity + nominee summary + recent contribution events + recent claim events + state machine status`.
    - **D1-A (CONFIRMED): a denormalized read-model TABLE (e.g. `member_search_projection`) maintained by the Story 3.1 member projector, queried in ONE join-free lookup per result.** Matches AR-65 "denormalized read store" + the N+1-prevention rule (`architecture.md:3919-3926` — "repository methods join everything the caller needs in one query; loops-with-sub-queries are anti-pattern").
      - **Projection ownership (BigDev refinement ii — LOAD-BEARING):** the projection is **write-owned EXCLUSIVELY by the Story 3.1 projector.** No HTTP handler, admin action, background job, or SQL trigger may write it directly — the ONLY writer is the projector consuming member-state events (mirrors the `members.state` projector-only + trigger-guarded discipline, `[[project_member_lifecycle_domain_substrate]]`). Enforce with the same posture the substrate already uses (projector-only write path + a CI/trigger guard against out-of-band writes).
      - **Freshness (BigDev refinement iii — operational definition):** **eventual consistency within the normal projector latency budget** — the projection lags member-state appends only by the projector's ordinary processing latency (targeted real-time-within-seconds per AR-65). Reviewers assess freshness against the projector's own latency budget, NOT an arbitrary fixed second-count; a stale projection is a projector-lag observation, not a correctness bug, and eligibility itself is always the canonical 4.6 payload (never the projection).
      - Incremental per-member refresh on member-state event append. The projection SHAPE holds all five sections now; the contribution/claim sections carry the D2 sentinel until their producers exist.
    - **D1-B (Postgres materialized view).** REJECTED/deferred — MV *full-refresh* is Story 4.8's mechanism for the validity cache and does not fit incremental per-member update; a second admin-search MV would duplicate/couple the two caches. Revisit only if D1-A's incremental projector cannot hit ~5s@4L.
    - **D1-C (live multi-join at admin-search time).** REJECTED — a per-result fan-out across identity/nominee/state/contribution/claim is exactly the N+1 anti-pattern §3919 flags and the reason AR-65 exists. The ~5s@4L budget presumes a precomputed store, not query-time assembly.
  - [x] **D2 (the contribution + claim sections of the read model — `producer_unavailable`, NOT empty arrays; inherited straight from 4.6 D2).** The AC names "recent contribution events + recent claim events." **Claims are Epic 6; contributions are Epic 8/9 — NEITHER producer exists** (`[[project_engine_never_infers_contribution_facts]]`; 4.6 shipped `contribution_history_summary: { status: 'producer_unavailable' }` for exactly this reason — `packages/validity-service/src/types.ts:62-66`).
    - **D2-A (CONFIRMED): the projection carries member identity + nominee summary + state-machine status NOW (all producible), and represents the contribution/claim sections as a distinct typed `producer_unavailable` sentinel — never an empty `[]`.** An empty array reads as "assessed, no events found," indistinguishable from an un-assessed member — the exact confusion the never-placeholder discipline (`[[CR-4.4-D3]]`, `[[CR-4.5-D1]]`) forbids. The panel renders those sections as an explicit "not yet available" affordance, not a blank list. Record a CR re-triggering at Epic 6 (claims) + Epic 8/9 (contributions).
    - **D2-B (fabricate empty contribution/claim arrays now).** REJECTED — placeholder that lies about assessment state.
  - [x] **D3 (admin member-search over ENCRYPTED PII — CONFIRMED, with the exact-match + AC-variance refinements).** The AC lists search "by name, mobile, Aadhaar masked, Pariwar ID, etc." Reality of the substrate: **only `mobile` has a deterministic blind index today** (`member_identities.mobile_blind_index`, HMAC-SHA-256, exact-match — `member_identities.ts:47-60`). Name + Aadhaar are Tier-1 encrypted with NO blind index; blind index is exact-match only (no fuzzy/prefix). `pariwar_id` + member id are plaintext scope keys.
    - **D3-A (CONFIRMED): v1 search = `mobile` (blind index, exists) + `member_id` + `Pariwar ID` (plaintext scope keys) — the existing searchable keys only.**
      - **No "member number" key (validation correction 2026-07-04, strike RE-CONFIRMED by BigDev):** the original confirmation transcript listed "member number" as a fourth v1 key, but **no member-number column or concept exists anywhere in the substrate** (`members` carries only `member_id` uuid + `pariwar_id` as plaintext keys; grep across domain/contracts/api finds nothing). Do NOT invent a membership-number column or search key. A human-readable membership number **IS a confirmed real product requirement** — but it lands with the future **membership-number identity feature** (schema + generation + backfill + indexing), and membership-number SEARCH lands alongside it, not here (Task 8 CR).
      - **Exact-match ONLY (BigDev refinement i — LOAD-BEARING):** all blind-index searches are **exact-match only. Prefix / fuzzy / partial search is explicitly OUT OF SCOPE** (blind index is a full-value HMAC — it cannot do prefix/fuzzy by construction). State this in the search API's documented capabilities so it does not creep later.
      - **Name + Aadhaar-as-search-key = DEFERRED as separate migration stories (CONFIRMED).** They require deterministic blind indexes + schema migration + backfill — out of scope here. **Record the variance AGAINST THE AC** (the AC enumerates name/Aadhaar as search dimensions; this story ships without them by design) — do NOT silently omit. Recorded as a CR (Task 8) naming the follow-up: "name/Aadhaar member-search blind index + migration + backfill."
      - **"Aadhaar masked" interpreted as a DISPLAY field, not a v1 search key (BigDev refinement v).** The AC wording "(by name, mobile, Aadhaar masked, Pariwar ID, etc.)" is ambiguous; the confirmed reading is that the **masked Aadhaar last-4 is a displayed identity field in the panel's identity section, not a search input** in v1 (Aadhaar-as-search-key is part of the deferred blind-index follow-up above). If a later product decision wants Aadhaar-as-search-key, it rides the same deferred migration story. Record this interpretation in the Dev Agent Record so a reviewer does not read the omission as a bug.
      - All search scoped by `pariwar_id` RLS + gated on the Story 4.6 `member.view_validity` permission.
    - **D3-B (a plaintext/searchable index over name/Aadhaar).** REJECTED — defeats the Tier-1 encryption posture (`architecture.md` §2.7 blind-index rationale).
  - [x] **D4 (`<MemberStatusPanel>` component placement across two incompatible render stacks — CONFIRMED).** admin = `apps/admin` (Vite + React + Tailwind + Radix, web DOM — `architecture.md:2476`); mobile = `apps/mobile` (Expo RN + Tamagui). They CANNOT share a rendered component. `packages/ui/src` is currently an empty barrel. The **signal derivation** — headline state (Active · pending-onboarding · suspended-with-reason · expired-renewable · expired-not-renewable per UX §`<MemberStatusPanel>` states, `ux-design-specification.md:1894`), which rule explanation, the validity window, appeal-CTA visibility — MUST be identical across variants (same canonical `MemberValidityPayload` → same answer), or the two panels drift on the single most-disputed surface.
    - **D4-A (CONFIRMED): a shared FRAMEWORK-AGNOSTIC presenter / view-model builder (pure TS, NO react / react-native import) that maps `MemberValidityPayload` (+ the compound projection) → a rendered-agnostic view-model `{ headlineState, sections[], ruleExplanations[], validityWindow, showAppealCta, redactionApplied }`.** Place it where both apps can import it with no RN-vs-web coupling — recommend `packages/ui/src/member-status/` as pure logic (the shared-UI home) OR a small `packages/member-status-view` if `@twt/ui` should stay presentational. Then `apps/admin` renders it with Radix/Tailwind + the admin-only provenance audit-trace; `apps/mobile` renders it with Tamagui + the simplified "what applies to you" copy + Hindi-first strings.
      - **Strict purity (BigDev refinement iv — LOAD-BEARING, because this presenter becomes shared infrastructure).** The presenter is `(payload, projection) → view-model` and NOTHING else. It MUST NOT: make API calls, read the DB, perform permission checks, or do i18n lookups with side effects. Redaction is ALREADY applied upstream by `@twt/validity-service` (D5) — the presenter only reads `redactionApplied`/absent fields, never re-checks scope. i18n is resolved by the RENDER layer (each app passes resolved strings or the presenter emits i18n KEYS the render layer looks up) — the presenter emits keys, it does not call the i18n runtime. It is a pure, synchronous, dependency-free function: same `(payload, projection)` in → same view-model out. Assert this with pure unit tests (no mocks needed because there is nothing to mock).
      - Human-readable rule explanations are emitted as `@twt/i18n` KEYS (full prose, not error codes — UX a11y note `ux-design-specification.md:1896`); the render layer resolves them, NOT the presenter.
    - **D4-B (duplicate the derivation in each app).** REJECTED — guarantees drift on eligibility, the surface UX flagged as "the most-disputed."
  - [x] **D5 (the apps/api contract boundary + wire snake_case mapping — 4.6 EXPLICITLY deferred this to THIS story — CONFIRMED).** Story 4.6 stopped at the framework-agnostic `@twt/validity-service`; its Project-Structure note lists "the `apps/api` HTTP handler + wire snake_case mapping (the contract boundary)" as out-of-scope for 4.6 (`4-6-…md:194,248`). THIS story wires it.
    - **D5-A (CONFIRMED): a new thin `apps/api` module (e.g. `member-validity/`) following the `member-home/` SURFACE pattern (`apps/api/src/modules/member-home/`) — thin handlers, `openScopeTx`/`closeScopeTx`, `deps.clock()`. Redaction + audit stay in `@twt/validity-service`; the module only maps camelCase→snake_case and constructs the `ValidityCaller`.** Endpoints: (1) member-self validity read (member JWT context → the redacted self payload, NOT audit-logged — 4.6's `redactForCaller` + self-call gate); (2) admin validity read + member-search returning the compound projection (admin session/grants → `assertCanReadValidity` with `member.view_validity`, audit-logged — 4.6's admin audit seam). Define the wire DTOs in `packages/contracts/src/members/` (`.strict()` Zod, snake_case wire names mapped from the service's camelCase, **no `@twt/domain` import** — the contracts browser-bundle rule, `packages/contracts/src/members/lock-in.ts:8-16`). Admin app consumes via its `api/client.ts` + `api/hooks.ts` TanStack-Query seam; member app via `@twt/api-client` + `apps/mobile/lib`.
    - **D5-B (put the redaction / audit decision in the app layer).** REJECTED — 4.6 deliberately made the service own its scope contract (`assertCanReadValidity` + `redactForCaller`); the app boundary only maps camelCase→snake_case and supplies the `ValidityCaller`. Do NOT re-implement redaction in `apps/api`.
  - [x] **D6 (member-facing variant — surface + Hindi-first freeze-row-10 + accessibility Story 0.10 — CONFIRMED).**
    - **D6-A (CONFIRMED): render the member variant on the member's own status surface reachable from Home (a new `apps/mobile` route — there is no profile tab today; `(tabs)` has index/panchayat/shradhanjali).** A `Membership Status` surface reachable from home (UX "Surfaces: Member profile; Sushil's home") rather than a new tab. Suppress identity/Aadhaar/KYC re-display (AC2a); simplify provenance to "what rule applies to you" (AC2b); enforce Hindi-first parity via `@twt/i18n` — every member-visible string carries Hindi, English never primary (**freeze row 10**, `epics.md:527`; `[[MMKV = the app's AsyncStorage]]` is unrelated but the i18n substrate is the same one Epic 2/3 member surfaces use). Accessibility (AC3): semantic labels, logical nav order, ARIA roles/landmarks; rules announced as a labeled list; failure explanations full prose; appeal-CTA reachable from every failure state (UX anatomy `ux-design-specification.md:1893-1896`). Wire the automated a11y CI scan for compound views (`architecture.md:2771-2775`) if not already inherited.
    - **Out of scope (record as such — do NOT build):** the admin **override controls** UX mentions ("admin-facing … with override controls; Trustee Panel review on overrides", `ux-design-specification.md:1895`) — that is a WRITE / eligibility-override action, NOT in the epic AC (which is read + search + render only). The 200ms validity **cache** (Story 4.8). The R7/R8 **contribution** producer + the **claim** producer (Epic 6 / 8/9). Surface any pressure to build these in the Dev Agent Record.

- [x] **Task 1 — The AR-65 compound read model (AC1) — per confirmed D1 + D2.**
  - [x] Define the denormalized projection SHAPE: `member identity (scope-safe fields) + nominee summary (from `getMemberNominees` — `packages/domain/src/nominee/declaration-read.ts:21`) + state-machine status (`members.state` — projector-only) + contribution section (D2 sentinel) + claim section (D2 sentinel)`. All five sections present; contribution/claim = `producer_unavailable` typed sentinel (D2-A), never `[]`.
  - [x] Build it per confirmed D1 (D1-A: a `member_search_projection`-style read-model table maintained by the Story 3.1 projector). A new table means a new drizzle schema file (`packages/domain/src/schema/member_search_projection.ts`) + ONE new migration — generate it once and never regenerate after it is applied (`[[project_live_db_test_gotchas]]`). **The projection is write-owned EXCLUSIVELY by the projector — no HTTP handler, admin action, background job, or SQL trigger writes it directly** (BigDev refinement ii; mirror the `members.state` projector-only discipline CONCRETELY: the migration-0018 state-writer trigger + the CI gate are the precedent to copy for this table, `[[project_member_lifecycle_domain_substrate]]`). Incremental refresh on member-state append; **freshness = eventual consistency within the normal projector latency budget** (BigDev refinement iii — assess lag against the projector's budget, not a fixed second-count). ONE join-free read per result (no N+1 — `architecture.md:3919-3926`); add the named "no-N+1" query-count assertion (`architecture.md:3924`).
  - [x] Scope every read by `pariwar_id` (RLS) — this projection is member PII; the admin-search path must be scope-respecting (AC1) via the same `pariwar_id`-first posture as the rest of the member surfaces.
  - [x] **Do NOT re-derive validity here.** The panel's eligibility/rule signals come from the Story 4.6 `MemberValidityPayload` (AC3 of 4.6 made it "sufficient … without additional queries"). The compound read model supplies the *surrounding* context (identity/nominee/state/contrib/claim), NOT a second validity computation.

- [x] **Task 2 — Admin member-search over encrypted PII (AC1) — per confirmed D3.**
  - [x] Implement search on the confirmed v1 key set (D3-A CONFIRMED, as corrected: `mobile` via `member_identities.mobile_blind_index`; `member_id`; `Pariwar ID` — there is NO member-number column in the substrate; do not invent one). Compute the blind index via the domain `blindIndex` primitive (`packages/domain/src/encryption/blind-index.ts`) under the correct field-class + per-Pariwar context — never search on plaintext PII (D3-B rejected). **Exact-match ONLY — prefix/fuzzy/partial search is out of scope** (BigDev refinement i); state this in the search API's documented capabilities.
  - [x] **Name + Aadhaar search are DEFERRED (CONFIRMED)** — separate migration stories (deterministic blind index + schema migration + backfill). Record the CR (Task 8) AND **record the variance against the AC** in the Dev Agent Record (the AC enumerates name/Aadhaar; this story ships without them by design) — do NOT silently drop them. Also record the confirmed interpretation that **"Aadhaar masked" in the AC is a DISPLAY field, not a v1 search key** (BigDev refinement v) — the masked last-4 shows in the panel identity section; Aadhaar-as-search rides the deferred blind-index story.
  - [x] Gate every search + read on `member.view_validity` (Story 4.6's key, `PERMISSION_CATALOG_VERSION` 3) via `hasPermission`/`requirePermission` (`packages/domain/src/rbac/check.ts:150,239`); fail-closed.

- [x] **Task 3 — The shared framework-agnostic presenter (AC1, AC2) — per confirmed D4.**
  - [x] Build the pure view-model builder: `MemberValidityPayload` (+ compound projection) → `{ headlineState (Active | pending-onboarding | suspended-with-reason | expired-renewable | expired-not-renewable), sections[a-g], ruleExplanations[] (i18n keys), validityWindow (active-from + valid-through), showAppealCta, redactionApplied }`. Map every headline state from the canonical payload's `isValid`/`isActive`/`lockInStatus.state`/`vyawasthaShulkStatus` — never a second source of truth.
  - [x] **Strict purity (D4-A, BigDev refinement iv — this becomes shared infrastructure).** The presenter is `(payload, projection) → view-model` and NOTHING else: NO react/react-native import, NO API call, NO DB read, NO permission check (redaction is already applied by `@twt/validity-service` — read `redactionApplied`/absent fields, never re-check scope), NO side-effecting i18n lookup (emit i18n KEYS; the render layer resolves them). Same `(payload, projection)` in → same view-model out. Assert with pure unit tests (nothing to mock).
  - [x] Section (d) contribution discipline + section (e) medical/concealment must honor the payload's typed states: `contributionHistorySummary: producer_unavailable` renders as "not yet available" (D2), NOT an empty grid; `pendingConcealmentFlag` (State-Trustee-scope-only, already redacted by 4.6 for narrower callers) renders section (g) `concealment_review_required` prominently only when present.
  - [x] Rule explanations are full prose via `@twt/i18n` keys, not error codes (UX a11y, `ux-design-specification.md:1896`).

- [x] **Task 4 — The apps/api contract boundary + wire DTOs (AC1, AC2) — per confirmed D5.**
  - [x] New `apps/api` SURFACE module (mirror `member-home/`): member-self validity read (member JWT → 4.6 self-redacted payload, NOT audit-logged); admin validity read + member-search (admin grants → `assertCanReadValidity` + `member.view_validity`, audit-logged via 4.6's seam). Reuse `openScopeTx`/`closeScopeTx` + `deps.clock()`.
  - [x] Wire DTOs in `packages/contracts/src/members/` — `.strict()` Zod, snake_case wire names mapped from the service's camelCase (`packages/contracts/src/members/lock-in.ts` pattern), **no `@twt/domain` import**. Reuse `MemberLifecycleStateWire` / `Iso8601Datetime` primitives rather than minting duplicates.
  - [x] Do NOT re-implement redaction/audit in the app (D5-B) — the app supplies the `ValidityCaller`; `@twt/validity-service` owns the scope contract.

- [ ] **Task 5 — Admin-facing panel render (AC1) — `apps/admin`.**
  - [ ] `<MemberLookupForm>` (admin member-record search — UX Phase 1 `ux-design-specification.md:2175`) + `<MemberStatusPanel>` admin variant rendering the shared presenter's view-model with Radix/Tailwind. Sections (a)–(g) per AC1; `concealment_review_required` highlighted prominently (Epic 6 verifier console consumer). Admin variant shows the full provenance audit-trace (vs the member variant's simplified view).
  - [ ] Consume via `apps/admin/src/api/client.ts` + `api/hooks.ts` (TanStack Query; the typed-client-parses-with-`@twt/contracts` seam, `apps/admin/src/api/client.ts:1-8`). Choose `staleTime` per the validity surface's freshness class (`architecture.md:3930+`); this is a support/dispute read surface, not the member self-service cached path.
  - [ ] Admin member-search results list uses virtualization (platform requirement — `architecture.md:2666-2678`, "Admin member-search results" is a named virtualized surface; web → TanStack Virtual / react-virtuoso per ADR).

- [ ] **Task 6 — Member-facing panel render + Hindi-first + a11y (AC2, AC3) — `apps/mobile`.**
  - [ ] `<MemberStatusPanel>` member variant (Expo RN + Tamagui) rendering the SAME shared presenter view-model: identity/Aadhaar/KYC NOT re-displayed (AC2a); provenance simplified to "what rule applies to you" (AC2b); Hindi-first parity via `@twt/i18n` (AC2c, freeze row 10). Surface per confirmed D6 (recommend a `Membership Status` route reachable from home).
  - [ ] Accessibility (AC3): semantic labels, logical nav order, ARIA roles/landmarks; rules announced as a labeled list; failure explanations full prose; appeal-CTA reachable from every failure state.
  - [ ] Consume via `@twt/api-client` + `apps/mobile/lib/member-api.ts` + `query-client.ts` (member-auth bearer-token seam).

- [ ] **Task 7 — Tests (AC1, AC2, AC3).**
  - [ ] **Pure specs (`*.test.ts`, DB-free):** the shared presenter — every headline-state mapping from canonical payloads; the D2 `producer_unavailable` → "not yet available" (never empty grid) rendering decision; the admin-vs-member view-model divergence (identity suppressed, provenance simplified); redaction-aware rendering (a narrower caller's redacted payload never surfaces `pendingConcealmentFlag`).
  - [ ] **Read-model + search:** the compound projection assembles all five sections in one join-free read (the named no-N+1 query-count assertion, `architecture.md:3924`); blind-index exact-match search returns only in-scope members; a cross-Pariwar search returns nothing (RLS); `member.view_validity` deny/allow tripwire.
  - [ ] **Live-DB integration (`integration/*.spec.ts`, `:5433`, own-committing — `[[project_live_db_test_gotchas]]`):** seed a member (+ nominee + state), assert admin-search returns the projection + the 4.6 validity payload with real provenance; assert self-read is redacted + NOT audit-logged, admin-read IS audit-logged (reuse 4.6's audit assertions). Key on membership / own rows, NEVER global counts.
  - [ ] **a11y CI scan** on the compound view (`architecture.md:2771-2775`) if the harness exists; else record the gap.
  - [ ] **p95 ~5s@4L** admin-search budget: measure + record against a synthetic ~4L dataset (mirror 4.6's D3-A measure-and-record harness, `packages/validity-service/tests/bench/p95-budget.md`). ~5s is the AR-65 budget for the *admin-search* compound read (distinct from the validity payload's 200ms@4L, which is Story 4.8's cache).

- [ ] **Task 8 — Gate + reconciliation + deferred-work (`[[project_ci_actions_suspension_local_mirror]]`).**
  - [ ] Record CRs: the contribution producer (Epic 8/9) + claim producer (Epic 6) re-trigger for the D2 read-model sections; the **confirmed-deferred name/Aadhaar member-search** (blind index + schema migration + backfill — separate migration stories, D3) WITH the variance recorded against the AC; the **confirmed-deferred membership-number identity feature** (human-readable member number: schema + generation + backfill + indexing, with membership-number SEARCH riding it — a real product requirement, BigDev-confirmed NOT 4.7 scope); the admin override-controls surface (out of scope, separate story); the 200ms validity cache handoff to Story 4.8.
  - [ ] Run the gate: `pnpm --filter <touched packages> lint` + `typecheck` + `test`; `DATABASE_URL=…:5433 pnpm ci:local` as the merge gate (mirrors all `ci.yml` jobs; integration needs `DATABASE_URL` on `:5433`). Confirm the documented-innocent `@twt/jobs` / `@twt/admin` concurrent-load flakes in isolation if they appear (`[[project_known_livedb_test_failures]]`).
  - [ ] Sprint-status: `4-7-…` flips `backlog → ready-for-dev` (this workflow), then dev/review flip it onward; `last_updated` is the top-of-file reverse-chron COMMENT ledger (`[[project_sprint_status_ledger]]`).

## Dev Notes

### Where this sits (read before touching anything)

Story 4.7 is the **second `[SURFACE]` of Epic 4** and the render/read layer over Story 4.6's canonical `MemberValidityPayload`. 4.6 built the framework-agnostic `@twt/validity-service` (`getValidity`/`getValidityAt` → the canonical payload, RBAC redaction, admin-audit seam, the P0 determinism gate) and made its payload **AC3-explicitly "sufficient to render `<MemberStatusPanel>` (Story 4.7) without additional queries."** 4.7 does NOT add a rule, does NOT compute validity, and is NOT a fact producer. It: (1) builds the AR-65 compound read model that precomputes the admin member-search context, (2) wires the `apps/api` contract boundary 4.6 deferred, (3) renders TWO variants (admin web / member RN) over ONE shared presenter, (4) satisfies the accessibility + Hindi-first gates.

**Read first, end to end:** `packages/validity-service/src/types.ts` (the exact payload you render — every sub-object + its typed gaps), `packages/validity-service/src/service.ts` + `redaction.ts` (the `getValidity*` entry points + `assertCanReadValidity`/`redactForCaller` scope contract you call, never re-implement), and `apps/api/src/modules/member-home/{handlers,routes,index}.ts` (the SURFACE-module pattern your `apps/api` module mirrors — thin read handlers, scope-tx, `deps.clock()`).

**Task 0 is settled.** BigDev CONFIRMED all six decisions on 2026-07-04 — D1→A (projector-maintained denormalized read-model table), D2→A (`producer_unavailable` sentinel for contribution/claim, inherited from 4.6), D3→A (v1 search = mobile + member_id + Pariwar ID, **exact-match only** — the "member number" key was struck at validation and the strike RE-CONFIRMED by BigDev: no such column exists in the substrate; a membership number is a real product requirement but lands with the future membership-number identity feature, search included; name/Aadhaar-search deferred as separate migration stories; "Aadhaar masked" = a display field, not a v1 search key), D4→A (shared **strictly-pure** presenter + thin platform renderers), D5→A (thin apps/api module; redaction/audit stay in `@twt/validity-service`), D6→A (member surface from Home + Hindi-first + a11y). Five refinements are folded into Task 0 / Tasks 1–3: exact-match-only search, projector-EXCLUSIVE projection ownership, operational freshness (eventual consistency within the projector latency budget), strict presenter purity, and the recorded name/Aadhaar AC variance. Take Task 0 as written — do not re-open.

### Load-bearing invariants (the Epic-4 through-lines this story inherits)

- **ONE canonical source of eligibility.** The panel's every eligibility/rule signal comes from the 4.6 `MemberValidityPayload`. Never compute a second validity answer in the read model or the presenter — that would fork the canonical source the whole Validity Service exists to be.
- **Never a placeholder (`[[project_engine_never_infers_contribution_facts]]`, `[[CR-4.4-D3]]`, `[[CR-4.5-D1]]`).** The contribution + claim read-model sections are `producer_unavailable` (typed), never empty arrays. 4.6 already shipped this for `contribution_history_summary`; 4.7 extends the SAME discipline to the compound read model + the panel render.
- **Scope-respecting, fail-closed.** Admin search + read are gated on `member.view_validity` (4.6, catalog v3) + `pariwar_id` RLS; the State-Trustee-only `pendingConcealmentFlag` is ALREADY redacted by 4.6 for narrower callers — the panel just renders whatever survived redaction, and must never re-derive or leak it. Redaction lives in `@twt/validity-service`, NOT the app (D5-B rejected).
- **Ladder precedence is provenance, not eligibility (`[[project_niyamavali_precedence_is_provenance.md]]`).** When the panel surfaces "which rule applies," it is ordering explanations by the payload's `precedence`/order — every applied clause already means eligible. Re-tune DATA, never add panel logic.
- **No N+1 on the compound surface (`architecture.md:3919-3926`).** The read model joins everything in one query per result; the named no-N+1 test asserts query counts. This is the whole point of AR-65 (`architecture.md:326-330`, §1.18).

### The payload you render — three source shapes already reconciled by 4.6

The `MemberValidityPayload` (`packages/validity-service/src/types.ts:156-183`) already carries: `isValid` / `isActive` / `ruleRegistryVersion` (top-level answers), `lockInStatus` (`in-lock-in`|`unlocked`|`never-entered` + `unlockDate`), `vyawasthaShulkStatus` (`paidThrough` + `daysUntilLapse` + `inRenewalGrace`), `contributionHistorySummary` (`producer_unavailable` — D2), `medicalDisclosureFlags` (non-PII presence/count + State-Trustee-only `pendingConcealmentFlag`), `retirementCoverage` (`isRetired`/`yearsOfCoverageEarned`/`coverageThrough`/`daysRemaining`/`active`, OR `clause_unavailable`), `specialFlags[]`, `applicableNiyamavaliClauses[]` (ordered clauseId+clauseVersionId+outcome+reasonCode), `provenanceTrace[]` (ordered), `validityPayloadHash`. **The panel maps these to the UX sections (a)–(g) + the five headline states — it does not re-shape or re-source them.** The epic↔PRD field variance was already resolved in 4.6 (retirement projection, `[[CR-4.5-D3]]`); do not re-litigate.

Map to UX `<MemberStatusPanel>` states (`ux-design-specification.md:1894`): Active (all rules pass) · pending-onboarding · suspended-with-reason · expired-renewable (Vyawastha Shulk lapsed but renewable) · expired-not-renewable (lock-in violation). Appeal-CTA appears on every failure state (UX + a11y).

### Existing surfaces you build on (READ these)

- `packages/validity-service/src/{types,service,redaction,index}.ts` — the payload contract + `getValidity`/`getValidityAt` + `assertCanReadValidity`/`redactForCaller` + the `ValidityCaller` shape you construct at the app boundary.
- `apps/api/src/modules/member-home/{handlers,routes,index}.ts` — the SURFACE-module pattern (thin read handler, `memberCtx` 401 gate, `openScopeTx`/`closeScopeTx`, `deps.clock()`). Your new `member-validity/` module mirrors it.
- `packages/contracts/src/members/lock-in.ts` + `index.ts` — the wire-DTO pattern: `.strict()` Zod, `Iso8601Datetime` primitive, reuse `MemberLifecycleStateWire`, NO `@twt/domain` import (browser-bundle rule).
- `apps/admin/src/api/{client,hooks}.ts` — the typed-client + TanStack-Query seam (client parses every response with the `@twt/contracts` schema; `createQueryClient` + `useQuery`/`useMutation` hooks). Your admin panel + lookup form consume through here.
- `apps/mobile/lib/{member-api,query-client,session}.ts` + `@twt/api-client` — the member-auth bearer-token fetch + Query setup; the `(life-events)` screens + `components/life-events/useLifeEventsSummaryQuery.ts` are the closest recent member-facing read-panel precedent.
- `packages/domain/src/nominee/declaration-read.ts:21` (`getMemberNominees`) — the nominee-summary source for the compound projection.
- `packages/domain/src/encryption/blind-index.ts` + `packages/domain/src/schema/member_identities.ts:47-60` — the blind-index primitive + the ONE existing member blind index (`mobile`), the substrate for D3 search.
- `packages/domain/src/rbac/check.ts:150,239` (`hasPermission`/`requirePermission`) + the `member.view_validity` key (added by 4.6, catalog v3) — the search/read gate.
- `packages/domain/src/member/{project,state,read}.ts` — the Story 3.1 projector + `members.state` (projector-only, trigger/CI-gate guarded per `[[project_member_lifecycle_domain_substrate]]`) the compound read model's projection follows.

### Testing standards

- Framework: Vitest. Pure specs (`*.test.ts`) DB-free; live-DB specs (`integration/*.spec.ts`) are `describe.skipIf(!hasDatabase)`, need `DATABASE_URL` on `:5433` (the `twt-test-pg` Docker, `[[project_live_db_test_gotchas]]`), own-committing → track seeded ids, clean in `afterAll`, assert membership / own rows / idempotent outcome, NEVER global counts.
- The **no-N+1** query-count assertion is first-class for the compound read model (`architecture.md:3924`). The **a11y CI scan** on compound views (`architecture.md:2771-2775`) is the AC3 automation.
- Real CI `test (unit)` runs with `DATABASE_URL` UNSET so live specs skip. A `@twt/jobs` timeout OR `@twt/admin` async-UI race under concurrent turbo load is the documented-innocent flake — prove innocence in isolation (`[[project_known_livedb_test_failures]]`).
- The admin app tests: the admin surfaces run with per-package eslint cwd (`[[project_eslint_config_per_package_cwd]]`) — carve-out globs are cwd-relative role globs; verify with `pnpm --filter @twt/admin lint`.

### Project Structure Notes

- **New:** the `apps/api` `member-validity/` SURFACE module (D5); `packages/contracts/src/members/` validity + search DTOs; the shared presenter (D4 — `packages/ui/src/member-status/` or a small dedicated package); the `apps/admin` `<MemberLookupForm>` + `<MemberStatusPanel>` (admin variant); the `apps/mobile` `<MemberStatusPanel>` (member variant) + its route; the compound read-model table (`packages/domain/src/schema/member_search_projection.ts` + ONE new migration, unconditionally — plus its 0018-style writer-guard trigger) + projector wiring (D1); tests + the p95 harness.
- **Updated:** `apps/admin/src/api/{client,hooks}.ts` (+ a route) and the admin router; `apps/mobile` navigation for the new member surface; `packages/domain/src/schema/index.ts` (exports the new D1 projection table — the D1 migration itself is unconditional, listed under **New** above; a name/Aadhaar blind index under D3 is deferred and NOT built here; either way, never regenerate an applied migration, `[[project_live_db_test_gotchas]]`); `.github/workflows/ci.yml` + `scripts/ci-local.sh` if a new gate (no-N+1 / a11y) is added; `_bmad-output/implementation-artifacts/{deferred-work.md,sprint-status.yaml}`.
- **Explicitly OUT of scope (record any drift):** admin override / eligibility-override controls + Trustee-Panel-review (a WRITE surface, not in the AC — `ux-design-specification.md:1895`); the 200ms validity cache / materialized view (Story 4.8); the R7/R8 contribution producer (Epic 8/9) + claim producer (Epic 6); fuzzy/prefix name search (D3, needs a different substrate). If rendering the panel forces a change to `@twt/validity-service`'s payload or redaction, surface it in the Dev Agent Record — it would signal the render layer leaked into the canonical service.

### References

- [Source: epics.md#Story 4.7 (lines 2009-2028)] — AC verbatim: admin member-search + compound read model (~5s@4L) + sections (a)–(g); the member-facing variant (identity-suppressed, simplified provenance, Hindi-first freeze row 10); the Story 0.10 accessibility gate.
- [Source: epics.md#Story 4.6 (lines 1982-2007), esp. 2007] — 4.6 made its payload "sufficient to render `<MemberStatusPanel>` (Story 4.7) without additional queries; compound read model (Story 4.7 admin variant) precomputes the ~5s admin-search load."
- [Source: epics.md#Epic 4 (line 1873)] — AR-65 (compound read models for operator surfaces — Story 4.7 admin variant) is an anchoring AR of Epic 4.
- [Source: epics.md#architectural-freeze row 10 (line 527)] — the Hindi-first bilingual-parity contract (AC2c).
- [Source: ux-design-specification.md#`<MemberStatusPanel>` (lines 1890-1897)] — purpose, anatomy (header · rule-by-rule grid · validity window · appeal CTA), the five states, member/admin variants, accessibility (rules as labeled list, prose not codes, appeal reachable), surfaces.
- [Source: ux-design-specification.md (lines 2175-2176, 2213)] — `<MemberLookupForm>` + `<MemberStatusPanel>` admin variant (Phase 1); the member-facing variant (Phase 3).
- [Source: architecture.md#§1.18 / item 18 (lines 326-330)] — AR-65 compound read models: ~5s load, no N+1, denormalized read store, freshness real-time-within-seconds.
- [Source: architecture.md#N+1 prevention (lines 3919-3928)] — repository-joins-everything, the named no-N+1 CI query-count test.
- [Source: architecture.md#§1.10 (lines 1044-1081)] — the FR-12A validity cache is Story 4.8's materialized view (NOT this story's read model); confirms the two caches are distinct.
- [Source: architecture.md (lines 2476, 2666-2678, 2771-2775, 3930+)] — admin stack (Vite/React/Tailwind/Radix); admin member-search results is a virtualized surface; compound-view a11y CI scan; per-surface `staleTime` classes.
- [Source: packages/validity-service/src/types.ts (lines 156-183 + sub-objects)] — the canonical payload the panel renders (every field + its typed gaps).
- [Source: packages/validity-service/src/{service,redaction}.ts] — `getValidity*` + `assertCanReadValidity`/`redactForCaller`/`ValidityCaller` — the scope contract the app boundary calls, never re-implements.
- [Source: apps/api/src/modules/member-home/{handlers,routes,index}.ts] — the SURFACE-module pattern the new `member-validity/` module mirrors.
- [Source: packages/contracts/src/members/lock-in.ts (lines 1-16)] — the wire-DTO discipline (`.strict()`, `Iso8601Datetime`, reuse `MemberLifecycleStateWire`, no `@twt/domain`).
- [Source: apps/admin/src/api/{client,hooks}.ts] — the typed-client + TanStack-Query seam the admin panel consumes.
- [Source: packages/domain/src/encryption/blind-index.ts + schema/member_identities.ts (47-60)] — the blind-index primitive + the ONE existing member blind index (`mobile`), the D3 search substrate.
- [Source: packages/domain/src/rbac/check.ts (150,239)] — the `member.view_validity` (catalog v3, from 4.6) search/read gate.
- [Source: packages/domain/src/nominee/declaration-read.ts (21)] — `getMemberNominees`, the nominee-summary source for the compound projection.
- [Source: 4-6-fr-12a-member-validity-service.md (esp. Project Structure Notes / File List)] — the deferred apps/api boundary + wire mapping this story picks up; the redaction/audit seam this story consumes.
- [Source: memory `[[project_member_lifecycle_domain_substrate]]`, `[[project_engine_never_infers_contribution_facts]]`, `[[project_niyamavali_precedence_is_provenance]]`, `[[project_live_db_test_gotchas]]`, `[[project_ci_actions_suspension_local_mirror]]`, `[[project_sprint_status_ledger]]`, `[[project_eslint_config_per_package_cwd]]`] — the projector/substrate, never-infer-facts, precedence-is-provenance, live-DB, CI-mirror, sprint-ledger, eslint-cwd disciplines this story operates under.

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (Claude Code `/bmad-dev-story`).

### Debug Log References

- `pnpm --filter @twt/domain db:generate` produced a bogus 0035 (diffed CURRENT schema against the stale 0020 snapshot — snapshots stop at 0020; 0021–0034 are hand-authored). Reverted the generated SQL + snapshot + journal entry and HAND-AUTHORED `0035_member-search-projection.sql` (mirroring 0030 + the 0018 trigger), per `[[project_live_db_test_gotchas]]`.
- Migration applied to the `twt-test-pg` :5433 test DB; verified the table columns + the `member_search_projection_write_guard` trigger landed.
- Domain suite (with `DATABASE_URL` on :5433): **523 passed / 1 skipped / 60 files, 0 failures** — no regressions from the projector now writing the projection on every member-state append.
- New `search-projection.spec.ts`: **6/6** (projector write + refresh; write-rejection trigger P0001; member_id + mobile-blind-index exact-match search; cross-Pariwar RLS = 0 rows; nominee summary capture; **no-N+1 = exactly 1 query** for the browse page).
- `@twt/ui` presenter unit tests: **14/14** (every headline-state mapping; D2 producer_unavailable → "not yet available" never empty; admin-vs-member divergence; redaction-aware rendering; appeal-CTA on failure states; rule-explanation ordering + i18n keys; lock-in deep-link; retirement visibility).
- member-state-invariant CI gate extended to guard `member_search_projection` writes — gate passes; its lib test **9/9**.
- `typecheck` + `lint` green on @twt/domain, @twt/contracts, @twt/ui, @twt/api.

### Completion Notes List

**Completed + VERIFIED (backend + shared-logic + contracts + apps/api boundary — Tasks 1–4):**
- ✅ **Task 1** — AR-65 `member_search_projection` compound read model: non-PII denormalized store (state + `state_event_version` + non-PII nominee summary + the D2 `producer_unavailable` contribution/claim sentinels); Tier-1 identity PII is JOINed at read, never materialized (§2.7 posture). Projector-EXCLUSIVE write (`member/search-projection.ts` called by `member/project.ts`) + a BEFORE INSERT/UPDATE write-rejection trigger (migration 0035, the 0018 precedent) + the extended member-state-invariant CI gate. One join-free read; the no-N+1 query-count assertion passes.
- ✅ **Task 2** — exact-match search accessor (`member/search-read.ts`): mobile blind index + `member_id` + `pariwar_id`; RLS + explicit `pariwar_id` predicate; the `member.view_validity` gate is enforced at the apps/api boundary (Task 4) + re-checked by the service. Name/Aadhaar deferred (CR-4.7-D3); membership-number deferred (CR-4.7-D4).
- ✅ **Task 3** — the strictly-pure `@twt/ui` `<MemberStatusPanel>` presenter (`packages/ui/src/member-status/`): `(payload, opts) → view-model`, NO react/api/db/permission/side-effecting-i18n; emits i18n KEYS; the SAME derivation feeds both variants (no eligibility drift). Consumes the `@twt/contracts` DTO (browser-safe — no `@twt/domain`/`@twt/validity-service` import).
- ✅ **Task 4** — `@twt/contracts` wire DTOs (`members/validity.ts`: the FR-12A payload DTO + the member-search request/response) + the apps/api `member-validity/` module (member-self read [redacted, NOT audited]; admin read [scope-gated, AUDITED]; admin search [identity decrypted for display]). Redaction + audit stay in `@twt/validity-service` (D5-B rejected); the module only builds the `ValidityCaller`, supplies the engine deps, and maps payload → wire. Wired into `server.ts`. Typechecked (its live-DB integration test is the Task 7 remainder).

**Factual corrections recorded (like the 4.6 MMKV precedent):**
- The `@twt/validity-service` header says wire keys become snake_case, but the SHIPPED repo convention is camelCase JSON (lock-in.ts/auth.ts + every handler). The validity DTO follows the real substrate — camelCase, so the apps/api boundary is a pure pass-through with NO key remapping. Documented in `contracts/members/validity.ts`.
- The AR-65 read model stores NON-PII only; identity display is JOINed + decrypted at read (materializing Tier-1 PII into a denormalized store would violate §2.7). "One join-free lookup per result" is honored as "one query for the page, no per-result loop" — asserted by the no-N+1 test.

**NOT YET BUILT — the render layers + full-integration surfaces (Tasks 5, 6, and the Task 7/8 remainder) — see CR-4.7-D7:**
- ⬜ **Task 5** — apps/admin `<MemberLookupForm>` + `<MemberStatusPanel>` (Radix/Tailwind) consuming the presenter via the TanStack-Query seam + a virtualized results list.
- ⬜ **Task 6** — apps/mobile `<MemberStatusPanel>` member variant (Tamagui) + its Home-reachable route + the Hindi-first `@twt/i18n` `member-status` locale keys (freeze row 10) + accessibility wiring (AC3).
- ⬜ **Task 7 (remainder)** — the apps/api live-DB integration spec (admin-read-audited vs self-read-not; reuse 4.6's audit assertions); the ~5s@4L p95 harness (mirror 4.6's measure-and-record); the compound-view a11y CI scan. (The pure-presenter specs + the read-model/search + no-N+1 + RLS + trigger specs are DONE.)
- ⬜ **Task 8 (remainder)** — the CRs are recorded in `deferred-work.md` (CR-4.7-D1…D7); the FULL `pnpm ci:local` merge-gate run + the sprint-status `→ review` flip are pending story completion.

**Why paused here (honest report):** the backend + shared-presenter + contracts + api-boundary vertical is complete and green; the two remaining render layers (admin web + mobile RN, two incompatible stacks) + i18n + a11y + p95 are a large, separately-verifiable surface best done as a render-layer continuation — the tested pure presenter + wire DTOs make them a thin, drift-free consumption of a single view-model. Story stays `in-progress` (DoD not yet met); sprint-status NOT flipped to `review`.

### File List

**New:**
- `packages/domain/src/schema/member_search_projection.ts` — the AR-65 read-model table + section types + D2 sentinel constants.
- `packages/domain/src/policies/member-search-projection-rls.ts` — tenant-isolation RLS (mirror members-rls).
- `packages/domain/src/member/search-projection.ts` — the projector-exclusive projection refresh writer.
- `packages/domain/src/member/search-read.ts` — the exact-match scope-respecting search accessor (one join-free read).
- `packages/domain/migrations/0035_member-search-projection.sql` — hand-authored: table + FK + index + GRANT + FORCE RLS + policies + the write-rejection trigger.
- `packages/domain/tests/integration/member/search-projection.spec.ts` — 6 live-DB specs.
- `packages/contracts/src/members/validity.ts` — the FR-12A payload DTO + member-search request/response DTOs.
- `packages/ui/src/member-status/{view-model,i18n-keys,presenter,index}.ts` — the pure presenter + view-model + i18n key catalogue.
- `packages/ui/tests/member-status/presenter.test.ts` — 14 pure presenter specs.
- `apps/api/src/modules/member-validity/{handlers,routes,index}.ts` — the member-self + admin validity reads + admin search.

**Updated:**
- `packages/domain/src/schema/index.ts` (export projection table), `packages/domain/src/policies/index.ts` (export RLS), `packages/domain/src/member/index.ts` (export refresh + search-read), `packages/domain/src/member/project.ts` (call the projection refresh in-tx), `packages/domain/migrations/meta/_journal.json` (0035 entry).
- `scripts/member-state-invariant/{lib,check}.ts` — extended the CI gate to guard `member_search_projection` writes.
- `packages/ui/src/index.ts` + `packages/ui/package.json` (@twt/contracts dep); `packages/contracts/src/members/index.ts`.
- `apps/api/src/server.ts` (register the module) + `apps/api/package.json` (@twt/validity-service dep).
- `_bmad-output/implementation-artifacts/deferred-work.md` (CR-4.7-D1…D7); `_bmad-output/implementation-artifacts/sprint-status.yaml` (→ in-progress).

## Change Log

| Date | Change |
|---|---|
| 2026-07-04 | **Code review (Fable) of the backend vertical + fixes.** 7 findings + 7 cleared areas. FIXED in-branch: (#1 High) migration 0035 now BACKFILLS `member_search_projection` from all existing members before RLS/trigger install (verified — populated 2231 pre-existing dev-DB members; the projection would otherwise only populate on each member's NEXT lifecycle event); (#2 Med/High) `adminMemberSearch` now writes a `member.search` hash-chain audit line (D5-A said search is audit-logged; it wasn't); (#3 Med) `adminValidityRead` now `memberExists`-probes → 404 (was fabricating a 200 payload + a phantom audit line for any/cross-tenant UUID); (#6 Low) the search handler now suppresses masked-mobile too for `anonymized` members (RTBF hygiene — was emitting `+91·····zed]`). DEFERRED as CRs: CR-4.7-D8 (presenter headline can't distinguish covered-in-lock-in / withdrawn from the payload alone — payload carries no lifecycle-state enum; resolve in the render-layer continuation), CR-4.7-D9 (`finally` SET-LOCAL reset can mask the real error with 25P02 — precedent-consistent with project.ts), CR-4.7-D10 (static gate bypasses — the DB trigger is the runtime backstop). Cleared: no PII in the projection, no cross-Pariwar leak, projector-exclusive write holds, migration/journal parity, contracts field-for-field match, presenter purity + redaction-safety, self-vs-admin caller construction. Re-verified: domain 523 pass/0 fail, contracts 195, ui 15, api typecheck+lint green. |
| 2026-07-04 | **Dev (backend + shared-logic vertical, Tasks 1–4 complete + verified; Tasks 5–8 remainder outstanding).** Built the AR-65 `member_search_projection` compound read model (non-PII denormalized store + D2 `producer_unavailable` sentinels; projector-EXCLUSIVE write via `member/search-projection.ts` + hand-authored migration 0035 with a write-rejection trigger [0018 precedent] + extended member-state-invariant CI gate), the exact-match scope-respecting search accessor (`member/search-read.ts`; one join-free read, no-N+1 asserted), the strictly-pure `@twt/ui` `<MemberStatusPanel>` presenter (`(payload,opts)→view-model`, emits i18n keys, one derivation → no variant drift), the `@twt/contracts` FR-12A payload + member-search wire DTOs (camelCase — matches the shipped substrate, NOT the header's snake_case), and the apps/api `member-validity/` module (member-self [redacted/not-audited] + admin [scope-gated/audited] validity reads + AR-65 search [identity decrypted for display]; redaction/audit stay in `@twt/validity-service` per D5). VERIFIED: domain suite 523 pass/0 fail (no projector-change regressions); 6/6 new read-model+search live-DB specs (incl. no-N+1=1 query + cross-Pariwar RLS + trigger P0001); 14/14 pure presenter specs; member-state-invariant gate 9/9; lint+typecheck green on domain/contracts/ui/api. CRs CR-4.7-D1…D7 recorded in deferred-work.md. OUTSTANDING (CR-4.7-D7): the apps/admin + apps/mobile render variants (Tasks 5/6), the apps/api live-DB integration + ~5s@4L p95 harness + a11y scan (Task 7 remainder), the full ci:local merge gate (Task 8). Story stays `in-progress` — DoD not yet met; sprint-status NOT flipped to review. |
| 2026-07-04 | BigDev RE-CONFIRMED the validation strike of "member number" from the D3-A v1 search-key set: a human-readable membership number IS a real product requirement, but it is NOT in the current substrate and NOT 4.7 scope — the story must not invent a `member_number` column or search key. v1 search stays mobile blind index + `member_id` + `pariwar_id`. Membership-number search will be introduced when the **membership-number identity feature** lands (schema + generation + backfill + indexing). Recorded as a confirmed-deferred CR in Task 8. |
| 2026-07-04 | Validation pass (validate-create-story): all ~25 source citations re-verified accurate (epics AC verbatim; architecture/UX line refs; every code path + line number; payload shape; permission key; packages). TWO corrections applied: (1) **"member number" struck from the D3-A v1 search-key set** — no member-number column or concept exists anywhere in the substrate (`members` carries only `member_id` uuid + `pariwar_id` as plaintext keys); v1 search = mobile blind index + `member_id` + `Pariwar ID`; a future human-readable member number rides the same deferred-migration path as name/Aadhaar (flagged for BigDev re-confirmation if it is a real product need — the confirmation transcript had listed it). (2) **D1 migration requirement made unconditional** — the `member_search_projection` table needs its own schema file + ONE new migration (never regenerate once applied) and a 0018-style writer-guard trigger; previously the migration discipline was cited only under the conditional D3 branch. |
| 2026-07-04 | BigDev CONFIRMED all six Task 0 decisions (settled, taken as written): **D1→A** (projector-maintained denormalized read-model table); **D2→A** (`producer_unavailable` sentinel for the contribution/claim sections, never empty arrays); **D3→A** (v1 search = mobile blind index + member number + `member_id` + Pariwar ID); **D4→A** (shared pure presenter + thin platform renderers); **D5→A** (thin apps/api module; redaction/audit stay in `@twt/validity-service`); **D6→A** (member surface from Home + Hindi-first + accessibility). FIVE refinements folded in: (i) all blind-index search is **exact-match only** — prefix/fuzzy out of scope; (ii) the projection is **write-owned exclusively by the Story 3.1 projector** — no HTTP handler/admin action/job/trigger writes it directly; (iii) freshness defined operationally as **eventual consistency within the normal projector latency budget**; (iv) the presenter is **strictly pure** — no API/DB/permission/side-effecting-i18n, `(payload, projection) → view-model` and nothing else; (v) **name + Aadhaar search deferred as separate migration stories with the variance recorded against the AC**, and "Aadhaar masked" interpreted as a **display field, not a v1 search key** (recorded so the omission does not read as a bug). Task 0 flipped to CONFIRMED; D1/D3/D4 bodies + Tasks 1/2/3/8 + the "Task 0 is settled" Dev Note propagated. |
| 2026-07-04 | Story 4.7 created (ready-for-dev) — `<MemberStatusPanel>` admin + member variants + the AR-65 compound read model, the SECOND `[SURFACE]` of Epic 4. Renders the Story 4.6 canonical `MemberValidityPayload` (which was built AC3-sufficient for exactly this panel); adds the admin member-search compound read model (~5s@4L, no N+1), the two render variants over ONE shared framework-agnostic presenter, and the `apps/api` contract boundary + wire snake_case mapping that 4.6 explicitly deferred here. SIX decisions FLAGGED for BigDev (not pre-confirmed): **D1** compound read model materialization (denormalized projector-maintained read-model table vs MV vs live-join); **D2** contribution/claim sections = `producer_unavailable`, never empty arrays (inherited from 4.6 D2 — producers are Epic 6 / 8/9); **D3** member-search over encrypted PII (only `mobile` has a blind index today — recommend mobile + Pariwar ID + member number in v1, name/Aadhaar blind indexes as an explicit follow-up, never silently drop AC-named dimensions); **D4** the panel component across two incompatible render stacks (shared pure presenter + thin admin-web / mobile-RN render layers); **D5** the apps/api boundary + wire DTOs 4.6 deferred; **D6** member surface + Hindi-first (freeze row 10) + accessibility (Story 0.10 P0-2c). Out of scope (recorded): admin override controls (a WRITE surface), the 200ms validity cache (Story 4.8), the R7/R8 contribution + claim producers. |
