# Gate Inventory

> **Purpose (AI-9):** Institutional memory converted to artifact. One-page map of every enforcement gate: what it is, what triggers it, its current status, and what it depends on. Written at Epic 1 close / Epic 2 pre-kickoff (2026-06-20).
>
> **Update cadence:** Revise when a gate flips status (no-op → enforcing, or deferred → active) or a new gate lands. Cross-link from the story that flips it.

---

## Enforcement Floor Status

| Mechanism | Status |
|---|---|
| GitHub Actions | ⚠️ **Suspended** (account under review since ~1.11). 1.11–1.17 merged via `pnpm ci:local` + pre-push hook (commit 480128e). |
| `pnpm ci:local` | ✅ Active as merge gate. Mirrors all 15 ci.yml gate jobs sequentially. Integration jobs need `DATABASE_URL` on `:5433`. _(`cadence-check` retired — ADR-0025; `domain-invariants` added — AI-2-2; both 2026-06-24.)_ |
| Pre-push hook | ✅ Active. `.githooks/pre-push` runs `ci:local` before every push. |
| AI-4 | ✅ Done (ADR-0017 authored, adopting `ci:local` + pre-push as the sanctioned merge gate). ⚠️ Formal trustee ratification of ADR-0017 still pending. |
| AI-5 | ✅ Done — `timeout-minutes` + `cache-dependency-path: pnpm-lock.yaml` applied across **all** ci.yml jobs (not just 1.16x); `tsx` pinned to an exact version in `package.json`. |

---

## Category A — CI Gates: Active and Enforcing on Every PR

| Gate (CI job name) | Story | Script / Turbo task | What it enforces | Status |
|---|---|---|---|---|
| `lint` | 1.1 | `pnpm turbo run lint` | ESLint per-package (cwd-relative globs) | ✅ Active |
| `typecheck` | 1.1 | `pnpm turbo run typecheck` | TypeScript strict across all packages | ✅ Active |
| `test` | 1.1 | `pnpm turbo run test` | Unit test suite | ✅ Active |
| `build` | 1.1 | `pnpm turbo run build` | Full monorepo build | ✅ Active |
| `db-check` | 1.2 | `pnpm db:check` | Drizzle schema + migration health (no drift) | ✅ Active |
| `contracts-check` (OpenAPI generator determinism) | 1.4 | `pnpm contracts:check-openapi-determinism` | `openapi/v1.yaml` byte-identical on every run; generator is deterministic | ✅ Active |
| `crypto-check` (encryption substrate) | 1.5 | `pnpm crypto:check` | KMS + Tink envelope encryption substrate health | ✅ Active |
| `tokens-theme-check` (FM-4 @theme sync) | 1.17 | `pnpm tokens:check-theme-determinism` | `src/theme.css` byte-identical to `renderThemeCss()` output (FM-4 sync gate) | ✅ Active |
| `friction-budget` (UX-DR3) | 1.16a | `pnpm friction:check` / `scripts/friction-budget/check.ts` | Bundle-size YAML ceilings + named-payer ledger. **`member-public-web` has TEETH as of Story 2.5** (apps/public emits `dist/page-weight.json`; js_bundle_bytes + page_weight_bytes baselines committed). ⭐ **Story 11a.2 (ruling D5(a)) discharged the PER-ROUTE restructure** — `page-weight.mjs` now emits `routes: {…}` for all 8 shipped routes (⚠ its trigger had fired TWICE — /terms 2.6, /blog 10.5 — and been MISSED both times). ⚠ Per-route figures are an **ATTRIBUTION of static client assets**, ⛔ not a measurement of dynamic SSR HTML; shared chunks are attributed to every route so they ⛔ do **not** sum to the aggregate; and ⛔ the block is **EMITTED, NOT ENFORCED** (`evaluateMetric` reads flat top-level keys, so the two aggregate metrics remain the gated numbers). ⭐ **Story 11a.3 (ruling D6(a)) PARTIALLY DISCHARGED `critical_render_path_ms`** — a REAL measurement of the **DYNAMIC SSR HTML** now lands (`pnpm --filter @twt/public weight:dynamic` → `dist/dynamic-html-weight.json`), taken against the real built standalone server at a full page at the FR-91 cap: `/members` = **19 234 B at the cap**, **4 220 B** single-row, **306 B/row** marginal. ⛔⛔ **A DIFFERENT QUANTITY from the per-route static attribution above — NEVER sum or compare them** (the static figure for the same route is 3 116 B; the dynamic HTML is ~6× it). ⛔ **Emitted, NOT gated.** Only the device-throttled Lighthouse harness remains deferred, with a **genuinely new** reason and trigger (see §C). | ✅ Active (static proxies enforcing; per-route attribution emitted; ⭐ dynamic-HTML measured at 11a.3; live-TIMING harness deferred — see §C) |
| `schema-diff` (FR-100 non-add guard) | 1.16c | `pnpm schema:check` | ZERO payout-destination surface across 4 scan roots (table / column / endpoint / Zod schema). Invariant scan — no `fetch-depth:0`. | ✅ Active and enforcing |
| `microcopy` (UX-DR71 / UX-DR73 / FM-14) | 1.17 | `pnpm microcopy:check` / `scripts/microcopy/check.ts` | Vocabulary register (passbook→Yogdaan Bahi etc.) + tone prohibitions + FM-14 #2 magic-number colors. `code_globs` bounded to `apps/admin`; **`copy_globs` POPULATED at Story 2.5** with the `niyamavali` member-copy locale files — member-register vocabulary (user/customer/donor) + Devanagari-numeral discipline now have teeth on the first member surface. | ✅ Active (admin code + first member-copy surface enforcing) |
| `domain-accessor-invariants` (AI-2-2 family a) | retro AI-2-2 | `pnpm domain-invariants:check` / `scripts/domain-accessor-invariants/check.ts` | Family (a) forced-pagination clamp: every dynamic `.limit(...)` in `packages/domain/src` must route through `clampLimit(...)` (or be an integer literal) — no unbounded `LIMIT`, no negative-limit `LIMIT -1` bypass (the 2.7 P2 class). Static TS-AST scan. Families (b) collection-input guards + (c) read-then-write FOR UPDATE are convention + required-test in `docs/domain-accessor-invariants.md`. | ✅ Active and enforcing (landed green-with-teeth; 5 live accessors fixed) |
| `governance-boundary` (FR-58C feature-flag capability bar) | 10.8 | `pnpm governance-boundary:check` / `scripts/governance-boundary/check.ts` | **Two legs.** (a) *Conformance* — the domain flag registry (`FLAG_DEFAULTS`) and the capability bar (`governance_boundary.yaml`) admit exactly the same keys in BOTH directions, with a `count` cross-check. (b) **⟵ LOAD-BEARING** *source scan* — no feature-flag evaluation is NAMED inside a governance module. **TWELVE roots** (`domain/src/{audit,rbac,consent,contribution,pool,claim}`, `validity-service/src`, `scripts`, `apps/api/src/{middleware,modules/rbac,audit,plugins}`), read from the bar's own `prohibited` list so scan scope and documented prohibitions cannot drift. **FIVE AST detection routes** (module specifier incl. non-literal dynamic import; named symbol incl. aliases; `featureFlags.*` property + computed access; namespace re-export; object-binding destructuring) — routes 4-5 were added after routes 1-3 were each independently defeated in review. Plus two anti-vacuity teeth: a prohibited root that does not resolve to a directory FAILS the gate (it used to print `clean (0 files)`), and a scan reading zero files overall FAILS. ⚠ **What it guarantees, precisely:** the DIRECT and single-hop NAMED routes are mechanically closed, so a violation cannot be committed by accident or a casual edit. It does NOT cover TRANSITIVE reachability — a per-file scanner resolves no specifiers and follows no import edges. (This row read "makes the prohibitions structurally impossible" until Review Pass 5; that overstated the mechanism and is retracted here to match `README.md` and `governance_boundary.yaml`.) Prohibition (e) (freeze-table rows) is additionally checked at *admission* by the bar parser's frozen-behaviour markers — a non-exhaustive substring heuristic, not exhaustive enforcement. Invariant scan — no `fetch-depth:0`. | ✅ Active and enforcing — wired in `.github/workflows/ci.yml` (job `governance-boundary`) and `scripts/ci-local.sh`; landed green-with-teeth, both legs re-proven to FAIL on live planted violations in Review Passes 2 and 5 |
| `custom-field-governance` (FR-54 per-Pariwar custom fields) | 10.12 | `pnpm custom-field:check` / `scripts/custom-field-governance/check.ts` | **Two legs.** (a) *Denylist superset* — every `forbidden_column` pattern in `fr-100-non-add.yaml` is covered by a **prefix-mode** entry in the domain's `CUSTOM_FIELD_FORBIDDEN_KEY_PATTERNS`; a segment-mode entry does NOT count as coverage (the two modes accept different key sets, so it would report coverage of a rule the runtime does not enforce). This is the drift guard: a v2 addition to the FR-100 registry that nobody mirrors would otherwise leave a newly-frozen name authorable as a JSONB key. (b) **⟵ LOAD-BEARING** *sole writer* — `insert(pariwarCustomFieldDefinitions)` appears ONLY in `packages/domain/src/custom-fields/registry.ts`, the one place the frozen-governance fence, the naked-PII detector, the PII-tier gate and the cardinality bound all run. AST-detected (four shapes: `db.insert(t)`, bare `insert(t)`, `schema.t`, multiple sites per file) — the table name in a comment or string literal never matches. Anti-vacuity teeth: a scan reading zero files FAILS; an unreadable `fr-100-non-add.yaml` FAILS leg (a) rather than comparing against an empty set; a stale `WRITER_ALLOWLIST` entry naming a file not on disk FAILS. ⚠ **SCOPE LIMIT, STATED RATHER THAN OVERCLAIMED:** custom-field definitions are **DATABASE ROWS authored at runtime**, so this gate proves NOTHING about their content in any tenant database — a CI gate needing a live tenant DB is not a CI gate. The runtime prohibition is layers 1 and 2: the domain fence (`custom-fields/frozen-governance.ts`, on every publish, normalizing case/`-`/`.`/whitespace so `Payout-Destinations` cannot launder past it) and the DB CHECK `pariwar_custom_field_definitions_frozen_key_ck` (migration 0095), which holds for writers that never reach the app layer. ⚠ Known soft spot, recorded: files under `tests/`/`__tests__/` and any `*.test.ts`/`*.spec.ts` are excluded from leg (b), because the revert-sanity specs must be able to plant rows directly. ⚠ `epics.md:3605` cites Story 1.16c (`schema-diff`) as the enforcer of this rule; that citation is **unenforceable** — `schema-diff` scans migrations, route literals and Zod exports, and a runtime-authored JSONB key is none of those. This gate is what that citation assumed existed. Invariant scan — no `fetch-depth:0`. | ✅ Active and enforcing — wired in `.github/workflows/ci.yml` (job `custom-field-governance`) and `scripts/ci-local.sh`; landed green-with-teeth, with planted-violation fixtures per detection route in `lib.test.ts` and **layer 2 empirically re-proven to FAIL when the CHECK constraint is dropped** (2026-08-06) |
| `integration-tests` (RLS + multi-tenant + events_log) | 1.6 | `pnpm test:integration` / `apps/api/tests/integration/` | Single-row RLS leak → CI fail; multi-tenant isolation; append-only `events_log` enforcement. Needs `DATABASE_URL` on `:5433`. | ✅ Active via `ci:local` (⚠️ GH Actions suspended) |

---

## Category B — CI Gates: Green-by-Construction, Awaiting Consumer

These gates are installed and passing, but are no-op or partially no-op until a downstream story provides the real consumer.

| Gate | Story | No-op reason | Re-trigger |
|---|---|---|---|
| `directory-abuse-rules` (anti-enumeration, FR-91/FR-92) | **11a.3** | `packages/contracts/public-pages/directory-abuse-rules.yaml` parsed by `parseDirectoryAbuseRules` (strict, ⛔ throws — a malformed file NEVER degrades to "no rules"), READ BY THE ROUTE at wiring time, evaluated per request | ⭐ **NOT A CI GATE — a RUNTIME detection surface**, listed here so it is not mistaken for one. Three ACTIVE rules (`high_volume_lookups`, `deep_crawl`, `rapid_pagination`) evaluate every request to the public Member Directory and emit a §1.5 audit line through `deps.auditSink.emit` under a **newly minted** `AuthAuditEventType` (`directory.abuse_suspected`) — ⛔ never a borrowed `abuse.honeypot`, which would corrupt that signal and break `security-headers.spec.ts`'s exact-count assertion. A fourth rule (`repeated_district_queries`) is declared `no_subject_yet` **with a required reason and activation trigger**, because this surface ships no filter to count — ⛔ declaring it active with an unreachable predicate would be a rule reporting green forever while detecting nothing. ⚠⚠ **WHAT IT DOES NOT PROVE, and says so in its own README:** the audit line is a **COUNTER, NOT A FORENSIC RECORD** (no column stores query context; it is hashed) · a **warm edge hides scraper traffic from the origin entirely** · the forwarded key is **caller-supplied** under `trustProxy` · the counters are **in-memory and per-instance** (§1.4, no Redis ⇒ effective threshold × instance count) · ⛔ **no console view exists yet**. Parser + detector proven by 17 unit cases, each defect planted independently. | ✅ Active at 11a.3 (runtime detection; ⛔ signal is real and reachable, ⛔ NOT triageable — see §C console deferral) |
| `pii-scrape` (FR-74 Public-vs-Private matrix gate) | 1.16b | ✅ **LIVE-RENDER ENFORCING as of Story 2.5.** The architecture-committed integration spec landed at `apps/public/tests/integration/public-pages/scrape-test.spec.ts` (the root `tests/integration/public-pages/` slot realized inside the `@twt/public` workspace — see root `tests/integration/README.md`). It imports the pure `@twt/contracts` engine and feeds it the REAL Niyamavali render HTML (built from fixture clauses via the pure render module), asserting `detectNakedPii` finds nothing + `evaluateSnapshot` passes, with a negative-control proving teeth. Runs in the `test` CI job on every PR. The FR-74 matrix is still empty (`surfaces: []`) so the tier-leak leg stays a no-op for `niyamavali` until Epic 11a populates it; the naked-PII leg is ACTIVE now. ⭐ **STORY 11a.1 POPULATED THE MATRIX** (8 surfaces, 23 fields, 1 escalation) and armed the tier-leak leg + three committed-source legs. ⭐ **STORY 11a.2 ADDED TWO MORE LEGS** (ruling D3(a), Decision `2026-08-20-141`), both reading the SAME `.astro` sources: **(4) `cache_policy_reconciliation`** — each surface's declared `cache_policy` vs the `Cache-Control` its page actually sets, **fail-closed on absence** (a rendering surface with NO header FAILS; this is what `/blog` shipped for a whole epic while nothing checked). ⛔ It proves what the **ORIGIN EMITS** — nothing about Cloudflare or any edge (not in this repo; contingent on DPDPA review, architecture §5.8a). **(5) `pagination_binding`** — a surface declaring `paginated: true` must have a page that calls `parsePageParams()`. ⚠ **FR-91 is enforced on `apps/public` by NOTHING ELSE**: the Story 1.14 guard walks the committed OpenAPI surface, which Astro routes do not emit — ⛔ do not cite 1.14 as coverage here. ⚠ It proves BINDING, not BEHAVIOUR. All five legs **proven live** at 11a.2 (each planted independently, each exit 1, each reverted). ⭐⛔ **STORY 11a.3 MADE THE `member-directory` TIER-LEAK LEG OPERATIVE — THE "ARMED AND EMPTY" CAVEAT IS RETIRED.** ⚠ This row previously read *"the field set is `[]`; a green check means 'renders no classified field', NOT 'the flagship directory is policed'"*. That was TRUE at 11a.2 and is ⛔ **FALSE NOW**: `/members` renders real member rows, `membersSurfaceFieldIds(model)` returns `['district', 'member_name', 'member_status']`, and `evaluateSnapshot` evaluates the leak rules on the flagship public surface on every run. The committed *"the field set IS empty"* assertion was **REPLACED** (⛔ not deleted) with one asserting the **exact** set, so a silently dropped field fails too — and the discharge is proven by **planting a real leak into a snapshot that passes clean**, ⛔ not by observing that the set is non-empty. ⇒ **`2026-08-19-136` cl.4 — the LAUNCH-BLOCKING clause — is DISCHARGED** (Decision `2026-08-20-143`). ⚠ **The matrix grew to 24 fields** (`member_status`, first classified here; ⛔ attested by the decision-log entry, **not** by an escalation ledger row — a first-time classification is not an escalation and `MatrixEscalationSchema` correctly rejects one). | ✅ **Resolved at Story 2.5** (live-render leg) / **11a.1** (matrix + 3 source legs) / **11a.2** (cache-policy + pagination-binding legs) / ⭐ **11a.3 (tier-leak leg on `member-directory` now OPERATIVE — caveat retired).** |
| `benefit-mechanism` check (a): rule-record tag scan | 1.16d | ✅ **ENFORCING as of Story 2.3.** The seed `packages/domain/seed/niyamavali-v1-clauses.sql` (3 `pool`-tagged clauses) is wired via `rule_sources.seed_globs`; `extractFromSqlInserts` + `validateRuleRecords` now scan 3 real records (every record `pool`; zero `reserve`). `pnpm benefit:check` green WITH teeth. | ✅ **Resolved at Story 2.3.** |
| `benefit-mechanism` check (c): rule-table schema-column find | 1.16d | ✅ **ENFORCING as of Story 2.3.** `clause_versions` is in the drizzle snapshot (`0014_snapshot.json`) carrying the `benefit_mechanism` NOT-NULL enum column; `scanRuleTableColumns` finds + validates it. | ✅ **Resolved at Story 2.3.** |
| `friction-budget` — `critical_render_path_ms` metric | 1.16a | Placeholder in `friction-budget.yaml` under `deferred_metrics`. No throttled-Lighthouse harness in CI. | **EXPLICITLY DEFERRED at Story 2.5 (Decision 2026-06-20-055(b), recorded in `friction-budget.yaml`):** the Astro shell landed WITHOUT a Lighthouse harness; the **static byte proxies acquired teeth instead** (`member-public-web` js_bundle_bytes + page_weight_bytes — baselines committed). Live-timing harness was re-targeted to Epic 11a (first additional public surface). ⚠ **THAT TRIGGER FIRED AT STORY 11a.2** when `/members` shipped, and was **answered in writing** (ruling D5(a), Decision `2026-08-20-141`): **RE-DEFERRED**, because a device-throttled Lighthouse-CI harness is separate INFRASTRUCTURE (runner profile, throttling config, results store, flake policy), ⛔ not a change to a script the shell story already touches. ⚠ **THAT TRIGGER THEN FIRED AGAIN AT STORY 11a.3**, and was answered by **PARTIAL DISCHARGE** (ruling D6(a), Decision `2026-08-20-143` cl.6) — ⛔ **not** a fourth consecutive re-deferral on the same reason, which is decay rather than deferral. **CLOSED BY EDIT:** the **dynamic SSR HTML** is now measured for real (`scripts/dynamic-html-weight.mjs`; `/members` = 19 234 B at the FR-91 cap, 306 B/row marginal), which is the roster-scaling quantity the static proxies explicitly do not capture. **STILL DEFERRED:** the device-throttled Lighthouse-CI **timing** harness — with a **NEW reason**, ⛔ not a restatement: *the dominant, data-scaling quantity on this surface is now known and measured; a timing harness answers a DEVICE-and-NETWORK question that cannot be answered meaningfully until there is a real deployed origin with a real edge decision behind it, and that decision is itself blocked on DPDPA legal review (§5.8a).* **NEW trigger: the first deployment to a real origin with a CDN/edge configured** — the same event that re-triggers the abuse-rules edge dependency. ⛔ **NOT** "the next public surface story", the open-ended shape that let this metric slip at 2.6, 10.5, 11a.2 and 11a.3. ⛔ Neither the per-route static attribution (11a.2) nor the dynamic-HTML measurement (11a.3) is a critical-render-path TIMING measurement, and neither may be cited as one. |
| Per-tag bundle-subsetting CI test (D4-1.4) | 1.4 | No member/admin JS bundle in `pnpm turbo run build` yet. | **Epic 2 / later** — member/admin bundles land with public Astro + member web/native. |
| `tone-review` publish gate — **Type: runtime Fastify pre-handler, NOT a CI lint** (tracked here for consumer-wiring visibility, not as a `pnpm` CI job) | 2.2 | No member-visible-copy publish endpoint exists yet. `apps/api/src/modules/tone-review/` (`requireToneReviewSignoff` + the dedicated `tone_review.*` audit seam) is installed + unit-tested but mounted by **no route** at 2.2; teeth proven with a stub resolver + fake audit sink (no live DB). | ✅ **ENFORCING as of Story 2.4** — `requireToneReviewSignoff` is mounted on the Niyamavali publish route's `preHandler` chain; the sign-off resolver (`resolveDraftSignoff` — content-bound) + durable persistence (`clause_drafts.tone_review_*`) are supplied. Proven with a live-DB API integration suite (publish-without-signoff → 409 `tone_review.required`; edit-after-signoff → 409). Future surfaces (News/Blog · T&C · push templates · helpdesk macros) re-trigger in their owning stories. |

**Note on `benefit-mechanism` checks (Story 2.3 update):** Check (b) (enum-definition cross-check, `BenefitMechanism` z.enum === `benefit-mechanism.yaml` `mechanisms`) has had teeth since 1.16d. Checks (a) (rule-record tag scan) and (c) (rule-table schema-column) **flipped from no-op to ENFORCING at Story 2.3** — the `clause_versions` table + the 3-record `pool`-tagged seed landed. **All three checks now have teeth** (the two rows above are retained for the audit trail of the flip).

**Note on the `tone-review` row (Story 2.2):** unlike every other row in this table, this is **not** a `pnpm` CI lint — it is a *runtime* Fastify publish guard (tone review is a human-judgment check that cannot be a static lint; the *automatable* vocabulary/tone/numeral subset is already the Story 1.17 `microcopy` CI gate in Category A). It is listed here because it shares the Category-B lifecycle: installed green-by-construction at 2.2, **full teeth on consumer wiring achieved at Story 2.4** (mounted on the Niyamavali publish route). The human process it enforces is `docs/tone-guide.md` + `docs/tone-review-checklist.md` (Story 2.2). Cross-ref: `_bmad-output/implementation-artifacts/2-2-tone-guide-vocabulary-enforcement-process.md`.

---

## Category C — Forward-Compat Hooks: Wired, Full Teeth on Re-trigger

These items are wired and green; they flip from narrow-scope to full enforcement when the consumer story lands.

| Hook | Story wired | Re-trigger story | What changes at re-trigger |
|---|---|---|---|
| `microcopy` full member-register scan | 1.17 | ✅ **Story 2.5 (done — TEETH)** | ✅ `microcopy.yaml` `scope.copy_globs` populated with the `niyamavali` member-copy locale files (the first member surface, apps/public). The member-register vocabulary (user/customer/donor → canonical) + Devanagari-numeral discipline now have teeth on real member copy. `pnpm microcopy:check` green-with-teeth (2 copy files scanned). Register grows surface-by-surface as Epic 11a/11b land. (Retained per audit-trail discipline — collected per Decision 2026-06-20-055 open-follow-up.) |
| `pii-scrape` live-render integration spec | 1.16b | ✅ **Story 2.5 (done — ENFORCING)** | ✅ Realized at `apps/public/tests/integration/public-pages/scrape-test.spec.ts` — feeds the pure engine real Niyamavali render HTML from fixtures; `detectNakedPii` active + negative-control teeth; runs in the `test` CI job every PR. Tier-leak rules re-trigger against the matrix at Epic 11a. (Retained per audit-trail discipline — collected per Decision 2026-06-20-055 open-follow-up.) |
| `schema-diff` + `benefit-mechanism` → full teeth at rule-registry | 1.16c/d | ✅ **Story 2.3 (done — ENFORCING)** | ✅ `clause_versions` table + 3 real `pool` records exist; both the benefit-mechanism tag scan (checks a/c) and the schema-diff non-add guard enforce against real data. `pnpm benefit:check` + `pnpm schema:check` green at Story 2.3. |
| TS-literal seed extractor | 1.16d | ✅ **Story 2.3 (closed)** | ✅ **Closed — not needed.** The Story 2.3 seed lands as `.sql` (`packages/domain/seed/niyamavali-v1-clauses.sql`); the existing `extractFromSqlInserts` extractor covers it. No TS-literal extractor required at 2.3 (see deferred-work.md L78). |

---

## Category D — DB-Enforced Invariants (Runtime, Not CI Gates)

These enforce at runtime regardless of CI status.

| Invariant | Story | Mechanism | Status |
|---|---|---|---|
| `events_log` append-only | 1.3 | Postgres trigger: `DELETE` + `UPDATE` raise exception | ✅ Active in all environments with a DB |
| RBAC fail-closed | 1.8 | Permission-key resolution returns `false` on unknown key; no wildcard grant | ✅ Active |
| RLS on all tenant tables | 1.6 | `FORCE ROW LEVEL SECURITY` + `USING (pariwar_id = current_setting('app.pariwar_id')::uuid)` | ✅ Active; adversarial test in CI |
| Turnstile fail-closed | 1.13 | `noopTurnstileVerifier` (always pass) until `TURNSTILE_SECRET_NAME` is set; real verifier fails closed on network error | ✅ Active (noop until secret provisioned — see §E) |

---

## Category E — Operator-Activation Gated (Authored, Not Applied)

Infrastructure is expressed in code / IaC but the `terraform apply` or activation has not been run. These are NOT gaps — they are the deliberate substrate pattern.

| Gate / Capability | Story | What's authored | Gate condition | Notes |
|---|---|---|---|---|
| **Cloudflare zone / WAF / Turnstile live apply** (D1-1.13) | 1.13 | `infra/cloudflare/` full IaC (WAF rules, bot management, ingress, Turnstile secrets) | ⛔ **DPDPA legal review clearance** (ADR-0010 OPEN) + zone/account provisioned | Do NOT assert DPDPA compliance. Decision 2026-06-20-051 keeps this off Epic 2 critical path. |
| Edge-only ingress guard (D2-1.13) | 1.13 | `infra/cloudflare/origin-ingress.tf` + Fastify guard (header secret / tunnel) | ⛔ Gated on D1-1.13 | Dokploy routes traffic directly; edge-auth guard NOT wired at origin. |
| FR-88 member-facing Turnstile widgets (D3-1.13) | 1.13 | `@twt/edge` verifier primitive + admin widget recipe | Story 2.5 / Epic 11a | Mount widget on signup / claim-filing / helpdesk forms when surfaces exist. |
| GCS off-site mirror cron graduation (D2-1.10 / D3-1.11a) | 1.10/1.11a | Mirror job + integrity cron authored in `apps/jobs` | Operator activation | Cron running in `pg-boss` but mirror destination not provisioned. |
| Multi-env KMS (staging/prod key rings) | 1.5 | `infra/gcp/kms.tf` per-env key rings | Operator activation + `terraform apply` per env | Dev KMS is functional; staging/prod rings gated on apply. |
| Image signing | 1.15 | IaC authored | Operator activation | |
| Prod grants (IAM / GCS write) | 1.15 | IaC authored | Operator activation | |

---

## Summary Table: What Enforces TODAY

| Layer | Enforcing now | No-op / awaiting | Operator-gated |
|---|---|---|---|
| **CI (per PR)** | lint · typecheck · test · build · db-check · contracts-check · crypto-check · tokens-theme-check · friction-budget · schema-diff · microcopy (admin) · governance-boundary · custom-field-governance · integration-tests | pii-scrape · benefit-mechanism (a)(c) · bundle-subsetting · friction-budget critical-render | — |
| **DB runtime** | append-only events_log · RBAC fail-closed · RLS | — | — |
| **Infra / edge** | Dokploy deploy pipeline | — | Cloudflare zone (DPDPA-gated) · KMS staging/prod · image signing · prod grants · GCS mirror |
| **CI floor** | ci:local + pre-push | — | GitHub Actions (suspended) |

---

## References

- Stories 1.16a/b/c/d, 1.17 — gate implementation
- ADR-0010 — Cloudflare / DPDPA posture (OPEN)
- Decision 2026-06-20-051 — Epic 2 posture (Cloudflare off critical path)
- `_bmad-output/implementation-artifacts/deferred-work.md` — D1-1.13, D2-1.13, D3-1.13, D2-1.10, D3-1.11a
- Epic 1 retrospective AI-9 — `epic-1-retro-2026-06-20.md`
