import { Stack } from 'expo-router'

// Contribution route GROUP (Story 8.3, Task 4) — a plain Stack owning the member-facing live-pool
// contributor-list view (`contributors`). Reached from the My Pool card's "View contributors" affordance
// (<ViewContributorsEntry> → router.push). Calm/neutral register — a read-only view of confirmed peers.
export default function ContributionLayout() {
  return <Stack />
}
