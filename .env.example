# Per-workspace environment variables are documented in each workspace's own
# .env.example; this file is the root index. Substantive env-var inventory
# lands in downstream stories.
#
# Story 1.2 — see packages/domain/.env.example for DATABASE_URL +
# GOOGLE_CLOUD_PROJECT + Secret Manager fallback semantics, and
# apps/api/.env.example for the API workspace's local-dev DB path.
