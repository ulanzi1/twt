# src/snapshot-adapters/

**Landing Story: 7.x** — Pool Engine snapshot-version adapters per architecture
§1.6 line 925-934.

Per-version adapters that translate between snapshot versions during the replay
migration. Property-tested per `tests/integration/snapshot-adapters/property.spec.ts`
(Story 7.x). Empty at Story 1.2.
