openapi: 3.1.0
info:
  title: TWT API v1
  version: 0.0.0-substrate
  description: TWT API contract surface — generated from Zod schemas in packages/contracts/. Story 1.4 substrate; substantive routes land at apps/api/ Stories 1.9+.
servers:
  - url: https://twt.local/api/v1
    description: placeholder
components:
  schemas:
    HealthResponse:
      type: object
      properties:
        status:
          type: string
          enum:
            - ok
            - degraded
        timestamp:
          type: string
          format: date-time
      required:
        - status
        - timestamp
      additionalProperties: false
    ErrorResponse:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: string
              minLength: 1
              pattern: \S
            message:
              type: string
            details: {}
            request_id:
              type: string
              format: uuid
          required:
            - code
            - message
            - request_id
          additionalProperties: false
      required:
        - error
      additionalProperties: false
    BrandingBundle:
      type: object
      properties:
        logo_url:
          type: string
          format: uri
        logo_url_dark:
          type: string
          format: uri
        primary_color:
          type: string
          pattern: ^#[0-9a-fA-F]{6}$
        secondary_color:
          type: string
          pattern: ^#[0-9a-fA-F]{6}$
        accent_color:
          type: string
          pattern: ^#[0-9a-fA-F]{6}$
      required:
        - logo_url
        - primary_color
        - secondary_color
      additionalProperties: false
    PariwarPassportResponse:
      type: object
      properties:
        pariwarId:
          type: string
          format: uuid
        displayNameEn:
          type: string
          minLength: 1
        displayNameHi:
          type: string
          minLength: 1
        legalName:
          type: string
          minLength: 1
        trustRegistrationId:
          type:
            - string
            - "null"
        brandingBundle:
          type: object
          properties:
            logo_url:
              type: string
              format: uri
            logo_url_dark:
              type: string
              format: uri
            primary_color:
              type: string
              pattern: ^#[0-9a-fA-F]{6}$
            secondary_color:
              type: string
              pattern: ^#[0-9a-fA-F]{6}$
            accent_color:
              type: string
              pattern: ^#[0-9a-fA-F]{6}$
          required:
            - logo_url
            - primary_color
            - secondary_color
          additionalProperties: false
        localeDefault:
          type: string
          enum: &a3
            - hi
            - en
        createdAt:
          type: string
          format: date-time
        createdBy:
          type:
            - string
            - "null"
          format: uuid
        updatedAt:
          type: string
          format: date-time
      required:
        - pariwarId
        - displayNameEn
        - displayNameHi
        - legalName
        - trustRegistrationId
        - brandingBundle
        - localeDefault
        - createdAt
        - createdBy
        - updatedAt
      additionalProperties: false
    ScopeDimension:
      type: string
      enum: &a1
        - global
        - pariwar
        - state
        - district
        - block
        - self
    PermissionKey:
      type: string
      pattern: ^[a-z_]+\.[a-z_]+$
    PermissionCatalog:
      type: object
      properties:
        catalogVersion:
          type: integer
          exclusiveMinimum: 0
        keys:
          type: array
          items:
            type: string
            pattern: ^[a-z_]+\.[a-z_]+$
      required:
        - catalogVersion
        - keys
      additionalProperties: false
    RoleBundle:
      type: object
      properties:
        role:
          type: string
          enum: &a2
            - super_admin
            - pariwar_admin
            - state_trustee
            - district_admin
            - block_admin
            - finance_officer
            - it_cell
            - media_comms
            - field_worker
            - verifier
            - auditor
            - helpline_operator
        permissions:
          type: array
          items:
            type: string
            pattern: ^[a-z_]+\.[a-z_]+$
        scopeCeiling:
          type: string
          enum: *a1
      required:
        - role
        - permissions
        - scopeCeiling
      additionalProperties: false
    RoleGrant:
      type: object
      properties:
        id:
          type: string
          format: uuid
        userId:
          type: string
          format: uuid
        pariwarId:
          type: string
          format: uuid
        role:
          type: string
          enum: *a2
        scopeDimension:
          type: string
          enum: *a1
        scopeValue:
          type:
            - string
            - "null"
        createdAt:
          type: string
          format: date-time
        createdBy:
          type:
            - string
            - "null"
          format: uuid
      required:
        - id
        - userId
        - pariwarId
        - role
        - scopeDimension
        - scopeValue
        - createdAt
        - createdBy
      additionalProperties: false
    AuditLogEntry:
      type: object
      properties:
        auditId:
          type: string
          format: uuid
        seq:
          type: integer
          minimum: 1
        pariwarId:
          type: string
          format: uuid
        actorId:
          type:
            - string
            - "null"
          format: uuid
        actorRole:
          type:
            - string
            - "null"
          minLength: 1
        action:
          type: string
          minLength: 1
        resourceLocator:
          type: string
          minLength: 1
        requestPayloadHash:
          type: string
          minLength: 1
        responseStatus:
          type: integer
          minimum: 100
          maximum: 599
        prevAuditHash:
          type:
            - string
            - "null"
          minLength: 1
        auditHash:
          type: string
          minLength: 1
        recordedAt:
          type: string
          format: date-time
        traceId:
          type:
            - string
            - "null"
          minLength: 1
      required:
        - auditId
        - seq
        - pariwarId
        - actorId
        - actorRole
        - action
        - resourceLocator
        - requestPayloadHash
        - responseStatus
        - prevAuditHash
        - auditHash
        - recordedAt
        - traceId
      additionalProperties: false
    AuditIntegrityCheckRequest:
      type: object
      properties: {}
      additionalProperties: false
    AuditIntegrityCheckResult:
      type: object
      properties:
        checkId:
          type: string
          format: uuid
        verifiedAt:
          type: string
          format: date-time
        chainValid:
          type: boolean
        startSeq:
          type:
            - integer
            - "null"
          minimum: 1
        startAuditId:
          type:
            - string
            - "null"
          format: uuid
        endSeq:
          type:
            - integer
            - "null"
          minimum: 1
        endAuditId:
          type:
            - string
            - "null"
          format: uuid
        firstBrokenSeq:
          type:
            - integer
            - "null"
          minimum: 1
        firstBrokenAuditId:
          type:
            - string
            - "null"
          format: uuid
        rowsVerified:
          type: integer
          minimum: 0
        verifierActor:
          type: string
          minLength: 1
        triggerSource:
          type: string
          minLength: 1
      required:
        - checkId
        - verifiedAt
        - chainValid
        - startSeq
        - startAuditId
        - endSeq
        - endAuditId
        - firstBrokenSeq
        - firstBrokenAuditId
        - rowsVerified
        - verifierActor
        - triggerSource
      additionalProperties: false
    AuditIntegrityCheckListItem:
      type: object
      properties:
        checkId:
          type: string
          format: uuid
        verifiedAt:
          type: string
          format: date-time
        chainValid:
          type: boolean
        startSeq:
          type:
            - integer
            - "null"
          minimum: 1
        startAuditId:
          type:
            - string
            - "null"
          format: uuid
        endSeq:
          type:
            - integer
            - "null"
          minimum: 1
        endAuditId:
          type:
            - string
            - "null"
          format: uuid
        firstBrokenSeq:
          type:
            - integer
            - "null"
          minimum: 1
        firstBrokenAuditId:
          type:
            - string
            - "null"
          format: uuid
        rowsVerified:
          type: integer
          minimum: 0
        verifierActor:
          type: string
          minLength: 1
        triggerSource:
          type: string
          minLength: 1
        acknowledgement:
          type:
            - object
            - "null"
          properties:
            acknowledgementId:
              type: string
              format: uuid
            checkId:
              type: string
              format: uuid
            acknowledgedAt:
              type: string
              format: date-time
            acknowledgedBy:
              type: string
              format: uuid
            ticketRef:
              type: string
              minLength: 1
              maxLength: 512
          required:
            - acknowledgementId
            - checkId
            - acknowledgedAt
            - acknowledgedBy
            - ticketRef
          additionalProperties: false
      required:
        - checkId
        - verifiedAt
        - chainValid
        - startSeq
        - startAuditId
        - endSeq
        - endAuditId
        - firstBrokenSeq
        - firstBrokenAuditId
        - rowsVerified
        - verifierActor
        - triggerSource
        - acknowledgement
      additionalProperties: false
    AuditIntegrityCheckList:
      type: array
      items:
        type: object
        properties:
          checkId:
            type: string
            format: uuid
          verifiedAt:
            type: string
            format: date-time
          chainValid:
            type: boolean
          startSeq:
            type:
              - integer
              - "null"
            minimum: 1
          startAuditId:
            type:
              - string
              - "null"
            format: uuid
          endSeq:
            type:
              - integer
              - "null"
            minimum: 1
          endAuditId:
            type:
              - string
              - "null"
            format: uuid
          firstBrokenSeq:
            type:
              - integer
              - "null"
            minimum: 1
          firstBrokenAuditId:
            type:
              - string
              - "null"
            format: uuid
          rowsVerified:
            type: integer
            minimum: 0
          verifierActor:
            type: string
            minLength: 1
          triggerSource:
            type: string
            minLength: 1
          acknowledgement:
            type:
              - object
              - "null"
            properties:
              acknowledgementId:
                type: string
                format: uuid
              checkId:
                type: string
                format: uuid
              acknowledgedAt:
                type: string
                format: date-time
              acknowledgedBy:
                type: string
                format: uuid
              ticketRef:
                type: string
                minLength: 1
                maxLength: 512
            required:
              - acknowledgementId
              - checkId
              - acknowledgedAt
              - acknowledgedBy
              - ticketRef
            additionalProperties: false
        required:
          - checkId
          - verifiedAt
          - chainValid
          - startSeq
          - startAuditId
          - endSeq
          - endAuditId
          - firstBrokenSeq
          - firstBrokenAuditId
          - rowsVerified
          - verifierActor
          - triggerSource
          - acknowledgement
        additionalProperties: false
    AuditIntegrityAcknowledgeRequest:
      type: object
      properties:
        ticketRef:
          type: string
          minLength: 1
          maxLength: 512
      required:
        - ticketRef
      additionalProperties: false
    AuditIntegrityAcknowledgement:
      type: object
      properties:
        acknowledgementId:
          type: string
          format: uuid
        checkId:
          type: string
          format: uuid
        acknowledgedAt:
          type: string
          format: date-time
        acknowledgedBy:
          type: string
          format: uuid
        ticketRef:
          type: string
          minLength: 1
          maxLength: 512
      required:
        - acknowledgementId
        - checkId
        - acknowledgedAt
        - acknowledgedBy
        - ticketRef
      additionalProperties: false
    AddPariwarRequest:
      type: object
      properties:
        displayNameEn:
          type: string
          minLength: 1
        displayNameHi:
          type: string
          minLength: 1
        legalName:
          type: string
          minLength: 1
        trustRegistrationId:
          type:
            - string
            - "null"
          minLength: 1
        localeDefault:
          type: string
          enum: *a3
        brandingBundle:
          type: object
          properties:
            logo_url:
              type: string
              format: uri
            logo_url_dark:
              type: string
              format: uri
            primary_color:
              type: string
              pattern: ^#[0-9a-fA-F]{6}$
            secondary_color:
              type: string
              pattern: ^#[0-9a-fA-F]{6}$
            accent_color:
              type: string
              pattern: ^#[0-9a-fA-F]{6}$
          required:
            - logo_url
            - primary_color
            - secondary_color
          additionalProperties: false
      required:
        - displayNameEn
        - displayNameHi
        - legalName
        - localeDefault
        - brandingBundle
      additionalProperties: false
    DeployStatusView:
      type: object
      properties:
        deployId:
          type: string
          minLength: 1
        status:
          type: string
          enum: &a4
            - unknown
            - triggered
            - succeeded
            - failed
        triggeredAt:
          type: string
          format: date-time
        detail:
          type:
            - string
            - "null"
      required:
        - deployId
        - status
        - triggeredAt
      additionalProperties: false
    DeployTriggerResponse:
      type: object
      properties:
        pariwarId:
          type: string
          format: uuid
        pathScope:
          type: string
          minLength: 1
        deploy:
          type: object
          properties:
            deployId:
              type: string
              minLength: 1
            status:
              type: string
              enum: *a4
            triggeredAt:
              type: string
              format: date-time
            detail:
              type:
                - string
                - "null"
          required:
            - deployId
            - status
            - triggeredAt
          additionalProperties: false
      required:
        - pariwarId
        - pathScope
        - deploy
      additionalProperties: false
    ProvisionedPariwar:
      type: object
      properties:
        passport:
          type: object
          properties:
            pariwarId:
              type: string
              format: uuid
            displayNameEn:
              type: string
              minLength: 1
            displayNameHi:
              type: string
              minLength: 1
            legalName:
              type: string
              minLength: 1
            trustRegistrationId:
              type:
                - string
                - "null"
            brandingBundle:
              type: object
              properties:
                logo_url:
                  type: string
                  format: uri
                logo_url_dark:
                  type: string
                  format: uri
                primary_color:
                  type: string
                  pattern: ^#[0-9a-fA-F]{6}$
                secondary_color:
                  type: string
                  pattern: ^#[0-9a-fA-F]{6}$
                accent_color:
                  type: string
                  pattern: ^#[0-9a-fA-F]{6}$
              required:
                - logo_url
                - primary_color
                - secondary_color
              additionalProperties: false
            localeDefault:
              type: string
              enum: *a3
            createdAt:
              type: string
              format: date-time
            createdBy:
              type:
                - string
                - "null"
              format: uuid
            updatedAt:
              type: string
              format: date-time
          required:
            - pariwarId
            - displayNameEn
            - displayNameHi
            - legalName
            - trustRegistrationId
            - brandingBundle
            - localeDefault
            - createdAt
            - createdBy
            - updatedAt
          additionalProperties: false
        pathScope:
          type: string
          minLength: 1
        latestDeploy:
          type:
            - object
            - "null"
          properties:
            deployId:
              type: string
              minLength: 1
            status:
              type: string
              enum: *a4
            triggeredAt:
              type: string
              format: date-time
            detail:
              type:
                - string
                - "null"
          required:
            - deployId
            - status
            - triggeredAt
          additionalProperties: false
      required:
        - passport
        - pathScope
        - latestDeploy
      additionalProperties: false
    ProvisioningStatusList:
      type: array
      items:
        type: object
        properties:
          passport:
            type: object
            properties:
              pariwarId:
                type: string
                format: uuid
              displayNameEn:
                type: string
                minLength: 1
              displayNameHi:
                type: string
                minLength: 1
              legalName:
                type: string
                minLength: 1
              trustRegistrationId:
                type:
                  - string
                  - "null"
              brandingBundle:
                type: object
                properties:
                  logo_url:
                    type: string
                    format: uri
                  logo_url_dark:
                    type: string
                    format: uri
                  primary_color:
                    type: string
                    pattern: ^#[0-9a-fA-F]{6}$
                  secondary_color:
                    type: string
                    pattern: ^#[0-9a-fA-F]{6}$
                  accent_color:
                    type: string
                    pattern: ^#[0-9a-fA-F]{6}$
                required:
                  - logo_url
                  - primary_color
                  - secondary_color
                additionalProperties: false
              localeDefault:
                type: string
                enum: *a3
              createdAt:
                type: string
                format: date-time
              createdBy:
                type:
                  - string
                  - "null"
                format: uuid
              updatedAt:
                type: string
                format: date-time
            required:
              - pariwarId
              - displayNameEn
              - displayNameHi
              - legalName
              - trustRegistrationId
              - brandingBundle
              - localeDefault
              - createdAt
              - createdBy
              - updatedAt
            additionalProperties: false
          pathScope:
            type: string
            minLength: 1
          latestDeploy:
            type:
              - object
              - "null"
            properties:
              deployId:
                type: string
                minLength: 1
              status:
                type: string
                enum: *a4
              triggeredAt:
                type: string
                format: date-time
              detail:
                type:
                  - string
                  - "null"
            required:
              - deployId
              - status
              - triggeredAt
            additionalProperties: false
        required:
          - passport
          - pathScope
          - latestDeploy
        additionalProperties: false
    ClauseVersionResponse:
      type: object
      properties:
        clauseVersionId:
          type: string
          format: uuid
        clauseId:
          type: string
          pattern: ^niy\.[a-z0-9]+(?:-[a-z0-9]+)*\.[a-z0-9]+(?:-[a-z0-9]+)*(?:\.[a-z0-9]+(?:-[a-z0-9]+)*)?$
        pariwarId:
          type: string
          format: uuid
        version:
          type: integer
          exclusiveMinimum: 0
        effectiveDate:
          type: string
          format: date-time
        payload:
          type: object
          additionalProperties: {}
        benefitMechanism:
          type: string
          enum: &a5
            - pool
            - reserve
        predecessorClauseIds:
          type: array
          items:
            type: string
            format: uuid
        supersededByVersion:
          type:
            - string
            - "null"
          format: uuid
        deprecatedAt:
          type:
            - string
            - "null"
          format: date-time
        authoredByActor:
          type:
            - string
            - "null"
          format: uuid
        authoredAt:
          type: string
          format: date-time
        auditId:
          type:
            - string
            - "null"
          format: uuid
      required:
        - clauseVersionId
        - clauseId
        - pariwarId
        - version
        - effectiveDate
        - payload
        - benefitMechanism
        - predecessorClauseIds
        - supersededByVersion
        - deprecatedAt
        - authoredByActor
        - authoredAt
        - auditId
      additionalProperties: false
    NiyamavaliAmendmentResponse:
      type: object
      properties:
        amendmentId:
          type: string
          format: uuid
        pariwarId:
          type: string
          format: uuid
        fromClauseVersionId:
          type: string
          format: uuid
        toClauseVersionId:
          type: string
          format: uuid
        diffDocument:
          type: object
          properties:
            added:
              type: object
              additionalProperties: {}
            removed:
              type: object
              additionalProperties: {}
            changed:
              type: object
              additionalProperties:
                type: object
                properties:
                  from: {}
                  to: {}
          required:
            - added
            - removed
            - changed
          additionalProperties: false
        affectedMemberScope:
          oneOf:
            - type: object
              properties:
                kind:
                  type: string
                  enum:
                    - all_members
              required:
                - kind
              additionalProperties: false
            - type: object
              properties:
                kind:
                  type: string
                  enum:
                    - past_lockin
              required:
                - kind
              additionalProperties: false
            - type: object
              properties:
                kind:
                  type: string
                  enum:
                    - rule_subclause
                clause_id:
                  type: string
                  pattern: ^niy\.[a-z0-9]+(?:-[a-z0-9]+)*\.[a-z0-9]+(?:-[a-z0-9]+)*(?:\.[a-z0-9]+(?:-[a-z0-9]+)*)?$
                subclause:
                  type: string
                  minLength: 1
              required:
                - kind
                - clause_id
                - subclause
              additionalProperties: false
            - type: object
              properties:
                kind:
                  type: string
                  enum:
                    - named_cohort
                definition:
                  type: string
                  minLength: 1
              required:
                - kind
                - definition
              additionalProperties: false
        createdAt:
          type: string
          format: date-time
        auditId:
          type:
            - string
            - "null"
          format: uuid
      required:
        - amendmentId
        - pariwarId
        - fromClauseVersionId
        - toClauseVersionId
        - diffDocument
        - affectedMemberScope
        - createdAt
        - auditId
      additionalProperties: false
    ClauseDraftResponse:
      type: object
      properties:
        draftId:
          type: string
          format: uuid
        pariwarId:
          type: string
          format: uuid
        clauseId:
          type: string
          pattern: ^niy\.[a-z0-9]+(?:-[a-z0-9]+)*\.[a-z0-9]+(?:-[a-z0-9]+)*(?:\.[a-z0-9]+(?:-[a-z0-9]+)*)?$
        operation:
          type: string
          enum:
            - create
            - amend
        payload:
          type: object
          additionalProperties: {}
        effectiveDate:
          type: string
          format: date-time
        benefitMechanism:
          type: string
          enum: *a5
        affectedMemberScope:
          oneOf:
            - type: object
              properties:
                kind:
                  type: string
                  enum:
                    - all_members
              required:
                - kind
              additionalProperties: false
            - type: object
              properties:
                kind:
                  type: string
                  enum:
                    - past_lockin
              required:
                - kind
              additionalProperties: false
            - type: object
              properties:
                kind:
                  type: string
                  enum:
                    - rule_subclause
                clause_id:
                  type: string
                  pattern: ^niy\.[a-z0-9]+(?:-[a-z0-9]+)*\.[a-z0-9]+(?:-[a-z0-9]+)*(?:\.[a-z0-9]+(?:-[a-z0-9]+)*)?$
                subclause:
                  type: string
                  minLength: 1
              required:
                - kind
                - clause_id
                - subclause
              additionalProperties: false
            - type: object
              properties:
                kind:
                  type: string
                  enum:
                    - named_cohort
                definition:
                  type: string
                  minLength: 1
              required:
                - kind
                - definition
              additionalProperties: false
            - type: "null"
        status:
          type: string
          enum: &a9
            - draft
            - in_review
            - signed_off
            - published
            - discarded
        authoredByActor:
          type: string
          format: uuid
        toneReviewedBy:
          type:
            - string
            - "null"
          format: uuid
        toneReviewedAt:
          type:
            - string
            - "null"
          format: date-time
        toneReviewContentHash:
          type:
            - string
            - "null"
        publishedClauseVersionId:
          type:
            - string
            - "null"
          format: uuid
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        auditId:
          type:
            - string
            - "null"
          format: uuid
      required:
        - draftId
        - pariwarId
        - clauseId
        - operation
        - payload
        - effectiveDate
        - benefitMechanism
        - affectedMemberScope
        - status
        - authoredByActor
        - toneReviewedBy
        - toneReviewedAt
        - toneReviewContentHash
        - publishedClauseVersionId
        - createdAt
        - updatedAt
        - auditId
      additionalProperties: false
    CreateDraftBody:
      oneOf:
        - type: object
          properties:
            operation:
              type: string
              enum:
                - create
            clauseId:
              type: string
              pattern: ^niy\.[a-z0-9]+(?:-[a-z0-9]+)*\.[a-z0-9]+(?:-[a-z0-9]+)*(?:\.[a-z0-9]+(?:-[a-z0-9]+)*)?$
            payload:
              type: object
              additionalProperties: {}
            effectiveDate:
              type: string
              format: date-time
            benefitMechanism:
              type: string
              enum: *a5
          required:
            - operation
            - clauseId
            - payload
            - effectiveDate
            - benefitMechanism
          additionalProperties: false
        - type: object
          properties:
            operation:
              type: string
              enum:
                - amend
            clauseId:
              type: string
              pattern: ^niy\.[a-z0-9]+(?:-[a-z0-9]+)*\.[a-z0-9]+(?:-[a-z0-9]+)*(?:\.[a-z0-9]+(?:-[a-z0-9]+)*)?$
            payload:
              type: object
              additionalProperties: {}
            effectiveDate:
              type: string
              format: date-time
            affectedMemberScope:
              oneOf:
                - type: object
                  properties:
                    kind:
                      type: string
                      enum:
                        - all_members
                  required:
                    - kind
                  additionalProperties: false
                - type: object
                  properties:
                    kind:
                      type: string
                      enum:
                        - past_lockin
                  required:
                    - kind
                  additionalProperties: false
                - type: object
                  properties:
                    kind:
                      type: string
                      enum:
                        - rule_subclause
                    clause_id:
                      type: string
                      pattern: ^niy\.[a-z0-9]+(?:-[a-z0-9]+)*\.[a-z0-9]+(?:-[a-z0-9]+)*(?:\.[a-z0-9]+(?:-[a-z0-9]+)*)?$
                    subclause:
                      type: string
                      minLength: 1
                  required:
                    - kind
                    - clause_id
                    - subclause
                  additionalProperties: false
                - type: object
                  properties:
                    kind:
                      type: string
                      enum:
                        - named_cohort
                    definition:
                      type: string
                      minLength: 1
                  required:
                    - kind
                    - definition
                  additionalProperties: false
            benefitMechanism:
              type: string
              enum: *a5
          required:
            - operation
            - clauseId
            - payload
            - effectiveDate
            - affectedMemberScope
          additionalProperties: false
    UpdateClauseDraftRequest:
      type: object
      properties:
        payload:
          type: object
          additionalProperties: {}
        effectiveDate:
          type: string
          format: date-time
        benefitMechanism:
          type: string
          enum: *a5
        affectedMemberScope:
          oneOf:
            - type: object
              properties:
                kind:
                  type: string
                  enum:
                    - all_members
              required:
                - kind
              additionalProperties: false
            - type: object
              properties:
                kind:
                  type: string
                  enum:
                    - past_lockin
              required:
                - kind
              additionalProperties: false
            - type: object
              properties:
                kind:
                  type: string
                  enum:
                    - rule_subclause
                clause_id:
                  type: string
                  pattern: ^niy\.[a-z0-9]+(?:-[a-z0-9]+)*\.[a-z0-9]+(?:-[a-z0-9]+)*(?:\.[a-z0-9]+(?:-[a-z0-9]+)*)?$
                subclause:
                  type: string
                  minLength: 1
              required:
                - kind
                - clause_id
                - subclause
              additionalProperties: false
            - type: object
              properties:
                kind:
                  type: string
                  enum:
                    - named_cohort
                definition:
                  type: string
                  minLength: 1
              required:
                - kind
                - definition
              additionalProperties: false
            - type: "null"
      additionalProperties: false
    ToneReviewSignoffRequest:
      type: object
      properties:
        confirm:
          type: boolean
          enum:
            - true
      required:
        - confirm
      additionalProperties: false
    DiffPreviewResponse:
      type: object
      properties:
        structuredDiff:
          type: object
          properties:
            added:
              type: object
              additionalProperties: {}
            removed:
              type: object
              additionalProperties: {}
            changed:
              type: object
              additionalProperties:
                type: object
                properties:
                  from: {}
                  to: {}
          required:
            - added
            - removed
            - changed
          additionalProperties: false
        renderedDiff:
          type: array
          items:
            type: object
            properties:
              field:
                type: string
              before:
                type:
                  - string
                  - "null"
              after:
                type:
                  - string
                  - "null"
              changed:
                type: boolean
            required:
              - field
              - before
              - after
              - changed
            additionalProperties: false
      required:
        - structuredDiff
        - renderedDiff
      additionalProperties: false
    PublishClauseResponse:
      type: object
      properties:
        clauseVersionId:
          type: string
          format: uuid
        clauseId:
          type: string
          pattern: ^niy\.[a-z0-9]+(?:-[a-z0-9]+)*\.[a-z0-9]+(?:-[a-z0-9]+)*(?:\.[a-z0-9]+(?:-[a-z0-9]+)*)?$
        version:
          type: integer
          exclusiveMinimum: 0
        auditId:
          type: string
          format: uuid
      required:
        - clauseVersionId
        - clauseId
        - version
        - auditId
      additionalProperties: false
    TcVersionResponse:
      type: object
      properties:
        tcVersionId:
          type: string
          format: uuid
        pariwarId:
          type: string
          format: uuid
        version:
          type: integer
          exclusiveMinimum: 0
        bodyMarkdown:
          type: string
        bodyHtmlRendered:
          type: string
        effectiveFrom:
          type: string
          format: date-time
        effectiveUntil:
          type:
            - string
            - "null"
          format: date-time
        legalReviewStatus:
          type: string
          enum:
            - pending
            - under-review
            - reviewed-with-changes-required
            - approved
            - superseded
        legalReviewerActorId:
          type:
            - string
            - "null"
          format: uuid
        authoredByActor:
          type:
            - string
            - "null"
          format: uuid
        authoredAt:
          type: string
          format: date-time
        auditId:
          type:
            - string
            - "null"
          format: uuid
        pinnedToClauseVersionIds:
          type: array
          items:
            type: string
            format: uuid
      required:
        - tcVersionId
        - pariwarId
        - version
        - bodyMarkdown
        - bodyHtmlRendered
        - effectiveFrom
        - effectiveUntil
        - legalReviewStatus
        - legalReviewerActorId
        - authoredByActor
        - authoredAt
        - auditId
        - pinnedToClauseVersionIds
      additionalProperties: false
    CreateTcVersionRequest:
      type: object
      properties:
        bodyMarkdown:
          type: string
          minLength: 1
        pinnedToClauseVersionIds:
          type: array
          items:
            type: string
            format: uuid
          minItems: 1
        effectiveFrom:
          type: string
          format: date-time
      required:
        - bodyMarkdown
        - pinnedToClauseVersionIds
        - effectiveFrom
      additionalProperties: false
    ApproveTcVersionRequest:
      type: object
      properties:
        confirm:
          type: boolean
          enum:
            - true
      required:
        - confirm
      additionalProperties: false
    LoginRequest:
      type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 12
          maxLength: 512
        turnstileToken:
          type: string
      required:
        - email
        - password
      additionalProperties: false
    LoginResponse:
      type: object
      properties:
        status:
          type: string
          enum:
            - mfa_required
        methods:
          type: array
          items:
            type: string
            enum:
              - webauthn
              - recovery_code
      required:
        - status
        - methods
      additionalProperties: false
    PasskeyRegisterOptionsRequest:
      type: object
      properties:
        enrollmentToken:
          type: string
        deviceLabel:
          type: string
          maxLength: 64
      additionalProperties: false
    PasskeyRegisterVerifyRequest:
      type: object
      properties:
        response:
          type: object
          additionalProperties: {}
        enrollmentToken:
          type: string
        deviceLabel:
          type: string
          maxLength: 64
      required:
        - response
      additionalProperties: false
    PasskeyRegisterVerifyResponse:
      type: object
      properties:
        verified:
          type: boolean
        recoveryCodes:
          type: array
          items:
            type: string
            minLength: 1
      required:
        - verified
      additionalProperties: false
    PasskeyAuthOptionsRequest:
      type: object
      properties: {}
      additionalProperties: false
    PasskeyAuthVerifyRequest:
      type: object
      properties:
        response:
          type: object
          additionalProperties: {}
      required:
        - response
      additionalProperties: false
    PasskeyAuthVerifyResponse:
      type: object
      properties:
        authenticated:
          type: boolean
          enum:
            - true
      required:
        - authenticated
      additionalProperties: false
    RecoveryConsumeRequest:
      type: object
      properties:
        code:
          type: string
          minLength: 1
          maxLength: 64
      required:
        - code
      additionalProperties: false
    RecoveryConsumeResponse:
      type: object
      properties:
        authenticated:
          type: boolean
          enum:
            - true
      required:
        - authenticated
      additionalProperties: false
    PasswordResetRequestRequest:
      type: object
      properties:
        email:
          type: string
          format: email
        turnstileToken:
          type: string
      required:
        - email
      additionalProperties: false
    PasswordResetRequestResponse:
      type: object
      properties:
        sent:
          type: boolean
          enum:
            - true
      required:
        - sent
      additionalProperties: false
    PasswordResetConsumeRequest:
      type: object
      properties:
        token:
          type: string
          minLength: 32
        newPassword:
          type: string
          minLength: 12
          maxLength: 512
      required:
        - token
        - newPassword
      additionalProperties: false
    PasswordResetConsumeResponse:
      type: object
      properties:
        reset:
          type: boolean
          enum:
            - true
        webauthnReenrollmentRequired:
          type: boolean
          enum:
            - true
      required:
        - reset
        - webauthnReenrollmentRequired
      additionalProperties: false
    StepUpRequestRequest:
      type: object
      properties:
        actionContext:
          type: string
          minLength: 1
          maxLength: 128
      required:
        - actionContext
      additionalProperties: false
    StepUpRequestResponse:
      type: object
      properties:
        sent:
          type: boolean
          enum:
            - true
        expiresInSeconds:
          type: integer
          exclusiveMinimum: 0
      required:
        - sent
        - expiresInSeconds
      additionalProperties: false
    StepUpVerifyRequest:
      type: object
      properties:
        otp:
          type: string
          minLength: 6
          maxLength: 8
      required:
        - otp
      additionalProperties: false
    StepUpVerifyResponse:
      type: object
      properties:
        elevated:
          type: boolean
          enum:
            - true
        elevatedUntil:
          type: string
          format: date-time
      required:
        - elevated
        - elevatedUntil
      additionalProperties: false
    SessionResponse:
      type: object
      properties:
        userId:
          type: string
          format: uuid
        nationalGrants:
          type: array
          items:
            type: string
      required:
        - userId
        - nationalGrants
      additionalProperties: false
    MemberOtpRequestRequest:
      type: object
      properties:
        mobile:
          type: string
          minLength: 10
          maxLength: 20
          pattern: ^[+0-9][0-9\s\-()]*$
      required:
        - mobile
      additionalProperties: false
    MemberOtpRequestResponse:
      type: object
      properties:
        sent:
          type: boolean
          enum:
            - true
        expiresInSeconds:
          type: integer
          exclusiveMinimum: 0
      required:
        - sent
        - expiresInSeconds
      additionalProperties: false
    MemberFullSession:
      type: object
      properties:
        sessionType:
          type: string
          enum:
            - full_session
        accessToken:
          type: string
          minLength: 1
        accessTokenExpiresAt:
          type: string
          format: date-time
        refreshToken:
          type: string
          minLength: 1
        deviceId:
          type: string
          minLength: 1
        memberId:
          type: string
          format: uuid
        pariwarId:
          type: string
          format: uuid
        droppedDevice:
          type: object
          properties:
            deviceId:
              type: string
              minLength: 1
            deviceLabel:
              type: string
              minLength: 1
            boundAt:
              type: string
              format: date-time
          required:
            - deviceId
            - boundAt
          additionalProperties: false
      required:
        - sessionType
        - accessToken
        - accessTokenExpiresAt
        - refreshToken
        - deviceId
        - memberId
        - pariwarId
      additionalProperties: false
    MemberOtpVerifyRequest:
      type: object
      properties:
        mobile:
          type: string
          minLength: 10
          maxLength: 20
          pattern: ^[+0-9][0-9\s\-()]*$
        otp:
          type: string
          minLength: 6
          maxLength: 8
        deviceId:
          type: string
          minLength: 1
          maxLength: 256
        deviceLabel:
          type: string
          minLength: 1
          maxLength: 128
      required:
        - mobile
        - otp
        - deviceId
      additionalProperties: false
    MemberOtpVerifyResponse:
      oneOf:
        - type: object
          properties:
            sessionType:
              type: string
              enum:
                - full_session
            accessToken:
              type: string
              minLength: 1
            accessTokenExpiresAt:
              type: string
              format: date-time
            refreshToken:
              type: string
              minLength: 1
            deviceId:
              type: string
              minLength: 1
            memberId:
              type: string
              format: uuid
            pariwarId:
              type: string
              format: uuid
            droppedDevice:
              type: object
              properties:
                deviceId:
                  type: string
                  minLength: 1
                deviceLabel:
                  type: string
                  minLength: 1
                boundAt:
                  type: string
                  format: date-time
              required:
                - deviceId
                - boundAt
              additionalProperties: false
          required:
            - sessionType
            - accessToken
            - accessTokenExpiresAt
            - refreshToken
            - deviceId
            - memberId
            - pariwarId
          additionalProperties: false
        - type: object
          properties:
            sessionType:
              type: string
              enum:
                - pariwar_select
            memberships:
              type: array
              items:
                type: object
                properties:
                  memberId:
                    type: string
                    format: uuid
                  pariwarId:
                    type: string
                    format: uuid
                  pariwarName:
                    type: string
                required:
                  - memberId
                  - pariwarId
                  - pariwarName
                additionalProperties: false
            selectToken:
              type: string
              minLength: 1
          required:
            - sessionType
            - memberships
            - selectToken
          additionalProperties: false
        - type: object
          properties:
            sessionType:
              type: string
              enum:
                - signup_continuation
            signupContinuationToken:
              type: string
              minLength: 1
            expiresAt:
              type: string
              format: date-time
          required:
            - sessionType
            - signupContinuationToken
            - expiresAt
          additionalProperties: false
    MemberSelectPariwarRequest:
      type: object
      properties:
        selectToken:
          type: string
          minLength: 1
        pariwarId:
          type: string
          format: uuid
      required:
        - selectToken
        - pariwarId
      additionalProperties: false
    MemberTokenRefreshRequest:
      type: object
      properties:
        refreshToken:
          type: string
          minLength: 1
      required:
        - refreshToken
      additionalProperties: false
    MemberStepUpRequestRequest:
      type: object
      properties:
        actionContext:
          type: string
          minLength: 1
          maxLength: 128
      required:
        - actionContext
      additionalProperties: false
    MemberStepUpRequestResponse:
      type: object
      properties:
        sent:
          type: boolean
          enum:
            - true
        expiresInSeconds:
          type: integer
          exclusiveMinimum: 0
      required:
        - sent
        - expiresInSeconds
      additionalProperties: false
    MemberStepUpVerifyRequest:
      type: object
      properties:
        otp:
          type: string
          minLength: 6
          maxLength: 8
      required:
        - otp
      additionalProperties: false
    MemberStepUpVerifyResponse:
      type: object
      properties:
        elevated:
          type: boolean
          enum:
            - true
        elevatedUntil:
          type: string
          format: date-time
      required:
        - elevated
        - elevatedUntil
      additionalProperties: false
    KycInitiateResponse:
      type: object
      properties:
        transactionId:
          type: string
          minLength: 1
        authorizationUrl:
          type: string
          format: uri
        expiresAt:
          type: string
          format: date-time
      required:
        - transactionId
        - authorizationUrl
        - expiresAt
      additionalProperties: false
    KycCallbackRequest:
      type: object
      properties:
        state:
          type: string
          minLength: 1
        code:
          type: string
          minLength: 1
      required:
        - state
        - code
      additionalProperties: false
    KycConfirmRequest:
      type: object
      properties:
        transactionId:
          type: string
          minLength: 1
      required:
        - transactionId
      additionalProperties: false
    KycManualSubmitRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        dob:
          type: string
          minLength: 1
          maxLength: 40
        photo:
          type: string
          maxLength: 7000000
          pattern: ^data:image\/(jpeg|jpg|png);base64,
      required:
        - name
        - dob
      additionalProperties: false
    KycProfileSummaryResponse:
      type: object
      properties:
        name:
          type: string
        dob:
          type: string
        aadhaarMaskedId:
          type:
            - string
            - "null"
        verificationStrength:
          type: string
          enum:
            - aadhaar_kyc
            - self_declared
            - unverified
        photoPresent:
          type: boolean
      required:
        - name
        - dob
        - aadhaarMaskedId
        - verificationStrength
        - photoPresent
      additionalProperties: false
    KycStatusResponse:
      type: object
      properties:
        transactionStatus:
          type: string
          enum:
            - pending
            - verified
            - failed
            - expired
        memberKycState:
          type: string
          enum:
            - none
            - digilocker_verified
            - manual_pending
        lifecycleState:
          type: string
          enum:
            - pending-kyc
            - pending-fee
            - pending-valid
            - lock-in
            - active
            - active-in-grace
            - lapsed-unpaid
            - withdrawn
            - anonymized
        manualFallbackEnabled:
          type: boolean
      required:
        - memberKycState
        - lifecycleState
        - manualFallbackEnabled
      additionalProperties: false
    NomineeDeclareRequest:
      type: object
      properties:
        nominees:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
                minLength: 1
                maxLength: 200
              relationship:
                type: string
                enum: &a6
                  - spouse
                  - child
                  - parent
                  - sibling
                  - other
              mobile:
                type: string
                minLength: 10
                maxLength: 20
                pattern: ^[+0-9][0-9\s\-()]*$
              address:
                type: string
                minLength: 1
                maxLength: 500
            required:
              - name
              - relationship
              - mobile
            additionalProperties: false
          minItems: 1
          maxItems: 2
      required:
        - nominees
      additionalProperties: false
    NomineeStatusResponse:
      type: object
      properties:
        nominees:
          type: array
          items:
            type: object
            properties:
              rank:
                anyOf:
                  - type: number
                    enum:
                      - 1
                  - type: number
                    enum:
                      - 2
              relationship:
                type: string
                enum: *a6
              splitPct:
                anyOf:
                  - type: number
                    enum:
                      - 100
                  - type: number
                    enum:
                      - 75
                  - type: number
                    enum:
                      - 25
              mobilePresent:
                type: boolean
              addressPresent:
                type: boolean
            required:
              - rank
              - relationship
              - splitPct
              - mobilePresent
              - addressPresent
            additionalProperties: false
      required:
        - nominees
      additionalProperties: false
    MedicalDiscloseRequest:
      type: object
      properties:
        conditionCodes:
          type: array
          items:
            type: string
            minLength: 1
            maxLength: 64
          maxItems: 50
        additionalContext:
          type: string
          minLength: 1
          maxLength: 2000
        imaListVersion:
          type: string
          minLength: 1
          maxLength: 128
        acknowledged:
          type: boolean
          enum:
            - true
        ackLocale:
          type: string
          enum: &a7
            - en
            - hi
      required:
        - conditionCodes
        - imaListVersion
        - acknowledged
        - ackLocale
      additionalProperties: false
    MedicalDisclosureStatusResponse:
      type: object
      properties:
        latest:
          type:
            - object
            - "null"
          properties:
            disclosedAt:
              type: string
              format: date-time
            imaListVersion:
              type: string
            conditionCount:
              type: integer
              minimum: 0
            hasAdditionalContext:
              type: boolean
            ackLocale:
              type: string
              enum: *a7
          required:
            - disclosedAt
            - imaListVersion
            - conditionCount
            - hasAdditionalContext
            - ackLocale
          additionalProperties: false
        historyCount:
          type: integer
          minimum: 0
      required:
        - latest
        - historyCount
      additionalProperties: false
    ImaListResponse:
      type: object
      properties:
        version:
          type: string
        conditions:
          type: array
          items:
            type: object
            properties:
              code:
                type: string
                minLength: 1
              labelEn:
                type: string
                minLength: 1
              labelHi:
                type: string
                minLength: 1
            required:
              - code
              - labelEn
              - labelHi
            additionalProperties: false
        ackText:
          type: object
          properties:
            en:
              type: string
              minLength: 1
            hi:
              type: string
              minLength: 1
          required:
            - en
            - hi
          additionalProperties: false
      required:
        - version
        - conditions
        - ackText
      additionalProperties: false
  parameters: {}
paths:
  /api/v1/_meta/health:
    get:
      summary: Service health probe
      description: Substrate-proof endpoint authored at Story 1.4. Production /_meta/health lives at apps/api/ per Story 1.9+.
      tags:
        - _meta
      responses:
        "200":
          description: Service is reachable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
        "503":
          description: Service is degraded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/login:
    post:
      summary: Admin login — first factor (email + password)
      tags:
        - admin-auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/passkey/register/options:
    post:
      summary: WebAuthn enrollment — generate registration options
      tags:
        - admin-auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasskeyRegisterOptionsRequest"
      responses:
        "200":
          description: WebAuthn PublicKeyCredentialCreationOptionsJSON (provider-controlled)
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Enrollment not authorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Device cap reached
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/passkey/register/verify:
    post:
      summary: WebAuthn enrollment — verify + persist the credential
      tags:
        - admin-auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasskeyRegisterVerifyRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasskeyRegisterVerifyResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Enrollment denied/failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Device cap reached
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/passkey/authenticate/options:
    post:
      summary: WebAuthn second factor — generate authentication options
      tags:
        - admin-auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasskeyAuthOptionsRequest"
      responses:
        "200":
          description: WebAuthn PublicKeyCredentialRequestOptionsJSON (provider-controlled)
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: No login in progress
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: No passkey enrolled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/passkey/authenticate/verify:
    post:
      summary: WebAuthn second factor — verify the assertion
      tags:
        - admin-auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasskeyAuthVerifyRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasskeyAuthVerifyResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/recovery/consume:
    post:
      summary: Recovery code second factor — consume + burn
      tags:
        - admin-auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecoveryConsumeRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecoveryConsumeResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Invalid recovery code
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/password-reset/request:
    post:
      summary: Request a password-reset link (anti-enumeration)
      tags:
        - admin-auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordResetRequestRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasswordResetRequestResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/password-reset/consume:
    post:
      summary: Consume a password-reset link (forces WebAuthn re-enrollment)
      tags:
        - admin-auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordResetConsumeRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasswordResetConsumeResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Invalid or expired link
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/step-up/request:
    post:
      summary: Request a step-up OTP for a gated action
      tags:
        - admin-auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StepUpRequestRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StepUpRequestResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/step-up/verify:
    post:
      summary: Verify a step-up OTP — elevate the session
      tags:
        - admin-auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StepUpVerifyRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StepUpVerifyResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Step-up verification failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/logout:
    post:
      summary: Logout — destroy the session and emit a login.logout audit event
      tags:
        - admin-auth
      responses:
        "204":
          description: Session destroyed
        "401":
          description: No authenticated session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: CSRF token missing or invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/audit/verify-integrity:
    post:
      summary: On-demand audit-log integrity verification (walk the global hash chain)
      description: Walks the entire global audit hash chain, records a verdict to audit_integrity_checks, and returns it. Requires an authenticated admin session.
      tags:
        - audit
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuditIntegrityCheckRequest"
      responses:
        "200":
          description: Integrity-check verdict
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditIntegrityCheckResult"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/auth/session:
    get:
      summary: Session introspection — the authenticated admin id + global-scope grants
      description: Returns the current admin session id + the permission keys held at the global ("national") scope ceiling. The admin SPA gates nav + routes on these grants (advisory; requireAdminSession is the real boundary on every endpoint).
      tags:
        - admin-auth
      responses:
        "200":
          description: Current admin session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/audit/integrity-checks:
    get:
      summary: Audit-integrity check history (last automated + recent checks)
      description: Returns recent integrity-check verdicts (default 30, most-recent first), each with its most-recent acknowledgement (null if never acknowledged). Optionally filtered to one trigger source. Requires an authenticated admin session.
      tags:
        - audit
      parameters:
        - schema:
            type: integer
            minimum: 1
            maximum: 200
          required: false
          name: limit
          in: query
        - schema:
            type: string
            minLength: 1
          required: false
          name: triggerSource
          in: query
      responses:
        "200":
          description: Integrity-check history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditIntegrityCheckList"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/audit/integrity-checks/{checkId}/acknowledge:
    post:
      summary: Acknowledge a (failed) integrity check with an investigation-ticket reference
      description: Records an append-only acknowledgement (separate audit_integrity_acknowledgements table — the verdict ledger stays immutable) capturing the external ticket reference. Requires an authenticated admin session.
      tags:
        - audit
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: checkId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuditIntegrityAcknowledgeRequest"
      responses:
        "200":
          description: Acknowledgement recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditIntegrityAcknowledgement"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Integrity check not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/provisioning/pariwars:
    post:
      summary: Provision a new Pariwar (mint id + persist passport)
      description: Mints a fresh pariwar_id (UUID v4), persists the Pariwar-Passport via a self-scoped write, emits a pariwar.provisioned audit event, and returns the created passport + its derived /p/<id>/ path-scope. GLOBAL surface; requires an authenticated admin holding pariwar.provision at global scope.
      tags:
        - provisioning
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddPariwarRequest"
      responses:
        "200":
          description: Pariwar provisioned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProvisionedPariwar"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — pariwar.provision at global scope required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    get:
      summary: Provisioning-status view (provisioned Pariwars + latest deploy status)
      description: Lists provisioned Pariwars (cross-readable passport rows) with their derived /p/<id>/ path-scope + latest deploy status. Forced-paginated (bounded limit, Story 1.14) + under the named read rate ceiling. Requires pariwar.provision at global scope.
      tags:
        - provisioning
      parameters:
        - schema:
            type: integer
            minimum: 1
            maximum: 100
          required: false
          name: limit
          in: query
      responses:
        "200":
          description: Provisioning-status list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProvisioningStatusList"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — pariwar.provision at global scope required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/provisioning/pariwars/{pariwarId}/deploy:
    post:
      summary: Trigger a Dokploy build for an existing Pariwar
      description: Invokes the deploy seam (env-resolved fake in dev/test, live Dokploy-API client in staging/prod), emits a pariwar.deploy_triggered audit event, and returns the deploy status + the path-scope. Requires pariwar.provision at global scope.
      tags:
        - provisioning
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: pariwarId
          in: path
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties: {}
      responses:
        "200":
          description: Deploy triggered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeployTriggerResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — pariwar.provision at global scope required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Pariwar not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/p/{pariwarId}/niyamavali/clauses:
    get:
      summary: List the Niyamavali registry (latest version per clause)
      description: Returns the chain-head (latest version) of every clause in the Pariwar, newest-first (forced-paginated). Requires niyamavali.amend at pariwar scope.
      tags: &a8
        - niyamavali
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: pariwarId
          in: path
        - schema:
            type: integer
            minimum: 1
            maximum: 100
          required: false
          name: limit
          in: query
      responses:
        "200":
          description: Registry clauses (latest version each)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ClauseVersionResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — the required niyamavali permission at this scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/p/{pariwarId}/niyamavali/clauses/{clauseId}/versions:
    get:
      summary: Version history of a clause (oldest → newest)
      description: Returns the most-recent `limit` version rows of a clause_id (forced-paginated, Story 1.14). Requires niyamavali.amend at pariwar scope.
      tags: *a8
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: pariwarId
          in: path
        - schema:
            type: string
          required: true
          name: clauseId
          in: path
        - schema:
            type: integer
            minimum: 1
            maximum: 100
          required: false
          name: limit
          in: query
      responses:
        "200":
          description: Clause version history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ClauseVersionResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — the required niyamavali permission at this scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/p/{pariwarId}/niyamavali/amendments:
    get:
      summary: List the amendment ledger (time-ordered)
      description: Returns the niyamavali_amendments ledger newest-first (the De4 (pariwar_id, created_at) index). Requires niyamavali.amend at pariwar scope.
      tags: *a8
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: pariwarId
          in: path
        - schema:
            type: integer
            minimum: 1
            maximum: 100
          required: false
          name: limit
          in: query
      responses:
        "200":
          description: Amendment ledger
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NiyamavaliAmendmentResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — the required niyamavali permission at this scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/p/{pariwarId}/niyamavali/clauses/drafts:
    get:
      summary: List clause drafts (optionally by lifecycle state)
      description: Returns drafts for the Pariwar, newest-first, optionally filtered by status (e.g. in_review to find drafts awaiting sign-off). Requires niyamavali.amend.
      tags: *a8
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: pariwarId
          in: path
        - schema:
            type: integer
            minimum: 1
            maximum: 100
          required: false
          name: limit
          in: query
        - schema:
            type: string
            enum: *a9
          required: false
          name: status
          in: query
      responses:
        "200":
          description: Clause drafts
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ClauseDraftResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — the required niyamavali permission at this scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Create a clause draft (create or amend)
      description: Creates a server-persisted draft (the non-author reviewer loads the exact pending content). Body is a discriminated union on operation. Requires niyamavali.amend.
      tags: *a8
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: pariwarId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDraftBody"
      responses:
        "200":
          description: Draft created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClauseDraftResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — the required niyamavali permission at this scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: A clause-id conflict or an existing open draft for this clause
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/p/{pariwarId}/niyamavali/clauses/drafts/{draftId}:
    get:
      summary: Load a single clause draft
      description: Returns the draft (incl. the pending payload) — the mechanism by which a NON-AUTHOR reviewer loads the exact content to review (AC1d). Requires niyamavali.amend or niyamavali.review.
      tags: *a8
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: pariwarId
          in: path
        - schema:
            type: string
            format: uuid
          required: true
          name: draftId
          in: path
      responses:
        "200":
          description: The draft
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClauseDraftResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — the required niyamavali permission at this scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Edit a clause draft (resets the tone-review sign-off)
      description: Patches a non-published draft. ANY edit resets the draft to `draft` and clears a prior tone-review sign-off (content-bound, AC1d). Requires niyamavali.amend.
      tags: *a8
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: pariwarId
          in: path
        - schema:
            type: string
            format: uuid
          required: true
          name: draftId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateClauseDraftRequest"
      responses:
        "200":
          description: Draft updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClauseDraftResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — the required niyamavali permission at this scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The draft is published or discarded and cannot be edited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/p/{pariwarId}/niyamavali/clauses/drafts/{draftId}/diff:
    get:
      summary: Preview the draft diff (structured + rendered)
      description: Returns BOTH the structured-payload diff (computePayloadDiff vs the current published version, or {} for a create) AND a rendered display-field diff (AC1c). Requires niyamavali.amend or niyamavali.review.
      tags: *a8
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: pariwarId
          in: path
        - schema:
            type: string
            format: uuid
          required: true
          name: draftId
          in: path
      responses:
        "200":
          description: Diff preview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DiffPreviewResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — the required niyamavali permission at this scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/p/{pariwarId}/niyamavali/clauses/drafts/{draftId}/submit-for-review:
    post:
      summary: Submit a draft for tone-review
      description: Transitions a draft → in_review, routing it to a non-author reviewer. Requires niyamavali.amend.
      tags: *a8
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: pariwarId
          in: path
        - schema:
            type: string
            format: uuid
          required: true
          name: draftId
          in: path
      responses:
        "200":
          description: Submitted for review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClauseDraftResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — the required niyamavali permission at this scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The draft is not in a submittable state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/p/{pariwarId}/niyamavali/clauses/drafts/{draftId}/tone-review:
    post:
      summary: Record a non-author tone-review sign-off
      description: A reviewer (niyamavali.review) records a sign-off bound to the exact reviewed payload. Rejects a self-review (author === reviewer) with 409. Emits the Story 2.2 tone_review.signoff audit line.
      tags: *a8
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: pariwarId
          in: path
        - schema:
            type: string
            format: uuid
          required: true
          name: draftId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ToneReviewSignoffRequest"
      responses:
        "200":
          description: Sign-off recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClauseDraftResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — the required niyamavali permission at this scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Self-review, or the draft is not in review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/p/{pariwarId}/niyamavali/clauses/drafts/{draftId}/publish:
    post:
      summary: Publish a clause draft (audit-logged, tone-review-gated)
      description: Mints the immutable clause_versions row + a single audit line carrying the diff hash + reviewer + clause_id + clause_version_id (AC2), then fires the member-notification hook (AC3 placeholder). Gated on a recorded non-author, content-current tone-review sign-off — without one, 409 tone-review-required (AC4). Requires niyamavali.amend.
      tags: *a8
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: pariwarId
          in: path
        - schema:
            type: string
            format: uuid
          required: true
          name: draftId
          in: path
      responses:
        "200":
          description: Published
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublishClauseResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — the required niyamavali permission at this scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: tone-review-required, or the draft is not signed off
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/p/{pariwarId}/terms/versions:
    post:
      summary: Create a T&C version (audit-logged)
      description: "Creates a version-pinned T&C: renders body_html_rendered from bodyMarkdown at write time (sanitized), pins the supplied clause versions (each validated to exist in the Pariwar), defaults legal_review_status → pending, and writes a single audit line first (audit-or-throw). Requires tc.publish at pariwar scope."
      tags: &a10
        - terms-and-conditions
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: pariwarId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTcVersionRequest"
      responses:
        "200":
          description: T&C version created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TcVersionResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — the required T&C permission at this scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Concurrent T&C version creation conflict — retry the request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: A pinned clause version does not exist in this Pariwar
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/p/{pariwarId}/terms/versions/{tcVersionId}/approve:
    post:
      summary: Approve a T&C version (audit-logged)
      description: "Marks a T&C approved: sets legal_reviewer_actor_id = the acting trustee, flips legal_review_status → approved, and supersedes the prior currently-effective version (set effective_until + status → superseded). Audit-or-throw. The superseded version stays queryable by tc_version_id. Requires tc.approve."
      tags: *a10
      parameters:
        - schema:
            type: string
            format: uuid
          required: true
          name: pariwarId
          in: path
        - schema:
            type: string
            format: uuid
          required: true
          name: tcVersionId
          in: path
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveTcVersionRequest"
      responses:
        "200":
          description: T&C version approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TcVersionResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Forbidden — the required T&C permission at this scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The version is already approved or superseded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/member/auth/otp/request:
    post:
      summary: Member login — request a mobile OTP (enumeration-safe, per-phone throttled)
      tags: &a11
        - member-auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MemberOtpRequestRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemberOtpRequestResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Too many OTP requests (per-phone 5/15min)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/member/auth/otp/verify:
    post:
      summary: Member login — verify OTP → full session / pariwar-select / signup-continuation
      tags: *a11
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MemberOtpVerifyRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemberOtpVerifyResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Member is withdrawn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Rate limited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/member/auth/otp/select-pariwar:
    post:
      summary: Member login — pick a Pariwar scope (multi-membership) → full session
      tags: *a11
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MemberSelectPariwarRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemberFullSession"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/member/auth/token/refresh:
    post:
      summary: Member session — rotate the refresh token (reuse detection)
      tags: *a11
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MemberTokenRefreshRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemberFullSession"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/member/auth/step-up/request:
    post:
      summary: Member step-up — request an OTP for a gated action
      tags: *a11
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MemberStepUpRequestRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemberStepUpRequestResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/member/auth/step-up/verify:
    post:
      summary: Member step-up — verify the OTP → elevate for the action_context
      tags: *a11
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MemberStepUpVerifyRequest"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemberStepUpVerifyResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/member/kyc/initiate:
    post:
      summary: Signup KYC — begin a DigiLocker pull (returns the authorization redirect)
      description: Resolves the active KYC provider and begins a DigiLocker authorization-code flow for the authenticated member (must be pending-kyc), returning the authorizationUrl the client opens + the transactionId the callback correlates. Requires a member session.
      tags: &a12
        - member-kyc
      responses:
        "200":
          description: KYC flow initiated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KycInitiateResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Member is not pending-kyc (KYC already completed)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: KYC provider unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/kyc/callback:
    post:
      summary: DigiLocker OAuth callback (PUBLIC; state-correlated) — verify + pull the profile
      description: The OAuth redirect target DigiLocker sends the browser to with ?state&code. PUBLIC (no member JWT) — correlated by the unguessable state, which resolves the kyc_transaction (member_id + pariwar_id). Verifies the eAadhaar signature, pulls the profile, and persists it awaiting member confirmation. On provider failure returns the normalized KYC error so the client can branch to the manual fallback (AC2).
      tags: *a12
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/KycCallbackRequest"
      responses:
        "200":
          description: Profile verified + persisted (awaiting confirm)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KycProfileSummaryResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: No transaction for the supplied state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: Verification failed / signature invalid / certificate stale
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: DigiLocker provider unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/member/kyc/confirm:
    post:
      summary: Signup KYC — confirm the verified DigiLocker profile (emits member.kyc_completed)
      description: "Confirms a verified DigiLocker profile: emits member.kyc_completed via the projector (pending-kyc → pending-fee) + an audit line. Idempotent (a re-confirm emits no second event). Requires a member session."
      tags: *a12
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/KycConfirmRequest"
      responses:
        "200":
          description: KYC confirmed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KycStatusResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: No verified transaction / stored profile to confirm
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Transaction not verified, or member not pending-kyc
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/member/kyc/manual:
    post:
      summary: Signup KYC — manual fallback (emits member.kyc_manual_fallback)
      description: Stores a self-declared KYC record (name/dob/optional photo, Tier-1 encrypted; verification_strength=self_declared, trustee_verified=false) and emits member.kyc_manual_fallback (pending-kyc → pending-fee). The AC2 fallback target. Requires a member session.
      tags: *a12
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/KycManualSubmitRequest"
      responses:
        "200":
          description: Manual KYC recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KycStatusResponse"
        "400":
          description: Request validation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Member is not pending-kyc
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/member/kyc/profile-summary:
    get:
      summary: Signup KYC — decrypt and return the stored KYC profile for member confirmation
      description: "Returns the decrypted stored KYC profile for the authenticated member: name, dob, masked-Aadhaar (last-4 only), verificationStrength, and photoPresent flag (never the raw photo bytes — Tier-1 discipline). Called by the mobile confirm screen after the DigiLocker callback has persisted the profile. Requires a member session."
      tags: *a12
      responses:
        "200":
          description: KYC profile summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KycProfileSummaryResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: No KYC profile found for this member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/member/kyc/status:
    get:
      summary: Signup KYC — the step entry/poll read (transaction + member KYC + manual-enabled seam)
      description: Returns the current DigiLocker transaction status (when a flow is in flight), the member's KYC standing, the lifecycle state, and the FR-58C manualFallbackEnabled seam flag (false → the UI hides the manual CTA + shows the hard-mandatory copy block). Requires a member session.
      tags: *a12
      responses:
        "200":
          description: KYC status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KycStatusResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/member/nominees:
    post:
      summary: Signup nominees — declare 1–2 nominees (emits member.nominees_declared)
      description: Declares 1 or 2 nominees (name/relationship/mobile + optional address; Tier-1 encrypted) with a SERVER-derived 75/25 split when two are declared (100% for one) — the client cannot override the split. Replaces any prior declaration (latest-wins) and emits member.nominees_declared (a non-PII audit marker; count + split only) on the member stream. NO nominee Aadhaar/KYC and NO nominee bank/IFSC are collected at signup (claim-time only, Epic 6). Requires a member session (no step-up at signup — Life Events adds it in 3.9).
      tags: &a13
        - member-nominee
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NomineeDeclareRequest"
      responses:
        "200":
          description: Nominees declared
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NomineeStatusResponse"
        "400":
          description: Request validation failed (0 or >2 nominees, or bad fields)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Member is in a terminal state (withdrawn / anonymized)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    get:
      summary: Signup nominees — the current effective declaration (NO PII echo-back)
      description: "Returns the member's current nominee declaration as NON-PII summaries: rank, relationship, the server-stamped splitPct, and presence flags for the encrypted fields. NEVER the raw name/mobile/address bytes (Tier-1 echo-back discipline). Requires a member session."
      tags: *a13
      responses:
        "200":
          description: Current nominee declaration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NomineeStatusResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/member/medical-disclosure:
    post:
      summary: Signup medical disclosure — submit IMA disclosures + concealment-denial ack
      description: Submits 0..N IMA-listed conditions (zero is valid — most members disclose nothing) + the optional free-text additional context (both Tier-1 encrypted) with a MANDATORY concealment-denial acknowledgment (server rejects acknowledged !== true). Records a consent (medical_disclosure_ack, referencing niy.concealment.r14) via the audit-or-throw chain, APPENDS a disclosure row (append-only history — Epic 4 walks the full history), and emits member.medical_disclosed (a non-PII marker; count + ima_list_version + ack only). Requires a member session (no step-up at signup — Life Events adds it in 3.9).
      tags: &a14
        - member-medical
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MedicalDiscloseRequest"
      responses:
        "200":
          description: Disclosure recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MedicalDisclosureStatusResponse"
        "400":
          description: Validation failed (ack not true, or an unknown IMA condition code)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: Member not found / terminal state, or a required clause (IMA list / concealment) is unresolvable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    get:
      summary: Signup medical disclosure — the latest disclosure status (NO PII echo-back)
      description: Returns the member's latest medical disclosure as a NON-PII summary (disclosedAt, imaListVersion, conditionCount, a presence flag for the free-text, ackLocale) + the total history count. NEVER the raw condition codes / free-text bytes (Tier-1 echo-back discipline). Requires a member session.
      tags: *a14
      responses:
        "200":
          description: Latest disclosure + history count
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MedicalDisclosureStatusResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/member/medical-disclosure/ima-list:
    get:
      summary: Signup medical disclosure — the IMA catalog + concealment-ack copy
      description: Returns the resolved IMA condition catalog (version = the niy.medical.ima-list clause_version_id + bilingual conditions) plus the concealment-ack copy (ackText.en / ackText.hi from niy.concealment.r14) the screen renders. Returns 503 when the registry is unprovisioned for the Pariwar (either clause absent). Requires a member session.
      tags: *a14
      responses:
        "200":
          description: IMA catalog + ack copy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImaListResponse"
        "401":
          description: Authentication required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: The IMA list / concealment clause is not provisioned for this Pariwar
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
webhooks: {}
