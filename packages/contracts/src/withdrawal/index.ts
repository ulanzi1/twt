// packages/contracts/src/withdrawal/index.ts
//
// Voluntary-withdrawal contracts barrel (Story 3.10). Consume via the `@twt/contracts` TOP barrel
// (the nominee/life-events convention — there is no subpath `exports` map):
//   import { WithdrawalConfirmRequest, WithdrawalStatusResponse, WithdrawalReasonCode } from '@twt/contracts';

export * from './withdrawal.js';
