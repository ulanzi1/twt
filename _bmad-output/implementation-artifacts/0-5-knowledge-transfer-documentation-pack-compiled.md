# Story 0.5: Knowledge-Transfer Documentation Pack Compiled

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

### Task progress

- Tasks 1, 2, 3, 4, 5, 6, 7 — **author-committed 2026-05-30** (artifacts in repo; see Dev Agent Record → File List). The `[CONTINUITY]` entry in `.decision-log.md` (Decision 2026-05-30-005) records framework status as "Author-committed; awaiting trustee sign-off."
- Tasks 8, 9, 10 — awaiting external action (Trustee Panel ≥2-trustee sign-off on the pack; comprehension-questionnaire administration to the contracted backup engineer per Story 0.6; ≥80% comprehension threshold close). Status flipped to `review` per Story 0.4 precedent on Tasks-1-7 closure. AC-1 + AC-2 full closure requires Task 8. AC-3 full closure requires Task 9 + Task 10 (both Story-0.6-gated). Per [[feedback_closure_language_precision]], AC-1 + AC-2 are **partially closed (Closed by [edit] for the author-committable legs; Resolved via explicit deferral for the trustee-sign-off leg)**; AC-3 is **fully Resolved via explicit deferral** pending Story 0.6 closure.

## Story

As a Trustee Panel and any future engineer activated under the bus-factor scenario,
I want a complete Knowledge-Transfer documentation pack compiled and stored in the trustee-accessible repo — covering (i) an index of ADRs with current status, (ii) a Niyamavali clause → FR mapping table, (iii) a deployment topology diagram + description, (iv) an on-call playbook covering incident triage + escalation paths, and (v) a third-party dependency inventory with vendor / contract / renewal-date / contact — plus a standardized comprehension questionnaire that the contracted backup engineer (Story 0.6) takes cold and scores ≥80%,
So that the trust's technical context is recoverable without Solo Builder, the 30-day-takeover property committed by PRD §9.1.1 + AR-67 is jointly discharged with Stories 0.3 + 0.4 + 0.6, and a contracted external engineer can take over the codebase + operations within the committed window using only the pack + Story 0.1 runbooks + Story 0.2 credential escrow + Story 0.3 code escrow + Story 0.4 degradation policy.

## Acceptance Criteria

1. **AC-1 — KT pack compiled with all five PRD §9.1.1 components + stored in the trustee-accessible repo**

   **Given** the PRD §9.1.1 commitment ("Knowledge-transfer documentation — at minimum: architecture decision records (ADRs), Niyamavali → FR mapping, deployment topology, on-call playbook, third-party dependency inventory with renewal dates"), AND the architecture commitments anchoring each component (§5.15 runbook inventory + `docs/adr/` directory commitment at lines 636 + 4170; §1.14 Member Lifecycle State Model with FR provenance for Niyamavali clauses; §5.1–§5.10 GCP `asia-south1` topology + environment structure; §5.10 Solo-Builder-named on-call + backup-engineer access posture; §Critical External Dependencies + §3.10 external-API deprecation monitoring), AND the AR-67 + PRD §9.1.1 30-day-takeover commitment joint-discharged across Stories 0.3 + 0.4 + 0.5 + 0.6
   **When** Solo Builder authors the KT pack framework + the five mandated component documents + the comprehension questionnaire + the ledger + cross-link edits to the existing Stories 0.1 / 0.2 / 0.3 / 0.4 frameworks
   **Then** `docs/knowledge-transfer/` exists alongside `docs/runbooks/`, `docs/escrow/`, and `docs/degradation-policy/` per the Story 0.2 + 0.3 + 0.4 framework-as-top-level-surface pattern, with the artifacts listed in Project Structure Notes (README + adr-index.md + niyamavali-fr-mapping.md + deployment-topology.md + on-call-playbook.md + third-party-dependency-inventory.md + comprehension-questionnaire.md + kt-pack-ledger.md)
   **And** the **ADR index** (`adr-index.md`) inventories every deferred-ADR slot already catalogued across architecture (§Deferred Decisions + Category-specific "Decisions deferred to subsequent categories" + the §5.10/§5.12/§5.15 "Decisions deferred to operations policy or implementation ADRs" lists), runbooks (the `[deferred ADR — placeholder procedure]` tags across `docs/runbooks/*.md`), escrow (the `pending-separation-mechanism` envelope classes in `credential-inventory.md` that block on pre-ADR sealing per `docs/escrow/README.md`), code escrow (mirror-destination ADR + mirror-credential-model ADR per Decision 2026-05-29-003 Open Follow-ups #1-2), and degradation policy (paging-integration ADR + operations-policy cadence ADR + Hindi-native-trustee ratification ADR per Decision 2026-05-29-004 Open Follow-ups) — each row records: slot identifier, owning architectural section / runbook / framework, current status (`slot-reserved-pre-write` | `drafted` | `under-trustee-review` | `ratified` | `superseded`), expected-author, expected-close trigger (the Story whose closure unblocks the ADR), cross-link to the originating commitment. The `docs/adr/` directory itself is scaffolded by Story 0.5 with a `README.md` (lifecycle + status legend + index symmetry note) and an `_adr-template.md` (the canonical body schema) — empty of substantive ADRs at Story 0.5 author-commit since substantive ADR drafting is implementation-time work per architecture §Implementation Handoff (lines 5069-5096) **PR-2 ADRs**. **The `docs/adr/` scaffold is the Decision 2026-05-29-003 Open Follow-up #6 closure leg.**
   **And** the **Niyamavali → FR mapping table** (`niyamavali-fr-mapping.md`) is authored as the skeleton mapping from each Niyamavali clause class committed in PRD §1 Niyamavali (R-1..R-15 named in PRD) + the load-bearing Account State Machine FRs (FR-1 through FR-12A per architecture §1.14) + the Niyamavali rule registry FRs (FR-7 through FR-11 per architecture §1.10 + §1.7) to the FRs that implement / enforce each clause. The table columns: `niyamavali_clause_id` (R-N) | `clause_summary` (1-line excerpt from PRD §1) | `implementing_FRs` (comma-separated list of FR-NNN identifiers) | `owning_epic` (Epic 2-4 typically) | `owning_story_keys` (sprint-status story keys that operationalize the clause) | `architecture_anchor` (the §1.x / §3.x / §4.x section that grounds the implementation) | `current_status` (`spec-only` | `in-implementation` | `partially-implemented` | `fully-implemented`) | `notes`. Rows are populated for the R-classes named in PRD §1 + the Account State Machine table from architecture §1.14 lines 1238-1246; Story 2.3 (Niyamavali rule-registry data model) + Epic 4 (rules engine + member validity service) operationalize the mapping. At Story 0.5 author-commit time, every row's `current_status` is `spec-only` — the mapping is the **specification → implementation cross-walk**, not the implementation itself. **Forbidden-removal rule applies** (per Story 0.3 review-decision precedent inherited via Story 0.4): rows are never removed; supersession is the only allowed lifecycle exit
   **And** the **deployment topology** (`deployment-topology.md`) documents the GCP `asia-south1` (Mumbai) topology committed in architecture §5.1 + the three-environment structure (`twt-dev`, `twt-staging`, `twt-prod`) + the dedicated audit-mirror GCP project (`twt-audit-mirror-prod`) per §5.5 + the Dokploy v1 deployment substrate + K8s migration path per §5.3 + Cloudflare edge + WAF + Bot Management per §5.8 + Cloud SQL Postgres regional HA per §5.7 + the per-Pariwar tenancy model per §5.14 + the workspace layout (`apps/api`, `apps/admin`, `apps/public`, `apps/mobile`, `apps/jobs`) per architecture lines 4178-4318. The document includes: (i) a textual diagram in ASCII / Mermaid notation (cache-safe for markdown-rendering Story 2.5 + 11a.5 surfaces if ever embedded; renderer-agnostic primary) showing GCP-project boundaries + cross-project IAM constraints; (ii) a service map mirroring architecture §5.2 (Cloud SQL, Cloud Storage, KMS, Secret Manager, IAM, networking, Cloud Run, Cloud Monitoring, Cloud Logging, Cloud Tasks); (iii) network topology mirroring §5.8 (edge-only ingress, VPC posture); (iv) backup + DR posture per §5.7 (Multi-AZ in-region + manual DR runbook + RTO 4h / RPO 1h + Cross-region replica trigger criteria); (v) per-Pariwar isolation strategy per §5.14 + 2nd-Pariwar provisioning option set. The document does NOT re-author architectural decisions — it is a **reader's map** keyed to architecture sections. The 2nd-Pariwar option set is documented as **per §5.14 trigger-time decision** (no commitment to a specific path)
   **And** the **on-call playbook** (`on-call-playbook.md`) follows the `docs/runbooks/_template.md` five-section schema verbatim for the on-call surface (NOT a per-task runbook — this is the meta-playbook above the seven Phase-0 runbooks), covering: (§1 Prerequisites) Solo Builder as v1-named on-call per architecture §5.10 + the backup engineer (A-13 / Story 0.6) as surge / continuity coverage + paging surface naming (operations-policy-deferred ADR slot per §5.10); (§2 Step-by-step incident triage) incident-class taxonomy mirroring architecture §1.5 audit-integrity + §5.6 capacity-planning indicators + §5.10 alarm routing — each class (audit-integrity failure, P0 capacity-indicator breach, push-provider outage, paging-SaaS outage, Dokploy substrate failure, Cloudflare edge outage, Cloud SQL HA failover, KMS unavailability, DigiLocker integration failure, WA Business suspension, helpdesk SLA breach) gets a triage step + the runbook OR ADR slot that grounds the resolution path + escalation trigger per architecture Cross-Cutting #9 (`{primary_actor, fallback_actor, escalation_trigger}`); (§3 Rollback / failure-mode procedure) what to do when triage cannot resolve within SLA — cite degradation policy activation (Story 0.4 §14 ceremony), credential-escrow quorum-open (Story 0.2), code-escrow switch-to-mirror (Story 0.3); (§4 Verification checks) every triage step has a deterministic pass/fail signal; (§5 Contact escalation list) Solo Builder + backup engineer + Trustee Panel chair + legal counsel per Story 0.13 — roles, not individuals
   **And** the **third-party dependency inventory** (`third-party-dependency-inventory.md`) enumerates every external dependency named in architecture §Critical External Dependencies (DigiLocker; Cloudflare edge/WAF/Turnstile; WhatsApp Business; Dokploy; bank statement intake — 5-bank allowlist; external-regulatory gates) + the partner integrations (Module Marketplace per Epic 12 — `deferred-with-ADR` until first partner) + the cloud-provider dependencies (GCP — Cloud SQL, Cloud Storage, KMS, Secret Manager, IAM, Cloud Monitoring, Cloud Logging, Cloud Tasks) + the third-party SaaS (paging provider, tracing/crash provider, telephony provider, push provider — each `pending-ADR-selection` per §5.10 + §5.6 + §3.5) + the OS-platform dependencies (Apple Developer Program, Google Play Console, FCM, APNs) + the DPDPA breach-reporting tooling per Story 14.3. Schema columns: `dependency_class` | `provider` (current OR `pending-ADR-selection`) | `usage` (1-line description) | `architectural_authority` (§Critical-External / §5.x section cite) | `owning_story` (sprint-status key) | `renewal_or_contract_event` (annual subscription / per-partner contract / partner-deprecation notice / Apple/Google account renewal / DLT registration / Meta template approval / etc.) | `next_renewal_date` (concrete date OR `pending-contract-signature` OR `per-rotation-cadence-ADR`) | `monitoring_owner` (per §3.10 — the named owner responsible for tracking deprecation announcements; Solo Builder at v1 with Trustee Panel escalation per architecture §5.10) | `vendor_contact` (support portal URL OR escalation-contact-as-recorded-in-credential-envelope OR `pending-vendor-onboarding`) | `cross_link` (credential-inventory.md row for the corresponding credential envelope, where applicable) | `status` (`active` | `pending-system-availability` | `pending-ADR-selection` | `deferred-with-ADR`). Rows where the dependency is not yet contracted are `pending-system-availability` (mirroring `credential-inventory.md` discipline). The inventory is **operating-discipline-focused**: it captures the renewal cadence + the monitoring owner + the vendor contact so a future engineer or trustee can renew a contract / respond to a deprecation notice / contact a vendor without Solo Builder consultation
   **And** the **closure precision** (per [[feedback_closure_language_precision]]): each AC-1 closure leg is one of three states — **Closed by [edit]** (each of the five components authored as a concrete file under `docs/knowledge-transfer/` with the schema and row content described above; ledger entry committed; `.decision-log.md` Decision 005 appended), **Resolved via explicit deferral** (deferred-ADR rows enumerated as `slot-reserved-pre-write`; deferred dependency rows enumerated as `pending-system-availability` / `pending-ADR-selection` / `deferred-with-ADR`; Niyamavali → FR mapping rows enumerated as `spec-only` until the implementing Stories close; the `docs/adr/` substantive content explicitly deferred to PR-2 / implementation-time per architecture §Implementation Handoff), or **Not addressed** (in scope but neither edited nor explicitly deferred — should not occur and triggers an Open Question)
   **And** ≥2-trustee sign-off, when it occurs (Task 8), is recorded in `kt-pack-ledger.md` "Trustee sign-off log" with each ratifying trustee, date, scope (per-component OR pack-as-a-unit ratification per the trustees' preference), and the supersession-schema marker per the Story 0.1 + 0.2 + 0.3 + 0.4 schema (the sign-off entry supersedes the "Author-committed; awaiting trustee sign-off" status on the Task-7 `.decision-log.md` entry; the prior is not modified)

2. **AC-2 — Pack stored in the trustee-accessible repo + cross-linked into the existing Stories 0.1 / 0.2 / 0.3 / 0.4 frameworks + Decision 2026-05-30-005 appended**

   **Given** the Story 0.3 commitment that release-branch pushes auto-mirror to a trustee-controlled location within 10 minutes AND the Story 0.2 credential-escrow custodial path for credentials that the dependency inventory references AND the Story 0.1 operational-readiness-ledger as the surface that records framework coverage AND the Story 0.4 degradation-policy-ledger as the surface that anchors the 30-day-takeover joint discharge
   **When** the dev agent stores the pack files under `docs/knowledge-transfer/` and applies the narrow cross-reference edits per Task 5
   **Then** the pack lives in the primary git repo, inheriting Story 0.3's mirror coverage once Story 0.3 Tasks 7-11 close (the symmetry: the KT pack itself enjoys the code-survival coverage it describes)
   **And** `docs/runbooks/operational-readiness-ledger.md` carries a new "KT pack coverage" section (mirroring the Story 0.3 "Mirror coverage" + Story 0.4 "Degradation policy coverage" section pattern): "As of Story 0.5 author-commit: the KT pack framework + five mandated components + comprehension questionnaire + ledger are authored. ≥2-trustee sign-off is `pending` per Story 0.5 Task 8 (`_AWAITING EXTERNAL ACTION_`); comprehension-questionnaire administration to backup engineer is `pending` per Story 0.5 Task 9 (Story 0.6 closure gate)." Existing sections (Sign-off table; Execution-validation log; Mirror coverage; Degradation policy coverage) NOT modified
   **And** `docs/runbooks/README.md` "Related runbooks expected from other stories" table carries a new row: "On-call playbook | Story 0.5 (Knowledge-Transfer Documentation Pack Compiled) | `docs/knowledge-transfer/on-call-playbook.md` — five-section meta-playbook covering incident triage + escalation paths; uses the same five-section runbook template; sign-off via `docs/knowledge-transfer/kt-pack-ledger.md` Trustee sign-off log." Append-only; existing rows NOT modified
   **And** `docs/escrow/credential-inventory.md` `dr-runbook-pdf-custody` row (line 92) carries a Notes-column append referencing the KT pack on-call playbook (the on-call playbook is structurally adjacent to the DR runbook — both are operational meta-documents — but they are SEPARATE artifacts; the on-call playbook lives in `docs/knowledge-transfer/`, the DR runbook lives in `docs/runbooks/` once authored). The append clarifies that Story 0.5 does NOT author the DR runbook (the DR runbook scope decision remains the Story 0.4 Decision 004 Open Follow-up); Story 0.5 authors the **on-call playbook** which is a distinct artifact per architecture §5.10 vs §5.7. Notes-column-only append; envelope_class / availability / re-seal trigger / Story citations NOT modified
   **And** `docs/degradation-policy/degradation-policy-ledger.md` "Table-top exercise log" section header carries an append cross-linking the KT pack as a co-discharger of the 30-day-takeover property (per Story 0.3 Decision 003 Open Follow-up): "Story 0.5 author-commit dated 2026-05-30 contributes the KT pack leg of the joint discharge — code accessibility (Story 0.3) + degradation framing (Story 0.4) + technical-context recoverability (Story 0.5) + backup-engineer contract (Story 0.6) jointly discharge the AR-67 / PRD §9.1.1 30-day takeover." Existing log content NOT modified
   **And** `docs/escrow/code-escrow/README.md` "Related code-survival surfaces owned elsewhere" table carries a new row referencing the KT pack (the KT pack is what an engineer reads after switching to the mirror per Story 0.3 AC-2). Append-only; existing Story 0.4 + DR-runbook rows NOT modified
   **And** `.decision-log.md` Decision 2026-05-30-005 is appended at the top of "## Decisions" per the Story 0.1 + 0.2 + 0.3 + 0.4 schema; Decisions 001 / 002 / 003 / 004 NOT modified; decision-type index amended with `**Story 0.5 — Knowledge-Transfer Pack** —` entry in numerical order between 0.4 and 0.6

3. **AC-3 — Backup engineer reads the pack cold and scores ≥80% on a standardized comprehension questionnaire**

   **Given** Story 0.6 has contracted a backup engineer with NDA + retainer + read-access to the repo, AND the KT pack at the trustee-accessible repo with AC-1 + AC-2 closed, AND a standardized comprehension questionnaire (`comprehension-questionnaire.md`) authored by Story 0.5 per the schema below
   **When** the contracted backup engineer reads the KT pack cold (no Solo Builder consultation; Solo Builder silent for the duration per the bus-factor simulation discipline inherited from Stories 0.1 AC-4 + 0.2 AC-3 + 0.3 AC-2 + 0.4 AC-2) AND completes the comprehension questionnaire under timed conditions (recommended ≤4 hours per administration; logged in the ledger)
   **Then** the questionnaire enumerates ≥25 questions across five sections — one section per PRD §9.1.1 component (ADRs / Niyamavali → FR / deployment topology / on-call / dependency inventory) — covering: (i) factual recall (e.g., "Which GCP region is the prod environment in?"; "Which FR governs the renewal-grace transition?"; "What is the Class-A pg-boss queue's role?"); (ii) cross-link comprehension (e.g., "If the DigiLocker integration deprecates its OAuth flow, which document records the monitoring owner and the deprecation-response procedure?"); (iii) procedural reasoning (e.g., "Walk through what happens when the audit-mirror replication-lag alarm fires at 4 min."); (iv) escalation-path reasoning (e.g., "If Solo Builder is unreachable and the audit-integrity-check job has failed for 12 hours, what is the escalation path?"); (v) structural-property reasoning (e.g., "Why does the code-escrow mirror NOT carry GCP service-account keys?")
   **And** the questionnaire has a **deterministic answer key** stored in `comprehension-questionnaire-answer-key.md` (separated from the questionnaire itself so the backup engineer reads the questionnaire cold without seeing the answers; the key is held in the trustee-accessible repo for trustee-facilitated scoring); **per-question scoring rubric** distinguishes `correct` (full credit), `partial` (≥50% correct reasoning OR correct conclusion but missing cite), `incorrect` (wrong conclusion), `unanswerable-from-pack` (the question's answer is not reachable from the pack — this is a GAP signal, the pack is incomplete)
   **And** the **80% threshold** is computed as `(correct * 1.0 + partial * 0.5) / total_questions ≥ 0.80`; `unanswerable-from-pack` answers count as 0 toward the score AND count as **framework gaps** that trigger pack revision per Task 10
   **And** the administration is logged in `kt-pack-ledger.md` "Comprehension administration log" with: administering trustee, backup engineer identity, administration date, time taken, per-section score breakdown, gap-list (the `unanswerable-from-pack` questions + the rationale for each gap), remediation owner per gap, remediation target date
   **And** AC-3 is **provisionally closed** when at least one administration of the questionnaire reaches the 80% threshold; **fully closed** when at least one administration reaches the 80% threshold AND all `unanswerable-from-pack` gaps are remediated in the pack OR explicitly deferred with rationale per [[feedback_closure_language_precision]]. Re-administration is permitted after gap remediation; the ledger records each administration as a new row. AC-3 full closure has a hard dependency on Story 0.6 closure (no contracted backup engineer, no administration) + Task 10 closure (gap discharge)
   **And** the **30-day-takeover joint-discharge** (per Story 0.3 Decision 003 + Story 0.4 Decision 004 Open Follow-ups) is **partially evidenced** by AC-3: the questionnaire administration demonstrates that a contracted external engineer can come up to speed on the trust's technical context within the pack-read-cold window (the AC-3 ≤4-hour recommendation). The end-to-end 30-day takeover is jointly discharged by Story 0.3 AC-1 + Story 0.3 AC-2 + Story 0.4 AC-1 + Story 0.4 AC-2 + Story 0.5 AC-1 + Story 0.5 AC-2 + Story 0.5 AC-3 + Story 0.6 (backup engineer contract). When all eight conditions close, a follow-up `.decision-log.md` `[CONTINUITY]` entry records the joint-discharge achievement per the Story 0.3 Decision 003 closure path

## Tasks / Subtasks

- [x] **Task 1 — Establish KT-pack scaffolding under `docs/knowledge-transfer/` + scaffold `docs/adr/`** (AC: 1)
  - [x] Verified `docs/knowledge-transfer/` did not exist; created via `mkdir -p`. NEW top-level surface under `docs/`, parallel to `docs/runbooks/`, `docs/escrow/`, `docs/degradation-policy/`, and `docs/adr/`.
  - [x] Verified `docs/adr/` did not exist; created via `mkdir -p`. Authored `docs/adr/README.md` (lifecycle + naming convention + body schema + relationship to runbooks/framework READMEs/decision-log + status legend + index symmetry note pointing at `docs/knowledge-transfer/adr-index.md`); authored `docs/adr/_adr-template.md` (canonical body schema: Title + Status + Date + Author + Ratifying trustees + optional Supersedes/Superseded-by + Context + Decision + Alternatives + Consequences + References + Changelog). **The `docs/adr/` scaffold discharges Decision 2026-05-29-003 Open Follow-up #6.**
  - [x] Authored `docs/knowledge-transfer/README.md` (§1 why top-level surface; §2 framework lifecycle; §3 four-way property/control/policy/gap-analysis discipline; §4 structural invariants — 9 invariants enumerated; §5 sign-off lifecycle with pack-as-a-unit OR per-component modes + ≥2-row quorum rule; §6 review cadence fallback — quarterly/annual/per-architectural-amendment/per-Story-closure/on-incident; §7 ledger-vs-component-files reconciliation; §8 Open ADR slots — 7 slots; §9 related continuity surfaces table; §10 30-day-takeover joint-discharge anchor; §11 mechanism-level revision path; §12 file index).

- [x] **Task 2 — Author the ADR index `adr-index.md` cataloguing every deferred-ADR slot already named in the existing planning + framework artifacts** (AC: 1)
  - [x] Created `docs/knowledge-transfer/adr-index.md` with the table schema specified in AC-1; populated **64 rows** at `slot-reserved-pre-write` across seven sections: Section A (architecture §Deferred Decisions Step 2 carryovers — 28 rows), Section B (architecture per-category sub-section ADR slots — 17 rows), Section C (runbook `[deferred ADR — placeholder procedure]` tags — 1 row from `deploy.md`), Section D (escrow framework — 2 rows: credential-escrow-mechanism + envelope-class-separation-mechanism), Section E (code-escrow framework — 6 rows including the `adr-directory-scaffold` slot whose closure leg is discharged by Task 1), Section F (degradation-policy framework — 7 rows), Section G (Module Marketplace + Crowdfunding — 3 rows). Status summary table + forbidden-removal rule + periodic review cadence + references all populated. Substantive ADR content explicitly deferred to PR-2 / implementation-time per architecture §Implementation Handoff.
  - [ ] Populate the table with the deferred-ADR slots already catalogued across the planning artifacts + framework docs. Categories (each row populated per the category):
    - **Architecture §Deferred Decisions (lines 143-239)** — backend language / runtime / framework / API style / auth model; native mobile stack (P0-5 ratify per Story 0.14); web stack composition (3-to-4-way decision); datastore engine; cloud hosting region (asia-south1 committed); deployment substrate (Dokploy → K8s migration trigger); Dokploy failure fallback [P1]; source-code hosting (GitHub primary; escrow mirror per Story 0.3); backup target + RTO/RPO targets (§5.7 committed values; mechanism ADR pending); design tokens (hand-rolled TS → Style Dictionary trigger); threat model + threat-actor inventory [P0]; DigiLocker signature verification policy [P0]; bank statement normalization schema [P0]; idempotency store; Pool Engine snapshot format; Pool Engine snapshot + audit-log storage volume sizing [P2]; audit log mirror target; reconciliation matcher mechanism (OQ-2); observability stack split [P1] (4 separate decisions); infrastructure on-call rotation [P1]; capacity-planning indicators [P1]; job runner for time-as-actor; feature-flag tool selection [P1]; Pariwar-Passport data model in v1; per-tenant custom fields via JSON columns; telephony integration; object storage for KYC docs / death certificates / Contribution Note PDFs / bank statements; bank statement intake pipeline.
    - **Architecture §5.10 + §5.12 + §5.15 "Decisions deferred to operations policy or implementation ADRs"** — push provider final choice; WhatsApp Business provider; telephony provider final selection; A11y CI scanner; edge-compatible responsive media pipeline provider; helpline console keep-alive mechanism; OpenAPI client-generation tool; SaaS tracing/crash provider; list virtualization library per surface; pg-boss worker counts; cost thresholds + alarm levels; per-Pariwar isolation at 2nd-Pariwar trigger; audit-loss-window mechanism.
    - **Architecture §1.5 + §2.7 + §2.8 + §3.4 + §5.6 + §5.9 + §5.11 — per-category ADR slots** — audit-log integrity algorithm + upgrade path; PII tier-1 KEK encryption library version pinning; DigiLocker key rotation + key-compromise procedure; channel-provider abstraction final selection per channel; observability tracing provider; secret-manager rotation cadence; pg-boss queue partitioning + per-class worker counts.
    - **Runbook `[deferred ADR — placeholder procedure]` tags** — read each existing `docs/runbooks/*.md` (deploy / rollback / secret-rotation / audit-log-integrity-verification / reconciliation-manual-intervention / rbac-seed-reset / multi-pariwar-provisioning) and catalogue every tagged slot. Each row carries the runbook file + the line cite + the placeholder procedure that holds until the ADR lands.
    - **Escrow framework — Story 0.2 pre-ADR sealing rule** — the `pending-separation-mechanism` envelope classes (`kek-root-tier-1-envelope-encryption`; `hmac-root-tier-2-blind-index`; `audit-mirror-write-service-account`; `audit-mirror-read-service-account`; `partner-jwt-signing-key-{partner}` per partner; `telephony-recording-storage-credentials`) — each is a row in this index with `expected_close_trigger` = corresponding Story closure + ADR ratification.
    - **Code-escrow framework — Story 0.3 Decision 003 Open Follow-ups #1-7** — mirror destination ADR; mirror credential model ADR; release-branch set ADR (Story 1.1 amendment trigger); restoration-drill cadence ADR; bus-factor switch-to-mirror facilitator path ADR (Story 0.6 closure trigger); `docs/adr/` scaffold (CLOSED by this Story 0.5 Task 1); mirror-workflow secret-rotation procedure ADR.
    - **Degradation-policy framework — Story 0.4 Decision 004 Open Follow-ups** — DR runbook authoring scope ADR; paging integration ADR; operations-policy cadence ADR; Hindi-native-trustee ratification path ADR; backup-engineer-as-facilitator authorization ADR; surface-inventory drift detection ADR.
    - **Module Marketplace + Crowdfunding boundary** — module manifest schema versioning ADR; partner JWT signing key per-partner storage ADR; module lifecycle state machine ADR; module-shelf grief-context structural suppression mechanism ADR.
  - [ ] Forbidden-removal rule applied: rows are never removed; supersession is the only allowed lifecycle exit. Status flips from `slot-reserved-pre-write` to `drafted` (or higher) under the authoring Story's authority — Story 0.5 does NOT itself author substantive ADR content (this is the PR-2 / implementation-time scope per architecture §Implementation Handoff).
  - [ ] Status summary table at top of file: counts of rows per status; total deferred-ADR slot inventory (expected ≥40 at author-commit). Cross-link to `docs/adr/README.md` for the directory's lifecycle + naming convention.

- [x] **Task 3 — Author the Niyamavali → FR mapping `niyamavali-fr-mapping.md`** (AC: 1)
  - [x] Created `docs/knowledge-transfer/niyamavali-fr-mapping.md` with primary mapping (14 clause rows: R5(C.2), R5(D), R5(E), R5(F), R7(A-G), R8/R8(A)/R8(B), R9, R9(A), R14-adapted, Lock-in TWT divergence, Annual renewal+grace, Retirement coverage extension, Member validity canonical answer, Niyamavali amendment workflow) all at `current_status: spec-only` per AC-1 at-author-commit discipline; inverse-lookup section (20 FR rows: FR-1..FR-12A + FR-42 + FR-43 + FR-43A + FR-47 + FR-79 + FR-94); Account State Machine extract verbatim from architecture.md §1.14 lines 1238-1246 (the single deliberate verbatim transcription per README §4 invariant 1); mapping discipline + status legend + status summary + forbidden-removal rule + references all populated.
  - [ ] Populate rows for each Niyamavali clause class named in PRD §1 + the Account State Machine FRs per architecture §1.14 lines 1238-1246. The dev agent reads PRD §1 to enumerate the R-classes; reads architecture §1.14 + epics Epic 2 + Epic 3 + Epic 4 to populate `implementing_FRs` + `owning_story_keys`. Each row's `current_status` is `spec-only` at author-commit (no implementation has shipped at Story 0.5).
  - [ ] Include header section: **Mapping discipline** — the table is a *cross-walk*, not a re-statement. The PRD is the canonical source for Niyamavali clauses (PRD §1); the architecture + epics are the canonical sources for FRs + Story keys; this mapping is the **reader's index**. Silent duplication is forbidden — if a clause excerpt diverges from PRD §1, that is a framework gap (raise as Open Question; do NOT silently re-author the clause text).
  - [ ] **Inverse-lookup convenience section** — a secondary table inverted on FR (`FR-NNN | implementing_clauses | owning_story_keys`) so an engineer reading an FR can find the Niyamavali clause(s) it implements. Same row-content discipline.
  - [ ] **Account State Machine extract section** — verbatim transcription of the architecture §1.14 lines 1238-1246 state table (with attribution `[Source: architecture.md §1.14 lines 1238-1246]`) so an engineer doesn't need to switch to architecture.md for the load-bearing state-FR-trigger details. This is permitted duplication (the state table is small, stable, and frequently referenced); the README §"Structural invariants" section documents the exception.
  - [ ] Forbidden-removal rule applied.

- [x] **Task 4 — Author the deployment topology `deployment-topology.md`** (AC: 1)
  - [x] Created `docs/knowledge-transfer/deployment-topology.md` with all 8 sections per AC-1: §1 GCP project topology (twt-dev / twt-staging / twt-prod / twt-audit-mirror-prod / twt-dokploy-prod with per-project IAM separation per §2.10a); §2 Cloud provider service map (mirroring architecture §5.2 — Cloud SQL/Cloud Storage with Object Retention Lock/Cloud KMS+HSM/Secret Manager/Artifact Registry/IAM/Cloud Monitoring/Cloud Logging/FCM/Cloudflare/SaaS tracing); §3 Network topology (edge-only ingress + break-glass + VPC + Private Service Connect + egress allowlist + PSC verification gate per §5.8 + §5.8a); §4 Deployment substrate (Dokploy v1 + Cloud Run/GKE Autopilot migration + Dokploy failure fallback + WIF binding + image signing per §5.3 + §5.4); §5 Backup + DR posture (Cloud SQL regional HA + PITR ≤35 days + Object Retention Lock + RTO 4h + RPO 1h + cross-region replica trigger criteria + DR runbook accessibility per §5.7); §6 Per-Pariwar tenancy (v1 RLS + `pariwar_id`; 2nd-Pariwar option set; cross-Pariwar audit governance; per-module infrastructure isolation per §5.14); §7 Workspace layout (high-level structure per architecture §Workspace Layout); §8 Cross-link index mapping every section to canonical architecture lines.
  - [x] Schematic ASCII diagram embedded showing edge ingress + GCP project boundaries + audit-mirror IAM separation + Dokploy isolation + external integrations (no Mermaid — renderer-agnostic per Open Question #3 resolution).
  - [x] "Structural property — what the topology document MUST NOT carry" section authored with 6 invariants (no secrets; no substantive automation; no per-Pariwar branding; no PII; no substantive ADR content; no specific NFR thresholds).
  - [ ] Include a Mermaid (or ASCII) diagram showing GCP-project boundaries + service map + network topology — renderer-agnostic ASCII as primary, Mermaid as secondary if helpful. Diagram is **schematic, not exhaustive** — captures the load-bearing IAM-isolation boundary (audit-mirror project separation per §2.10a) + the edge-only ingress + the three-environment structure. Avoid premature precision (specific instance sizes / pool counts belong in operations policy + capacity-planning ADRs).
  - [ ] **Structural property — what the topology document MUST NOT carry** section: (i) no operational secrets (those live in `docs/escrow/` per Story 0.2); (ii) no substantive deployment automation (those live in `.github/workflows/` + `infra/dokploy/` per Story 1.15 + Story 1.1); (iii) no per-Pariwar branding configuration (that lives in `packages/tokens/per-pariwar/` per architecture §Workspace Layout); (iv) no PII / member data of any shape (the topology is non-load-bearing for confidentiality but the structural invariant prevents accidental drift); (v) no substantive ADR content (those live in `docs/adr/` post-PR-2 per architecture §Implementation Handoff).

- [x] **Task 5 — Author the on-call playbook `on-call-playbook.md`** (AC: 1)
  - [x] Created `docs/knowledge-transfer/on-call-playbook.md` using `docs/runbooks/_template.md` five-section schema verbatim. §1 Prerequisites (Solo Builder v1 on-call + Story 0.6 backup engineer + paging surface placeholder + capacity-planning indicators + audit-mirror posture + cross-link to Story 0.4 §14 activation ceremony + backup-engineer access posture + multi-actor degraded-mode property). §2 Step-by-step incident triage with **13 incident classes** (audit-integrity failure / P0 capacity-indicator breach / push-provider outage / paging SaaS outage / Dokploy substrate failure / Cloudflare edge outage / Cloud SQL HA failover / KMS unavailability / DigiLocker integration failure / WA Business suspension / helpdesk SLA breach / member-mass-event / [v1-pending] DPDPA breach response deferred to Story 14.3 per Open Question #4 resolution) — each with trigger / action (cross-linked to per-task runbook OR ADR slot OR framework) / verification check / escalation trigger. §3 Rollback per failure mode (degradation-policy activation / escrow quorum-open / code-escrow switch-to-mirror / State Trustee escalation / Story 0.13 legal). §4 Verification checks (per-class + cross-class baseline). §5 Contact escalation list (Solo Builder / backup engineer / Trustee Panel / audit-mirror on-call / legal counsel / helpline operator / vendor-side / cross-link to Story 0.4 contact list). Structural-invariant block at top ("This is the META-PLAYBOOK above the seven Phase-0 runbooks"). Changelog row recorded for initial author-commit dated 2026-05-30.
  - [ ] **Multi-actor in degraded mode property cross-check** — per architecture §"Multi-actor controls in degraded mode" (lines 4031-4037), multi-actor controls (≥2-trustee sign-off; ≥2-trustee credential quorum-open; ≥2-trustee KT pack ratification) operate in degraded mode until staffing assumptions hold. The on-call playbook documents the degraded mode behavior per §1 Prerequisites + §3 Rollback (substitute paths per the Story 0.1 AC-4 model).
  - [ ] Structural invariant block at top: "**This is the META-PLAYBOOK above the seven Phase-0 runbooks.** It does NOT replace the per-task runbooks in `docs/runbooks/`; it routes an on-call incident to the right runbook OR ADR slot OR escalation path. A substantive operational procedure that lives in this file but should live in a per-task runbook is a framework violation — refactor to the per-task runbook + cross-link from here."

- [x] **Task 6 — Author the third-party dependency inventory `third-party-dependency-inventory.md`** (AC: 1)
  - [x] Created `docs/knowledge-transfer/third-party-dependency-inventory.md` with the AC-1 schema (11 columns including `dependency_class | provider | usage | architectural_authority | owning_story | renewal_or_contract_event | next_renewal_date | monitoring_owner | vendor_contact | cross_link | status`); populated **34 rows** across 7 sections: Section A (GCP cloud provider — 6 rows), Section B (Edge/WAF + external integrations — 9 rows including Cloudflare + DigiLocker + WA + Telegram + 5 banks), Section C (Push + OS-platform — 4 rows), Section D (SaaS pending-ADR — 4 rows including telephony + paging + tracing + DLT), Section E (Regulatory + governance — 7 rows including Indian Trust Act + 12A/12AB + GST + 80G + DPDPA + RBI/UPI + TDS §194H), Section F (Continuity — 2 rows: backup engineer + legal counsel), Section G (Deferred-with-ADR — 2 rows: Module Marketplace partners + Crowdfunding). Monitoring-owner column populated per §3.10 (Solo Builder default; Trustee Panel primary for regulatory-gate rows per Open Question #5 resolution). Cross-link discipline applied — every row with corresponding credential envelope cites it (not duplicates). Periodic review cadence + forbidden-removal rule + status legend + references all populated.
  - [ ] Create `docs/knowledge-transfer/third-party-dependency-inventory.md` with table schema per AC-1: `dependency_class` | `provider` | `usage` | `architectural_authority` | `owning_story` | `renewal_or_contract_event` | `next_renewal_date` | `monitoring_owner` | `vendor_contact` | `cross_link` | `status`. Populate rows for:
    - **Cloud provider** — GCP (Cloud SQL, Cloud Storage, KMS, Secret Manager, IAM, VPC, Cloud Run, Cloud Monitoring, Cloud Logging, Cloud Tasks) per architecture §5.1 + §5.2; renewal = monthly billing + per-service quota review per §5.6 quarterly capacity review.
    - **Cloud edge / WAF** — Cloudflare per §5.8a; renewal = annual subscription tier review + DPDPA legal review per §5.8a; substitution boundaries per §5.8a pivot disposition.
    - **DigiLocker** — Govt API per §2.8 + Story 3.3a/3.3b; renewal = govt provider-approval gating per A-4; monitoring owner = Solo Builder per §3.10.
    - **WhatsApp Business** — Meta per §3.4 + Story 5.3; renewal = template-approval cadence + provider tier review; monitoring owner = Solo Builder + Trustee Panel.
    - **Telegram Bot API** — Telegram per §3.4 + Story 5.5; renewal = none formal; monitoring owner = Solo Builder.
    - **Bank statement intake — 5-bank allowlist** — SBI, PNB, BoB, BoI, Bihar cooperative per architecture §3.6 + Story 9.2; renewal = per-bank contract / API token rotation; status = `pending-system-availability` (Story 9.2 closure).
    - **Push provider** — FCM (Google) + APNs (Apple) per §3.4 + Story 5.2; renewal = annual Apple Developer Program + Google Play Console; monitoring owner = Solo Builder.
    - **OS-platform dependencies** — Apple Developer Program (annual renewal), Google Play Console (one-time + payout tax review), App Store Connect / Play Console signing certs.
    - **Telephony provider** — `pending-ADR-selection` per §3.5 + §3.5 + ADR territory (Twilio India / Exotel / Knowlarity / Plivo); Story 10.3.
    - **Paging SaaS** — `pending-ADR-selection` per §5.10 (may colocate with tracing/crash SaaS).
    - **Tracing/crash SaaS** — `pending-ADR-selection` per §5.6 (Sentry / Honeycomb / Grafana Cloud Traces).
    - **DPDPA breach-reporting tooling** — Story 14.3 + Story 0.13; `pending-system-availability`.
    - **DLT registration (SMS)** — TRAI DLT per §2.2 + §3.4; renewal = per-template registration cadence; Story 5.6.
    - **Module Marketplace partner integrations** — `deferred-with-ADR` until first partner contract (Epic 12).
    - **External-regulatory gates** — Indian Trust Act registration (Bihar); 12A/12AB; GST; 80G (Phase 2/3); DPDPA Data Fiduciary registration; RBI/UPI rate-limit workaround; TDS §194H. Each row carries the regulatory authority + next compliance event date + the trustee/legal-counsel monitoring owner.
    - **Backup engineer contract** — Story 0.6; renewal = monthly retainer per A-13.
    - **Legal counsel engagement** — Story 0.13; renewal = per-engagement letter terms.
  - [ ] Each row's `monitoring_owner` defaults to Solo Builder at v1 per §3.10 + §5.10; rows that target a Trustee Panel concern (regulatory gates) have the Trustee Panel as primary with Solo Builder as secondary. The monitoring-owner column is the single most important operational handle — it answers "who watches for the deprecation announcement / contract renewal / quota change?" per the §3.10 architectural property.
  - [ ] **Cross-link discipline** — every row that has a corresponding credential envelope (e.g., Cloudflare admin → `cloudflare-account-admin` envelope in Story 0.2 inventory) cross-links to the inventory row via path + line cite. The KT pack dependency inventory does NOT duplicate the credential inventory; it cross-references it.
  - [ ] Forbidden-removal rule applied.

- [x] **Task 7 — Author the comprehension questionnaire + answer key + ledger + append Decision 2026-05-30-005 + apply cross-reference edits** (AC: 1, 2)
  - [x] Authored `docs/knowledge-transfer/comprehension-questionnaire.md` with 30 questions across 5 sections (Section A ADR awareness — 6 questions; Section B Niyamavali → FR — 6 questions; Section C deployment-topology — 6 questions; Section D on-call-playbook — 6 questions; Section E dependency-inventory — 6 questions). Administration discipline header (cold-read; ≤4-hour budget; bus-factor simulation silence; timed conditions; 80% threshold rule; no-question-lowering invariant). Question count selected at 30 (within the AC-3 ≥25 floor + Open Question #6 ≤35 ceiling).
  - [x] Authored `docs/knowledge-transfer/comprehension-questionnaire-answer-key.md` with deterministic answers per question + per-question scoring rubric (`correct` × 1.0 / `partial` × 0.5 / `incorrect` × 0 / `unanswerable-from-pack` × 0 + gap trigger); 80% threshold computation rule; per-question `partial` signals enumerated. Score-sheet template embedded for trustee-facilitator administration recording. Open Question #7 resolution: answer key visible in directory listing; trust bus-factor simulation discipline + trustee facilitator to enforce cold-read; surface stronger separation as future ADR slot if needed.
  - [x] Authored `docs/knowledge-transfer/kt-pack-ledger.md` with sections: Framework-commit record (Story 0.5 row dated 2026-05-30 inserted; status `Author-committed; awaiting trustee sign-off`); Trustee sign-off log (pack-as-a-unit OR per-component modes; ≥2-row quorum rule per Story 0.4 review precedent; supersession-schema marker column; material-edit re-sign thresholds); Comprehension administration log (30-day-takeover joint-discharge anchor in section header per Story 0.3 + 0.4 precedent; per-administration row schema with per-section scores + gap-list cite + joint-discharge contribution; AC-3 closure rule with closure-language precision); Pack-revision log (supersession-schema marker; forbidden-removal rule); Periodic re-attestation log (quarterly / annual / per-architectural-amendment / per-owning-Story-closure cadences); Procedure-revision log; Cross-links into related framework ledgers.
  - [x] Appended Decision 2026-05-30-005 at top of `.decision-log.md` "## Decisions" section per Story 0.1 + 0.2 + 0.3 + 0.4 schema. Decisions 001 / 002 / 003 / 004 NOT modified. Decision-type index amended at line 13 with `**Story 0.5 — Knowledge-Transfer Pack** —` entry inserted in numerical order between 0.4 and 0.6. Decision 005 body covers 9 numbered commitments (directory location + 8 component documents + `docs/adr/` scaffold discharging Decision 003 OF#6 + 64 deferred-ADR slots + Niyamavali → FR mapping at `spec-only` + reader's-map discipline for topology/dependency/on-call + 30-question comprehension questionnaire with no-question-lowering rule + 30-day-takeover joint-discharge anchor across eight conditions + Tasks 8-10 closure-language precision). Open follow-ups list populated with 12 entries (Trustee Panel sign-off, Story 0.6 closure, comprehension administration outcomes, per-architectural-amendment refresh, per-owning-Story-closure row status flips, quarterly dependency-renewal cadence, annual comprehension re-administration, `docs/adr/` substantive ADR drafting, 30-day takeover joint-discharge condition, Hindi-native trustee ratification, on-incident post-mortem hook, Decision 003 OF#6 closure).
  - [x] **Cross-reference edits applied** (verify-before-editing per Story 0.3 Task 5 + Story 0.4 Task 4 pattern):
    - `docs/runbooks/operational-readiness-ledger.md` — appended new "KT pack coverage" section before "Re-sign protocol" header (mirroring Story 0.3 "Mirror coverage" + Story 0.4 "Degradation policy coverage" patterns). Existing Sign-off table + Execution-validation log + Mirror coverage + Degradation policy coverage NOT modified.
    - `docs/runbooks/README.md` — appended new row to "Related runbooks expected from other stories" table for the Story 0.5 on-call playbook. Existing Story 0.4 row + other rows NOT modified.
    - `docs/escrow/credential-inventory.md` — Notes-column append on `dr-runbook-pdf-custody` row (line 92) clarifying the on-call playbook (Story 0.5) vs DR runbook (Story 0.4 OF) distinction. Envelope_class / availability / re-seal trigger / Story citations NOT modified.
    - `docs/escrow/code-escrow/README.md` — updated the existing Story 0.5 placeholder row in "Related code-survival surfaces owned elsewhere" with the 2026-05-30 author-commit stamp + complete component-file inventory + 30-day takeover joint-discharge cross-link.
    - `docs/degradation-policy/degradation-policy-ledger.md` "Table-top exercise log" section header — appended cross-link to KT pack as a co-discharger of the 30-day takeover property; the eight-condition joint-discharge expansion records the Story 0.5 contribution (AC-1 + AC-2 author-committed; AC-3 awaits Story 0.6). Existing log content NOT modified.
  - [ ] Create `docs/knowledge-transfer/comprehension-questionnaire.md` per AC-3 schema: ≥25 questions across five sections (one per PRD §9.1.1 component). Each question carries: question text; expected-coverage cite (which component file + section the answer is in); answer-format hint (cite OR procedural walkthrough OR escalation-path narrative). Section breakdown:
    - **Section A — ADR awareness** (≥5 questions): "Name three deferred-ADR slots and the Story closure that unblocks each."; "What is the difference between an ADR and a runbook?"; "Where does the `docs/adr/` directory live and what is the naming convention?"; "If you discover a decision is being made without an ADR, what do you do?"; "Why is the `docs/adr/` directory empty of substantive content at Phase 0?"
    - **Section B — Niyamavali → FR comprehension** (≥5 questions): "Which FR governs renewal-grace transition?"; "Walk through the state machine from `pending-fee` to `active`."; "Which Niyamavali clause class governs special-case death rules?"; "If a member is in `active_in_grace` and the grace period elapses, what state do they enter?"; "Where is the canonical home for the member-state state machine?"
    - **Section C — Deployment topology comprehension** (≥5 questions): "Which GCP region is prod?"; "Why is the audit-mirror in a separate GCP project?"; "What is the deployment substrate at v1 and what triggers the K8s migration?"; "What is the cross-region replica trigger criterion?"; "What is the per-Pariwar isolation strategy at v1?"
    - **Section D — On-call playbook comprehension** (≥5 questions): "If the audit-mirror replication lag alarm fires at 4 min, what is the triage path?"; "If FCM is unavailable, what is the channel fallback?"; "Solo Builder is unreachable for 8 hours and the audit-integrity-check has failed. What is the escalation path?"; "If Dokploy substrate fails, what runbook + ADR + framework do you consult?"; "What is the difference between the on-call playbook and the per-task runbooks?"
    - **Section E — Dependency inventory comprehension** (≥5 questions): "Who is the monitoring owner for DigiLocker?"; "What is the renewal cadence for the Apple Developer Program?"; "If Cloudflare announces a DPDPA-incompatible policy change, what is the response procedure?"; "Where is the WhatsApp Business template-approval cadence recorded?"; "If a 5-bank parser format changes, what is the response path?"
  - [ ] Create `docs/knowledge-transfer/comprehension-questionnaire-answer-key.md` with the deterministic answer per question + the per-question scoring rubric per AC-3 (full credit / partial / incorrect / unanswerable-from-pack). Header note: "The answer key is held in the trustee-accessible repo for trustee-facilitated scoring. The backup engineer reads the questionnaire cold — the answer key is NOT shown to the engineer until scoring is complete."
  - [ ] Create `docs/knowledge-transfer/kt-pack-ledger.md` with sections: **Framework-commit record** (Story 0.5 row dated 2026-05-30 inserted; awaiting ≥2-trustee sign-off); **Trustee sign-off log** (per-component OR pack-as-a-unit ratification; supersession-schema marker for the corresponding `.decision-log.md` entry; quorum note ≥2 rows per sign-off event per the Story 0.4 review-decision precedent); **Comprehension administration log** (per AC-3: administering trustee + backup engineer + date + time taken + per-section score breakdown + gap list + remediation owner + target date + re-administration schedule; **30-day takeover joint-discharge** anchor in this section's header per Story 0.3 + 0.4 precedent); **Pack-revision log** (mechanism-revision triggers + supersession schema markers — when a component file is revised in response to a comprehension gap, the revision is logged here with the gap-list row that triggered it); **Periodic re-attestation log** (quarterly dependency-inventory renewal-date review; annual comprehension re-administration; per-architectural-amendment ADR-index refresh); **Procedure-revision log** (mechanism-level revision triggers per the Story 0.3 + 0.4 supersession schema).
  - [ ] Append Decision 2026-05-30-005 at the top of `.decision-log.md` "## Decisions" section per the Story 0.1 + 0.2 + 0.3 + 0.4 load-bearing template (reverse-chronological per file header comment). Decisions 001 / 002 / 003 / 004 NOT modified; entry template NOT modified. Entry title verbatim: `Decision 2026-05-30-005: Knowledge-transfer documentation pack compiled + comprehension questionnaire scaffolded; trustee sign-off + backup-engineer comprehension administration pending`. **Decision type** = `Story 0.5 — Knowledge-Transfer Pack`; **Status** = `Author-committed; awaiting trustee sign-off`. **Decision body (numbered 1-9):** (1) `docs/knowledge-transfer/` canonical location as top-level surface parallel to existing framework surfaces; (2) seven framework documents (README + ADR index + Niyamavali → FR mapping + deployment topology + on-call playbook + dependency inventory + comprehension questionnaire) + ledger + answer key as governance instruments inheriting Story 0.2 + 0.3 + 0.4 framework patterns; (3) `docs/adr/` directory scaffolded (README + template; substantive content deferred to PR-2 per architecture §Implementation Handoff) — **discharges Decision 2026-05-29-003 Open Follow-up #6**; (4) ADR-index populated with ≥40 deferred-ADR slots already named in architecture + runbooks + escrow + degradation-policy frameworks; (5) Niyamavali → FR mapping at `spec-only` status — implementation status flips under owning Story closure; (6) deployment-topology + dependency-inventory + on-call-playbook are *reader's maps* keyed to architecture canonical sources — silent duplication forbidden per structural invariants; (7) comprehension questionnaire ≥25 questions across 5 sections + answer key + scoring rubric (≥80% threshold; `unanswerable-from-pack` = framework gap); (8) **30-day takeover joint-discharge** anchored in kt-pack-ledger Comprehension administration log header per Story 0.3 + 0.4 precedent — the joint-discharge is fully closed only when Stories 0.3 + 0.4 + 0.5 + 0.6 jointly close (all eight conditions); (9) framework ready for Tasks 8-10 with closure-language precision per [[feedback_closure_language_precision]] + Story 0.6 dependency for backup-engineer-administered comprehension. **Decision-type index amended** with `**Story 0.5 — Knowledge-Transfer Pack** —` entry inserted in numerical order between 0.4 and 0.6 per Story 0.2 + 0.3 + 0.4 review-decision precedent. **Open follow-ups list populated** — 10+ entries covering: Tasks 8-10 ratification + administration path; Story 0.6 closure → comprehension administration unblock; quarterly dependency-renewal-date review cadence; annual comprehension re-administration; per-architectural-amendment ADR-index refresh trigger; Niyamavali → FR mapping row status flips under each owning Story closure; ADR drafting cadence for the ≥40 catalogued slots (PR-2 / implementation-time per architecture §Implementation Handoff); on-call playbook revision trigger on post-incident review; KT pack revision trigger on comprehension-administration gap; 30-day takeover joint-discharge condition per Story 0.3 Decision 003 precedent (Stories 0.3 + 0.4 + 0.5 + 0.6 jointly discharge).
  - [ ] **Apply cross-reference edits** (verify-before-editing per Story 0.3 Task 5 + Story 0.4 Task 4 pattern):
    - `docs/runbooks/operational-readiness-ledger.md` — append new "KT pack coverage" section per AC-2 (mirroring the Story 0.3 "Mirror coverage" + Story 0.4 "Degradation policy coverage" pattern). Existing Sign-off table + Execution-validation log + Mirror coverage + Degradation policy coverage sections NOT modified.
    - `docs/runbooks/README.md` — append new row to "Related runbooks expected from other stories" table for the Story 0.5 on-call playbook per AC-2. Existing rows NOT modified.
    - `docs/escrow/credential-inventory.md` — Notes-column append on existing `dr-runbook-pdf-custody` row at line 92 clarifying the on-call playbook vs DR runbook distinction per AC-2. Envelope_class / availability / re-seal trigger / Story citations NOT modified.
    - `docs/escrow/code-escrow/README.md` — append new row to "Related code-survival surfaces owned elsewhere" table for the KT pack per AC-2. Append-only; existing Story 0.4 + DR-runbook rows NOT modified.
    - `docs/degradation-policy/degradation-policy-ledger.md` "Table-top exercise log" section header — append cross-link to KT pack as a co-discharger of the 30-day takeover property per AC-2. Existing log content NOT modified.

- [ ] **Task 8 — ≥2-trustee sign-off on KT pack** (AC: 1, 2) — _AWAITING EXTERNAL ACTION (Trustee Panel; requires Tasks 1-7 closure)_
  - [ ] Coordinate with the Trustee Panel to schedule the sign-off session. **Permitted ratification modes** (Trustee Panel preference; mirrors Story 0.4 Task 7 pattern): (a) **pack-as-a-unit** — panel ratifies the seven component files + ledger + answer key + ADR scaffold as a single unit; one ledger entry; one Decision 006 supersession; (b) **per-component** — panel ratifies each component file individually; one ledger entry per component. Mode recorded in the sign-off log header.
  - [ ] Each ratifying trustee records in `docs/knowledge-transfer/kt-pack-ledger.md` "Trustee sign-off log" the date, component(s) ratified, ratification mode, signature line per the Story 0.2 + 0.4 sign-off convention. Quorum note: ≥2 rows per sign-off event (single-row sign-off is incomplete).
  - [ ] A new `.decision-log.md` `[CONTINUITY]` entry supersedes the Task-7 Decision 005 "Author-committed; awaiting trustee sign-off" status per the Story 0.1 + 0.2 + 0.3 + 0.4 supersession schema (the new entry references the prior; the prior is not modified). New entry status = `Trustee-ratified`.
  - [ ] AC-1 + AC-2 are **fully closed** when ≥2 trustees have signed off. Surface rows owned by Stories not yet closed (the Niyamavali → FR mapping `spec-only` rows; the dependency inventory `pending-system-availability` rows) are enumerated as `pending-system-availability` in the sign-off Notes column — they do NOT block AC-1/AC-2 full closure.

- [ ] **Task 9 — Comprehension questionnaire administration to contracted backup engineer** (AC: 3) — _AWAITING EXTERNAL ACTION (Story 0.6 backup engineer + Trustee Panel facilitator; requires Story 0.6 closure + Task 8 ≥1-trustee sign-off)_
  - [ ] Coordinate with the Trustee Panel + the contracted backup engineer per Story 0.6 to schedule the first administration. **Facilitator** = a trustee (not Solo Builder; the bus-factor simulation discipline requires Solo Builder silent for the duration). **Administration mode** = backup engineer reads the KT pack cold (≤4 hours recommended per AC-3) → completes the questionnaire under timed conditions → trustee facilitator scores per the answer key + rubric.
  - [ ] The administration is logged in `kt-pack-ledger.md` "Comprehension administration log" with: administering trustee, backup engineer identity, administration date, time taken, per-section score breakdown, gap list (the `unanswerable-from-pack` questions + the rationale for each gap), remediation owner per gap, remediation target date.
  - [ ] **Bus-factor simulation discipline** (inherited from Story 0.1 AC-4 + Story 0.2 AC-3 + Story 0.3 AC-2 + Story 0.4 AC-2): Solo Builder is silent for the duration including side channels. Any question to Solo Builder is a gap signal; the artifact is insufficient.
  - [ ] **Closure-language precision** per [[feedback_closure_language_precision]]: an administration that scores ≥80% with NO `unanswerable-from-pack` gaps closes AC-3 as **Closed by [edit]**. An administration that scores ≥80% with `unanswerable-from-pack` gaps closes AC-3 as **Provisionally closed; full closure pending gap discharge per Task 10**. An administration that scores <80% does NOT close AC-3 — the pack revision per Task 10 is triggered, then a re-administration is scheduled.

- [ ] **Task 10 — 80% threshold met + gap discharge** (AC: 3) — _AWAITING EXTERNAL ACTION (depends on Task 9 administration outcome + iterative pack revisions)_
  - [ ] If Task 9 administration scored ≥80% with no gaps: AC-3 is fully closed. Log the closure in the ledger; append a `.decision-log.md` `[CONTINUITY]` entry recording the threshold close (the entry references Decision 005 + Decision 006 per the supersession schema).
  - [ ] If Task 9 administration scored ≥80% with `unanswerable-from-pack` gaps OR scored <80%: **pack revision per gap** is triggered. The revision is authored under the Story 0.5 framework authority — each gap-discharging edit is logged in the ledger "Pack-revision log" with the gap-list row that triggered it + the revised component file + the revision-supersession-schema marker. After all gaps are discharged, a re-administration is scheduled. Re-administration is the same procedure as Task 9; the re-administration row is appended to the Comprehension administration log.
  - [ ] **AC-3 fully closes** when an administration reaches the 80% threshold AND no `unanswerable-from-pack` gaps remain (OR the gaps are explicitly deferred with rationale per [[feedback_closure_language_precision]] — e.g., a gap pertains to a Story not yet closed, deferred to that Story's closure).
  - [ ] **30-day-takeover joint-discharge anchor** (per Story 0.3 Decision 003 + Story 0.4 Decision 004 Open Follow-ups): once AC-3 fully closes AND Story 0.6 (backup engineer contract) fully closes AND Stories 0.3 + 0.4 fully close, a follow-up `.decision-log.md` `[CONTINUITY]` entry records the joint-discharge achievement per the Story 0.3 Decision 003 closure path.

### Scope discipline — what this story is and is not

This story is **governance + framework + content-distillation + first execution of the comprehension questionnaire (post-backup-engineer-contracting)**, in that order. The dev agent's job is to:

- Author the KT-pack framework documents under `docs/knowledge-transfer/` (Tasks 1, 2, 3, 4, 5, 6) following the Story 0.2 + 0.3 + 0.4 framework-as-top-level-surface pattern (README + ADR index + Niyamavali → FR mapping + deployment topology + on-call playbook + dependency inventory + comprehension questionnaire + answer key + ledger)
- Scaffold `docs/adr/` (README + template; substantive ADR content explicitly deferred to PR-2 / implementation-time per architecture §Implementation Handoff)
- Cross-link to Story 0.1's operational-readiness-ledger, Story 0.1's runbook README, Story 0.2's credential-inventory, Story 0.3's code-escrow framework, Story 0.4's degradation-policy ledger (the narrowly-scoped cross-reference updates per Task 7 — do NOT broaden the edit beyond the named lines or surface)
- Append a `[CONTINUITY]` entry to `.decision-log.md` per the Story 0.1 + 0.2 + 0.3 + 0.4 schema (Task 7). Append-only; the decision-type index gets a Story 0.5 line per the Story 0.2 + 0.3 + 0.4 review-decision precedent
- **Leave Tasks 8, 9, 10 explicitly unchecked + tagged `_AWAITING EXTERNAL ACTION_`**, the same way Story 0.1 Tasks 4-5, Story 0.2 Tasks 6-9, Story 0.3 Tasks 7-11, and Story 0.4 Tasks 7-9 were tagged. The dev agent cannot ratify the pack (Trustee Panel authority); cannot administer the comprehension questionnaire (requires backup engineer per Story 0.6); cannot discharge the 80% threshold (requires the administration outcome)

### What this story does NOT modify (and what it touches narrowly)

- **No substantive ADR content authored.** Story 0.5 scaffolds `docs/adr/` (README + template) and inventories the deferred-ADR slots in `adr-index.md`; substantive ADR drafting is PR-2 / implementation-time work per architecture §Implementation Handoff (lines 5069-5096). If a substantive ADR is authored within Story 0.5 scope, that is a scope-discipline violation — surface as an Open Question.
- **No substantive Niyamavali rule registry implementation.** The mapping table is the cross-walk specification; Story 2.3 + Epic 4 operationalize the registry.
- **No substantive deployment automation.** The topology document is a reader's map; Story 1.1 + Story 1.15 author the substrate + auto-deploy pipeline.
- **No changes to architecture, PRD, epics docs.** If a divergence is observed (e.g., a Niyamavali clause class in PRD §1 cannot be cleanly mapped to FRs), record it as an Open Question; do not patch architecture / PRD / epics from a Story.
- **No changes to Story 0.1 / 0.2 / 0.3 / 0.4 framework documents** beyond the narrow cross-reference updates named in Task 7. The verify-before-editing discipline from Story 0.3 Task 5 + Story 0.4 Task 4 applies — do NOT broaden the edits beyond the named lines or sections.
- **No DR runbook authoring.** The on-call playbook is structurally adjacent to the DR runbook (both are operational meta-documents), but they are SEPARATE artifacts. Story 0.5 authors the on-call playbook (per architecture §5.10); the DR runbook (per architecture §5.7) remains the Story 0.4 Decision 004 Open Follow-up.
- **No new GitHub Actions workflows.** Story 0.5 is content authoring + framework scaffolding, not pipeline authoring.
- **No comprehension administration during dev-story.** Task 9 is `_AWAITING EXTERNAL ACTION_` — the dev agent leaves the administration log unstarted (empty section with schema only). Administration requires the contracted backup engineer per Story 0.6.

### `.decision-log.md` schema is load-bearing — follow Story 0.1 + 0.2 + 0.3 + 0.4's template exactly

Story 0.1 established the `.decision-log.md` schema; Stories 0.2 + 0.3 + 0.4 are the existing downstream consumers; Story 0.5 is the fifth. The template must be followed verbatim:

- Title format: `Decision YYYY-MM-DD-NNN: <title>`
- Fields: Decision type, Status, Author, Ratifying trustees, Context, Decision (numbered if multi-commit), Open follow-ups, References
- Status values: `Author-committed; awaiting trustee sign-off` | `Trustee-ratified` | `Trustee-ratified (conditional on Story 0.13 counsel return)` | `Reversed by Decision YYYY-MM-DD-NNN`
- Decision-type tag for Story 0.5: `Story 0.5 — Knowledge-Transfer Pack` (matches the index entry pattern from Story 0.1 + 0.2 + 0.3 + 0.4)
- Append at the **top** of the existing decisions section (reverse-chronological per the file's header comment)
- The decision-type index gets a `**Story 0.5 — Knowledge-Transfer Pack** — …` line per Story 0.2 + 0.3 + 0.4 precedent, inserted in numerical order between 0.4 and 0.6

If the dev agent discovers the template is insufficient (missing field, ambiguous status), the change is itself a `.decision-log.md` `[GOV]` entry proposing the schema amendment — not a silent rewrite.

### Property / control / policy / **gap-analysis** discipline applied to the KT pack

This is a direct application of [[feedback_architecture_vs_adr_boundary]] + [[feedback_architecture_vs_prd_boundary]] + [[feedback_gap_analysis_observational]] to the knowledge-transfer domain. The four-way split (mirroring Story 0.4's extension):

| Layer | What it commits | Where it lives in Story 0.5 |
|---|---|---|
| **Property** (architecture-equivalent) | Technical context is recoverable; ADR index is the canonical inventory of deferred-decision slots; Niyamavali → FR mapping is the canonical cross-walk; deployment topology is a reader's map keyed to architecture; on-call playbook routes incidents to runbooks / ADRs / escalation paths; dependency inventory captures monitoring owner + renewal cadence + vendor contact per dependency; comprehension administration discharges the 80% threshold property | `docs/knowledge-transfer/README.md` "Structural invariants" section; AC text in this story; the per-component file schemas |
| **Policy** (PRD-equivalent) | What the trust does to recover technical context under bus-factor scenario (PRD §9.1.1: KT pack at minimum includes ADRs / Niyamavali → FR / deployment topology / on-call / dependency inventory; backup engineer per A-13 reads the pack and answers ≥80% of a comprehension questionnaire); review-cadence policy fallback (quarterly / annual / per-event) | `docs/knowledge-transfer/README.md` §"Review cadence fallback"; per-component file content; comprehension questionnaire section coverage |
| **Control** (ADR territory) | Specific paging integration for on-call surface (per §5.10 deferred); specific provider for telephony / paging / tracing / push (per §3.5 + §5.6 + §5.10 deferred); specific renewal cadence per dependency class (per operations policy); specific Niyamavali rule-registry data model (per Story 2.3 + Epic 4); specific deployment automation (per Story 1.1 + 1.15) | `docs/knowledge-transfer/README.md` "Open ADR slots" section; `adr-index.md` deferred-slot rows; cross-references to `docs/adr/` once substantive content lands per PR-2 |
| **Gap analysis** (observational) | Comprehension administration observes which questions are `unanswerable-from-pack`; periodic re-attestation observes which dependency renewal-dates are stale; ADR-index review observes which deferred slots have lingered without authoring; Niyamavali → FR mapping observes which clauses lack implementing FRs | `kt-pack-ledger.md` "Comprehension administration log" gap-list rows; "Periodic re-attestation log" stale-date rows; "Pack-revision log" gap-discharge rows; README §"Open ADR slots" |

The gap-analysis layer does NOT prescribe sprint planning or override architecture — it observes incompleteness/risk and proposes conditional escalation paths per [[feedback_gap_analysis_observational]].

### Closure-language precision applied per AC leg

Per [[feedback_closure_language_precision]], each AC leg must distinguish three states with precision:

- **Closed by [edit]** — the KT-pack scaffolding is authored; the seven component files exist with the schemas described above; the ADR scaffold (`docs/adr/`) is created; the ledger has a framework-commit row; the Decision 005 entry is appended; the cross-reference edits to Stories 0.1 / 0.2 / 0.3 / 0.4 frameworks are applied. These are produced artifacts in the repo; the framework is operational from day one (modulo trustee ratification + backup-engineer administration).
- **Resolved via explicit deferral** — ≥2-trustee sign-off is deferred to Trustee Panel (Task 8); comprehension administration is deferred to Story 0.6 closure + Task 9; 80% threshold close is deferred to Task 10 outcome; substantive ADR content is deferred to PR-2 / implementation-time per architecture §Implementation Handoff; Niyamavali → FR mapping row status flips are deferred to each owning Story closure; dependency inventory `pending-system-availability` rows are deferred to each owning Story closure; the 30-day takeover joint discharge is deferred to the union of Stories 0.3 + 0.4 + 0.5 + 0.6. The deferral rationale is recorded; the revisit trigger is named.
- **Not addressed** — should not occur. If a step has no rationale and no owning future action, raise it as an Open Question at the end of this story.

**Never collapse "deferred" with "closed."** Doing so would let a reader believe the KT pack is fully discharging the 30-day takeover when in fact the comprehension administration remains pending — exactly the kind of audit drift the closure-language precision feedback is meant to prevent.

### Cross-Story dependency map (read before Tasks 8-10)

Story 0.5 has high cross-Story coupling because trustee ratification, comprehension administration, and the 80% threshold close all depend on Stories that have not yet closed. The dependency direction: **Story 0.5 produces the framework + the seven mandated components + the comprehension questionnaire** that those Stories' verifications run against; the framework is usable from day one and is incrementally exercised as the dependencies close.

| Dependency | Owning Story | Story 0.5 framework status |
|---|---|---|
| ≥2-trustee sign-off on KT pack | Story 0.5 Task 8 (Trustee Panel authority) | Author-committed framework is the unit ratified; pack-as-a-unit OR per-component mode is the panel's choice |
| Comprehension administration | Story 0.6 (backup-engineer-contracted-with-trustee-authorization) | Forward-deferred; AC-3 full closure has a hard dependency on Story 0.6 closure |
| 80% threshold close | Story 0.5 Task 10 (depends on Task 9 outcome) | Iterative; pack revisions discharge gaps; re-administration confirms close |
| Substantive ADR content drafting | PR-2 / implementation-time per architecture §Implementation Handoff (Story 1.1 + downstream Stories) | Forward-deferred; Story 0.5 scaffolds the `docs/adr/` directory + populates `adr-index.md` with deferred-slot rows; substantive content lands per the owning Story closure |
| Niyamavali rule registry implementation | Story 2.3 (data model) + Epic 4 (rules engine + member validity service) | Forward-deferred; mapping rows flip from `spec-only` to `in-implementation` / `partially-implemented` / `fully-implemented` under owning Story closure authority |
| Niyamavali public render | Story 2.5 (public-Astro-SSR shell foundation) + Story 2.4 (amendment workflow + admin UI) | Forward-deferred; the KT pack mapping table cites the rule registry, not the render surface |
| Deployment automation | Story 1.1 (turborepo monorepo bootstrap) + Story 1.15 (Dokploy auto-deploy pipeline) | Forward-deferred; topology document is a reader's map keyed to architecture, NOT a duplicate of automation |
| On-call paging integration | Story 5.x dispatcher OR Story 1.16x CI/observability + operations-policy ADR per §5.10 | Forward-deferred; on-call playbook §1 Prerequisites marks the paging surface `[deferred ADR — placeholder procedure]` |
| Per-dependency renewal-date population | Per-Story closure for the corresponding dependency class | Forward-deferred; dependency-inventory rows carry `pending-system-availability` until the owning Story closes |
| Code-survival posture (the "if Solo Builder is unavailable >7 days" upstream) | Story 0.3 (code-escrow framework + mirror pipeline) | **Closed by [edit] author-commit** as of 2026-05-29; Tasks 7-11 of Story 0.3 are themselves `_AWAITING EXTERNAL ACTION_` but Story 0.5 does not block on them — the framework is the upstream input |
| Degradation framing (the "Solo Builder unavailable >7 days" comms-and-stance upstream) | Story 0.4 (degradation policy framework) | **Closed by [edit] author-commit** as of 2026-05-29 + Tasks 1-6 + 2026-05-30 review closure; Tasks 7-9 of Story 0.4 remain `_AWAITING EXTERNAL ACTION_` but Story 0.5 does not block on them |
| Credential-survival posture | Story 0.2 (credential-escrow framework) | **Closed by [edit] author-commit** as of 2026-05-29; Story 0.5 cross-references credential envelopes in the dependency inventory but does not block on Story 0.2 Tasks 6-9 closure |
| Backup engineer contract | Story 0.6 (backup-engineer-contracted-with-trustee-authorization) | Forward-deferred; Story 0.6 closure unblocks Task 9 + Task 10 + the 30-day takeover joint-discharge |
| Legal counsel engagement (for the dependency inventory's regulatory-gate rows) | Story 0.13 (legal-counsel-concurrent-review-engagement-signed) | Forward-deferred; legal counsel review of regulatory-gate rows is post-engagement work |
| Architectural launch-gate inventory | Story 0.15 | Forward-deferred; the launch-gate inventory + the ADR-index are related but distinct artifacts |

**Path through AC-1 + AC-2 + AC-3 full closure.** AC-1 + AC-2 full closure requires Task 8 (≥2-trustee sign-off). AC-3 full closure requires Task 9 (administration) + Task 10 (gap discharge + 80% threshold close). Provisional closure paths exist for AC-3 at Story 0.5 author-commit + post-Task-8 closure — the trust accepts provisional closure as the practical state during Phase 0 per [[feedback_closure_language_precision]].

### Self-sufficiency execution guardrails (Task 9)

**Bus-factor simulation discipline** (inherited from Story 0.1 AC-4 + Story 0.2 AC-3 + Story 0.3 AC-2 + Story 0.4 AC-2): when the comprehension administration is conducted, Solo Builder is silent for the duration of the administration including side channels. Do not answer questions. If a question is asked, that is the gap to log — the pack artifact is insufficient.

**Facilitator selection** (Task 9): the facilitator MUST be a trustee (NOT Solo Builder). Strong default: Trustee Panel chair OR delegated trustee per panel preference. The substitute path is permitted only if the panel is unavailable for an extended window — recorded in `.decision-log.md` as a `[CONTINUITY]` entry citing the deferral.

**Gap-list recording discipline** (Task 9 + Task 10): every gap surfaced in administration MUST cite a specific questionnaire question + the per-section expected coverage + the rationale for why the pack is insufficient. A gap that resolves on "the engineer mentioned this without an artifact cite" is a gap-recording gap — re-record per the schema.

### Cross-Story Dependency Note — pack completeness drift detection

**Pack completeness.** Tasks 1-6 commit the v1-in-scope content. As Epic 1-14 Stories close, the pack content drifts:
- The ADR-index needs new rows as new deferred-decision slots emerge in implementation;
- The Niyamavali → FR mapping rows flip status as implementing Stories close;
- The dependency inventory rows flip from `pending-system-availability` to `active` as dependencies are contracted;
- The deployment topology evolves as substrate decisions land (Dokploy → K8s migration; 2nd-Pariwar provisioning);
- The on-call playbook absorbs runbook references as new runbooks are authored per architecture §5.15.

The drift-amendment cadence is not Story 0.5's responsibility — it is the **operating discipline** committed in the README §Review cadence fallback (quarterly dependency review surfaces drift; per-architectural-amendment ADR-index refresh; per-Story-closure mapping + inventory row status flips). Each drift-amendment is a new ledger entry per the supersession schema.

### `docs/knowledge-transfer/` is on the trustee-accessible storage surface (inheriting Story 0.3's mirror coverage)

Per AC-1 + AC-2, the trustee-accessible storage requirement is satisfied via the primary git repo. Once the mirror is live (Story 0.3 Tasks 7-11 closed), the KT pack framework documents themselves enjoy the same code-escrow coverage they describe — useful symmetry. There is no separate trustee-only viewing surface needed.

The comprehension-questionnaire **answer key** lives in the same trustee-accessible repo per AC-3 (the backup engineer reads the questionnaire cold WITHOUT the answer key; the answer key is trustee-facilitated scoring discipline). No separate escrow surface for the answer key — the bus-factor scenario assumes the trustee facilitator OR a substitute trustee opens the answer key during scoring.

### Project Structure Notes

**Alignment with unified project structure** (architecture.md §Workspace Layout, lines 4154-4175):

- `docs/knowledge-transfer/` is a **new top-level surface** under `docs/`, parallel to `docs/runbooks/`, `docs/escrow/`, `docs/degradation-policy/`, and the newly-scaffolded `docs/adr/`. The architecture's §Workspace Layout commits `docs/` as the directory for cross-cutting documentation surfaces; the KT pack is one such surface per PRD §9.1.1 + architecture Cross-Cutting #20. The new surface follows the Story 0.2 + 0.3 + 0.4 framework-as-top-level-surface precedent.
- `docs/adr/` is the architecture-committed directory at lines 636 + 4170 — Story 0.5 scaffolds it (README + template) per the Decision 2026-05-29-003 Open Follow-up #6 closure.
- All file paths use lowercase-kebab-case per the project naming convention (per architecture §Workspace Layout convention examples).

**Detected variances** — none from the architecture's committed structure. The `docs/knowledge-transfer/` directory is not explicitly enumerated by architecture but is the natural fit for the KT-pack cross-cutting concern committed in PRD §9.1.1. Alternative considered: place the components under `docs/runbooks/` (the on-call playbook) + `docs/architecture/` (the topology + mapping) + cross-link from a top-level `KT-PACK.md` index — rejected because: (a) the framework includes multiple component types (ADR index + mapping + topology + on-call + dependency inventory + questionnaire + ledger) that is broader than any single existing directory's scope; (b) the architecture commitments support a separate `docs/<framework>/` directory per cross-cutting concern (per `docs/escrow/` + `docs/degradation-policy/` precedent); (c) the trustee-accessible-storage commitment in PRD §9.1.1 is best discharged by a unified directory the trustees can reference as a single surface. The choice is recorded in the README §1 + Decision 005 body.

### Previous Story Intelligence

**Story 0.1 (Operational Runbooks) — patterns inherited:**

- Five-section runbook template (`docs/runbooks/_template.md`) — used verbatim for `on-call-playbook.md`
- Ledger pattern (`docs/runbooks/operational-readiness-ledger.md`) — `kt-pack-ledger.md` follows the same structure (sign-off table; per-administration log; coverage cross-link; supersession-schema marker)
- `.decision-log.md` schema (Decision YYYY-MM-DD-NNN with Decision type, Status, Author, Ratifying trustees, Context, Decision, Open follow-ups, References) — used verbatim for Decision 2026-05-30-005
- Substitute-engineer authorization model from Story 0.1 AC-4 — applied to the comprehension administration facilitator (Task 9) when Trustee Panel chair is unavailable
- Bus-factor simulation discipline — Solo Builder is silent during the comprehension administration

**Story 0.2 (Credential Escrow) — patterns inherited:**

- Framework scaffolding pattern (README + inventory + ledger + sub-files) — used as the structural template for `docs/knowledge-transfer/` (seven parallel artifacts: README + ADR index + mapping + topology + on-call + dependency inventory + questionnaire + ledger + answer key)
- Three-way property/control/policy discipline — extended to **four-way** with the gap-analysis observational layer per [[feedback_gap_analysis_observational]] (mirroring Story 0.4's extension)
- Inventory-row-addition sign-off lifecycle (≥1 trustee acknowledgment + `.decision-log.md` `[CONTINUITY]` entry) — applied to dependency-inventory row additions when new dependencies are contracted
- Author-committed-vs-awaiting-external-action task separation — Tasks 1-7 are author-committable; Tasks 8-10 are explicitly `_AWAITING EXTERNAL ACTION_`
- Forbidden-removal rule on inventory rows — applied to `adr-index.md` + `niyamavali-fr-mapping.md` + `third-party-dependency-inventory.md`

**Story 0.3 (Code Escrow + Mirror) — patterns inherited:**

- Framework-as-top-level-surface precedent — `docs/knowledge-transfer/` is the fifth such surface (after runbooks + escrow + code-escrow + degradation-policy); `docs/adr/` is the sixth, scaffolded by this Story per Decision 003 Open Follow-up #6
- 30-day takeover joint-discharge anchor — explicitly cited in `kt-pack-ledger.md` "Comprehension administration log" section header (mirroring Story 0.3's anchor); the Story 0.5 contribution to the joint discharge is recorded per the Story 0.3 Decision 003 closure path
- Cross-reference update discipline — Task 7 follows the Story 0.3 Task 5 + Story 0.4 Task 4 verify-before-editing pattern; do not broaden the edit beyond the named lines
- Open Question persistence — implementation-time divergences are recorded as Open Questions at the end of the Story, not silently patched into other Stories' artifacts
- Supersession-schema marker per the new `.decision-log.md` entry referencing the prior; the prior is not modified

**Story 0.4 (Per-Surface Degradation Policy) — patterns inherited:**

- Four-way property/control/policy/gap-analysis discipline — applied above per the property table
- Closure-language precision discipline ("Closed by [edit]" / "Resolved via explicit deferral" / "Not addressed") — applied per AC leg
- `[deferred ADR — placeholder procedure]` tagging convention — applied in the on-call playbook §1 Prerequisites (paging surface) + the ADR-index slot status
- Ledger structural-section schema (Framework-commit record + Trustee sign-off log + Administration log + Pack-revision log + Periodic re-attestation log + Procedure-revision log) — applied to `kt-pack-ledger.md`
- README structural-invariants block — applied to `docs/knowledge-transfer/README.md` §"Structural invariants — what the KT pack MUST NOT violate"
- "Not addressed is a framework-gap trigger, not a valid state" closure rule — applied

**Story 0.4 review-decision precedents — applied to Story 0.5:**

- ≥2-quorum sign-off schema column (Supersession-schema marker) — applied to `kt-pack-ledger.md` Trustee sign-off log schema
- Per-decision-point artifact citation requirement (Story 0.4 review patch) — applied to comprehension gap-list rows (every gap cites question ID + section coverage + rationale)
- Activation/deactivation ceremony pattern (Story 0.4 review patch §14) — adapted: the KT pack does not have an "activation" surface (it is always-on once authored), but the comprehension administration has an analogous *administration-event ceremony* — facilitator named, bus-factor silence verified, administration logged

### Web research notes (latest tech specifics)

No external web research is needed for Story 0.5. The story is governance + content distillation (markdown framework documents synthesizing from existing PRD / architecture / epics / framework artifacts) with no external library or framework dependencies. The architectural citations (§5.1-§5.10 + §1.14 + §1.5 + §2.7 + §2.8 + §3.4 + §3.10 + Cross-Cutting #20 + §Critical External Dependencies + §Implementation Handoff) are authoritative within the repo; no version-pinned external tech requires currency check. If a deferred-ADR slot in `adr-index.md` references a specific library version pinning (e.g., Google Tink per architecture §2.7), the row carries the architectural cite — substantive version pinning is the implementation-time ADR's job, not Story 0.5's.

### Open Questions

_Implementation-time Open Questions surfaced during dev-story execution will be appended here. Pre-author-commit questions for review:_

1. **ADR-index row population from runbooks** — the `[deferred ADR — placeholder procedure]` tags in the existing seven Phase-0 runbooks need to be catalogued in `adr-index.md`. Should the dev agent read each runbook end-to-end to extract every tagged slot, OR rely on the README §"Open ADR slots" sections of each Story framework? Recommended: read each runbook end-to-end during Task 2 execution (the runbooks are short; the tags are grep-able; the comprehensive catalogue is the discipline). Decision required at Task 2 execution time.
2. **Niyamavali → FR mapping vs PRD §1 structure** — the PRD §1 Niyamavali section is the canonical source. If the section's structure (R-1 through R-15 named in PRD) is not yet finalized in the PRD draft Story 0.5 reads at author-commit time, what is the fallback? Recommended: populate rows for whatever R-N classes the PRD §1 currently enumerates; defer subsequent rows to the PRD amendment cycle + record as Open Follow-up in the ledger. Decision required at Task 3 execution time.
3. **Deployment topology diagram format** — ASCII vs Mermaid vs both. Recommended: ASCII as primary (renderer-agnostic; markdown-rendering surfaces all support it); Mermaid as optional supplement if the dev agent assesses ASCII as insufficient for the cross-project IAM boundary visualization. Decision at Task 4 execution time.
4. **On-call playbook incident-class taxonomy completeness** — is the AC-1 list (audit-integrity / capacity-indicator / push-provider / paging / Dokploy / Cloudflare / Cloud SQL / KMS / DigiLocker / WA / helpdesk / member-mass-event) exhaustive at v1 scope, or are there classes deferred to Epic 5 + Epic 10 + Epic 14 (e.g., DPDPA breach response per Story 14.3 = a meta-class)? Recommended: author the AC-1 list as v1 commitment; mark `[deferred to Story 14.3]` for DPDPA-breach-response; add as Open Follow-up. Decision at Task 5 execution time.
5. **Dependency inventory monitoring-owner default** — Solo Builder per §3.10 at v1, but several regulatory-gate rows (Indian Trust Act registration, DPDPA registration, GST) are Trustee Panel + legal-counsel concerns. Should those rows show Trustee Panel as primary monitoring owner, or Solo Builder with Trustee Panel as escalation? Recommended: primary monitoring owner = Trustee Panel for regulatory-gate rows; Solo Builder = secondary; legal counsel per Story 0.13 = legal escalation. Decision at Task 6 execution time.
6. **Comprehension questionnaire question count vs ≤4-hour administration constraint** — AC-3 says ≥25 questions; the ≤4-hour recommendation. If the dev agent authors 35-40 questions to be more comprehensive, the administration may exceed 4 hours. Recommended: ≥25 minimum; cap at 30-35 to preserve the time budget; surface as Open Follow-up if the trustees prefer more depth. Decision at Task 7 execution time.
7. **Answer key separation discipline** — the answer key lives in the same repo as the questionnaire. Is there a risk that a backup engineer with `git log` access discovers the answer key file path and reads it before administration? Recommended: name the file `comprehension-questionnaire-answer-key.md` (visible in directory listing); trust the bus-factor simulation discipline + the trustee facilitator to enforce the cold-read; surface as Open Follow-up if a stronger separation is needed (e.g., separate trustee-only Git branch). Decision at Task 7 execution time.

## Dev Notes

### Relevant architecture patterns and constraints

- **PRD §9.1.1 paragraph 5 "Knowledge-transfer documentation"** (PRD lines 1378-1379) — commits ADRs + Niyamavali → FR mapping + deployment topology + on-call playbook + third-party dependency inventory with renewal dates as the minimum content. Story 0.5 owns the deliverable.
- **Cross-Cutting #20 "Solo-build operational continuity"** (architecture lines 332-336) — commits the KT pack as one of the discharge surfaces for the bus-factor-of-one risk alongside runbooks (Story 0.1), credential escrow (Story 0.2), code escrow (Story 0.3), degradation policy (Story 0.4), backup engineer (Story 0.6).
- **§5.10 Operations: capacity planning + on-call + Dokploy fallback** (architecture lines 3375-3414) — Solo Builder named v1 on-call; backup engineer A-13 for surge + continuity; alert routing to paging SaaS (operations-policy ADR pending); backup alert path (≥2 delivery paths for P0 alarms); backup engineer access posture (read-only default, write/admin per-action approval, credential rotation cadence). Story 0.5 on-call playbook operationalizes this.
- **§5.6 Observability stack (split per concern)** (architecture lines 3122-3184) — managed (Cloud Monitoring + Cloud Logging) + self-hosted (Grafana + Loki for audit-log read in trustee-controlled project) + SaaS tracing + SaaS crash; capacity-planning indicators wired to alarms; quarterly capacity review by Solo Builder + trustee + backup engineer. Story 0.5 on-call playbook references this.
- **§5.7 Backup + Disaster Recovery** (architecture lines 3186-3233) — Cloud SQL HA + automated backups + PITR ≤35 days + RTO 4h / RPO 1h + cross-region replica trigger + DR runbook accessibility (PDF in credential escrow). Story 0.5 deployment topology documents this; the DR runbook itself remains Story 0.4 Decision 004 Open Follow-up.
- **§5.15 Operational runbook inventory** (architecture lines 3559-3574) — named v1 runbooks + cadence-driven review. Story 0.5 on-call playbook is structurally the meta-playbook; the per-task runbooks live in `docs/runbooks/` per Story 0.1.
- **§1.14 Member Lifecycle State Model** (architecture lines 1217-1283) — canonical state machine + FR provenance for member-state transitions. Story 0.5 Niyamavali → FR mapping references the §1.14 state table + the FR provenance.
- **§Critical External Dependencies** (architecture lines 240-269) — DigiLocker / Cloudflare / WhatsApp Business / Dokploy / bank statement intake / external-regulatory gates. Story 0.5 dependency inventory enumerates these.
- **§3.10 External-API deprecation monitoring** (architecture lines 2349-2371) — every external API has a named monitoring owner. Story 0.5 dependency inventory captures the owner per row.
- **§Implementation Handoff** (architecture lines 5069-5096) — PR-2 ADRs are transcription of architectural decisions; many ADRs deferred to implementation time. Story 0.5 scaffolds `docs/adr/` (README + template) and inventories the deferred slots; substantive content is PR-2 work.
- **AR-67 + AR-69** — operational-continuity invariant + ADR-backlog ratification. Story 0.5 partially discharges AR-67 (KT pack leg of 30-day takeover) + scaffolds the surface AR-69's ADR backlog will populate.
- **Architectural authority for `docs/adr/`** (architecture lines 636 + 4170) — `docs/adr/` is committed as the ADR directory. Story 0.5 Task 1 scaffolds it per the architecture commitment + Decision 003 Open Follow-up #6.

### Source tree components to touch

- **NEW (author-committed by dev agent):**
  - `docs/knowledge-transfer/README.md`
  - `docs/knowledge-transfer/adr-index.md`
  - `docs/knowledge-transfer/niyamavali-fr-mapping.md`
  - `docs/knowledge-transfer/deployment-topology.md`
  - `docs/knowledge-transfer/on-call-playbook.md`
  - `docs/knowledge-transfer/third-party-dependency-inventory.md`
  - `docs/knowledge-transfer/comprehension-questionnaire.md`
  - `docs/knowledge-transfer/comprehension-questionnaire-answer-key.md`
  - `docs/knowledge-transfer/kt-pack-ledger.md`
  - `docs/adr/README.md`
  - `docs/adr/_adr-template.md`

- **MODIFIED narrowly (verify-before-editing per Story 0.3 Task 5 + Story 0.4 Task 4 pattern):**
  - `docs/runbooks/operational-readiness-ledger.md` — append new "KT pack coverage" section (mirroring Story 0.3 + Story 0.4 patterns); existing sections NOT modified
  - `docs/runbooks/README.md` — append new row to "Related runbooks expected from other stories" table for the Story 0.5 on-call playbook; existing rows NOT modified
  - `docs/escrow/credential-inventory.md` — Notes-column append on existing `dr-runbook-pdf-custody` row at line 92 (clarifying on-call playbook vs DR runbook distinction); envelope_class / availability / re-seal trigger / Story citations NOT modified
  - `docs/escrow/code-escrow/README.md` — append new row to "Related code-survival surfaces owned elsewhere" table for the KT pack; existing rows NOT modified
  - `docs/degradation-policy/degradation-policy-ledger.md` "Table-top exercise log" section header — append cross-link to KT pack as a co-discharger of the 30-day takeover; existing log content NOT modified
  - `.decision-log.md` — append Decision 2026-05-30-005 at the top of "## Decisions" section; append the decision-type index line for Story 0.5

- **NOT modified (out of Story 0.5 scope):**
  - Story 0.1's runbooks (the seven Phase-0 runbooks); Story 0.1's `.decision-log.md` entry
  - Story 0.2's framework documents beyond the named `dr-runbook-pdf-custody` Notes append
  - Story 0.3's framework documents beyond the named cross-reference row append
  - Story 0.4's framework documents beyond the named ledger header cross-link
  - Any architecture / PRD / epics doc
  - `.github/workflows/` — no new workflows authored
  - `docs/adr/` substantive ADR content — only README + template scaffolded; substantive ADRs are PR-2 / implementation-time work

### Testing standards summary

Story 0.5 is **content authoring + framework scaffolding**, not code implementation. Standard "testing" in the dev-agent sense (unit tests, integration tests) does not apply. The verification posture per AC:

- **AC-1 verification:** scaffolding exists (file presence check via `ls`); each of the seven component files carries the schemas + sections committed in the Tasks; `adr-index.md` has ≥40 rows (grep `slot-reserved-pre-write` count); `niyamavali-fr-mapping.md` has rows for each R-N class named in PRD §1 + the §1.14 state table extract section; `deployment-topology.md` has all seven sections + the schematic diagram; `on-call-playbook.md` follows the `_template.md` five-section schema; `third-party-dependency-inventory.md` has rows for every dependency named in architecture §Critical External Dependencies + §5.x cloud provider rows + §3.10 monitoring-owner schema; `comprehension-questionnaire.md` has ≥25 questions across 5 sections; `comprehension-questionnaire-answer-key.md` has per-question answers + rubric; cross-reference edits applied (verify-before-editing per Task 7).
- **AC-2 verification:** `.decision-log.md` Decision 005 appended at top of Decisions section (line-level verify); decision-type index has Story 0.5 line inserted in numerical order; cross-reference edits to Stories 0.1 / 0.2 / 0.3 / 0.4 frameworks land on the named lines without broadening.
- **AC-3 verification:** the comprehension questionnaire + answer key follow the schema; the administration log section in `kt-pack-ledger.md` is empty (no administration has occurred) but the schema is in place; the 80% threshold + rubric is documented; the bus-factor simulation discipline is named in the questionnaire header.
- **Markdown-link-validity check:** the dev agent should ensure all `[[wiki-style]]` memory references resolve to the corresponding memory files (`feedback_architecture_vs_adr_boundary`, `feedback_architecture_vs_prd_boundary`, `feedback_gap_analysis_observational`, `feedback_closure_language_precision`) and all relative-path cross-references resolve to existing files. CI tooling for link validity is deferred to Story 1.16a per the Story 0.3 deferred-work list.
- **ADR-index drift check (one-shot)**: the dev agent should grep `architecture.md` + each runbook + each framework README for `ADR` + `deferred` + `[deferred ADR — placeholder procedure]` + `pending-ADR` strings; count vs the populated `adr-index.md` row count; the populated count should be a superset (Story 0.5 adds rows the architecture commits but the runbooks haven't yet referenced). Drift discovered during this check is logged as an Open Question.

### Project Structure Notes

See the "Project Structure Notes" section above (under Scope discipline). The `docs/knowledge-transfer/` directory is a NEW top-level surface following the Story 0.2 + 0.3 + 0.4 framework-as-top-level-surface pattern; the `docs/adr/` directory is committed by architecture lines 636 + 4170 — Story 0.5 scaffolds it (README + template).

### References

- [Source: `_bmad-output/planning-artifacts/epics.md`, lines 771-783] — Story 0.5 user-story statement + ACs + `[CONTINUITY]` tag
- [Source: `_bmad-output/planning-artifacts/prds/prd-TWT-2026-05-22/prd.md`, lines 1371-1382] — PRD §9.1.1 "Solo-build operational continuity (bus-factor mitigation)" — bullet 5 commits the KT pack content
- [Source: `_bmad-output/planning-artifacts/architecture.md`, Cross-Cutting #20 (lines 332-336)] — Solo-build operational continuity commitment including KT pack
- [Source: `_bmad-output/planning-artifacts/architecture.md`, §1.14 (lines 1217-1283)] — Member Lifecycle State Model with FR provenance + canonical home + states + transitions + policy consumers + time-driven transitions + cache invalidation + claim-filing concurrency + audit-log emission
- [Source: `_bmad-output/planning-artifacts/architecture.md`, §5.1-§5.10 (lines 2920-3414)] — cloud provider GCP `asia-south1`; GCP service map; deployment substrate (Dokploy v1 + K8s migration); CI/CD pipeline; environment topology (three environments + audit-mirror project); observability stack split; backup + DR + cross-region replica trigger; network topology; edge / WAF capability bar + pivot disposition; secret management + rotation; operations + on-call + backup engineer access posture + backup alert path
- [Source: `_bmad-output/planning-artifacts/architecture.md`, §5.15 (lines 3559-3574)] — operational runbook inventory + cadence
- [Source: `_bmad-output/planning-artifacts/architecture.md`, §Critical External Dependencies (lines 240-269)] — DigiLocker / Cloudflare / WhatsApp Business / Dokploy / bank statement intake / external-regulatory gates
- [Source: `_bmad-output/planning-artifacts/architecture.md`, §3.10 (lines 2349-2371)] — External-API deprecation monitoring + named monitoring owner per integration
- [Source: `_bmad-output/planning-artifacts/architecture.md`, §3.4 (lines 1915-2097)] — channel-provider abstraction + per-channel fallbacks + lifecycle-driven dispatch suppression
- [Source: `_bmad-output/planning-artifacts/architecture.md`, "Multi-actor controls in degraded mode" (lines 4031-4037)] — Multi-actor controls in degraded mode until staffing assumptions hold
- [Source: `_bmad-output/planning-artifacts/architecture.md`, §Implementation Handoff (lines 5069-5096)] — PR-2 ADRs are transcription; substantive ADR drafting is implementation-time work; `docs/adr/` directory commitment
- [Source: `_bmad-output/planning-artifacts/architecture.md`, §Workspace Layout (lines 4131-4439)] — `docs/` directory structure with `docs/adr/`, `docs/runbooks/`, `docs/escrow/`, and committed naming convention
- [Source: `_bmad-output/planning-artifacts/architecture.md`, AR-67 + AR-69] — operational continuity invariant + ADR backlog ratification
- [Source: `_bmad-output/implementation-artifacts/0-1-operational-runbooks-authored-trustee-signed.md`] — Story 0.1 patterns: runbook template, ledger pattern, .decision-log.md schema, substitute-engineer model, bus-factor simulation
- [Source: `_bmad-output/implementation-artifacts/0-2-credential-escrow-established-with-trustee-quorum-open.md`] — Story 0.2 patterns: framework-as-top-level-surface, property/control/policy discipline, inventory-row-addition lifecycle, author-committable vs `_AWAITING EXTERNAL ACTION_` split
- [Source: `_bmad-output/implementation-artifacts/0-3-code-escrow-auto-mirror-pipeline-live.md`] — Story 0.3 patterns: 30-day takeover joint-discharge anchor, supersession schema, verify-before-editing cross-reference discipline, forbidden-removal inventory rule, `docs/adr/` Decision 003 Open Follow-up #6
- [Source: `_bmad-output/implementation-artifacts/0-4-per-surface-degradation-policy-authored.md`] — Story 0.4 patterns: four-way property/control/policy/gap-analysis discipline, closure-language precision per AC leg, structural-invariants section, 30-day-takeover joint-discharge partial evidencing, review-decision precedents
- [Source: `.decision-log.md`] — Decision 2026-05-29-001, 002, 003, 004 — schema and supersession precedent for Decision 005
- [Source: `docs/runbooks/_template.md`] — Five-section runbook template (Prerequisites / Step-by-step / Rollback / Verification / Contact escalation) used verbatim for `on-call-playbook.md`
- [Source: `docs/runbooks/README.md`] — Operational runbook directory README; KT pack cross-references it
- [Source: `docs/escrow/credential-inventory.md`, line 92] — Existing `dr-runbook-pdf-custody` row citing Story 0.4 (verified); Story 0.5 Notes-column append clarifies on-call playbook vs DR runbook distinction
- [Source: `docs/escrow/README.md`] — Escrow framework README; KT pack dependency inventory cross-references it for credential-envelope rows
- [Source: `docs/escrow/code-escrow/README.md`] — Code-escrow framework README; KT pack as a related code-survival surface
- [Source: `docs/degradation-policy/README.md`] — Degradation policy framework README; KT pack as a co-discharger of the 30-day takeover
- [Source: `docs/degradation-policy/degradation-policy-ledger.md`] — Table-top exercise log header; KT pack contribution to 30-day takeover joint-discharge cross-link
- Memory: [[feedback_architecture_vs_adr_boundary]] — property-driven design; ADRs commit cloud controls
- Memory: [[feedback_architecture_vs_prd_boundary]] — architecture commits state/transitions/events; PRD commits policy/eligibility/cadence
- Memory: [[feedback_gap_analysis_observational]] — gap analysis observes incompleteness; proposes conditional escalation paths; does not prescribe sprint planning or override architecture
- Memory: [[feedback_closure_language_precision]] — distinguish "Closed by [edit]" from "Resolved via explicit deferral" from "Not addressed"

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (Fast mode); bmad-dev-story workflow executed 2026-05-30.

### Debug Log References

- **Pre-execution verification.** Confirmed `docs/knowledge-transfer/` and `docs/adr/` did not exist (`ls` returned no such directories). Confirmed existing artifacts from Stories 0.1-0.4: `docs/runbooks/` (10 files inc. operational-readiness-ledger), `docs/escrow/` (5 files + `code-escrow/` sub-directory with 5 files), `docs/degradation-policy/` (5 files + `comms-templates/` sub-directory with 5 files), `.decision-log.md` (270 lines, Decisions 001-004 present), runbook `_template.md` (5-section template), `runbook README.md` (line 76: existing Story 0.4 table-top row), `credential-inventory.md` (line 92: existing `dr-runbook-pdf-custody` row citing Story 0.4), `code-escrow/README.md` (line 183: existing Story 0.5 placeholder row in "Related code-survival surfaces" table), `degradation-policy-ledger.md` (line 99: existing 30-day-takeover anchor).
- **Open Question pre-decisions taken at execution time** (per Story 0.5 spec Open Questions §):
  - OQ#1 (ADR-index row population from runbooks) — **resolved**: grep-based catalogue of `[deferred ADR — placeholder procedure]` tags performed during Task 2 execution; only `docs/runbooks/deploy.md:31` carries an explicit tag at Story 0.5 author-commit time. Other runbooks cite ADR commitments inline but do not carry placeholder tags; Section C documented this clearly with the note that future runbook authoring will add new rows per the supersession schema.
  - OQ#2 (Niyamavali → FR mapping vs PRD §1 structure) — **resolved**: PRD §1 Niyamavali section title not present (PRD §4.2 "Niyamavali (Rules Engine)" is the canonical FR-7..FR-12A source); the R-classes are named inline (R5(C.2), R5(D), R5(E), R5(F), R7(A-G), R8/R8(A)/R8(B), R9, R9(A), R14-adapted) per FRs in PRD §4.2 + the §1.14 state machine. Mapping populated with 14 clause rows covering all named R-classes + load-bearing transitions; per-row status flip threshold deferred to operations policy.
  - OQ#3 (deployment topology diagram format) — **resolved**: ASCII as primary (renderer-agnostic; markdown-rendering surfaces all support it); Mermaid not needed at this scope. The ASCII schematic captures the load-bearing IAM-isolation boundary (audit-mirror project separation per §2.10a) + edge-only ingress + three-environment structure + external integrations egress.
  - OQ#4 (on-call playbook incident-class taxonomy completeness) — **resolved**: AC-1 list (audit-integrity / capacity-indicator / push-provider / paging / Dokploy / Cloudflare / Cloud SQL / KMS / DigiLocker / WA / helpdesk / member-mass-event) authored as v1 commitment + §2.13 added for DPDPA breach response with `[deferred to Story 14.3]` marker + interim escalation per the recommendation. Total 13 incident classes.
  - OQ#5 (dependency inventory monitoring-owner default) — **resolved**: Solo Builder default per §3.10 for application-tier dependencies; Trustee Panel primary for regulatory-gate rows (7 rows in Section E); Story 0.13 legal counsel for legal escalation across regulatory rows. README §3 property/control/policy table updated implicitly via this rationale.
  - OQ#6 (questionnaire question count) — **resolved**: 30 questions selected (6 per section × 5 sections), within the ≥25 floor + ≤35 ceiling. Time budget ≤4 hours sustainable at this depth.
  - OQ#7 (answer key separation discipline) — **resolved**: answer key lives in `docs/knowledge-transfer/` with visible filename `comprehension-questionnaire-answer-key.md`; bus-factor simulation discipline + trustee facilitator enforces cold-read; stronger separation surfaced as future ADR slot per `docs/adr/README.md` index-symmetry note (not formally added to `adr-index.md` at this commit; can be added under operations-policy if needed).
- **Cross-reference verification.** All `[[wiki-style]]` memory refs (`feedback_architecture_vs_adr_boundary`, `feedback_architecture_vs_prd_boundary`, `feedback_gap_analysis_observational`, `feedback_closure_language_precision`) appear correctly in the framework documents. Relative-path cross-references verified before edit per Story 0.3 Task 5 + Story 0.4 Task 4 discipline (verify-before-editing confirmed for all 5 cross-link target lines).
- **ADR-index drift check.** Grep performed on `architecture.md` for `ADR` + `deferred` + `pending-ADR` strings → ≥30 distinct architecture-anchored slots identified; cross-validated against the 28 + 17 = 45 architecture-source rows in `adr-index.md` Sections A + B (the additional architecture references are covered by category-level rows rather than per-instance rows — this is consistent with the architecture's commitment to "specific tool versions deferred to ADRs at implementation time"). Framework-source rows in Sections D-G add 18 more for a total of 64 slots.
- **Decision-log integrity.** Decision 005 appended at top of "## Decisions" section (above Decision 004); Decisions 001 / 002 / 003 / 004 NOT modified; decision-type index amended at line 13 with `**Story 0.5 — Knowledge-Transfer Pack** —` entry inserted in numerical order between 0.4 and 0.6 per Story 0.2 + 0.3 + 0.4 review-decision precedent.
- **Sprint-status mutation.** `0-5-knowledge-transfer-documentation-pack-compiled: backlog → ready-for-dev → in-progress → review` on 2026-05-30; header comments updated to reflect transitions.

### Completion Notes List

**Tasks 1-7 author-committed 2026-05-30.** Story 0.5 ships the Knowledge-Transfer documentation pack framework (8 component files in `docs/knowledge-transfer/`) + the `docs/adr/` scaffold (closing Decision 2026-05-29-003 Open Follow-up #6) + 5 narrow cross-link edits + Decision 2026-05-30-005 entry.

**Closure-language precision per [[feedback_closure_language_precision]]:**

- **AC-1** — partially closed:
  - **Closed by [edit]**: KT-pack framework scaffolding authored at `docs/knowledge-transfer/`; ADR-index populated with 64 deferred-ADR slots (Sections A-G); Niyamavali → FR mapping populated with 14 clause rows + 20 FR inverse-lookup rows + verbatim §1.14 Account State Machine extract; deployment-topology authored with 8 sections + ASCII schematic; on-call-playbook authored as 5-section meta-playbook covering 13 incident classes; third-party-dependency-inventory populated with 34 rows across 7 sections; comprehension-questionnaire authored with 30 questions across 5 sections + deterministic answer key + scoring rubric; kt-pack-ledger authored with Framework-commit record + Trustee sign-off log + Comprehension administration log (30-day-takeover joint-discharge anchor) + Pack-revision log + Periodic re-attestation log + Procedure-revision log + Cross-links into related framework ledgers; `docs/adr/` scaffolded (README + `_adr-template.md`). Decision 2026-05-30-005 appended to `.decision-log.md` per Story 0.1+0.2+0.3+0.4 schema.
  - **Resolved via explicit deferral**: ≥2-trustee sign-off awaiting Task 8 (Trustee Panel authority); substantive ADR content in `docs/adr/` deferred to PR-2 / implementation-time per architecture §Implementation Handoff; Niyamavali → FR mapping rows at `spec-only` until owning Stories close; dependency inventory rows at `pending-system-availability` / `pending-ADR-selection` / `deferred-with-ADR` until owning Stories close.
- **AC-2** — partially closed:
  - **Closed by [edit]**: pack stored in primary git repo (inherits Story 0.3 mirror coverage when 0.3 Tasks 7-11 close); cross-link edits applied to `docs/runbooks/operational-readiness-ledger.md` ("KT pack coverage" section) + `docs/runbooks/README.md` ("Related runbooks" on-call playbook row) + `docs/escrow/credential-inventory.md` (`dr-runbook-pdf-custody` Notes append) + `docs/escrow/code-escrow/README.md` (Story 0.5 row update with 2026-05-30 stamp + component inventory) + `docs/degradation-policy/degradation-policy-ledger.md` ("Table-top exercise log" 30-day-takeover anchor expanded to eight conditions); Decision 005 appended.
  - **Resolved via explicit deferral**: ≥2-trustee sign-off awaiting Task 8.
- **AC-3** — fully deferred:
  - **Resolved via explicit deferral**: comprehension administration awaiting Story 0.6 closure + Task 9 execution; 80% threshold close + gap discharge awaiting Task 10 outcome.

**30-day takeover joint-discharge contribution** (per Story 0.3 Decision 003 + Story 0.4 Decision 004 Open Follow-ups): Story 0.5 author-commit partially evidences the discharge — the trust now has a documented technical-context recoverability surface across all five PRD §9.1.1 KT pack components + a comprehension questionnaire + the `docs/adr/` directory scaffold. Full joint discharge requires closure of all **eight** conditions: Story 0.3 AC-1 + Story 0.3 AC-2 + Story 0.4 AC-1 + Story 0.4 AC-2 + Story 0.5 AC-1 + Story 0.5 AC-2 + Story 0.5 AC-3 + Story 0.6 closure.

**Anti-pattern guards explicitly enforced:**

- No substantive ADR content authored in `docs/adr/` (PR-2 / implementation-time per architecture §Implementation Handoff lines 5069-5096).
- No silent duplication of canonical PRD / architecture / epics content in KT pack component files (`docs/knowledge-transfer/README.md` §4 invariant 1; single permitted exception is the architecture §1.14 lines 1238-1246 verbatim transcription in `niyamavali-fr-mapping.md` §Account State Machine extract, deliberately documented).
- No orphan rows in Niyamavali → FR mapping (every row cross-links to PRD clause + implementing FR + owning Story).
- No dependency rows omit monitoring owner per §3.10.
- No ADR-index rows omit expected-author + expected-close trigger.
- No comprehension-questionnaire revision lowers the 80% threshold (`docs/knowledge-transfer/README.md` §4 invariant 5).
- No leakage of operational secrets into topology / on-call / dependency-inventory documents (cross-references credential-inventory rows by name; never inlines secret values).
- No PII / member data anywhere in the pack.
- `[deferred ADR — placeholder procedure]` tagging convention applied in on-call-playbook §1 (paging surface) + KMS unavailability §2.8 (emergency-fallback path) per Story 0.4 inheritance.
- Forbidden-removal rule applied to `adr-index.md` + `niyamavali-fr-mapping.md` + `third-party-dependency-inventory.md` (rows superseded, never removed).

**Cross-Story dependency closures from Story 0.5:**

- **Decision 2026-05-29-003 Open Follow-up #6** (`docs/adr/` scaffold) — **Closed by Story 0.5 Task 1**; Decision 005 records the closure explicitly.

**Open Questions surfaced during execution.** None beyond the 7 pre-author-commit Open Questions documented in the Story 0.5 spec (each resolved in Debug Log References above). No implementation-time Open Questions discovered.

### File List

**NEW (author-committed by dev-story agent 2026-05-30):**

- `docs/knowledge-transfer/README.md` (12 numbered sections — framework existence + lifecycle + property/control/policy/gap-analysis discipline + structural invariants (9 invariants) + sign-off lifecycle + review cadence fallback + ledger reconciliation + Open ADR slots (7 slots) + related continuity surfaces + 30-day-takeover joint-discharge anchor + mechanism-level revision path + file index)
- `docs/knowledge-transfer/adr-index.md` (status legend + status summary + forbidden-removal rule + 64 deferred-ADR slot rows across 7 sections + periodic review cadence + references)
- `docs/knowledge-transfer/niyamavali-fr-mapping.md` (mapping discipline + status legend + 14-row primary mapping + 20-row inverse-lookup section + Account State Machine extract verbatim from architecture.md §1.14 + references)
- `docs/knowledge-transfer/deployment-topology.md` (8 sections + ASCII schematic + structural property "what the topology document MUST NOT carry" + references)
- `docs/knowledge-transfer/on-call-playbook.md` (5-section runbook template + structural-invariant block + 13 incident classes in §2 + §3 Rollback per failure mode + §4 Verification checks + §5 Contact escalation list + Changelog)
- `docs/knowledge-transfer/third-party-dependency-inventory.md` (schema + status legend + forbidden-removal rule + 34 rows across 7 sections (Cloud / Edge / Push+OS / SaaS / Regulatory / Continuity / Deferred) + cross-link discipline + periodic review cadence + references)
- `docs/knowledge-transfer/comprehension-questionnaire.md` (administration discipline + format + 30 questions across 5 sections (Sections A-E) + end-of-administration scoring rule + references)
- `docs/knowledge-transfer/comprehension-questionnaire-answer-key.md` (scoring rubric + per-question deterministic answers + per-question `partial` signals + score sheet template + references)
- `docs/knowledge-transfer/kt-pack-ledger.md` (quorum rule + Framework-commit record + Trustee sign-off log + Comprehension administration log + Pack-revision log + Periodic re-attestation log + Procedure-revision log + cross-links + references)
- `docs/adr/README.md` (lifecycle + naming convention + body schema + authoring discipline + relationship to runbooks/framework READMEs/decision-log + status legend + index symmetry + file index + references)
- `docs/adr/_adr-template.md` (canonical body schema: Title + Status + Date + Author + Ratifying trustees + Supersedes/Superseded-by + Context + Decision + Alternatives considered + Consequences + References + Changelog)

**MODIFIED narrowly (verify-before-editing per Story 0.3 Task 5 + Story 0.4 Task 4 pattern):**

- `docs/runbooks/operational-readiness-ledger.md` — inserted new "KT pack coverage" section before "Re-sign protocol" header (mirroring Story 0.3 "Mirror coverage" + Story 0.4 "Degradation policy coverage" patterns); existing Sign-off table + Execution-validation log + Mirror coverage + Degradation policy coverage NOT modified
- `docs/runbooks/README.md` — appended new row to "Related runbooks expected from other stories" table for Story 0.5 on-call playbook; existing Story 0.4 row + other rows NOT modified
- `docs/escrow/credential-inventory.md` — Notes-column append on existing `dr-runbook-pdf-custody` row (line 92) clarifying on-call playbook (Story 0.5) vs DR runbook (Story 0.4 Open Follow-up) distinction; envelope_class / availability / re-seal trigger / Story citations NOT modified
- `docs/escrow/code-escrow/README.md` — updated the existing Story 0.5 row in "Related code-survival surfaces owned elsewhere" table with 2026-05-30 author-commit stamp + complete component-file inventory + 30-day takeover joint-discharge cross-link
- `docs/degradation-policy/degradation-policy-ledger.md` "Table-top exercise log" section header — appended Story 0.5 contribution + expanded the joint-discharge condition list from six to eight conditions (Story 0.5 AC-1 + AC-2 + AC-3 split per AC structure); existing log content NOT modified
- `.decision-log.md` — Decision 2026-05-30-005 appended at top of "## Decisions" section (above Decision 004); decision-type index amended at line 13 with `**Story 0.5 — Knowledge-Transfer Pack** —` entry inserted in numerical order between 0.4 and 0.6; Decisions 001/002/003/004 NOT modified
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — `0-5-knowledge-transfer-documentation-pack-compiled: ready-for-dev → in-progress → review`; header comments updated
- `_bmad-output/implementation-artifacts/0-5-knowledge-transfer-documentation-pack-compiled.md` — this file: Status → `review`; Task progress block marked Tasks 1-7 closed; Dev Agent Record populated; Change Log row added

**NOT modified (out of Story 0.5 scope):**

- Story 0.1's seven Phase-0 runbooks
- Story 0.2's framework documents beyond the named `dr-runbook-pdf-custody` Notes append
- Story 0.3's framework documents beyond the verification-stamp on the existing KT pack reference
- Story 0.4's framework documents beyond the named ledger header cross-link
- Any architecture / PRD / epics doc
- `.github/workflows/` — no new workflows authored
- `docs/adr/` substantive ADR content — only README + template scaffolded; substantive ADRs are PR-2 / implementation-time work per architecture §Implementation Handoff

### Change Log

| Date | Change | Author |
|---|---|---|
| 2026-05-30 | Story 0.5 author-committed via bmad-dev-story: scaffolded `docs/knowledge-transfer/` with 9 NEW files (README + ADR-index with 64 deferred-ADR slots + Niyamavali → FR mapping with 14 clause rows + deployment-topology with 8 sections + ASCII schematic + on-call-playbook 5-section meta-playbook covering 13 incident classes + third-party-dependency-inventory with 34 rows + comprehension-questionnaire with 30 questions + answer-key + kt-pack-ledger); scaffolded `docs/adr/` with 2 NEW files (README + `_adr-template.md`) discharging Decision 2026-05-29-003 Open Follow-up #6; applied 5 narrow cross-reference edits to Stories 0.1/0.2/0.3/0.4 framework artifacts; appended Decision 2026-05-30-005 to `.decision-log.md` per Story 0.1/0.2/0.3/0.4 schema; sprint-status flipped `ready-for-dev → in-progress → review`. Tasks 1-7 closed; Tasks 8-10 remain `_AWAITING EXTERNAL ACTION_` (≥2-trustee sign-off; Story-0.6-gated comprehension administration; 80% threshold close + gap discharge). | Solo Builder (BigDev) via dev-story agent |
| 2026-06-05 | Trustee questionnaire ratification transcribed per `phase-0-trustee-questionnaire.md` Q5.1+Q5.2 + QC.2 (Dhiraj Rahul + Kalpana Bharti signed 2026-06-05). Decision 2026-06-05-021 appended to `.decision-log.md` superseding Decision 2026-05-30-005 framework-leg + Decision 2026-06-05-033 (QC.2 annual re-attestation cadence). **Task 8 ratification-leg = Closed by [edit]** — KT pack-as-a-unit sign-off (Q5.1) covering 7 component files + ledger + answer key + ADR scaffold. **Task 9 ratification-leg = Closed by [edit]** — comprehension administration sequenced behind Story 0.6 Task 10 named-engineer landing (full-closure-path per Q5.2). **Annual re-attestation cadence = 2027-07-01** per QC.2 — 12-month interval from Story 0.15 Q15.3 first monthly review. **Tasks 9–10 execution-leg = Resolved via explicit deferral** — actual ≥2-trustee sign-off events (Task 8), comprehension administration to contracted backup engineer (Task 9), 80% threshold close + gap discharge (Task 10) all pending. Status remains `done`. | claude-opus-4-7 |

### Review Findings

_Code review run 2026-05-30 by bmad-code-review (blind + edge + acceptance layers). 2 decision-needed, 11 patch, 8 deferred, ~16 dismissed._

**Decision-needed**

_All resolved — converted to patch items below._

**Patch**

- [x] [Review][Patch] Dependency inventory row count inconsistency — status summary claims 20 rows; actual count is 34 (A:6+B:9+C:4+D:4+E:7+F:2+G:2). This wrong count propagates to kt-pack-ledger §1 ("30 rows") and Decision 2026-05-30-005 ("30 dependency rows"). All three locations need correction to 34. [`docs/knowledge-transfer/third-party-dependency-inventory.md:7-10`; `docs/knowledge-transfer/kt-pack-ledger.md:19`; `.decision-log.md` Decision 005]
- [x] [Review][Patch] ADR-NNNN-cloudflare-pivot missing from adr-index.md — questionnaire Q-E.3 and answer key Q-E.3 both cite `ADR-NNNN-cloudflare-pivot (substitution boundaries)` but no such row exists in any section of `adr-index.md`. Add the missing slot row (Section B or A, per §5.8a architecture anchor). [`docs/knowledge-transfer/adr-index.md`; `docs/knowledge-transfer/comprehension-questionnaire.md:228-231`; `docs/knowledge-transfer/comprehension-questionnaire-answer-key.md`]
- [x] [Review][Patch] adr-directory-scaffold row status contradiction — Section E last row has `Status: slot-reserved-pre-write` but Notes says "Closed by [edit] 2026-05-30 (Story 0.5 Task 1)". Status should be updated to `ratified` (or a new `closed-by-framework-commit` value) to match. [`docs/knowledge-transfer/adr-index.md` Section E, last row]
- [x] [Review][Patch] R5(E) architecture anchor incorrect — the §architecture_anchor column cites §1.14 (Member Lifecycle State Machine) for the nominee-split rule. §1.14 is the state machine; the nominee split / partial-benefit calculation should anchor to the correct architecture section (§1.10 or §1.7 per rule-registry). [`docs/knowledge-transfer/niyamavali-fr-mapping.md` R5(E) row]
- [x] [Review][Patch] On-call playbook Status header contains verbatim template text — line 3 reads the raw template placeholder `drafted / signed-off (with ledger entry at git SHA <sha>) / superseded` instead of just the current state `drafted`. [`docs/knowledge-transfer/on-call-playbook.md:3`]
- [x] [Review][Patch] On-call playbook Changelog git SHA is `<pending>` — project has no git repo at author-commit. Should read `n/a (pre-git at author-commit)` to be non-misleading and to discharge correctly when git is initialized. [`docs/knowledge-transfer/on-call-playbook.md` Changelog section]
- [x] [Review][Patch] Answer key Q-E.6 references non-existent label — answer cites "Open Question #5 resolution in `docs/knowledge-transfer/README.md` §3" but README §3 is a property/control/policy discipline table with no such label. Either add the label to README §3 or correct the citation to the actual anchoring text. [`docs/knowledge-transfer/comprehension-questionnaire-answer-key.md` Q-E.6 answer]
- [x] [Review][Patch] README §6 review cadence fallback has no named owner — five cadence triggers (quarterly, annual, per-architectural-amendment, per-owning-Story-closure, on-incident) are defined but none name the responsible party who initiates the review. Add owner column or inline owner name per trigger. [`docs/knowledge-transfer/README.md:§6`]
- [x] [Review][Patch] Acronyms not expanded at first use — DPDPA, WA Business, and DLT appear in cold-read component files without expansion. Cold-read engineer in a bus-factor scenario may not know these. Expand on first use or add to a glossary (ties to decision D2 if a glossary is added). [`docs/knowledge-transfer/third-party-dependency-inventory.md`; `docs/knowledge-transfer/on-call-playbook.md`]
- [x] [Review][Patch] Sign-off mode tie-breaking rule missing — kt-pack-ledger §2 "Mode header" is populated "at first sign-off" but defines no procedure if two trustees prefer different modes (one wants per-component, other wants pack-as-a-unit). Add a tie-breaking rule (e.g., pack-as-a-unit is the default; per-component requires both trustees to agree). [`docs/knowledge-transfer/kt-pack-ledger.md:26-36`]
- [x] [Review][Patch] `deferred-with-ADR` status has no `active` transition trigger — the status legend defines three statuses (`pending-system-availability`, `pending-ADR-selection`, `deferred-with-ADR`) but only `pending-system-availability` and `pending-ADR-selection` have implied transitions. `deferred-with-ADR` has no documented trigger for when it transitions to `active`. [`docs/knowledge-transfer/third-party-dependency-inventory.md` status legend]
- [x] [Review][Patch] Quorum-unavailable fallback path missing — kt-pack-ledger §2 and README §5 require ≥2-trustee sign-off but define no fallback when only 1 trustee is available. Add emergency single-trustee ratification path: valid only under documented trustee incapacitation, time-bounded (90 days), recorded as `.decision-log.md` `[CONTINUITY]` entry; ≥2-trustee quorum remains default. [`docs/knowledge-transfer/kt-pack-ledger.md:8-9`; `docs/knowledge-transfer/README.md:§5`]
- [x] [Review][Patch] Domain glossary missing — cold-read backup engineer encounters "Pariwar", "Niyamavali", "AR-67", "FR", "DPDPA", "DLT", "WA Business" with no definitions anywhere in the KT pack. Add a domain glossary section to `docs/knowledge-transfer/README.md` (resolves P9 acronym-expansion finding simultaneously). [`docs/knowledge-transfer/README.md`]

**Deferred**

- [x] [Review][Defer] FR-1 inverse-lookup clause "TBD per Trustee ratification" [`docs/knowledge-transfer/niyamavali-fr-mapping.md` FR-1 row] — deferred, pre-existing; governance-pending by design
- [x] [Review][Defer] FR-2 parenthetical-only clause name [`docs/knowledge-transfer/niyamavali-fr-mapping.md` FR-2 row] — deferred, pre-existing; acceptable at spec-only stage
- [x] [Review][Defer] "No PII" invariant lacks formal DPDPA definition scope [`docs/knowledge-transfer/README.md:§4 invariant 6`] — deferred, pre-existing; operations-policy territory (Story 14.3)
- [x] [Review][Defer] Capacity-indicator alarm thresholds absent in §2.2 triage [`docs/knowledge-transfer/on-call-playbook.md:§2.2`] — deferred, pre-existing; ADR-deferred by design
- [x] [Review][Defer] Material-edit re-sign threshold granularity for borderline edits [`docs/knowledge-transfer/README.md:§5`] — deferred, pre-existing; operations-policy territory
- [x] [Review][Defer] No procedure for backup engineer failing comprehension re-administration twice [`docs/knowledge-transfer/kt-pack-ledger.md:§3`] — deferred, pre-existing; operations-policy edge case
- [x] [Review][Defer] Niyamavali row supersession procedure during per-Pariwar amendments [`docs/knowledge-transfer/niyamavali-fr-mapping.md`] — deferred, pre-existing; complex governance not required at spec-only stage
- [x] [Review][Defer] Deferred ADR slots with no confirmed Story close trigger [`docs/knowledge-transfer/adr-index.md` several rows] — deferred, pre-existing; inherent framework risk per architecture §Implementation Handoff deferral model
