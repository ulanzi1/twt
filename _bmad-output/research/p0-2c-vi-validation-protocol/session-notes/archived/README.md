# Session Notes — archived placeholder

> **Empty at Task 5 author-commit.** Per Story 0.9 P-13 review-patch precedent — per-session notes are archived (NOT destroyed) here after the 6-month active-retention window in `session-notes/` per ethics-protocol §6.

## Purpose

- Cross-reference for further synthesis revision (e.g., NFR-22 Phase-2 audit consumes Story 0.10 synthesis + accessibility-debt tracker as authoritative input — archived per-session notes provide ground-truth citation).
- Audit trail for trustee review of historical synthesis claims.
- NOT for active research consumption — archived notes are read-only.

## Archival rule

- Per-session note moves from `../` (active `session-notes/` directory) to `./` (archived) after **6 months** post-synthesis.
- Original filename preserved (`vi-member-1.md` stays `vi-member-1.md`).
- Move-event recorded in `../trustee-review-log.md` Pack-revision log column.

## Destruction exception

Per ethics-protocol §6: if participant requests immediate destruction, per-session note is destroyed AT participant request time — not archived. Destruction event recorded in `../recruitment-log.md` `withdrawal_status` column with date.
