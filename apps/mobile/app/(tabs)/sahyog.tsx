import { MemberDriveList } from 'components/drive-list/MemberDriveList'

// Tab 4 — the member's drive list (Story 11b.15). Every Sahyog Drive the member's Pariwar has run:
// the one collecting now, the ones finished, and the ones fully checked.
//
// ⭐ A PEER OF MY POOL (`2026-09-04-194` cl.2), ⛔ not a route buried inside it — see the tab
// registration and its supersession note in `_layout.tsx`.
export default function SahyogDrivesTab() {
  return <MemberDriveList />
}
