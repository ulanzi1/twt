// Member pool-onboarding-tutorial contract barrel — Story 7.10 (Task 5). Re-exported from the
// `@twt/contracts` top barrel (no subpath export wired — the repo convention). Backs the member-
// session-gated completion/skip outcome endpoint.

export { PoolOnboardingOutcomeSchema, PoolOnboardingOutcomeRequest } from './tutorial.js';
