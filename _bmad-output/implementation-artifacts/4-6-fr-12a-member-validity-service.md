---
baseline_commit: 87a197fdc0a8d4a3d5f3a059e4123cd224e7a443
---

# Story 4.6: FR-12A Member Validity Service — Canonical Payload + p95 < 200ms + Rule Evaluation Order Determinism `[SURFACE]`

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As any admin surface or member's own profile,
I want a Member Validity Service that returns the canonical "is this member valid right now?" payload deterministically, idempotently, with rule-by-rule provenance, p95 < 200ms at 4L scale,
so that every eligibility decision in the system reads from a single canonical source with replay-reproducible output.

## Acceptance Criteria

> Verbatim from `epics.md` §Story 4.6 (lines 1982–2007). This is the FIRST `[SURFACE]` of Epic 4 — the consumer that composes the Story 4.1 engine primitive + its four `[CONSUMER]` ladders (R7 4.2 · R8 4.3 · R5/R9/R14 4.4 · R12 4.5) into ONE canonical payload, and is the **first real PRODUCER** of the caller-injected facts those ladders read.

**AC1 — the canonical payload + scope-respecting access + p95 < 200ms at 4L**

**Given** FR-12A + Sprint Change Proposal Item 5 + AR-65 + architectural-freeze row 11
**When** the Member Validity Service is implemented
**Then** the service exposes `getValidity(member_id)` and `getValidityAt(member_id, timestamp)` — both returning the canonical payload: `{ member_id, evaluated_at, lock_in_status, vyawastha_shulk_status (from Story 3.8), contribution_history_summary, medical_disclosure_flags, retirement_coverage, special_flags, applicable_niyamavali_clauses[] (ordered, each carrying clause_id + clause_version_id), provenance_trace[] (ordered), validity_payload_hash }`
**And** access is scope-respecting via Story 1.8 RBAC
**And** p95 < 200ms at 4L (400,000) member scale — measured under realistic load in CI / pre-launch validation

**AC2 — the rule evaluation order determinism invariant (this story's load-bearing commitment)**

**Given** the **rule evaluation order determinism invariant**
**When** the same `(member_id, rule_registry_version, member_state_hash)` is evaluated twice
**Then** the rule evaluation order is **deterministic and stable across replays** — same rules fire in the same order
**And** `applicable_niyamavali_clauses[]` ordering is stable across replays — never varies based on parallel execution, hash-map iteration order, async scheduling, or non-deterministic concurrency
**And** `provenance_trace[]` ordering is stable across replays — provenance entries appear in the same sequence
**And** `validity_payload_hash` is identical across replays — the canonical hash over the (ordered, deterministic) payload is reproducible
**And** a CI determinism-replay test runs the same evaluation 100 times across multiple threads and asserts byte-identical payload hashes for all 100 runs; **any variance fails CI as a P0 architectural violation**
**And** any future optimization (parallel rule execution, async rule evaluation, hash-map traversal in indeterminate order) **must preserve the determinism invariant** or be rejected at code review

**AC3 — the payload is sufficient to render `<MemberStatusPanel>` (Story 4.7) with no extra queries**

**Given** Epic 6 verifier console + Epic 10 admin surfaces consuming the Validity Service
**When** they call the service in scope-respecting contexts
**Then** the payload is sufficient to render `<MemberStatusPanel>` (Story 4.7) without additional queries; compound read model (Story 4.7 admin variant) precomputes the ~5s admin-search load

## Tasks / Subtasks

- [x] **Task 0 — Decisions CONFIRMED by BigDev 2026-07-04 (settled, taken as written — do NOT re-open).**

  This is the FIRST `[SURFACE]` of Epic 4 and the point where every prior `[PRIMITIVE]`/`[CONSUMER]` deferral comes due at once. Five load-bearing decisions touch package placement, the contribution-producer gap, the p95 scope split with Story 4.8's cache, the determinism-gate thread realism, and RBAC redaction placement. **BigDev CONFIRMED every recommended option: D1→A, D2→A, D2m→A, D3→A, D4→A, D5→A** — with the D5 refinement "reuse an existing permission if one already matches before minting a new key." That reuse check is now RESOLVED (see D5): the permission catalog is entirely action/write-oriented (`member.suspend`/`member.moderate` are the only `member.*` keys, both writes; the verifier/auditor/helpline roles that must READ validity hold neither), so **no existing key matches a member-validity READ** → `member.view_validity` is warranted (the documented append-per-epic pattern). The recommendations below are the settled spec; the rejected alternatives are retained only so the dev agent understands *why*.

  - [x] **D1 (placement of the Validity Service).** The service is a CONSUMER that composes `@twt/niyamavali-engine` (which depends on `@twt/domain`) + the `@twt/domain` read surfaces + assembles the FR-12A payload. It CANNOT live in `@twt/domain` (domain must never depend on the engine — barrel header `packages/niyamavali-engine/src/index.ts:5`) and should not couple to a web framework.
    - **D1-A (RECOMMENDED): new package `@twt/validity-service`** depending on both `@twt/niyamavali-engine` and `@twt/domain`. Keeps the engine a pure primitive; keeps the composition framework-agnostic so `apps/admin`, `apps/mobile`, `apps/jobs`, and Epic 6/10 consume the SAME service (AC3, PRD FR-12A "consistent across admin and member apps"). The engine barrel already names "the consuming surfaces (4.6 validity, Epic 6)" as EXTERNAL consumers of its exported `cache-key`/`audit` helpers (`index.ts:10`) — consistent with a separate package, not an in-engine module.
    - **D1-B (a module inside `@twt/niyamavali-engine`, e.g. `src/validity/`).** Cheaper wiring but blurs the "engine = pure interpreter primitive; validity = its consumer" seam the whole epic is built on; the engine would then both BE the primitive and CONSUME itself. Rejected unless BigDev prefers to defer a package split.
    - **D1-C (in `apps/api`).** Rejected — couples the canonical service to Fastify + one app; `apps/admin`/`apps/mobile`/`apps/jobs` could not reuse it without an HTTP hop.
    - **Note on architecture.md drift:** the source-tree diagram and requirements-traceability table (`architecture.md:4277`, `:4520`) still show the FR-12A Validity Service nested under `apps/api/src/modules/validity/` — a leftover from before Epic 4's actual package split (that same source tree has no entry at all for `packages/niyamavali-engine/`, which has existed since Story 4.1). Treat those two lines as stale, not a freeze violation of D1; BigDev's D1-A confirmation is the current source of truth for placement.
  - [x] **D2 (the contribution / R7 / R8 producer GAP — the biggest scoping decision).** The canonical payload names `contribution_history_summary` + R7/R8 sub-clause state, and the R7 (4.2) / R8 (4.3) ladders READ caller-injected `contribution.*` / `claim.*` facts. **There is NO contribution event source in the codebase** (`find packages/domain/src | grep contrib` → empty) — the contribution-fact producer is **Epic 8/9**, per `[[project_engine_never_infers_contribution_facts]]` ("producer is Epic 8/9 via 4.6 Validity Service … no contribution reader stubbed in Epic 4"). The never-supply-a-placeholder discipline (`[[CR-4.4-D3]]`, `[[CR-4.5-D1]]`) FORBIDS feeding fabricated `contribution.*` facts to R7/R8 — an absent fact resolves identically to an explicit `false`/`0`, so a placeholder would make an un-assessed member indistinguishable from a clean-record member.
    - **D2-A (RECOMMENDED): assemble every genuinely-producible field now; represent contribution + R7/R8 as an explicit typed `producer_unavailable` state, NOT zeros.** The service PRODUCES the four fields it can source truthfully today — `lock_in_status` (Story 3.6 snapshot + `getLockInClock`), `vyawastha_shulk_status` (Story 3.8 `getVyawasthaShulkStatus`), `retirement_coverage` (Story 4.5 R12 engine + the calendar-correct `valid_membership_years` derivation this story builds), `medical_disclosure_flags` (Story 3.9 `getMedicalDisclosures`) — and surfaces `contribution_history_summary: { status: 'producer_unavailable' }` (a distinct, typed sentinel), OMITTING R7/R8 from `applicable_niyamavali_clauses[]` until the Epic 8/9 producer supplies real `contribution.*` facts. Wire the R7/R8 ladder calls behind a **fact-availability gate**: the service calls `evaluateR7LadderLive`/`evaluateR8LadderLive` ONLY once genuine contribution facts exist; until then those clauses are absent from the payload, never `granted_years:0`-style defaults. Record a CR re-triggering at the Epic 8/9 contribution producer.
    - **D2-B (build a minimal contribution producer here).** REJECTED — out of Epic 4 scope, no contribution schema/events exist, speculative, and directly violates the "engine never infers contribution facts / producer is Epic 8/9" invariant.
  - [x] **D2m (medical / concealment scope sub-decision — pairs with D2).** The demoable closure C7 (`epics.md:1877`) says the Validity Service returns `special_flags: [concealment_review_required]` for an undeclared-IMA-illness member. BUT R14 concealment (Story 4.4) is **claim-linked** — `evaluateConcealment*` reads `claim.concealed_ima_condition_linked`, a CLAIM-time fact (`[[CR-4.4-D3]]`), and there is no claim at member-standing validity time.
    - **D2m-A (RECOMMENDED): the member-standing `pending_concealment_flag` / `medical_disclosure_flags` is derived from the Story 3.9 disclosure history + Story 2.x IMA-list assessment at MEMBER level (undeclared IMA-listed condition present in disclosure history) — NOT from the R14 claim-time evaluation.** The R14 `evaluateConcealment*` claim-linked path stays in Epic 6 claim filing. The C7 demo beat is satisfied by the member-standing flag derived from `getMedicalDisclosures` + IMA-list membership, surfaced State-Trustee-scope-only (see D5). This honors `[[CR-4.4-D3]]`: only supply the concealment signal once the disclosure/IMA assessment is COMPLETE, never as a placeholder.
    - **D2m-B (run R14 at member-standing with a synthetic claim fact).** REJECTED — fabricates a `claim.*` fact, exactly the placeholder `[[CR-4.4-D3]]` forbids.
  - [x] **D3 (p95 < 200ms at 4L — measure-and-budget split with Story 4.8's cache).** The materialized-view + per-cohort cache that actually DELIVERS 200ms@4L is **Story 4.8** (architecture §1.10 "FR-12A Member Validity Service cache — Postgres materialized view + cache-aside"; the read-amplification `[[CR-4.2-D1]]`/`[[CR-4.3-D2]]` explicitly re-trigger "when Story 4.6 runs the 100×-thread gate and measures ladder latency"). The uncached multi-clause compute path in 4.6 cannot be assumed to hit 200ms@4L pre-cache.
    - **D3-A (RECOMMENDED): 4.6 builds the uncached compute path + per-request memoization (architecture §1.10) + a p95 benchmark harness against a synthetic ~4L dataset that ESTABLISHES + RECORDS the measurement as a tracked budget; the 200ms@4L target is DELIVERED by Story 4.8's cache.** 4.6's gate = the harness runs, the budget is recorded, and the read-amplification (`[[CR-4.2-D1]]`/`[[CR-4.3-D2]]`) is measured and fixed-or-deferred-with-rationale. Matches the AC's "measured under realistic load in CI / pre-launch validation" (measurement, not necessarily green-at-4L pre-cache). Follows `[[project_friction_budget_baseline_ratchet]]` discipline for the recorded budget.
    - **D3-B (must hit 200ms@4L uncached now).** Likely infeasible pre-cache; would force pulling Story 4.8's materialized view forward into 4.6. Rejected unless BigDev wants the cache built here.
  - [x] **D4 (the determinism-replay CI gate — thread realism).** AC2 mandates "runs the same evaluation 100 times **across multiple threads** and asserts byte-identical payload hashes … any variance fails CI as a P0". The pure interpreter is already Date-free/pure (`interpret.ts:10-14`) and the `computed.values` map is sorted (Story 4.5) — the risk this gate guards is the SERVICE-level composition: hash-map iteration order over the rule set, async scheduling / `Promise.all` completion order feeding `applicable_niyamavali_clauses[]`/`provenance_trace[]`, and per-cohort recompute races.
    - **D4-A (RECOMMENDED): run the same pinned-instant `getValidityAt` evaluation 100× across real OS worker threads (Node `worker_threads`, or vitest's threaded pool with explicit fan-out) and assert exactly ONE distinct `validity_payload_hash`.** A P0 CI job wired into `ci.yml` alongside the engine's existing gates. This genuinely exposes hash-map/async-order nondeterminism that a single-threaded 100× loop would not.
    - **D4-B (100× single-threaded in-process loop).** Weaker — cannot surface true cross-thread scheduling nondeterminism the AC names. Acceptable only as a supplement, not the gate.
  - [x] **D5 (RBAC field-level redaction — placement + permission key).** Access is scope-respecting (AC1); `pending_concealment_flag` is State-Trustee-scope-only (PRD FR-12A); the member's own-profile self-call sees the payload minus internal flags and is NOT audit-logged, while admin-side calls ARE (PRD FR-12A "member-side self-calls are not logged").
    - **D5-A (CONFIRMED): the service returns the FULL canonical payload internally, then applies a scope-keyed redaction at the service boundary using Story 1.8 `hasPermission`/`AuthzContext` (`packages/domain/src/rbac/check.ts:150`).** One redaction function keyed by the caller's grants (member-self vs. admin-scope) so every consumer inherits identical redaction. Member-self path resolves via the `self` scope dimension (`scope.ts:SCOPE_DIMENSIONS`); admin-side calls fire the audit seam, self-calls do not.
      - **REUSE CHECK RESOLVED (BigDev's D5 refinement).** The full permission catalog (`permissions.ts:82-96` `SEED_PERMISSION_KEYS`) is action/write-oriented: `claim.approve`, `member.suspend`, `member.moderate`, `pariwar.amend_rule`, `pariwar.provision`, `niyamavali.amend`, `niyamavali.review`, `tc.publish`, `tc.approve`, `audit.export`, `audit.verify`. The only `member.*` keys are `member.suspend`/`member.moderate` — both WRITE actions. `verifier` currently holds `member.moderate` (`roles.ts:138`) — a write-oriented moderation grant, not a read grant; `auditor`/`helpline_operator` hold no `member.*` key at all. Reusing `member.moderate` to also gate a READ would be semantically wrong (conflating "may moderate" with "may view") AND under-permissive (`auditor`/`helpline_operator` need read access and hold nothing to reuse). **No existing key legitimately signals "may read validity" → introduce `member.view_validity`** (the catalog's documented "add the key the owning story references" append-per-epic pattern; header `permissions.ts:16-23`). Add it to `SEED_PERMISSION_KEYS`, bump `PERMISSION_CATALOG_VERSION` 2→3 (`permissions.ts:74`), grant it to the FR-46 roles that call FR-12A (the read-capable set — pariwar_admin/state_trustee/district_admin/block_admin/verifier/auditor/helpline_operator per Trustee-confirmable `defaultRoleBundles`), and extend `tests/rbac/roles.test.ts` (referential integrity) + `tests/rbac/check.test.ts` (the deny/allow tripwire).
      - **The State-Trustee-only `pending_concealment_flag` gate reuses ROLE/SCOPE, not a second key.** PRD FR-12A scopes the concealment flag to "State Trustee scope"; gate it on the `state_trustee` role / State scope via the existing grant model (`check.ts`), NOT a new `member.view_concealment` key — minting a second key for one field would be the "manufacture keys for endpoints that don't exist" anti-pattern the catalog header warns against (`permissions.ts:16-22`). Mint a distinct key ONLY if a role check proves insufficient during implementation (surface it if so).
    - **D5-B (redact at the `apps/api` contract boundary).** Rejected — pushes a security-critical redaction rule into one app, risking divergence across admin/mobile; the canonical service should own its own scope contract.

- [x] **Task 1 — Scaffold the Validity Service module + canonical payload contract (AC1)** — location per confirmed **D1**.
  - [x] Create the module (D1-A: new `packages/validity-service` package with `package.json` depending on `@twt/niyamavali-engine` + `@twt/domain`, `tsconfig.json`, `eslint.config.js`, `vitest.config.ts` mirroring `packages/niyamavali-engine`'s; register in `pnpm-workspace.yaml` if not glob-covered). Define the canonical payload TypeScript contract: `{ memberId, evaluatedAt (ISO-8601 DB-authoritative string — mirror `Provenance.evaluatedAt`, `types.ts:Provenance`), ruleRegistryVersion, isValid, isActive, lockInStatus, vyawasthaShulkStatus, contributionHistorySummary, medicalDisclosureFlags, retirementCoverage, specialFlags[], applicableNiyamavaliClauses[] (ordered — clauseId + clauseVersionId + outcome), provenanceTrace[] (ordered), validityPayloadHash }`.
  - [x] **Field-naming discipline (`clause_versions.ts:17-22`, echoed in `types.ts` header):** TS fields camelCase; any key that crosses the wire / becomes a transport field is snake_case, mapped at the `apps/api` contract boundary (`packages/contracts`), NOT inside the service. Reconcile the epic's `retirement_coverage_extension{granted_years,days_remaining,active}` naming vs. the PRD's `retirement_coverage{is_retired,years_of_coverage_earned,coverage_through}` — this is the Story 4.5 D3 variance the service RESOLVES (`[[CR-4.5-D3]]`): the engine emits raw `granted_years`/`is_retired`; the service maps to the PRD FR-12A shape AND does the date projection (`coverage_through = retired_at + granted_years` via calendar-correct interval math; `days_remaining`; `active = is_retired && days_remaining > 0`).
  - [x] **`validity_payload_hash` = `sha256hex(canonicalJsonStringify(<the ordered, redaction-FREE canonical payload minus the hash field itself>))`** using the domain canonicalizer (`packages/domain/src/canonical-json.ts` / the same hasher `Provenance.payloadHash` uses). The hash is over the FULL deterministic payload (pre-redaction) so replay reproducibility (AC2) is independent of the caller's scope. `evaluated_at` is EXCLUDED from — or pinned in — the replay-hash input per AC2's `(member_id, rule_registry_version, member_state_hash)` replay key (a live `getValidity` advances the instant by design — `evaluate.ts:12-14`); document exactly which fields participate in the replay-stable hash vs. the wire payload.

- [x] **Task 2 — Build the fact PRODUCER: derive the caller-injected facts the engine consumers read (AC1) — closes/advances `[[CR-4.4-D3]]`, `[[CR-4.5-D1]]`, `[[CR-4.5-D2]]`, `[[CR-4.5-D3]]`.**
  - [x] **`member.valid_membership_years` (int) + `member.is_retired` (bool) — for R12 (Story 4.5).** Derive `is_retired` from `getMemberPostingRetiredEver` (`packages/domain/src/member/posting.ts:61` — the Story 3.9 permanent first-ever-retirement anchor). Derive `valid_membership_years` **calendar-correctly** (AI-3-1 / `r7-ladder.ts:57-58` — `date_trunc`/`interval`, NEVER fixed-ms) from the Story 3.6 signup event (`member.signup_initiated`, `events.ts:209` — `joined_at` is carried in the event payload, NOT a `members` table column; the schema has no `joined_at` field) to `retired_at` (or `evaluatedAt` if not retired). **Apply the lapse-netting policy per `[[CR-4.5-D2]]`** — the D4 lapse question is `policy_review_required` on `niy.retirement-coverage.r12`; this producer is where that policy is finally pinned. Surface the chosen lapse semantics explicitly (and re-flag to BigDev if still unresolved).
  - [x] **NEVER supply a placeholder (`[[CR-4.5-D1]]`, `[[CR-4.5-D3]]`).** Supply `valid_membership_years`/`is_retired` ONLY once genuinely derived; a non-retired member with sufficient tenure earns a NONZERO `granted_years` = `years_of_coverage_earned` (per `[[CR-4.5-D3]]` — coverage earned but not yet active), which the service reads to project `coverage_through` once the member retires. Do NOT collapse an un-derived fact to `0`/`false`.
  - [x] **contribution / claim facts (R7/R8): DO NOT PRODUCE (confirmed D2-A).** No contribution source exists. Gate the R7/R8 ladder calls behind fact-availability; surface `contribution_history_summary: { status: 'producer_unavailable' }`; omit R7/R8 from `applicable_niyamavali_clauses[]`. Record the CR (Task 7).
  - [x] **medical concealment fact (D2m-A): derive the member-standing `pending_concealment_flag` from `getMedicalDisclosures` (`disclosure-read.ts:28`) + IMA-list membership** at member level — NOT via the R14 claim-time `evaluateConcealment*` path (that stays Epic 6). Only set the flag once the disclosure/IMA assessment is COMPLETE (`[[CR-4.4-D3]]`).

- [x] **Task 3 — Multi-clause evaluation at ONE pinned instant + deterministic ordering (AC1, AC2) — closes deferred-work W6 (`deferred-work.md:53`).**
  - [x] The service resolves DB `now()` ONCE (or the `getValidityAt` timestamp) and threads that SAME instant through EVERY engine call (`evaluateRetirementCoverageLive`, and — once D2 producer exists — `evaluateR7LadderLive`/`evaluateR8LadderLive`/`evaluateSpecialDeathLadderLive`), so all clauses resolve against ONE `rule_registry_version` with consistent provenance. The engine's `evaluate.ts:12-14` header explicitly anticipates this: "Pinning one instant also closes the deferred-work W6 mixed-provenance window for a future multi-clause evaluation (Story 4.6)." Use `evaluateAt`-family calls with the pinned instant, not the live `evaluate` family, to guarantee the single instant.
  - [x] **Deterministic rule order (AC2 — load-bearing).** Build `applicable_niyamavali_clauses[]` + `provenance_trace[]` from an EXPLICITLY-ORDERED rule list (a `const` ordered array of the rule families the service runs), NEVER hash-map iteration order, NEVER `Promise.all` completion order. If rules evaluate concurrently for latency, collect results into their declared-order slots (index-preserving), then serialize in that fixed order. Every array the payload emits is sorted/ordered by an explicit, replay-stable key.
  - [x] **Purity at the composition layer.** No `new Date()`/`Date.now()`/`Math.random()`/mutable module state in the assembly path — time enters only via the pinned DB instant (§1.11). The `validity_payload_hash` must be byte-identical across the 100×-thread replay (Task 6 / AC2).
  - [x] `getValidityAt(member_id, timestamp)` uses `getMemberStateAt(member_id, at)` (`read.ts:61`) + the as-of renewal read (`getVyawasthaShulkStatus` already takes an `atTimestamp` — `renewal-read.ts:107`) + the Niyamavali version effective at `at` + facts derived as-of `at` (Task 2's `valid_membership_years` computed to `at`), for replay-correct historical evaluation.

- [x] **Task 4 — Idempotency + audit (AC1)** — reuse Story 1.10/1.12 seams via the engine's DI, not new infra.
  - [x] Each `getValidity*` call is audit-logged for ADMIN callers via the Story 1.10 audit writer (the engine's `auditCompute` seam / `servicePool` — `evaluate.ts:EvaluateDeps`), writing `{ actor_id, member_id, evaluated_at, outcome_digest = validity_payload_hash, rule_registry_version }` (PRD FR-12A). **Member-side self-calls are NOT audit-logged** (PRD FR-12A — privacy + volume); gate the audit on caller scope (D5).
  - [x] Idempotent: identical `(member_id, rule_registry_version, member_state_hash, pinned_instant)` returns a byte-identical payload. The per-clause engine memo (Story 4.1 `keyedStore`) already dedupes each clause eval; the service-level payload is a pure function of the ordered clause results at the pinned instant. Do NOT introduce a second persistent cache here (that is Story 4.8).

- [x] **Task 5 — RBAC scope-respecting access + field redaction (AC1) — per confirmed D5.**
  - [x] Apply scope-keyed redaction at the service boundary via `hasPermission`/`AuthzContext` (`check.ts:150`, `check.ts:83`). `pending_concealment_flag` / internal `special_flags` → State-Trustee scope only; member-self (`self` dimension — `scope.ts:SCOPE_DIMENSIONS`) sees own payload minus internal flags. Fail-closed (`check.ts` invariant): any uncertain scope → redact.
  - [x] Add the confirmed **new** `member.view_validity` key (D5 reuse-check resolved: no existing key matches a member-validity READ) to `SEED_PERMISSION_KEYS` (`permissions.ts:82`), bump `PERMISSION_CATALOG_VERSION` 2→3 (`permissions.ts:74`), grant it to the read-capable FR-46 roles in `defaultRoleBundles` (`roles.ts`), and update `tests/rbac/roles.test.ts` (referential integrity) + `tests/rbac/check.test.ts` (the deny/allow tripwire). Gate the State-Trustee-only `pending_concealment_flag` on the `state_trustee` role/State scope — NOT a second permission key (see D5).

- [x] **Task 6 — Tests, incl. the P0 determinism-replay CI gate (AC1, AC2, AC3).**
  - [x] **Pure specs (`*.test.ts`, DB-free):** payload assembly from synthetic ordered clause results; the PRD↔epic field mapping + retirement date projection (`coverage_through`/`days_remaining`/`active` from `granted_years`/`is_retired`, `[[CR-4.5-D3]]`); `contribution_history_summary: producer_unavailable` + R7/R8 omitted (D2-A); the member-standing concealment flag (D2m-A); scope redaction matrix (member-self vs. State-Trustee vs. district-admin) incl. fail-closed; `validity_payload_hash` byte-stability + explicit-ordering assertions.
  - [x] **P0 determinism-replay gate (AC2 — per confirmed D4):** run the same pinned-instant evaluation 100× across threads (D4-A: `worker_threads`/threaded vitest pool) and assert exactly ONE distinct `validity_payload_hash`. **Any variance FAILS CI as a P0 architectural violation.** Wire it into `ci.yml` alongside the engine gates (the engine already rides `test`/`integration-tests` — `ci.yml:556`). Add a code-review note that any future parallel/async rule-execution optimization must preserve this gate (AC2 final bullet).
  - [x] **Live-DB integration (`integration/*.spec.ts`, `:5433`, own-committing — mirror `integration/retirement-coverage.spec.ts`):** seed a member (signup + optional retirement posting + receipt + medical disclosure) + the R12 clause; assert `getValidity`/`getValidityAt` return the genuinely-producible fields (lock-in, vyawastha_shulk, retirement_coverage, medical) with real provenance + the pinned-instant single-`rule_registry_version`; assert the audit row on an admin call + NO audit on a self-call; assert idempotent byte-identical replay. Key on membership / own rows / idempotent outcome, **NEVER global counts** (`[[project_live_db_test_gotchas]]`). Real CI `test (unit)` runs with `DATABASE_URL` UNSET so live specs skip.
  - [x] **p95 benchmark harness (AC1 — per confirmed D3-A):** a synthetic ~4L-scale dataset harness that measures + RECORDS the uncached-path p95 as a tracked budget; measure the R7/R8 ladder read-amplification (`[[CR-4.2-D1]]`/`[[CR-4.3-D2]]`) under concurrent load and fix-or-defer-with-rationale. The 200ms@4L target is Story 4.8's cache to green.

- [x] **Task 7 — Gate + full reconciliation + deferred-work (AC1) — `[[project_ci_actions_suspension_local_mirror]]`.**
  - [x] Record deferred-work CRs: the Epic 8/9 contribution-producer gap (D2-A re-trigger — the R7/R8 wiring goes live when real `contribution.*` facts exist); the p95-at-4L → Story 4.8 cache handoff (D3-A); any read-amplification deferral (`[[CR-4.2-D1]]`/`[[CR-4.3-D2]]`); confirm `[[CR-4.5-D1]]`/`[[CR-4.5-D2]]`/`[[CR-4.5-D3]]`/`[[CR-4.4-D3]]` are addressed-or-carried by this story's producer (Task 2).
  - [x] Run the gate: `pnpm --filter @twt/validity-service lint` + `typecheck` + `test`; the new determinism CI job; `DATABASE_URL=…:5433 pnpm ci:local` as the merge gate (`[[project_ci_actions_suspension_local_mirror]]` — mirrors all ci.yml jobs; integration needs `DATABASE_URL` on `:5433`). Confirm the documented-innocent `@twt/jobs`/`@twt/admin` concurrent-load flakes if they appear by running the suspect spec in isolation (`[[project_known_livedb_test_failures]]`).
  - [x] Sprint-status: flip `4-6-…` `backlog → ready-for-dev` now (this workflow) and let dev/review flip it onward; `last_updated` is the top-of-file reverse-chron COMMENT ledger (`[[project_sprint_status_ledger]]`).

### Review Findings

- [x] [Review][Patch] Self-call authorization trusts a caller-supplied `isSelf` boolean with no independent verification — `packages/validity-service/src/redaction.ts:56-57` (`assertCanReadValidity`). **Resolved: fix now** — verify `caller.resource` is a `self`-dimension resource whose value matches `caller.actorId` before trusting `isSelf`, so a mis-constructed `ValidityCaller` can't bypass the RBAC gate.
- [x] [Review][Patch] The service-level audit digest stores a wrapper-hash `sha256({outcome_digest, rule_registry_version, evaluated_at})`, not the literal `outcome_digest = validity_payload_hash` Task 4 specifies — `packages/validity-service/src/service.ts:166-188`. **Resolved: fix now** — store `payload.validityPayloadHash` directly as `requestPayloadHash`.
- [x] [Review][Patch] R12 fact-unavailability (no tenure anchor) makes `buildRuleDescriptors` skip the engine call entirely rather than calling it with no facts injected — `packages/validity-service/src/rules.ts:70-71`. **Resolved: fix now** — call `evaluateRetirementCoverageAt` with no facts injected instead of `continue`, so a genuine data gap surfaces as `rule.inputs_unavailable` rather than an absent clause.
- [x] [Review][Patch] `getValidityAt`/`getValidity` treat an omitted `opts.caller` as "an internal/system call" and return the FULL, unredacted, unaudited payload — `packages/validity-service/src/service.ts:46-58,72,128-133`. **Resolved: fix now** — require an explicit `opts.internal: true` marker for unaudited full access instead of it being the default when `opts.caller` is simply omitted.
- [x] [Review][Patch] `validity_payload_hash` is not actually stable across time-of-call as documented, because `provenanceTrace[].evaluatedAt` (per-clause) is never stripped from the hash input — only the top-level `evaluatedAt` is excluded — `packages/validity-service/src/payload.ts:166-172,245-251`.
- [x] [Review][Patch] Medical disclosure flags ignore the pinned `at` instant entirely — `produceMedicalDisclosureFlags` (`packages/validity-service/src/producer.ts:150-157`, called from `service.ts:98`) calls `medical.getMedicalDisclosures(db, pariwarId, memberId)` with no timestamp (`packages/domain/src/medical/disclosure-read.ts:28-43` has no `at` parameter), so `getValidityAt` for a historical instant always reads present-day disclosures — breaking the one-pinned-instant replay guarantee (W6) for this sub-object.
- [x] [Review][Patch] `getMemberRetirementAnchorAt` has no deterministic tie-break when two `is_retirement=true` postings share an identical `created_at` — `packages/domain/src/member/posting.ts:89-109` orders by `asc(createdAt)` only; add a secondary sort key (e.g. `asc(postingId)`).
- [x] [Review][Patch] Six independent DB reads in `getValidityAt` are `await`-ed sequentially instead of run concurrently — `packages/validity-service/src/service.ts:77-102` (`getMemberStateAt`, `getMemberSignupInstantAt`, `getMemberRetirementAnchorAt`, `getLockInClock`, `getVyawasthaShulkStatus`, `produceMedicalDisclosureFlags` don't depend on each other). Avoidable latency tax for a p95-budgeted service.
- [x] [Review][Patch] `deriveRetirementFacts` silently clamps to `validMembershipYears: 0` when `retiredAt` precedes `signupAt` (corrupt data) instead of surfacing the anomaly — `packages/validity-service/src/producer.ts:65-70`. Story 4.5 routes an equivalent bad-input case to `rule.inputs_unavailable` rather than a fabricated result.
- [x] [Review][Defer] The P0 100×-thread determinism CI gate (AC2 "any variance FAILS CI as a P0 architectural violation") only exercises synthetic, hand-built `EvaluationResult` fixtures — `packages/validity-service/tests/determinism-runner.ts` never calls the real producer or engine evaluator. Deferred — expanding the harness to a DB-backed real path is substantial work better scoped alongside Story 4.8; recorded as CR-4.6-D9 so the P0 claim's actual scope is documented.
- [x] [Review][Defer] The PRD-named `medical_disclosure.declared_illnesses[]` field is entirely absent from the wire payload — `packages/validity-service/src/types.ts:83-91`. Deferred — no current consumer needs the full list (Story 4.7 not yet built); recorded as CR-4.6-D10.
- [x] [Review][Defer] p95 "regression guard" ceiling (2000ms) is a >200x margin over the recorded baseline (~9.6ms p95), and the harness measures a single member sequentially rather than the D3-A-specified synthetic ~4L dataset under concurrent load — `packages/validity-service/tests/integration/p95-bench.spec.ts:29,32,104` — deferred, pre-existing scope already tracked under CR-4.6-D2/D3 (Story 4.8 cache handoff).
- [x] [Review][Defer] `ruleRegistryVersion` is computed only from clauses that actually resolved, conflating "which rules are configured" with "which facts happened to be available for this member" — `packages/validity-service/src/payload.ts:178-179`. Inert today (only R12 exists in `VALIDITY_RULE_ORDER`); revisit when R7/R8 land (Epic 8/9).
- [x] [Review][Defer] `lapseNetting` is threaded through `ValidityServiceOptions`/`RetirementFactsInput` but never read in `deriveRetirementFacts` — `packages/validity-service/src/producer.ts:65-70`. Harmless while `LapseNettingPolicy` has one member (`'gross'`, CR-4.5-D2/CR-4.6-D6), but whoever pins a second policy value will need to add an actual branch, not just swap the enum as CR-4.6-D6 implies.
- [x] [Review][Defer] `assertCanReadValidity` and `redactForCaller` are independently exported with no structural enforcement that both are always invoked together — `packages/validity-service/src/redaction.ts` + `index.ts`. Currently only `service.ts` calls them together; a future direct consumer could import one without the other.
- [x] [Review][Defer] The live-DB integration spec doesn't clean up the `audit_log_entries` rows it creates, unlike every other table it seeds — `packages/validity-service/tests/integration/validity-service.spec.ts`. Test-hygiene inconsistency, not a correctness issue given delta-counting assertions.
- [x] [Review][Defer] Nearly all of this package's DB-backed correctness guarantees only run when `DATABASE_URL` is set (`describe.skipIf`), compounded by the documented GitHub Actions suspension — pre-existing repo-wide condition (`[[project_ci_actions_suspension_local_mirror]]`), not unique to this diff.

## Dev Notes

### Where this sits (read before touching anything)

Story 4.6 is the **first `[SURFACE]` of Epic 4** — the canonical "is this member valid right now?" service. Stories 4.1–4.5 built the machinery: 4.1 the pure interpreter + DB shell (`evaluate`/`evaluateAt`/`interpretClause`), 4.2 R7, 4.3 R8, 4.4 R5/R9/R14, 4.5 R12 (the first COMPUTED rule). **4.6 does NOT add a new rule** — it COMPOSES those consumers into one payload, becomes the first real PRODUCER of the facts they read, guarantees deterministic rule-evaluation ordering under concurrency (the load-bearing AC2 commitment + the P0 100×-thread CI gate), applies RBAC redaction, audit-logs admin calls, and establishes the p95 measurement. Read `packages/niyamavali-engine/src/index.ts` (the barrel — every consumer wrapper you compose) and `packages/niyamavali-engine/src/evaluate.ts` **end to end** first — its header (`evaluate.ts:12-14`) explicitly names YOU as "the future multi-clause evaluation (Story 4.6)" that pins one instant and closes deferred-work W6.

**Task 0 is settled.** BigDev CONFIRMED all six decisions on 2026-07-04 — D1→A (new `@twt/validity-service` package), D2→A (assemble producible fields + `producer_unavailable`, never placeholder facts), D2m→A (member-standing concealment from disclosure/IMA; R14 claim-path stays Epic 6), D3→A (measure-and-budget here; 200ms@4L delivered by Story 4.8's cache), D4→A (100× across real OS threads), D5→A (service-boundary redaction; reuse-check resolved → new `member.view_validity` key, State-Trustee concealment gate reuses role/scope). Take Task 0 as written — do not re-open.

Load-bearing invariants (freeze rows 11 + 14; the Epic-4 through-lines):

- **Determinism is the P0 commitment of THIS story (AC2).** The pure interpreter is already Date-free/pure and Story 4.5's `computed.values` is sorted — the NEW risk 4.6 introduces is at the COMPOSITION layer: hash-map iteration over the rule set, `Promise.all` completion order feeding `applicable_niyamavali_clauses[]`/`provenance_trace[]`, async scheduling. Build the ordering from an EXPLICIT ordered rule list, collect concurrent results into index-preserving slots, serialize in fixed order. The 100×-thread byte-identical `validity_payload_hash` gate FAILS CI as a P0 on any variance.
- **ONE pinned instant across all clauses (closes W6 — `deferred-work.md:53`).** Resolve DB `now()` once; thread it through every `evaluateAt`-family call so all clauses share one `rule_registry_version` + consistent provenance. Never let each clause resolve its own `now()`.
- **NO hardcoded rule logic.** Adding a rule to the registry = ZERO code change to the service (PRD FR-12A). The service ORCHESTRATES registry-driven engine calls + assembles their outputs; it never branches on clause identity or re-implements a rule.
- **The engine EVALUATES facts, the service PRODUCES them — but only genuinely-derived ones (`[[project_engine_never_infers_contribution_facts]]`, `[[CR-4.4-D3]]`, `[[CR-4.5-D1]]`).** 4.6 is the producer named across Epic 4's deferrals. It produces `member.valid_membership_years`/`member.is_retired` (calendar-correct, AI-3-1) NOW; the `contribution.*`/`claim.*` producer is Epic 8/9 — surface `producer_unavailable`, never a placeholder `0`/`false`.
- **`granted_years` is tenure-derived, `is_retired`-independent (`[[CR-4.5-D3]]`).** A non-retired member with sufficient tenure earns nonzero `years_of_coverage_earned`; the service reads it to project `coverage_through` once the member retires. Do NOT assume zero for non-retirees.
- **Retirement coverage is an EXTENSION, never a denial.** `benefit_mechanism='pool'`; Epic-4 never-auto-deny posture. R14 concealment produces a FLAG (State-Trustee review), never an auto-deny (SM-1 C7).

### The canonical payload — three source shapes to reconcile

| Field | Epic AC (`epics.md:1992`) | PRD FR-12A (`prd.md:388-407`) | Source surface (this story wires) |
|---|---|---|---|
| lock-in | `lock_in_status` | `lock_in_status{days_at_join,unlock_date,state}` | `getLockInClock` / Story 3.6 snapshot (`lock-in.ts`, `read.ts:116`) |
| renewal | `vyawastha_shulk_status` | `vyawastha_shulk_status{paid_through,days_until_lapse}` | `getVyawasthaShulkStatus` (Story 3.8, `renewal-read.ts:107`) — wire renames `daysUntilGraceEnds`→`days_until_lapse` at the contract boundary |
| contribution | `contribution_history_summary` | `contribution_history{...,R7_subclause_state,R8_subclause_state}` | **`producer_unavailable` (D2-A)** — Epic 8/9 |
| medical | `medical_disclosure_flags` | `medical_disclosure{declared_illnesses[],pending_concealment_flag}` | `getMedicalDisclosures` (Story 3.9) + IMA-list (D2m-A) |
| retirement | `retirement_coverage` | `retirement_coverage{is_retired,years_of_coverage_earned,coverage_through}` | R12 engine (Story 4.5) + this story's date projection (`[[CR-4.5-D3]]`) |
| flags | `special_flags` | `special_flags[]` | assembled from fired rules |
| clauses | `applicable_niyamavali_clauses[]` (ordered) | `applicable_niyamavali_clauses[]` | ordered rule-list output (AC2) |
| — | `provenance_trace[]`, `validity_payload_hash` | (determinism/replay/audit consequences) | this story |

The PRD ALSO names top-level `is_valid` / `is_active` / `rule_registry_version` (`prd.md:388-390`) not in the epic AC's enumerated list — include them (they are the "covered for support if death today" / "valid AND past lock-in AND not suspended" answers the whole service exists to give). Record the epic↔PRD field-set variance in the Dev Agent Record.

### Existing surfaces you build on (READ these)

- `packages/niyamavali-engine/src/index.ts` — the barrel: `evaluate`/`evaluateAt`, `evaluateR7Ladder*`/`R8*`/`SpecialDeath*`/`Concealment*`/`RetirementCoverage*`, `buildCacheKey`/`memberStateHash`/`niyamavaliVersionHash`, `auditCompute`, all result types. Every consumer you compose is here.
- `packages/niyamavali-engine/src/evaluate.ts` — the DB shell + `EvaluateDeps` DI (scoped `db`, `keyedStore`, `servicePool`, `actor`). `evaluate` pins `now()` once (`evaluate.ts:12-14` — the W6/multi-clause note). The engine constructs no connections — the service supplies them via DI.
- `packages/niyamavali-engine/src/types.ts` — `EvaluationResult`/`RuleOutcome` (incl. the Story 4.5 `computed.values` channel)/`Provenance` (`evaluatedAt` is an ISO string, DB-authoritative). Your payload's `provenance_trace[]` is a list of these.
- `packages/niyamavali-engine/src/retirement-coverage.ts` — `evaluateRetirementCoverage*` + `R12_MEMBER_FACT_KEYS` (`member.valid_membership_years`, `member.is_retired`). You are its real PRODUCER (Task 2). The engine emits `granted_years`/`is_retired`; you map + project (`[[CR-4.5-D3]]`).
- `packages/domain/src/member/read.ts` — `getMemberStateAt(member_id, at)` (`:61`, the replay authority for `getValidityAt`) + `getLockInClock` (`:116`).
- `packages/domain/src/member/renewal-read.ts` — `getVyawasthaShulkStatus(db, pariwarId, memberId, atTimestamp)` (`:107`) — as-of renewal status; internal `daysUntilGraceEnds` maps to wire `days_until_lapse` at the contract boundary (`renewal-read.ts:12-14`).
- `packages/domain/src/member/posting.ts` — `getMemberPostingRetiredEver` (`:61`, Story 3.9 permanent retirement anchor) — the `retired_at`/`is_retired` source Task 2 derives from.
- `packages/domain/src/medical/disclosure-read.ts` — `getMedicalDisclosures` (`:28`) / `getLatestMedicalDisclosure` (`:49`) — the member-standing medical/concealment source (D2m-A).
- `packages/domain/src/rbac/check.ts` — `hasPermission` (`:150`), `AuthzContext` (`:83`), `requirePermission` (`:239`), fail-closed invariant. `packages/domain/src/rbac/scope.ts` — `SCOPE_DIMENSIONS` (incl. `self`). `packages/domain/src/rbac/permissions.ts` — `SEED_PERMISSION_KEYS`/`PERMISSION_CATALOG_VERSION` (D5).
- `packages/domain/src/canonical-json.ts` — the RFC-8785 canonicalizer the `validity_payload_hash` uses (same hasher `Provenance.payloadHash` uses); rejects `Date` (Story 4.1 note) — hash over ISO strings only.
- `packages/domain/src/member/events.ts` (`member.signup_initiated`, `:209`) — the signup `joined_at` anchor lives in this event's payload, not a `members` table column (the schema has no `joined_at` field) — the tenure-derivation input Task 2 reads.

### Producer derivation (Task 2 — the calendar-correct discipline)

- `valid_membership_years` = calendar-correct integer years from `joined_at` (`member.signup_initiated`) to `retired_at` (or `evaluatedAt` if not retired), lapse-policy applied (`[[CR-4.5-D2]]`) — via `date_trunc`/`interval`, NEVER fixed-ms (AI-3-1, `r7-ladder.ts:57-58`; the same rule `renewal-read.ts` follows with `setDate` calendar arithmetic). This is the producer the engine's R12 clause was built against; the engine reads the pre-derived integer verbatim, does the pure grant ladder, and emits `granted_years`. The service then projects `coverage_through`/`days_remaining`/`active`.
- `is_retired` = `getMemberPostingRetiredEver` truthy (monotonic once set, Story 3.9). Also accepts an admin trustee-marked retirement event per FR-12.
- **Absent ≠ zero (`[[CR-4.5-D1]]`, `[[CR-4.4-D3]]`).** Supply a fact ONLY once genuinely derived; the engine already routes an absent/wrong-type fact to `rule.inputs_unavailable` (Story 4.5) — the producer's job is to never DEFEAT that by injecting a placeholder `0`/`false`.

### Testing standards

- Framework: Vitest. Pure specs (`*.test.ts`) run without a DB; live-DB specs (`integration/*.spec.ts`) are `describe.skipIf(!hasDatabase)` and need `DATABASE_URL` on `:5433` (the `twt-test-pg` Docker, `[[project_live_db_test_gotchas]]`).
- Live-DB specs are **own-committing** (idempotency + audit writers COMMIT their own tx) — track seeded ids, clean in `afterAll`, assert membership / own rows / idempotent outcome, never global counts. Real CI `test (unit)` runs with `DATABASE_URL` UNSET so live specs skip (`env -u DATABASE_URL pnpm turbo run test`).
- **Determinism is the first-class P0 test target (AC2).** The 100×-thread byte-identical `validity_payload_hash` gate is a NEW CI job. Assert explicit ordering of every emitted array; assert the composition path is pure (no clock/randomness/mutable state). This is exactly the gate the whole determinism epic converges on.
- A `@twt/jobs` unit-job timeout OR a `@twt/admin` async-UI race under concurrent turbo load is the documented-innocent flake — prove innocence by running the suspect spec in isolation (`[[project_known_livedb_test_failures]]`).

### Project Structure Notes

- New (per confirmed D1-A): `packages/validity-service/` (service + payload contract + producer + tests). Depends on `@twt/niyamavali-engine` + `@twt/domain`; framework-agnostic (no app coupling). If D1-B is confirmed instead: a `packages/niyamavali-engine/src/validity/` module.
- Updated: `.github/workflows/ci.yml` (the new P0 determinism gate; the validity-service test filter — the engine already at `ci.yml:556`); `packages/domain/src/rbac/permissions.ts` (add `member.view_validity` to `SEED_PERMISSION_KEYS` + bump `PERMISSION_CATALOG_VERSION` 2→3 — CONFIRMED D5), `packages/domain/src/rbac/roles.ts` (grant it to the read-capable FR-46 roles), `tests/rbac/roles.test.ts` + `tests/rbac/check.test.ts` (the referential-integrity + deny/allow tripwires).
- **Explicitly OUT of scope (record any drift in the Dev Agent Record):** the contribution-fact producer (Epic 8/9); the R14 claim-time concealment path (Epic 6); the materialized-view / per-cohort cache that delivers 200ms@4L (Story 4.8); `<MemberStatusPanel>` rendering + the compound admin read model (Story 4.7); the `apps/api` HTTP handler + wire snake_case mapping (the contract boundary — this story stops at the framework-agnostic service, unless BigDev wants the API route wired here too).
- No expected change to the engine's rule logic, the seed clauses, or the R12 computation — 4.6 CONSUMES them. **If composing the service forces a change to `interpret.ts`/the engine rule surfaces, surface it in the Dev Agent Record** (it would signal the composition leaked into the primitive).

### References

- [Source: epics.md#Story 4.6 (lines 1982-2007)] — AC verbatim: `getValidity`/`getValidityAt`, the canonical payload field list, scope-respecting access, p95<200ms@4L, the rule-evaluation-order determinism invariant + the 100×-thread P0 CI gate, the AC3 `<MemberStatusPanel>`-sufficiency.
- [Source: epics.md#Epic 4 (lines 1865-1881)] — Epic framing; FR-12A in the FR list; freeze rows 11 (`epics.md:528`) + 14 (`epics.md:531`); demoable closure incl. SM-1 C7 (`epics.md:1877`); AR-57 (determinism/replay) + AR-65 (compound read models) anchors (`epics.md:1873`).
- [Source: prds/prd-TWT-2026-05-22/prd.md#FR-12A (lines 381-426)] — the canonical payload shape (`is_valid`/`is_active`/`rule_registry_version` + the seven sub-objects); "adding a rule = zero code change"; deterministic/idempotent/audit-logged; admin-calls-logged-self-calls-not; p95<200ms@4L; `pending_concealment_flag` State-Trustee-scope-only.
- [Source: architecture.md#§1.10 (lines 1043-1081)] — the FR-12A cache = Postgres materialized view + cache-aside, 60s TTL, per-cohort invalidation + conservative all-members fallback (Story 4.8); stampede protection; Redis is a FUTURE trigger. Confirms 200ms@4L is delivered by the cache (D3).
- [Source: architecture.md#§1.11 (lines 1083-1101)] — DB-authoritative time; app-server clocks untrusted across audit/replay/integrity boundaries (the pinned-instant discipline).
- [Source: architecture.md (lines 1254-1275, 2343-2347)] — Validity service as the canonical read path; FR-12A "not cache-first globally" (cached self-service path vs. non-cached support/dispute path — the declaration lives in the caller's contract).
- [Source: packages/niyamavali-engine/src/evaluate.ts (lines 1-14, EvaluateDeps)] — the DB shell + DI + the one-pinned-instant / W6 / multi-clause-Story-4.6 note.
- [Source: packages/niyamavali-engine/src/index.ts] — the consumer barrel (every wrapper the service composes; the "consuming surfaces (4.6 validity, Epic 6)" comment).
- [Source: packages/niyamavali-engine/src/types.ts] — `EvaluationResult`/`RuleOutcome`(+`computed.values`)/`Provenance` shapes; ISO-string `evaluatedAt`; snake_case-payload / camelCase-field discipline.
- [Source: packages/domain/src/member/read.ts (61,116), renewal-read.ts (107,15-19), posting.ts (61), medical/disclosure-read.ts (28)] — the genuinely-producible field sources.
- [Source: packages/domain/src/rbac/check.ts (83,150,239), scope.ts (SCOPE_DIMENSIONS), permissions.ts (74,82)] — Story 1.8 RBAC: `hasPermission`/`AuthzContext`/fail-closed, the `self` scope dimension, the permission catalog (D5).
- [Source: 4-5-fr-12-retirement-coverage-extension-computation.md] — the R12 producer contract this story fulfills; `[[CR-4.5-D1]]`/`[[CR-4.5-D2]]`/`[[CR-4.5-D3]]` re-triggering at 4.6; the D2-i/D3 field-name variance the service resolves.
- [Source: _bmad-output/implementation-artifacts/deferred-work.md (53, 1686, 1699, 1706-1718)] — W6 mixed-provenance (closed here); `[[CR-4.2-D1]]`/`[[CR-4.3-D2]]` read-amplification (measured here); `[[CR-4.4-D3]]` concealment absent-fact; `[[CR-4.5-D1/D2/D3]]` R12 producer re-triggers.
- [Source: memory `[[project_engine_never_infers_contribution_facts]]`] — the contribution producer is Epic 8/9; no contribution reader is stubbed in Epic 4 (D2).

## Dev Agent Record

### Agent Model Used

claude-opus-4-8 (Claude Code, dev-story workflow), 2026-07-04.

### Debug Log References

- P0 determinism gate: initial `worker_threads` approach used a `.ts` worker + `execArgv: ['--import','tsx']` — Node 22.12's native type-stripping loaded the `.ts` worker but did NOT remap nested `.js`→`.ts`, and a bare `--import tsx` in worker execArgv did not register tsx's resolve hook. Resolved by a plain-JS `.mjs` worker that registers tsx AT RUNTIME (`tsx/esm/api` `register()`) then dynamic-imports the type-checked TS run body. Gate is green (100 threaded evals → 1 distinct hash).
- `ci:local` (`DATABASE_URL` on :5433): `test (unit)` + `integration-tests` reported red ONLY because `@twt/jobs#test` timed out under concurrent turbo load — the documented-innocent flake ([[project_known_livedb_test_failures]]). Proven innocent by running `@twt/jobs` in isolation (26/26 pass) and `@twt/domain` in isolation (517/517 pass). All 16 static gates + the new `determinism-replay` gate passed. My touched packages all green in isolation.
- p95 harness (D3-A) recorded uncached single-member: p50≈5.6ms, p95≈9.6–11ms, p99≈13–16ms (local :5433). Well under the loose 2000ms sanity ceiling; 200ms@4L is Story 4.8's cache to green.

### Completion Notes List

**Delivered** the framework-agnostic `@twt/validity-service` package (D1-A) composing the Story 4.1 engine + the R12 consumer into the canonical FR-12A payload, as the first real PRODUCER of the engine's caller-injected facts.

- **AC1 (canonical payload + scope-respecting access + p95 measured):** `getValidity`/`getValidityAt` return the canonical payload (`memberId, evaluatedAt, ruleRegistryVersion, isValid, isActive, lockInStatus, vyawasthaShulkStatus, contributionHistorySummary, medicalDisclosureFlags, retirementCoverage, specialFlags[], applicableNiyamavaliClauses[], provenanceTrace[], validityPayloadHash`). Scope-respecting via Story 1.8 RBAC (`assertCanReadValidity` + `redactForCaller`). p95 measured + recorded (D3-A; `tests/bench/p95-budget.md`).
- **AC2 (rule-evaluation-order determinism — the P0 commitment):** ordering built from an EXPLICIT `VALIDITY_RULE_ORDER` with index-preserving concurrent collection (`rules.ts`); the payload hash excludes `evaluatedAt` so it is a pure function of `(member, rule_registry_version, member-state, facts, ordered outcomes)`. The P0 gate runs the composition 100× across real OS `worker_threads` (D4-A) and asserts ONE distinct hash — wired into `ci.yml` as `determinism-replay` (+ the `ci-local.sh` mirror).
- **AC3 (`<MemberStatusPanel>` sufficiency):** the payload carries every FR-12A sub-object needed to render the panel with no extra queries; the compound admin read model is Story 4.7 (out of scope, recorded).
- **W6 CLOSED:** one pinned instant threads through all clauses + producer derivations (integration-asserted).

**Honest refinements to the story's Task prose (all recorded as deferred-work CRs):**
- Task 2 said derive `is_retired` from `getMemberPostingRetiredEver` — that accessor returns a BOOL only, but the coverage date projection needs `retired_at` (a DATE). Added `getMemberRetirementAnchorAt` (domain `posting.ts`) returning the FIRST retirement posting's `created_at`, plus `getMemberSignupInstantAt` (domain `read.ts`) for the tenure-start anchor (the signup event's `occurred_at`; no `joined_at` column exists). Both use `.limit(1)` (domain-invariants gate compliant).
- Task 2 / D2m-A said derive the member-standing concealment flag from `getMedicalDisclosures` + IMA-list membership. The disclosed condition codes are **Tier-1 ENCRYPTED**, so the framework-agnostic service cannot decrypt-and-compare, and the true C7 concealment is death-CLAIM-linked (Epic 6 R14). Implemented honestly: the member-standing flags surface NON-PII summary only (presence/count/`ima_list_version`), and `pendingConcealmentFlag` is set ONLY from a COMPLETED, injected `concealmentAssessment` seam — never fabricated ([[CR-4.4-D3]]). See CR-4.6-D4.
- Lapse-netting ([[CR-4.5-D2]]) is a `policy_review_required` policy the Trustee Panel has NOT resolved. The producer uses documented, swappable `gross` tenure (`LapseNettingPolicy`), not a placeholder. See CR-4.6-D6.
- `is_valid`/`is_active` are a documented state composition (`VALID_STATES`/`ACTIVE_STATES` in `payload.ts`), not an enumerated AC. See CR-4.6-D7.
- The engine's per-clause `rule.evaluate` compute-audit fires for self-calls too; only the service-level `validity.evaluate` audit is self-call-gated (PRD FR-12A honored at the service boundary). See CR-4.6-D5.

**epic↔PRD field-set variance recorded:** the PRD's top-level `is_valid`/`is_active`/`rule_registry_version` (not in the epic AC's enumerated list) ARE included. The retirement shape resolves the Story 4.5 D3 variance ([[CR-4.5-D3]]): engine emits raw `granted_years`/`is_retired`; the service maps to PRD `{is_retired, years_of_coverage_earned, coverage_through}` + projects `coverage_through`/`days_remaining`/`active`.

**Benign engine change:** added `selectDbNow` to the engine barrel (`niyamavali-engine/src/index.ts`) — the service needs one DB-now for live `getValidity`. Additive re-export only; NO rule-logic/interpreter change (composition did not leak into the primitive). Resolves the [[CR-4.2-D4]] "second consumer needs `selectDbNow`" re-trigger.

**Tests:** 62 validity-service tests (calendar, producer, payload, redaction pure specs + the P0 determinism gate + live-DB integration + the p95 harness) — all green. Domain rbac tests updated for the catalog v3 bump + the new `member.view_validity` key (permissions/roles/check tests). `ci:local` static gates + `determinism-replay` green; the only red was the documented-innocent `@twt/jobs` concurrent-load flake (passes in isolation).

**Out of scope (recorded):** the Epic 8/9 contribution producer (R7/R8 gated off); the Epic 6 R14 claim-time concealment path; Story 4.8's materialized-view cache (delivers 200ms@4L); Story 4.7's `<MemberStatusPanel>` + compound admin read model; the `apps/api` HTTP handler + wire snake_case mapping (contract boundary).

### File List

**New — `packages/validity-service/`:**
- `package.json`, `tsconfig.json`, `eslint.config.js`, `vitest.config.ts`
- `src/index.ts` — package barrel
- `src/types.ts` — canonical payload contract + sub-object shapes
- `src/calendar.ts` — pure calendar-correct date math (leap-safe)
- `src/producer.ts` — the fact producer (retirement facts + medical flags; never a placeholder)
- `src/rules.ts` — `VALIDITY_RULE_ORDER` + ordered multi-clause evaluation (AC2)
- `src/payload.ts` — pure payload assembly + retirement projection + the replay-stable hash
- `src/redaction.ts` — RBAC scope-keyed read-access + field redaction (D5)
- `src/hash.ts` — sha256 hex seam
- `src/service.ts` — `getValidity`/`getValidityAt` (the DB shell composition + audit)
- `tests/calendar.test.ts`, `tests/producer.test.ts`, `tests/payload.test.ts`, `tests/redaction.test.ts`
- `tests/determinism.test.ts` + `tests/determinism.worker.mjs` + `tests/determinism-runner.ts` (the P0 gate)
- `tests/integration/validity-service.spec.ts` + `tests/integration/p95-bench.spec.ts`
- `tests/fixtures/eval-results.ts`, `tests/fixtures/r12-clause.ts`
- `tests/bench/p95-budget.md` — the recorded p95 budget (D3-A)

**Modified:**
- `packages/niyamavali-engine/src/index.ts` — barrel: export `selectDbNow` (benign, additive)
- `packages/domain/src/member/read.ts` — add `getMemberSignupInstantAt` (tenure anchor)
- `packages/domain/src/member/posting.ts` — add `getMemberRetirementAnchorAt` (retirement date anchor)
- `packages/domain/src/rbac/permissions.ts` — add `member.view_validity`; bump `PERMISSION_CATALOG_VERSION` 2→3
- `packages/domain/src/rbac/roles.ts` — grant `member.view_validity` to the read-capable FR-46 roles
- `packages/domain/tests/rbac/permissions.test.ts`, `roles.test.ts`, `check.test.ts` — catalog v3 + the new key
- `.github/workflows/ci.yml` — the `determinism-replay` P0 job + `@twt/validity-service` in the integration filter
- `scripts/ci-local.sh` — mirror the two ci.yml changes
- `pnpm-lock.yaml` — the new workspace
- `_bmad-output/implementation-artifacts/deferred-work.md` — Story 4.6 CRs (CR-4.6-D1..D7; W6 closed)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — 4-6 → in-progress → review

## Change Log

| Date | Change |
|---|---|
| 2026-07-04 | **Story 4.6 IMPLEMENTED (→ review).** Built `@twt/validity-service` (D1-A) composing the Story 4.1 engine + R12 into the canonical FR-12A payload, as the first real fact PRODUCER. AC1 (canonical payload + Story 1.8 scope-respecting access + p95 measured/recorded), AC2 (deterministic rule-order + the P0 100×-`worker_threads` hash-invariance gate wired into `ci.yml`), AC3 (`<MemberStatusPanel>`-sufficient payload) all satisfied. Producer derives `member.valid_membership_years`/`member.is_retired` calendar-correctly (added domain anchors `getMemberSignupInstantAt` + `getMemberRetirementAnchorAt`); R7/R8 gated off with `contribution_history_summary: producer_unavailable` (Epic 8/9, D2-A); member-standing concealment is a gated NON-PII seam (D2m-A, encrypted conditions → true C7 stays Epic 6). RBAC: new `member.view_validity` key (catalog v3), State-Trustee concealment gate via role/scope. Closed W6 (one pinned instant). Recorded CR-4.6-D1..D7 in deferred-work. 62 package tests green; domain rbac + engine + all `ci:local` static gates green (the only red was the documented-innocent `@twt/jobs` concurrent-load flake, passes in isolation). Benign engine-barrel `selectDbNow` export. |
| 2026-07-04 | BigDev CONFIRMED all six Task 0 decisions (settled, taken as written): **D1→A** (new `@twt/validity-service` package depending on `@twt/niyamavali-engine` + `@twt/domain`); **D2→A** (assemble the genuinely-producible fields + surface `contribution_history_summary: producer_unavailable`, NEVER placeholder facts — contribution producer is Epic 8/9); **D2m→A** (member-standing concealment from disclosure/IMA; the R14 claim-time path stays Epic 6); **D3→A** (measure-and-budget the uncached p95 here; 200ms@4L delivered by Story 4.8's materialized-view cache); **D4→A** (determinism gate runs 100× across real OS `worker_threads`); **D5→A** with the reuse-first refinement RESOLVED — the permission catalog is action/write-oriented (`member.suspend`/`member.moderate` are the only `member.*` keys, both writes; `verifier` holds `member.moderate` but that's a write-oriented moderation grant, not a read grant, and `auditor`/`helpline_operator` hold no `member.*` key at all), so no existing key legitimately signals a member-validity READ → introduce `member.view_validity` (bump `PERMISSION_CATALOG_VERSION` 2→3), and gate the State-Trustee-only `pending_concealment_flag` on the `state_trustee` role/scope rather than a second key. Task 0 flipped to CONFIRMED; D5 body, Task 5, Project Structure Notes, and the "Task 0 is settled" Dev Note propagated. |
| 2026-07-04 | Quality-review pass (validate-create-story): all epics.md/PRD/architecture.md/source-code citations verified against current files. Two corrections applied: (1) the `packages/domain/src/schema/members.ts:24-25` citation for the `joined_at` anchor was wrong — no `joined_at` column exists on `members`; `joined_at` lives only in the `member.signup_initiated` event payload (`events.ts:209`), corrected in Task 2 and References; (2) Task 0 D5's reuse-check stated `verifier`/`auditor`/`helpline_operator` "hold neither" `member.suspend` nor `member.moderate` — `verifier` actually holds `member.moderate` (`roles.ts:138`); corrected the rationale to note it's a write-oriented moderation grant, not a read grant, which still supports minting `member.view_validity` (the recommendation is unchanged). Also corrected minor line-citation drift (`index.ts:9`→`:10`, `renewal-read.ts:15-19`→`:12-14`, `permissions.ts:82-99`→`:82-96`, architecture.md §1.10/§1.11 ranges), and flagged that architecture.md's source-tree/traceability entries (`:4277`, `:4520`) placing the Validity Service under `apps/api/src/modules/validity/` predate Epic 4's actual package split and don't override D1's confirmed `packages/validity-service/`. Story stays ready-for-dev. |
| 2026-07-04 | Story 4.6 created (ready-for-dev) — FR-12A Member Validity Service, the FIRST `[SURFACE]` of Epic 4. Composes the 4.1 primitive + the R7/R8/R5-R9/R14/R12 consumers into the canonical payload; becomes the first real PRODUCER of the engine's caller-injected facts; guarantees rule-evaluation-order determinism (the load-bearing AC2 + the P0 100×-thread CI gate); pins ONE DB instant across all clauses (closes deferred-work W6); applies Story 1.8 RBAC redaction; audit-logs admin calls (self-calls not); establishes the p95 measurement. FIVE decisions flagged for BigDev (unlike 4.5's pre-confirmed Task 0): **D1** placement (new `@twt/validity-service` package vs. in-engine module vs. apps/api); **D2** the contribution/R7/R8 producer GAP (assemble producible fields + explicit `producer_unavailable`, NEVER placeholder facts — contribution producer is Epic 8/9 per `[[project_engine_never_infers_contribution_facts]]`, `[[CR-4.4-D3]]`, `[[CR-4.5-D1]]`) + **D2m** member-standing concealment flag from disclosure/IMA (R14 claim-path stays Epic 6); **D3** p95<200ms@4L is measured-and-budgeted here, DELIVERED by Story 4.8's cache (materialized view, architecture §1.10); **D4** the determinism gate runs 100× across real OS threads (`worker_threads`); **D5** RBAC field redaction at the service boundary via `hasPermission`/`AuthzContext` + a `member.view_validity` permission key. Resolves the Story 4.5 D3 field-name variance (`[[CR-4.5-D3]]` — engine emits raw `granted_years`/`is_retired`, service maps to FR-12A `{is_retired,years_of_coverage_earned,coverage_through}` + does the date projection). |
