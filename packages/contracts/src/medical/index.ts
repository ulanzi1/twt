// Barrel for the medical-disclosure transport DTOs — Story 3.5 (Task 5).
// Re-exported from `@twt/contracts` (see ../index.ts). The fourth signup-wizard SURFACE;
// registers real OpenAPI components + paths (see scripts/emit-openapi.ts).

export * from './disclosure.js';
