// packages/contracts/src/rtbf/index.ts
//
// RTBF (Right-To-Be-Forgotten) contracts barrel (Story 3.12). Consume via the `@twt/contracts` TOP
// barrel (the withdrawal/data-export convention — there is no subpath `exports` map):
//   import { RtbfConfirmRequest, RtbfStatusResponse } from '@twt/contracts';

export * from './rtbf.js';
