# Synthesis Schema — P0-2d Operator Shadowing

> 12-section schema for the AC-named synthesis at `_bmad-output/research/p0-2d-operator-shadowing.md`. Per Story 0.9 + Story 0.10 12-section precedent + P0-2d-distinct §4 operator-workflow-call-pattern observation worksheet evaluation load-bearing section.
>
> **Substantive synthesis is Task 9 _AWAITING EXTERNAL ACTION_.** At Task 6 author-commit, the synthesis file is scaffolded with the schema headers + per-dimension `_AWAITING_SHADOWING_CONDUCT_` placeholders.

## §1 — Header

```yaml
synthesis_title: "P0-2d Operator Shadowing Synthesis"
authoring_status: <_AWAITING_SHADOWING_CONDUCT_ | DRAFT-PENDING-TRUSTEE-REVIEW | TRUSTEE-REVIEWED-ACCEPTED | TRUSTEE-REVIEWED-ACCEPTED-WITH-REVISIONS | TRUSTEE-REVIEWED-REJECTED-PENDING-REWORK | SUSPENDED-PENDING-SUBSTITUTE>
synthesis_date: YYYY-MM-DD
operator_pseudonym: Operator-1
host_helpline_pseudonym: HostHelpline-1
host_institution_type: <welfare-trust | cooperative-trust | community-trust>
total_shifts_conducted: N
total_observed_call_time_hours: <decimal>
total_observed_call_count: N
critical_hypothesis_count_at_authoring: 17
total_assumption_count_at_authoring: 32
divergence_log_row_count_at_authoring: N
trustee_review_status: <pending | post-synthesis-001 reviewed>
operator_review_status: <not-offered | offered-declined | offered-accepted-pre-trustee-review>
host_helpline_review_status: <not-offered | offered-declined | offered-accepted-pre-trustee-review>
```

## §2 — Host-helpline-engagement summary

```yaml
host_helpline_engagement_summary:
  engagement_path: <TSCT-trustee-referral | NSCT-analogous-referral | trustee-personal-network | other>
  host_helpline_approval_date: YYYY-MM-DD
  trustee_pre_shadowing_approval_date: YYYY-MM-DD
  operator_consent_obtained_date: YYYY-MM-DD
  shift_count: N
  shift_dates: [YYYY-MM-DD, YYYY-MM-DD, ...]
  total_observed_call_time_hours: <decimal>
  caller_consent_decline_rate: <percentage>
  caller_consent_revocation_mid_call_rate: <percentage>
  caller_ended_call_abruptly_rate: <percentage>
  emotional_overload_observer_discretion_end_rate: <percentage>
```

> **Rate denominator for all four rate fields:** `total_in_scope_calls` = `caller_consent_given` + `caller_consent_declined` + `caller_consent_no_response` + `caller_consent_revoked_mid_call` + `caller_ended_call_abruptly` + `emotional_overload_observer_discretion_end` (cumulative across all shifts). Each rate = category count ÷ `total_in_scope_calls`.

## §3 — Per-dimension synthesis (six AC-named dimensions + 7th cross-cutting)

For each of the 6 AC-named observation dimensions + the 7th cross-cutting register-grammar dimension, the synthesis populates:

### §3.1 — Call category distribution

`_AWAITING_SHADOWING_CONDUCT_` at scaffold. At synthesis-commit, this section will carry:
- Distribution percentages across observed categories (with underlying observed-call N + explicit acknowledgment that small-N small-trust observations cannot statistically forecast TWT operational distribution)
- Qualitative-pattern observations capturing the dimensional finding regardless of statistical limit
- Per-shift + per-call citations (`Operator-1 shift-N call-X §dimension-1`)
- PRD/UX/architecture assumption validation status per `assumption-inventory.md` Cat-1 rows

### §3.2 — Average handling time

`_AWAITING_SHADOWING_CONDUCT_` at scaffold. At synthesis-commit, this section will carry:
- Average handling time per category (with N caveat)
- Time-to-first-substantive-response distribution
- Per-step time breakdown (member-lookup / category-selection / body-capture / read-back / submit / escalation-handover)
- Per-shift + per-call citations
- Assumption validation status per Cat-2 rows

### §3.3 — Common caller pain points

`_AWAITING_SHADOWING_CONDUCT_` at scaffold. At synthesis-commit, this section will carry:
- Pain-point category distribution (categorical-only, NO caller-personal-content)
- Pain-point cadence patterns
- Pain-point resolution clarity rate
- Per-shift + per-call citations

### §3.4 — Escalation patterns

`_AWAITING_SHADOWING_CONDUCT_` at scaffold. At synthesis-commit, this section will carry:
- Escalation rate (% of calls)
- Escalation trigger distribution
- Handover protocol patterns (warm-transfer / cold-transfer / case-held / queue-routing)
- Operator-attribution at handover
- Assumption validation status per Cat-5 supervisor-escalation rows

### §3.5 — Operator-wishlist (what the operator wishes their tooling did)

`_AWAITING_SHADOWING_CONDUCT_` at scaffold. At synthesis-commit, this section will carry:
- Wishlist category distribution
- Wish-concreteness distribution
- Recurrence patterns
- Verbatim operator-quotes for re-consented quotes (with `[quote-re-confirmed YYYY-MM-DD]` markers)
- Per-shift + per-call citations

### §3.6 — Operator-improvisation (where the operator improvises around process gaps)

`_AWAITING_SHADOWING_CONDUCT_` at scaffold. At synthesis-commit, this section will carry:
- Improvisation category distribution
- Improvisation trigger distribution
- Routine-pattern vs one-time-response rates
- Improvisation-outcome distribution (resolved / escalated)
- Per-shift + per-call citations

### §3.7 — Cross-cutting operator-caller register grammar (7th dimension)

`_AWAITING_SHADOWING_CONDUCT_` at scaffold. At synthesis-commit, this section will carry:
- Call-opening register patterns
- Closing patterns
- Emotional-load pacing patterns
- UX-DR55 operator-facing precise-technical-register validation (the carve-out is justified OR not)
- Hindi register switching patterns
- Per-shift + per-call citations

## §4 — Operator-workflow-call-pattern observation worksheet evaluation (LOAD-BEARING AC SURFACE)

`_AWAITING_SHADOWING_CONDUCT_` at scaffold. At synthesis-commit, this section will carry the per-pattern verdict for each pre-stated worksheet row:

### §4.1 — Routing-policy category candidates × verdict matrix
For each row in `operator-workflow-call-pattern-observation-worksheet.md` §2: verdict + shadowing citation + proposed-revision text for any `requires-revision-with-proposed-change` verdict + observation-coverage-acknowledgment for `not-evaluated` rows.

### §4.2 — SLA-target candidates × verdict matrix
For each row in §3.

### §4.3 — Helpline-call-to-ticket-workflow-step × verdict matrix
For each row in §4.

### §4.4 — Member-lookup-form pattern × verdict matrix
For each row in §5.

### §4.5 — Read-back-card pattern × verdict matrix
For each row in §6.

### §4.6 — Operator-facing UX-DR55 precise-technical-register × verdict matrix
For each row in §7.

### §4.7 — Intake-Console-pattern-(b) decision-strip × verdict matrix
For each row in §8.

### §4.8 — Verifier-console-context-from-helpline × verdict matrix
For each row in §9.

## §5 — Divergence summary

`_AWAITING_SHADOWING_CONDUCT_` at scaffold. At synthesis-commit, this section will carry:
- Total divergence-log rows authored
- Per-severity tally (`routing-policy-revision-required` / `sla-target-revision-required` / etc.)
- Per-Story affected: Story 10.1 / Story 10.3 / Story 6.3 / Story 6.10 / UX-DR amendments
- Critical-hypothesis-tagged assumption resolution: validated / refuted / nuanced / not-evaluated (out of 17 critical)
- Coverage-gap acknowledgment per `not-evaluated-due-to-host-helpline-context-mismatch` or `not-evaluated-due-to-observation-coverage-gap` rows

## §6 — Implications for Epic 10 (helpdesk + helpline)

`_AWAITING_SHADOWING_CONDUCT_` at scaffold. At synthesis-commit, this section will carry per-Story implication:

### §6.1 — Story 10.1 Helpdesk Subsystem Data Model + Routing-Policy Registry
- Routing-policy category enumeration findings: validated / refuted / nuanced + proposed revisions
- SLA-target enumeration findings: validated / refuted / nuanced + proposed revisions

### §6.2 — Story 10.2 Member-Facing Helpdesk Ticket Filing
- Category-selection discipline findings (cross-link to operator-side category vs member-self-categorization)
- UX-DR55 dignified copy + operator shadowing findings (epics line 3349)

### §6.3 — Story 10.3 Helpline Call-to-Ticket Operator Surface
- 5-step workflow findings: validated / refuted / nuanced + proposed step-order or step-addition revisions
- Operator-attribution findings
- SM-1 demo beat C3 implications

### §6.4 — Story 10.4 Helpdesk Admin Console + SLA Tracking + Cross-Link Integration
- SLA-budget-per-category surfacing findings
- Cross-link to claim / reconciliation / module-marketplace findings

## §7 — Implications for Story 6.3 (helpline-mediated claim filing)

`_AWAITING_SHADOWING_CONDUCT_` at scaffold. At synthesis-commit, this section will carry:
- UX-DR45 `<MemberLookupForm>` findings: 4-criteria validation + disambiguation pattern + no-match pattern
- UX-DR46 `<ReadBackCard>` findings: 3-field validation + verbal confirmation pattern + correction pattern
- `claim.intake.initiated` event shape findings (`claimant_actor_id: operator_acting_for_<caller_id>` + `operator_attribution`)
- Supervisor-escalation findings (`intake_pending` state + escalation trigger patterns)

## §8 — Implications for Story 6.10 (verifier console signals panel)

`_AWAITING_SHADOWING_CONDUCT_` at scaffold. At synthesis-commit, this section will carry:
- Operator's call-notes shape: structured / free-text / hybrid findings
- Operator's supervisor-escalation-context findings
- Operator's caller-disambiguation-signals findings
- Hypothesis evaluation: operator-mediated case context flowing to Anita's signals panel as informative vs noise
- ~5s-load + cross-Pariwar-scope-handling + signals-as-advisory-not-auto-adjudicating design implications

## §9 — Cross-cutting findings

`_AWAITING_SHADOWING_CONDUCT_` at scaffold. At synthesis-commit, this section will carry:
- Operator-caller register grammar findings spanning dimensions
- UX-DR55 operator-facing precise-technical-register carve-out validation
- Two-actor design discipline validation (UX spec line 1513)
- UX-DR49 + UX-DR62 three-tier recovery ladder operator-fallback validation
- Intake-Console-pattern-(b) decision-strip validation (UX spec line 1688)

## §9.bis — Surprises sub-section

(Per Story 0.9 P-24 + Story 0.10 precedent.)

`_AWAITING_SHADOWING_CONDUCT_` at scaffold. At synthesis-commit, this section will carry:
- Observations not fitting any pre-stated assumption or AC-named dimension
- Categorical-only descriptions
- Per-shift + per-call citations
- Cross-link to potential new assumption rows or new worksheet patterns

## §10 — Trustee review log cross-link

```yaml
trustee_review_log_cross_link:
  pre_shadowing_001_host_helpline_approval: <YYYY-MM-DD>
  pre_shadowing_002_trustee_pre_shadowing_approval: <YYYY-MM-DD>
  post_synthesis_001_trustee_review: <YYYY-MM-DD or pending>
```

## §11 — Divergence log cross-link

```yaml
divergence_log_cross_link:
  total_rows: N
  per_severity:
    routing-policy-revision-required: N
    sla-target-revision-required: N
    helpline-call-to-ticket-flow-revision-required: N
    helpline-mediated-claim-filing-revision-required: N
    verifier-console-context-from-helpline-revision-required: N
    ux-dr55-operator-facing-register-revision-required: N
    intake-console-pattern-b-decision-strip-revision-required: N
    factual-contradiction: N
    nuance: N
    extension: N
  per_affected_story:
    story_10_1: N
    story_10_2: N
    story_10_3: N
    story_10_4: N
    story_6_3: N
    story_6_10: N
    ux_dr_amendments: N
```

## §12 — Sign-off attestation

```yaml
sign_off_attestation:
  trustee_review_verdict: <accepted | accepted-with-revisions | rejected-pending-rework | pending>
  reviewing_trustee: <named-trustee>
  review_date: YYYY-MM-DD
  sign_off_note: <explicit attestation: "Epic 10 + Story 6.3 + Story 6.10 design freeze may proceed" — or gating note explaining what must close>
  cross_link_to_decision_log: ".decision-log.md Decision 2026-05-31-011 sub-entry"
  cross_link_to_trustee_review_log: "trustee-review-log.md post-synthesis-001 row"
  p0_2_terminal_leg_discharge_attestation: <true (if all 4 legs closed) | pending (if 0.8/0.9/0.10 not yet all closed)>
```

## Pack-revision log

```yaml
pack_revision_log:
  - revision_date: YYYY-MM-DD
    revision_scope: <synthesis-only | full-pre-shadowing-cycle | granular-quote-withdrawal | per-shift-note-supplementary-addendum | other>
    revision_reason: <re-consent-fallback-timeout | operator-granular-withdrawal | trustee-review-rework | divergence-flag-resolution | other>
    revision_supersedes: <previous-row-cite>
    revision_description: <free-text>
```
