---
baseline_commit: 52e6f7d67a5348731dcb5d31225fe927725f00b2
---

# Story 10.8: Feature Flags Per Cohort + Capability Bar + Governance-Boundary Invariant `[PRIMITIVE]`

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As Solo Builder authoring the feature-flag primitive,
I want per-cohort feature flags with a capability bar + a governance-boundary invariant that makes it structurally impossible for a flag to bypass audit, consent, validity, RBAC, or any other frozen governance control,
so that staged rollout is safe, the DigiLocker hard-mandatory cutover has a real mechanism instead of an ad-hoc config edit, and every architectural invariant holds identically across all flag states.

## Scope Boundary (read first — prevents over-build AND under-build)

**This is a `[PRIMITIVE]`, but NOT the pure-domain-only kind 10.6 was.** 10.6 shipped a domain harness with no table, no route, no admin UI, and no RBAC key, because a bulk-op harness has no state of its own. 10.8 *does* have state (a flag is a durable, versioned, tenant-scoped, audit-anchored record) and two of its ACs are **visibility** properties — "flag inventory visible to Pariwar Admin and above" and "no secret flags" (`prd.md:892`, `architecture.md:325-326`). A flag store with no inventory surface literally cannot satisfy "no secret flags." So 10.8 = **domain module + migration + contracts + a NEW repo-global CI gate + `apps/api` routes + a minimal `apps/admin` inventory console + ONE real consumer wired**.

**The three things that make this story hard are not the CRUD.** They are:
1. **The governance-boundary invariant must be MECHANIZED, not asserted.** The AC (`epics.md:3520`) names a `governance_boundary.yaml` allowlist and a CI test. That file does not exist anywhere in the repo — you are authoring it plus its gate. Per [[feedback_mechanization_split_commitment]] and [[project_gate_scope_semantic_coverage]], a gate that only checks "every flag key appears in a YAML list" is **vacuous** — it proves nothing about bypass. The load-bearing leg is the *source-scan* leg (AC5b) that proves no flag read reaches inside a governance module. Ship it with a revert-sanity negative control or it has no teeth.
2. **"Capability bar" means two different things in the source artifacts and you owe BOTH.** See Decision 2. Getting this wrong under-delivers half the story silently.
3. **A producer-less flag store is the anti-pattern this repo has already called out twice** (Story 5.6 producer-less pg-boss worker; Story 5.7 producer-less toggle column — `deferred-work.md:2066`). Three consumer seams are already sitting in the codebase with comments pointing at *this story*. Wire exactly one end-to-end (Decision 8).

**You are extending five already-shipped substrates + authoring one new gate. Read these before starting:**
- **The 10.1 versioned-registry pattern (your structural template — clone it, do NOT invent versioning):**
  - `packages/domain/src/helpdesk/registry.ts` — the default-owns-version-1-as-a-code-constant trick (overrides start at 2, so `(pariwar_id, version)` is an unambiguous replay pin with no extra version-id column); `routingPolicyVersionInForce(db, pariwarId, at)`; `routingPolicyDocumentForVersion(db, pariwarId, version)` for replay reconstruction; `CreateRoutingPolicyVersionInput.auditId` as a **pre-generated anchor** (the audit LINE is the caller's obligation — narrow-write posture); `RoutingPolicyVersionConflictError` on PG `23505`; `defaultRoutingPolicy()` returns a **fresh copy**, never the shared constant.
  - `packages/domain/src/helpdesk/routing.ts` — the **PURE first-match resolver** you are cloning for flag evaluation. Read its header contract: rules are a plain **array** (explicit order is the ONLY precedence source), no `Object.keys()`/`Map`/`Set` iteration deciding outcome, no clock, no I/O, no async. Note especially `computeTicketSlaDueDates` — the time-dependent derivation is **split out** of the pure decision. Your `effective_from`/`effective_until` resolution follows the same split.
  - `packages/domain/src/schema/helpdesk_routing_policy_versions.ts` + `policies/helpdesk-routing-policy-versions-rls.ts` + `migrations/0084_helpdesk-tickets-and-routing-policy.sql` — the immutable-version-row table shape (an update INSERTs a new row; prior rows never mutated except a `superseded_by_version` forward-pointer).
  - `packages/domain/tests/helpdesk/default-policy-hash.test.ts` — the default-policy hash pin. Do the same for the seeded capability bar.
- **The `schema-diff` gate — the YAML-allowlist gate template (`scripts/schema-diff/` + `fr-100-non-add.yaml`).** Read `fr-100-non-add.yaml:17-22`: its `allow: []` entries take the shape `{ kind, artifact, rationale, adr }` with **trustee-attested ADR governance per Story 14.7 (capability-bar update)** — that is *exactly* the governance posture AC6 asks for; copy the entry shape and the header prose. `scripts/schema-diff/lib.ts:70-129` (`parseFr100Config`) is the loud-throwing parser to imitate; `check.ts:47-51` (scan roots) + `:135-157` (findings → exit 1 with a remediation message naming the YAML) is the entrypoint shape.
  - Second reference for a **`count` cross-check as revert-sanity teeth** + a documented admission workflow in the YAML header: `packages/bank-parsers/src/allowlist.ts:78-80` + `bank-allowlist.yaml:21-31`.
- **Story 4.8's cache POSTURE (not necessarily its machinery)** — `packages/validity-service/src/cache.ts:8-26`: *"cache participation is OPTIONAL; correctness is MANDATORY"*; reads best-effort → degrade to recompute; writes best-effort + non-blocking. **And the load-bearing one for AC5c: D5-A — only the compute-core is cached; the ACCESS/audit layer stays OUTSIDE the cached core, so the audit fires on a HIT exactly as on a miss.** That is precisely `epics.md:3522` ("flag-evaluation code paths cannot disable surrounding audit logging").
- **The audit writer** — `packages/domain/src/audit/write.ts` (`writeAuditEntry(servicePool, input)`, `AuditEntryInput`; `action` is a free-form dotted lowercase string validated by the regex at `write.ts:99-102`, **no separate action registry**; `requestPayloadHash` is a sha256 digest, NEVER the payload; self-commits on the SERVICE pool + single global advisory-locked hash chain).
- **The RBAC catalog** — `packages/domain/src/rbac/permissions.ts:293` (`PERMISSION_CATALOG_VERSION = 26`, verified at HEAD) + `roles.ts`. The bump ritual is a prose `// Bumped 26 → 27 at Story 10.8 (added N keys): ...` block above the constant + key literals with provenance comments in `SEED_PERMISSION_KEYS`. **No migration** — pure-domain metadata.
- **The three pre-committed consumer seams** (all three carry code comments naming this story):
  - `apps/api/src/modules/kyc/provider-registry.ts:48-49` — literally contains the intended call: `// const key = featureFlags.read('kyc_provider', ctx.pariwarId) ?? config.activeProviderKey;`
  - `apps/api/src/modules/kyc/manual-fallback-seam.ts` — `isManualFallbackEnabled(deps)`, the single config read-point for the FR-2 hard-mandatory flip.
  - `apps/api/src/modules/channel-config/composition.ts:149-156` — `resolveCostOptimizationToggle()` returning a hardcoded `false`; `deferred-work.md:2066` re-triggers it at "the Epic 10 FR-58C feature-flag story."
- **The reserved contracts dir already specifies your route shapes** — `packages/contracts/src/feature-flags/README.md` (README-only, no `.ts` yet): catalog at `/api/v1/global/feature-flags/...`, per-Pariwar overrides at `/api/v1/p/<pariwar_id>/feature-flags/...`; `.strict()` everywhere; no type-shadowing in `apps/api/src/modules/feature-flags/`.
- Memory: [[project_bulk_operations_open_closed_invariant]] · [[project_reports_exports_surface_substrate]] · [[project_helpdesk_primitive_substrate]] · [[project_gate_scope_semantic_coverage]] · [[feedback_mechanization_split_commitment]] · [[project_access_wrapper_gate_pending_scope]] · [[project_rbac_geo_scope_containment]] · [[project_live_db_test_gotchas]] · [[project_ci_local_concurrency_oversubscription]] · [[project_ci_local_double_run_pollution]] · [[project_domain_limit_clamp_and_savepoint_retry]] · [[project_ci_actions_suspension_local_mirror]] · [[project_contracts_domain_bundle_boundary]] · [[project_admin_display_name_attribution]] · [[project_nominee_vpa_deferred_seam]] · [[feedback_record_unattested_no_backfill]] · [[feedback_verify_before_committing_governance_claims]]

| In scope (10.8) | Out of scope → owning story / seam |
|---|---|
| **NEW migration `packages/domain/migrations/0087_feature-flags.sql` + schema `feature_flag_versions`** — immutable versioned rows, the 0084 shape: tenant-isolated, `GRANT SELECT, INSERT, UPDATE TO twt_app`, `ENABLE`+`FORCE ROW LEVEL SECURITY`, Story-1.6 closed-failure policies. `pariwar_id` is **nullable** for global flags — see Decision 3's RLS note (a nullable tenant column needs an explicit `OR pariwar_id IS NULL` read leg, which is a deliberate, tested carve-out, not an oversight). | A 6th **event-derived-state primitive** (`current_state` projector + DB trigger + `feature-flag-state-invariant` CI gate) → **rejected** (Decision 3). Replay-safety is delivered by immutable version rows; a projector is real cost for no property gain. |
| **NEW domain module `packages/domain/src/feature-flags/`** — the versioned registry (`flagVersionInForce`, `flagVersionForVersion`, `createFlagVersion` with the pre-generated `auditId` anchor + `FlagVersionConflictError` on 23505), the **PURE `evaluateFlag(flagDoc, memberContext) → FlagDecision`** first-match resolver, the **capability-bar loader** (parses `governance_boundary.yaml`, loud-throwing), and typed errors. | A general-purpose expression language / JSONLogic / `eval`-backed cohort DSL → **rejected** (Decision 5): a bounded declarative predicate only. |
| **NEW `governance_boundary.yaml` at repo root** (alongside `fr-100-non-add.yaml`, `bank-allowlist.yaml`, `benefit-mechanism.yaml`, `microcopy.yaml`) — the **capability bar in the runtime sense**: the allowlist of flag-toggleable behaviors + the prohibited-behavior patterns + a `count` cross-check + a header documenting the trustee-attested-PR admission workflow. | Silently expanding the bar. Every addition = a new `allow` entry with `{ kind, artifact, rationale, adr }` + trustee attestation (AC6). |
| **NEW repo-global CI gate `scripts/governance-boundary/{lib,lib.test,check,README}.ts`** + `governance-boundary:check`/`:test` in root `package.json` + a `run "governance-boundary" ...` line in `scripts/ci-local.sh` + a mirroring job in `.github/workflows/ci.yml` + a **Category A row in `gate-inventory.md`**. Two legs: (a) registry-vs-allowlist conformance; (b) **the load-bearing source-scan** proving no flag read reaches inside a governance module (AC5b). | Extending an existing gate's scope instead of minting a new one → **rejected**: this is a genuinely new invariant family (cf. [[project_access_wrapper_gate_pending_scope]] — Epic 7's lesson was to know when a NEW gate beats a vacuous scope extension). |
| **NEW `docs/adr/ADR-0036-feature-flag-tool-selection.md`** — the **capability bar in the vendor-selection sense** (Decision 2): records in-house Postgres-backed flags as the v1 selection and demonstrates each of Item 9's seven properties against the shipped implementation. Closes the `ADR-NNNN-feature-flag-tool-selection` slot (`docs/knowledge-transfer/adr-index.md:110`) and dispositions launch-gate Row 12 (`docs/launch-gate-inventory/inventory-roster.md:177-183`). | Selecting a third-party vendor (LaunchDarkly/Unleash/etc.) → **rejected** (Decision 2): the capability bar's tenant-isolation + DPDPA-no-PII-outbound + offline-resilience properties all argue for in-house on Postgres, which is also the §1.4 no-Redis posture. |
| **Contracts `packages/contracts/src/feature-flags/`** — replacing the README-only stub, at the route shapes the README already commits. Pure-Zod `.strict()`, snake_case wire, NO `@twt/domain` source import ([[project_contracts_domain_bundle_boundary]]). Register in `scripts/emit-openapi.ts` + regen `openapi/v1.yaml`. | A client-side flag **SDK in `packages/platform-adapters/`** (`architecture.md:4570`) → **deferred seam**: no client consumer exists in this story; the mobile/admin read path is the API. Record the forward commitment; do NOT ship a producer-less SDK. |
| **`apps/api/src/modules/feature-flags/`** — inventory list (catalog + per-Pariwar), flag-version detail, and the flip/create-version write route (audit-logged). NEW RBAC keys minted (catalog v26→v27; Decision 7 names them). | A member-facing flag surface. Flags are admin metadata; members only ever see *behavior*. |
| **`apps/admin/src/modules/feature-flags/`** — a minimal inventory console (the 10.7 `ReportsPage` precedent, reusing the 6.3 admin shell). This is what makes "no secret flags" real. | A rich canary-cohort dashboard / rollout-percentage slider UI → out of scope; the state machine + a scope-narrowing cohort predicate is the v1 mechanism. |
| **ONE consumer wired end-to-end: the FR-2 DigiLocker seam** (`provider-registry.ts` + `manual-fallback-seam.ts`) — the canonical use case named by PRD FR-58C, `epics.md:1663/1677`, and the Epic-10 demoable closure. | The other two seams (cost-optimization toggle, Telegram mirror) → recorded forward commitments, not wired here (Decision 8). |
| **`FlagHealthSignal` port declared + `rolled_back` state + a manual rollback write path**, audit-logged. | **Automatic** synthetic-error-spike detection + auto-rollback → **first-class deferred seam** (Decision 6): no error-rate metrics substrate exists in this repo. Record it un-built ([[feedback_record_unattested_no_backfill]]); do NOT fabricate a metrics pipeline. |
| Unit + live-DB tests + the new gate's own `lib.test.ts` with a revert-sanity negative control; `pnpm ci:local` green. | — |

## Acceptance Criteria

**AC1 — The flag primitive: versioned, tenant-scoped, replay-safe rows (Decision 3).**
Given FR-58C + Cross-Cutting #15 (`architecture.md:325-326`) + the Item 9 capability bar (`architecture.md:200-229`),
When the feature-flag primitive is authored in `packages/domain/src/feature-flags/` + `migrations/0087_feature-flags.sql`,
Then a flag version row carries: `flag_key`, `pariwar_id` (**nullable** — null = global), `version`, `cohort_definition` (the Decision-5 bounded predicate, JSONB), `state` (`off | canary | rollout | full | rolled_back`), `fallback_default` (boolean — the offline-resilience default required by `architecture.md:217-219`), `owner` + `dead_by` (the lifecycle-accountability pair required by `architecture.md:220-221` + `:4094-4098`), `audit_id`, `effective_from`, `effective_until` (nullable), `actor_who_flipped`, `superseded_by_version` (nullable forward-pointer), `created_at`;
And **version rows are immutable** (the 10.1 posture): a flip INSERTs a new version; prior rows are never mutated except the `superseded_by_version` forward-pointer, so `(pariwar_id, flag_key, version)` is an unambiguous replay pin and *"historical flag states are queryable for past evaluations"* (`architecture.md:214-216`) is satisfied by a row read, not a projector;
And `flagVersionInForce(db, flagKey, pariwarId, at)` resolves the version whose `effective_from <= at` and (`effective_until` is null or `> at`), preferring a per-Pariwar override over the global row, else the code-default; `flagVersionForVersion(...)` reconstructs any historical version for replay;
And a live-DB test proves tenant isolation **including the nullable-`pariwar_id` carve-out**: tenant A cannot read tenant B's override, both can read the global row, and an unset scope reads zero (the `report-exports-policy-regression` spec shape — this table's own pos/neg/withCheck/fail-closed regression is mandatory, not optional; it was the one real gap the 10.7 review found).

**AC2 — Deterministic, pure, replay-identical evaluation (Decision 4, 5).**
Given the Item 9 property *"same cohort + same flag identity + same version yields the same result; output is reproducible for replay"* (`architecture.md:207-210`) + NFR-FR58C `< 5 ms` (`prd.md:891`),
When `evaluateFlag(flagDoc, memberContext)` runs,
Then the resolver is **PURE** — the 10.1 `resolveRoute` contract: `cohort_definition.clauses` is a plain **array** whose explicit order is the ONLY precedence source; no `Object.keys()`/`Map`/`Set` iteration decides the outcome; no clock, no randomness, no I/O, no async, no mutable module state; the returned `FlagDecision` carries `{ enabled, flagVersion, matchedClauseIndex, reason }` so the decision is audit-replayable;
And time-dependence is **split out** of the pure core exactly as `computeTicketSlaDueDates` is split from `resolveRoute`: `effective_from`/`effective_until` windowing happens in the *version-in-force lookup* (AC1), never inside `evaluateFlag`;
And a determinism test runs the same `(flagDoc, memberContext)` 100× and asserts byte-identical decisions (the `ladder.ts` / Story 4.6 rule-order precedent); a fixture-pinned hash test pins the seeded capability bar + the default flag documents (the `default-policy-hash.test.ts` precedent);
And the cohort predicate is a **bounded declarative form**, not an expression language (Decision 5): a flat array of clauses `{ dimension: 'pariwar_id'|'member_state'|'district'|'block'|'role'|'cohort_tag', op: 'in'|'eq', values: string[] }` evaluated as first-match; an unknown dimension or op **fails closed** to the flag's `fallback_default` with a typed reason, never throws into the caller's request path.

**AC3 — Every flag change is audit-logged; the flip write path (Story 1.10).**
Given `epics.md:3513` + `architecture.md:214-216` (*"every flag-state change emits a tamper-evident audit line (§1.5 hash chain)"*),
When an actor creates or flips a flag version,
Then the write follows the 10.1 narrow-write posture — `CreateFlagVersionInput.auditId` is a **pre-generated anchor** stamped on the row, and the caller writes the audit LINE via `writeAuditEntry` with a dotted action (`feature_flag.version_created` / `feature_flag.rolled_back`), `actorId`/`actorRole` from the flipping admin ([[project_admin_display_name_attribution]] — `users.display_name` is the attribution source; a missing name blocks the action with a typed error, no email-derived fallback), and `requestPayloadHash` = sha256 of the canonical input (never the raw `cohort_definition`);
And the audit context is NON-PII and records **`flag_key`, prior version, new version, prior state, new state, and the rationale string** — FR-58C requires *"flag changes audit-logged with actor + rationale"* (`prd.md:890`), so `rationale` is a REQUIRED, bounded, non-empty field on the write contract, not optional;
And a concurrent double-flip loses on the `(pariwar_id, flag_key, version)` unique index → `FlagVersionConflictError` → `409`, never a silent overwrite (the `RoutingPolicyVersionConflictError` shape).

**AC4 — Inventory visible to Pariwar Admin and above; no secret flags.**
Given `prd.md:892` (*"No 'secret' flags — flag inventory is visible to Pariwar Admin role and above"*) + `architecture.md:325-326`,
When an authorized admin opens the flag inventory,
Then `GET /api/v1/global/feature-flags` (catalog) and `GET /api/v1/p/<pariwar_id>/feature-flags` (this tenant's effective flags, each showing global-vs-override provenance) return the **complete** flag set for that scope — there is no `hidden`/`internal` field and no code path that omits a registered flag from the inventory response;
And `apps/admin/src/modules/feature-flags/` renders that inventory (key, state, cohort summary, owner, dead-by, effective window, last flip actor + rationale) plus the flip form, reusing the 6.3 admin shell (the 10.3/10.4/10.5/10.7 `apps/admin/src/modules/*` precedent);
And a test asserts the **no-secret-flags property with teeth**: every key in the domain flag registry appears in the inventory response for an authorized actor — a flag registered but omitted from inventory fails the test.

**AC5 — The governance-boundary invariant, MECHANIZED (this story's load-bearing commitment).**
Given `epics.md:3516-3522` + the Architectural Freeze Boundaries table (`epics.md:510-543`),
When any feature flag is defined or evaluated,
Then **(a) conformance leg** — `governance_boundary.yaml` at repo root declares the capability bar (the allowlist of flag-toggleable behaviors, each `{ kind, artifact, rationale, adr }` per the `fr-100-non-add.yaml` entry shape) plus the prohibited-behavior patterns; the gate asserts **every registered flag key resolves to exactly one allowlist entry**, an unlisted key fails, and a `count` cross-check catches a silently-dropped entry (the `bank-allowlist.ts:78-80` revert-sanity trick);
And **(b) the load-bearing source-scan leg** — the gate asserts that **no flag-evaluation import reaches inside a governance module**: scanning `packages/domain/src/audit/`, `packages/domain/src/rbac/`, `packages/domain/src/consent/`, `packages/validity-service/src/`, `packages/domain/src/contribution/` (where `CONFIRMED_EVENT_TYPE` and the confirmed-truth reads live — the actual enforcement roots the 9.5 `canonical-financial-truth.test.ts` fence itself scans, not the test file), and `scripts/*/` (the CI gates themselves), **any import of `@twt/domain`'s feature-flags namespace or of `evaluateFlag` is a gate failure** — this is what makes prohibitions (a) disable audit logging, (b) bypass Story 2.7 consent, (c) override Validity Service eligibility, (d) escalate RBAC, (f) disable a 1.16a-d gate, and (g) bypass the canonical-financial-truth fence *structurally impossible* rather than merely documented. **A green run of leg (a) alone proves nothing — leg (b) is the invariant with semantic coverage** ([[project_gate_scope_semantic_coverage]]);
And prohibition (e) *"altering any architectural freeze table row"* is covered by the allowlist's rationale requirement: an `allow` entry naming a frozen-table behavior (`epics.md:518-532`) is rejected by the parser with the freeze-row cited;
And **(c) the audit-outside-the-cache leg** — flag evaluation cannot suppress its surrounding audit: the cached core is the *decision computation only*, and any audit/observation the caller performs sits OUTSIDE it, so it fires on a cache HIT exactly as on a miss (the Story 4.8 D5-A structure, `packages/validity-service/src/cache.ts:22-26`). A test asserts the audit fires on both paths;
And the gate ships with a **revert-sanity negative control** in `lib.test.ts`: a fixture flag key absent from the allowlist fails leg (a), and a fixture governance-module source importing the evaluator fails leg (b). A gate that cannot be made to fail has no teeth.

**AC6 — The capability bar is itself a governance artifact (Decision 2 — BOTH senses).**
Given `epics.md:3521` (*"additions to the capability bar require trustee-attested PRs with explicit rationale; the bar cannot be silently expanded"*) + `epics.md:4423` (the ADR must document *"what's flag-toggleable + governance-boundary invariant + selection criteria for vendor"*),
Then the **runtime capability bar** is `governance_boundary.yaml`, whose header documents the admission workflow verbatim in the `fr-100-non-add.yaml:17-22` / `bank-allowlist.yaml:21-31` style: an addition requires a rationale + a trustee-attested ADR reference, and the `count` field must be bumped in the same commit;
And the **vendor-selection capability bar** is `docs/adr/ADR-0036-feature-flag-tool-selection.md`, which records the v1 selection (in-house, Postgres-backed) and demonstrates **each of Item 9's seven properties** against what this story actually shipped — deterministic evaluation (AC2), tenant isolation (AC1), replay safety (AC1), auditability (AC3/AC4), offline resilience (the per-flag `fallback_default`), lifecycle accountability (`owner` + `dead_by`), DPDPA-compatible posture (evaluation is in-process against local rows; no PII leaves the tenant) — citing `architecture.md:200-229` as the bar it satisfies and following the `ADR-0010` structure (`docs/adr/_adr-template.md`);
And the ADR **dispositions launch-gate Row 12** (`docs/launch-gate-inventory/inventory-roster.md:177-183`) and flips the `slot-reserved-pre-write` marker at `docs/knowledge-transfer/adr-index.md:110`. Per [[feedback_verify_before_committing_governance_claims]], verify those two files' live state before writing the closure claim, and use precise closure language ([[feedback_closure_language_precision]]).

**AC7 — Staged rollout: the state machine ships; auto-rollback is a recorded seam (Decision 6).**
Given AR-64 / Cross-Cutting #15 (*"canary → graduated cohorts + automatic rollback on error-rate spike"*),
Then the `off | canary | rollout | full | rolled_back` state set ships with its legal-transition map, and each transition is a new immutable version row + an audit line (AC3); a `rolled_back` flip is a **manual, audited, one-write operation** that takes effect on the next evaluation with no consumer code change;
And the **automatic** error-spike detector is a **first-class deferred seam**, not silently omitted: a `FlagHealthSignal` port is declared with an explicit `{ available: false }` v1 resolution ([[project_nominee_vpa_deferred_seam]] discipline), because **no error-rate metrics substrate exists in this repo** (verified: no observability/metrics package; `packages/validity-service/src/cache-observability.ts` and `packages/domain/src/claim/peer-mesh-metric-registry.ts` are the only metric-shaped modules and neither emits an error rate). Record it in `deferred-work.md` with an AR-31-observability re-trigger ([[feedback_record_unattested_no_backfill]] — do NOT reconstruct a fake signal to make the AC look green).

**AC8 — One real consumer wired; the other two recorded (Decision 8).**
Given that three consumer seams already carry comments naming this story, and that a producer-less flag store repeats the anti-pattern Stories 5.6/5.7 explicitly rejected,
Then the **FR-2 DigiLocker seam is wired end-to-end**: `apps/api/src/modules/kyc/manual-fallback-seam.ts` resolves through the flag evaluator (falling back to `config.digilocker.manualFallbackEnabled` when no flag version is in force), and `provider-registry.ts:48-49`'s commented-out call becomes real;
And flipping the flag to `full` for one Pariwar hides the manual CTA for that tenant only, with the hard-mandatory copy block shown (`epics.md:1677`), and changes nothing for other tenants — proven by a live-DB integration test, which is also the Epic-10 demoable-closure beat;
And the other two seams (`resolveCostOptimizationToggle`, the FR-73 Telegram mirror) are left returning their fail-safe defaults with a comment + a `deferred-work.md` entry naming their re-trigger story. Do NOT wire them here.

**AC9 — Tests + gates green.**
Given `pnpm ci:local` (`--concurrency=4`, DB on :5433) is the sanctioned merge gate ([[project_ci_actions_suspension_local_mirror]], [[project_ci_local_concurrency_oversubscription]]),
Then domain unit tests cover: the pure resolver's determinism (100× identical) + fail-closed unknown dimension/op; version-in-force windowing + override-beats-global precedence; the immutability posture (a flip INSERTs, never UPDATEs a prior row); the capability-bar loader's loud-throwing parse; the seeded-bar hash pin;
And the new gate's `lib.test.ts` covers both legs **plus the revert-sanity negative controls** (AC5);
And live-DB tests cover: the `feature_flag_versions` RLS policy regression (pos/neg/withCheck/unset-scope, **including the nullable-`pariwar_id` global-row leg**); the flip → audit-line round-trip against the real `writeAuditEntry` (assert `traceId` grouping / membership, **not counts** — the chain self-commits and accumulates, [[project_live_db_test_gotchas]]); the AC8 DigiLocker end-to-end flip; the AC4 inventory-completeness property;
And contracts tests cover the DTO round-trip + a domain↔contracts enum sync-guard (test-only cross-import, [[project_contracts_domain_bundle_boundary]]); `emit-openapi.ts` + `openapi/v1.yaml` regenerated; `governance_boundary.yaml` + the new gate registered in root `package.json`, `scripts/ci-local.sh`, `.github/workflows/ci.yml`, and as a Category A row in `gate-inventory.md`; the new migration trips no `db:check`/`schema-diff`/`pii-scrape`/`domain-accessor-invariants` gate; per-package lint ([[project_eslint_config_per_package_cwd]]); `pnpm ci:local` green — and **verify that claim live before committing it** ([[feedback_verify_before_committing_governance_claims]]; the 10.7 review found the Dev Record claiming a gate green that was in fact RED).

## Load-Bearing Decisions

Each carries a firm recommendation + the rejected alternative (the 10.4–10.7 house style).

**Decisions 2, 4, and 6 — the three that were open — are CONFIRMED by the product owner (2026-07-31). Implement the recommendations as written; their rejected alternatives are closed.** Concretely that ratifies: the flag tool is **in-house Postgres-backed** and `ADR-0036` records it against Item 9's seven properties (D2); "cached per Story 4.8 pattern" means 4.8's **invariants**, not its table + cohort-epoch machinery (D4); and AR-64's **automatic** error-spike rollback ships as a recorded absent seam rather than a fabricated signal (D6). The remaining decisions were firm recommendations, not open questions. All per-decision text below is retained as the rationale of record — why the primitive is shaped this way — not as live debate.

1. **Story shape — RECOMMEND a `[PRIMITIVE]` that ships persistence + a thin surface, NOT 10.6's pure-domain-only shape.** 10.6 correctly shipped no table/route/UI because a bulk-op harness holds no state. A flag *is* state, and two ACs are visibility properties ("inventory visible to Pariwar Admin and above"; "no secret flags") that a headless module cannot satisfy. So: domain + migration + contracts + gate + API + a minimal admin console + one wired consumer. *Rejected:* a pure-domain flag module with the surfaces deferred — rejected because it would ship a store nobody can see or use, failing AC4 on its face and repeating the producer-less anti-pattern.

2. **"Capability bar" is overloaded — RECOMMEND producing BOTH artifacts, because the epic requires both and they are different lists.** Sprint Change Proposal Item 9 (`architecture.md:200-229`) defines a *vendor-selection acceptance-criteria bar*: seven properties a chosen tool must demonstrate (deterministic evaluation, tenant isolation, replay safety, auditability, offline resilience, lifecycle accountability, DPDPA posture). Story 10.8's AC (`epics.md:3512`) defines a *runtime allowlist of flag-toggleable behaviors*. `epics.md:4423` merges both into one ADR obligation: *"capability bar ADR documents what's flag-toggleable + governance-boundary invariant + selection criteria for vendor."* So this story produces `governance_boundary.yaml` (runtime bar) **and** `ADR-0036` (vendor bar, demonstrating the seven properties against the in-house implementation). *Rejected:* treating them as one list — rejected because it silently drops half the commitment, and because the seven properties are *acceptance criteria for the implementation*, not entries in a toggle allowlist. **Sub-recommendation (CONFIRMED 2026-07-31):** select **in-house Postgres-backed** as the v1 tool. Tenant isolation, DPDPA "flag evaluation does not require PII outbound," and offline resilience all argue against a SaaS flag vendor, and §1.4 already commits Postgres-only (no Redis). This is a real governance call — it closes ADR slot row 24 and dispositions launch-gate Row 12. Write `ADR-0036` as a *selection* ADR (in-house is a selection, not a deferral): per [[feedback_architecture_vs_adr_boundary]] the architecture already committed the properties at `:200-229`, so the ADR's job is to name the control and demonstrate the seven properties against shipped code — not to restate the bar.

3. **State model — RECOMMEND immutable versioned rows (the 10.1 routing-policy shape), NOT a 6th event-derived-state primitive.** The repo has five event-derived-state primitives (members, claims, pools, alerts, helpdesk_tickets), each carrying a projector-only `current_state` + a DB trigger + its own CI gate. The temptation is to make flags the sixth. **Don't.** The architectural requirement is *replay safety* — *"historical flag states are queryable for past evaluations; flag changes carry version + effective-at timestamps"* (`architecture.md:214-216`) — and immutable version rows deliver exactly that, with `(pariwar_id, flag_key, version)` as the replay pin. A flag's `state` is an **authored attribute of a version**, not a lifecycle derived from external events: nothing outside the admin write path ever moves a flag, so there is no projector-vs-writer divergence for a gate to police. Story 10.1 faced the identical choice and split it correctly — its *tickets* are event-derived-state, its *routing-policy registry* is versioned-immutable-rows. A flag is the registry, not the ticket. *Rejected:* a 6th `current_state` + trigger + `feature-flag-state-invariant` gate — rejected as cost with no property gain, and it would dilute the meaning of the five existing state gates. ⚠ **The one real cost of this choice:** `pariwar_id` is nullable (global flags), so the standard RLS predicate needs an explicit `OR pariwar_id IS NULL` read leg. That is a deliberate, tested carve-out (AC1) — call it out in the migration comment so a future reader doesn't "fix" it.

4. **Caching — RECOMMEND adopting Story 4.8's POSTURE and invariants, and the cheapest cache that meets `< 5 ms`; DEFER 4.8's table + cohort-epoch machinery.** The AC says *"deterministic + cached per Story 4.8 pattern"* (`epics.md:3514`). The load-bearing parts of 4.8 are its *invariants*, and you must inherit all of them: cache-optional/correctness-mandatory; reads degrade to recompute; writes best-effort and non-blocking; and — critically for AC5c — **the audit/access layer stays OUTSIDE the cached core** (`cache.ts:22-26`, D5-A). What you should *not* automatically clone is 4.8's `member_validity_cache` table + `cohort_invalidation_epochs` + the `events_log` AFTER-INSERT trigger: that machinery exists because a validity recompute is expensive over a large rule registry. A flag lookup is one indexed row from a table with tens of rows. Start with per-request memoization + a short-TTL in-process snapshot keyed by `(flag_key, pariwar_id)`, measure, and record the full cache-aside table as a seam if `< 5 ms` is ever at risk. *Rejected:* cloning 4.8's full cache substrate now — rejected as premature; a second cache table + epoch bump + trigger is a meaningful maintenance surface bought for a read that is already sub-millisecond. **This is a deliberate narrowing of the epic's "per Story 4.8 pattern" wording to 4.8's invariants rather than its implementation — CONFIRMED 2026-07-31.** Record the narrowing explicitly in the Dev Agent Record so the epic-vs-implementation delta is legible to a later reader ([[feedback_closure_language_precision]]): the invariants are *adopted*, the cache substrate is *deferred pending measurement* — neither is "not addressed."

5. **Cohort definition — RECOMMEND a bounded declarative predicate, NOT an expression language.** `epics.md:3511` says `cohort_definition` is a *"rule expression evaluable against member context."* The nearest existing precedent is Epic 11c's module `eligibility_filter` (`epics.md:4018`), which is likewise unspecified. Specify it *narrowly*: a flat, ordered array of clauses `{ dimension, op: 'in'|'eq', values[] }` over a **fixed dimension enum** (`pariwar_id`, `member_state`, `district`, `block`, `role`, `cohort_tag` — matching FR-58C's stated gating axes at `prd.md:886`), ANDed within a clause, first-match across clauses, unknown dimension/op fails closed to `fallback_default`. *Rejected:* JSONLogic, a mini-DSL, or any `eval`-backed evaluator — rejected because it makes determinism unprovable, makes the rule un-reviewable in a trustee-attested PR, executes data as code, and would need its own parser + fuzz surface. The niyamavali engine's rule holds here too ([[project_niyamavali_precedence_is_provenance]], `ladder.ts:11-26`): the engine interprets DATA, never hardcodes logic — but the data's *shape* must be bounded.

6. **Canary auto-rollback — RECOMMEND shipping the state machine + a manual audited rollback + a declared `FlagHealthSignal` port; DEFER automatic error-spike detection as a first-class absent seam.** AR-64 requires *"automatic rollback on error-rate spike."* There is **no error-rate metrics substrate in this repo** — no observability package, no metrics sink; AR-31's vendor is itself a deferred ADR. Building a synthetic error-rate pipeline inside a feature-flag story would be inventing a producer to satisfy a consumer, which is the exact anti-pattern Story 5.6 rejected. Ship what is real (the five-state machine, the audited manual `rolled_back` flip that takes effect with zero consumer code change) and record the detector honestly as un-built with an AR-31 re-trigger. *Rejected:* implementing a naive in-process error counter and calling AR-64 satisfied — rejected under [[feedback_record_unattested_no_backfill]]: a fake signal that auto-flips production behavior is worse than an honest absence, and it would make a rollback mechanism whose trigger nobody trusts. **This is a knowing partial delivery of AR-64 — CONFIRMED 2026-07-31.** Because it is knowing, it must be recorded as such and carried as risk, never quietly closed ([[feedback_record_unattested_no_backfill]]): the `deferred-work.md` entry names AR-31 observability as the re-trigger, and the ADR's offline-resilience/auditability sections state plainly that the *manual* rollback path is what ships. An un-gated re-commitment decays — tie the seam to the AR-31 observability story rather than leaving it as an open intention.

7. **Authorization — RECOMMEND two new keys, no umbrella.** Mint `feature_flag.view` (inventory read — granted to `pariwar_admin` and above per `prd.md:892`, plus `auditor`) and `feature_flag.flip` (the write, narrower). Catalog **v26 → v27** (verified: `permissions.ts:293` is 26 at HEAD). Both are `dimension: 'pariwar'`; per [[project_rbac_geo_scope_containment]] a district-ceiling grant cannot satisfy a pariwar-dimension check, so **do not grant these to `district_admin`** — that would be an inert capability, the exact trap 10.3 and 10.4 documented. *Rejected:* one `feature_flag.manage` umbrella key — rejected: it collapses "everyone at Pariwar-Admin-and-above can *see* the inventory" (a transparency property) into "everyone who can see can also flip" (a governance authority). The visibility requirement is deliberately broader than the write requirement; one key destroys that.

8. **Consumer wiring — RECOMMEND wiring exactly ONE seam (FR-2 DigiLocker) end-to-end; record the other two.** All three seams carry comments pointing here, and `deferred-work.md:2066` explicitly re-triggers the cost-optimization toggle at "the Epic 10 FR-58C feature-flag story." But FR-2 is the *canonical* use case in every source artifact (PRD FR-58C consequence #1, `epics.md:1663/1677`, the Epic-10 demoable closure, and the ADR's decision-gate acceptance case), and wiring it proves the whole chain: flag → cohort → evaluation → per-tenant behavior change → no consumer code edit. *Rejected (a):* wire zero and ship a headless store — rejected as the producer-less anti-pattern. *Rejected (b):* wire all three — rejected on scope; the cost-optimization toggle also needs its per-Pariwar admin form (`architecture.md:2082-2095`), which is its own surface. Leave both returning fail-safe defaults with a `deferred-work.md` entry.

9. **The "beta UX patterns" seed entry — RECOMMEND dropping it from the initial capability bar and recording why.** `epics.md:3512`'s seed list is `e.g., DigiLocker hard-mandatory, alternative KYC provider, beta UX patterns`. The first two trace to concrete FRs (FR-2, AR-43/`epics.md:530`). **"Beta UX patterns" appears exactly once in the entire corpus** with no supporting FR, AR, or UX-spec entry — the UX spec has zero FR-58C content. Seeding an allowlist entry for an undefined behavior would create exactly the silently-expandable bar AC6 prohibits. Seed the bar with behaviors that have a named owner and a real consumer: `kyc_manual_fallback` (FR-2, wired in AC8), `kyc_provider_selection` (AR-43 seam), and — as recorded-but-unwired entries with their re-trigger stories — `wa_cost_optimization` (AR-18) and `telegram_mirror` (FR-73). *Rejected:* seeding `beta_ux_patterns` to match the epic's prose literally — rejected: an allowlist entry with no definable artifact cannot be gate-checked, and admitting it now normalizes exactly the silent expansion the story exists to prevent.

## Tasks / Subtasks

- [x] **Task 1 — Migration + schema: `feature_flag_versions`** (`packages/domain/migrations/0087_feature-flags.sql`, `packages/domain/src/schema/feature_flag_versions.ts`, `packages/domain/src/policies/feature-flag-versions-rls.ts`) (AC1)
  - [x] Hand-author mirroring `0084_helpdesk-tickets-and-routing-policy.sql`: `GRANT SELECT, INSERT, UPDATE TO twt_app`, `ENABLE`+`FORCE ROW LEVEL SECURITY`, Story-1.6 closed-failure policies using `nullif(current_setting('app.pariwar_id', true), '')::uuid`, statements joined with `--> statement-breakpoint`. **The nullable-`pariwar_id` global-row leg needs an explicit `OR pariwar_id IS NULL` in the SELECT policy** — comment it as deliberate (Decision 3) so nobody "fixes" it.
  - [x] Columns per AC1. Unique index on `(pariwar_id, flag_key, version)`; lookup index on `(flag_key, pariwar_id, effective_from)`.
  - [x] ⚠ NEVER `db:generate` — snapshots stop at 0020; drizzle skips by journal `when` → `42P07` ([[project_live_db_test_gotchas]]). Hand-author, no snapshot, and append `{ "idx": 87, "version": "7", "when": <prev + 86400000>, "tag": "0087_feature-flags", "breakpoints": true }` to `migrations/meta/_journal.json` (0086's `when` is `1788147600000`). Copy the standing DO-NOT-REGENERATE warning block from `0086:20-25`.
  - [x] **Added beyond the subtask list (all three forced by the nullable tenant column):** `NULLS NOT DISTINCT` on the uniqueness guard — declared as a UNIQUE **constraint** (drizzle 0.45 exposes `nullsNotDistinct()` only on `unique()`, not `uniqueIndex()`); **no** composite self-FK (MATCH SIMPLE is vacuous when `pariwar_id` is NULL, so it would silently skip the global half of the table); an append-only immutability trigger as the real backstop. Plus `FeatureFlagVersionId` + barrel exports. RLS regression spec written here (AC1 names it) and green: 10/10.
- [x] **Task 2 — Domain: the versioned flag registry** (`packages/domain/src/feature-flags/{types,registry,store,errors,index}.ts`) (AC1, AC3)
  - [x] Clone `helpdesk/registry.ts`: code-default owns version 1 (a fresh copy per call, never the shared constant); overrides start at 2; `flagVersionInForce(db, flagKey, pariwarId, at)` with **override-beats-global** precedence + the `effective_from/until` window; `flagVersionForVersion(...)` for replay; `createFlagVersion(db, input)` with the pre-generated `auditId` anchor + `FlagVersionConflictError` on `23505` (read the code on `err.cause.code`, [[project_domain_limit_clamp_and_savepoint_retry]]).
  - [x] Immutability posture: a flip INSERTs; prior rows untouched except the `superseded_by_version` forward-pointer.
  - [x] `clampLimit(...)` on any dynamic `.limit()` — the `domain-accessor-invariants` gate reads a named const as unclamped (the exact 10.7 miss).
- [x] **Task 3 — Domain: the PURE evaluator + the capability-bar loader** (`packages/domain/src/feature-flags/{evaluate,capability-bar}.ts`) (AC2, AC5)
  - [x] `evaluateFlag(flagDoc, memberContext) → FlagDecision` — clone `helpdesk/routing.ts:100-128`'s first-match shape and its header contract verbatim (array-order precedence, no clock/IO/async/mutable state). Unknown dimension/op → fail closed to `fallback_default` with a typed reason, never throw into the caller's path. Time-windowing lives in Task 2's lookup, NOT here (the `computeTicketSlaDueDates` split).
  - [x] `loadCapabilityBar()` — parse `governance_boundary.yaml` in the loud-throwing `parseFr100Config` style (`scripts/schema-diff/lib.ts:70-129`), validating `kind` against a tuple and cross-checking `count` (`bank-parsers/src/allowlist.ts:78-80`). **Use the existing `yaml` package** (`yaml@^2.6.1`, already a root dependency; `import { parse as parseYaml } from 'yaml'` — the identical import in `scripts/schema-diff/lib.ts:21`, `scripts/microcopy/lib.ts:20`, `packages/bank-parsers/src/allowlist.ts:11`). Do NOT add `js-yaml` or any second YAML parser.
  - [x] `canonicalJsonStringify` (RFC 8785) for any hashing — never bespoke `JSON.stringify` (the `validity-cache/store.ts` rule).
  - [x] **Added beyond the subtask list:** `cache.ts` (Decision 4's Story-4.8 posture — the cached core is the LOOKUP ONLY, and `resolveFlagAudited` places the access observation OUTSIDE it: that IS the AC5c structure) and `health-signal.ts` (AC7's declared-absent `FlagHealthSignal`, `{available:false}` first-class). Resolution is THREE-tier (override ≻ global row ≻ code default) — the nullable `pariwar_id` adds the cross-tenant middle tier the 10.1 registry has no analogue for. `store.ts`'s `listEffectiveFlags` iterates the CODE REGISTRY, not existing rows: a row-driven listing would omit every not-yet-flipped flag, making "omitted because no row" indistinguishable from "deliberately hidden" (AC4). A malformed clause SHORT-CIRCUITS rather than being skipped. `yaml@^2.6.1` added to `packages/domain/package.json` (it was a ROOT devDependency only). 55/55 unit tests green.
- [x] **Task 4 — `governance_boundary.yaml` + the NEW repo-global gate** (`governance_boundary.yaml`, `scripts/governance-boundary/{lib,lib.test,check,README}.ts`) (AC5, AC6)
  - [x] YAML: `version`, `count`, `allow[]` of `{ kind, artifact, rationale, adr }`, `prohibited[]` patterns, and a header documenting the trustee-attested admission workflow in the `fr-100-non-add.yaml:17-22` style. Seed per Decision 9.
  - [x] Gate leg (a) conformance: every registered flag key ∈ allowlist; `count` cross-check.
  - [x] Gate leg (b) **the load-bearing source scan**: no import of the feature-flags namespace / `evaluateFlag` inside `packages/domain/src/{audit,rbac,consent,contribution}`, `packages/validity-service/src`, or `scripts/*/`. **Scan `packages/domain/src/contribution/` (the production module defining `CONFIRMED_EVENT_TYPE` and the confirmed-truth reads), NOT `packages/domain/tests/contribution/canonical-financial-truth.test.ts`** — the test file has no enforcement logic to bypass; scanning it would be exactly the vacuous-gate trap this story's own Trap 1 warns against. Model the AST/import scan on `scripts/kyc-provider-boundary/` (the existing import-allowlist gate).
  - [x] Leg (c) test: the audit fires identically on a cache HIT and a miss.
  - [x] `lib.test.ts` with **revert-sanity negative controls for both legs** — a gate that can't be made to fail has no teeth ([[project_gate_scope_semantic_coverage]]).
  - [x] Register: root `package.json` (`"governance-boundary:check"` / `"governance-boundary:test"`, the `kyc-provider:*` pair shape at `package.json:46-47`), one `run "governance-boundary" "pnpm governance-boundary:test && pnpm governance-boundary:check"` line in `scripts/ci-local.sh` (after the other invariant gates), a mirroring job in `.github/workflows/ci.yml`, and a **Category A row in `gate-inventory.md`** (note that file is stale through Epic 2 — add your row; a broader refresh is optional).
- [x] **Task 5 — RBAC keys** (`packages/domain/src/rbac/permissions.ts`, `roles.ts`, + their tests) (AC4, AC3)
  - [x] Mint `feature_flag.view` + `feature_flag.flip`, `dimension: 'pariwar'`. Bump `PERMISSION_CATALOG_VERSION` 26 → 27 with the prose `// Bumped 26 → 27 at Story 10.8 (added 2 keys): ...` block. Grant per Decision 7 — **NOT to `district_admin`** ([[project_rbac_geo_scope_containment]]); document the deferral as 10.3/10.4/10.7 did. Update `tests/rbac/{permissions,roles}.test.ts` (version + key count + grant assertions).
- [x] **Task 6 — Contracts** (`packages/contracts/src/feature-flags/`) (AC4)
  - [x] Replace the README-only stub with real DTOs at the route shapes the README already commits. Pure-Zod `.strict()`, snake_case wire ([[project_story_validate_footguns]]), NO `@twt/domain` source import ([[project_contracts_domain_bundle_boundary]]). Register in `packages/contracts/scripts/emit-openapi.ts`; regen `openapi/v1.yaml`. Contracts test: round-trip + a domain↔contracts state/dimension enum sync-guard (test-only cross-import).
- [x] **Task 7 — API module** (`apps/api/src/modules/feature-flags/{routes,handlers,index}.ts`) (AC3, AC4)
  - [x] `GET /api/v1/global/feature-flags` (catalog) + `GET /api/v1/p/:pariwarId/feature-flags` (effective flags with global-vs-override provenance) behind `feature_flag.view`; `POST …/feature-flags/:flagKey/versions` (create/flip) behind `feature_flag.flip`, with the REQUIRED bounded `rationale` (AC3) and the audit line written AFTER the row insert. `409` on `FlagVersionConflictError`. No type-shadowing of contracts types (the README's explicit rule). Watch the Fastify onSend foot-gun ([[project_fastify_onsend_doublesend]]).
- [x] **Task 8 — Admin console** (`apps/admin/src/modules/feature-flags/`) (AC4)
  - [x] Minimal inventory + flip form reusing the 6.3 admin shell (the 10.7 `ReportsPage.tsx` precedent): key, state, cohort summary, owner, dead-by, effective window, last flip actor + rationale. Wire the route in `apps/admin/src/router.tsx` + the client in `apps/admin/src/api/client.ts`. Note `architecture.md:4553` never listed an admin feature-flags module — you are creating it; that is intended, not a deviation.
- [x] **Task 9 — Wire the FR-2 DigiLocker consumer** (`apps/api/src/modules/kyc/{manual-fallback-seam,provider-registry}.ts`) (AC8)
  - [x] `isManualFallbackEnabled` resolves through the evaluator, falling back to `config.digilocker.manualFallbackEnabled` when no version is in force. Make `provider-registry.ts:48-49`'s commented call real. Update both files' header comments — they currently say the flag infrastructure "is NOT built."
  - [x] Leave `resolveCostOptimizationToggle` and the Telegram mirror at their fail-safe defaults; add `deferred-work.md` entries naming their re-trigger.
- [x] **Task 10 — ADR + governance artifacts** (AC6, AC7)
  - [x] `docs/adr/ADR-0036-feature-flag-tool-selection.md` per Decision 2 (verify 0036 is the next free number before writing). Demonstrate all seven Item-9 properties against what shipped; cite `architecture.md:200-229`.
  - [x] Flip the `slot-reserved-pre-write` marker at `docs/knowledge-transfer/adr-index.md:110`; disposition launch-gate Row 12 at `docs/launch-gate-inventory/inventory-roster.md:177-183`. **Verify both files' live state first** ([[feedback_verify_before_committing_governance_claims]]); use precise closure language ([[feedback_closure_language_precision]]).
  - [x] Record the auto-rollback detector seam + the `platform-adapters` client SDK seam in `deferred-work.md`.
- [x] **Task 11 — Tests + gates** (AC9)
  - [x] Domain unit: 100× determinism; fail-closed unknown dimension/op; window + override-beats-global precedence; INSERT-not-UPDATE immutability; loader loud-throw; seeded-bar hash pin.
  - [x] Live-DB: `feature_flag_versions` RLS policy regression **including the nullable-`pariwar_id` global leg**; flip → audit round-trip (assert `traceId` grouping / membership, **not counts** — [[project_live_db_test_gotchas]]); AC8 DigiLocker end-to-end; AC4 inventory completeness.
  - [x] Run suspect specs in ISOLATION to confirm innocence ([[project_ci_local_double_run_pollution]], [[project_known_livedb_test_failures]]). Full `pnpm ci:local` (`--concurrency=4`, `DATABASE_URL` on :5433) + per-package lint ([[project_eslint_config_per_package_cwd]]). **Verify green before claiming green.**

### Review Findings

- [x] [Review][Defer] `governance_boundary.yaml`'s six enforcement roots (`audit`, `rbac`, `consent`, `validity-service`, `contribution`, `scripts`) are hand-maintained with nothing checking that list stays exhaustive as new governance-sensitive modules are added elsewhere later [governance_boundary.yaml, scripts/governance-boundary/lib.ts] — deferred, no exhaustive detection mechanism exists (there's no reliable way to auto-detect which new modules are "governance-sensitive" without a hand-maintained taxonomy; revisit when a concrete new governance-relevant module is added)
- [x] [Review][Patch] Add a global (tenant-null) flag-authoring route — `POST /api/v1/global/feature-flags/:flagKey/versions`, `super_admin`-gated, reusing `createFlagVersion` with `pariwarId=null` — so the global tier is operator-reachable instead of SQL-only. [apps/api/src/modules/feature-flags/routes.ts]
- [x] [Review][Patch] Wire a minimal version-history view into the admin console calling the existing `listFeatureFlagVersions` endpoint — currently built and tested but never rendered. [apps/admin/src/modules/feature-flags/FeatureFlagsPage.tsx, apps/admin/src/api/client.ts]
- [x] [Review][Patch] Wire real runtime enforcement for `FlagKeyNotAllowlistedError`: have `createFlagVersion`/`validateFlagVersionInput` also check the key against `loadCapabilityBar()`, throwing on mismatch — makes the existing doc comment true and adds defense-in-depth beyond the CI gate. [packages/domain/src/feature-flags/errors.ts, packages/domain/src/feature-flags/registry.ts]
- [x] [Review][Patch] Loosen `GET /api/v1/global/feature-flags` from `super_admin`-only to `pariwar_admin`+, matching AC4/PRD's literal "visible to Pariwar Admin role and above." [apps/api/src/modules/feature-flags/routes.ts]
- [x] [Review][Patch] Add `owner`/`dead_by` edit fields to the admin flip form (currently silently resubmitted or hardcoded to a `2027-06-30` fallback), and set distinct, reasoned `dead_by` estimates per seeded flag in `FLAG_DEFAULTS` instead of a copy-pasted placeholder. [apps/admin/src/modules/feature-flags/FeatureFlagsPage.tsx, packages/domain/src/feature-flags/registry.ts]
- [x] [Review][Patch] `cache.ts`'s `cacheKey()` embeds a literal raw NUL byte as a delimiter instead of a normal character/escape, which makes the entire file render as binary in git (`Binary files /dev/null and b/... differ`) — invisible to diff, blame, and PR review despite being the file that implements the AC5c audit-outside-cache invariant. [packages/domain/src/feature-flags/cache.ts:47]
- [x] [Review][Patch] `resolveFlagAudited`'s `onAccess` callback — the "fires on every resolution, hit and miss alike" guarantee AC5c depends on — is skipped entirely when the underlying lookup throws (the `backend_error` path in `flagVersionInForceCached`), so a lookup failure produces no audit/access observation at all. [packages/domain/src/feature-flags/cache.ts:99-104,148]
- [x] [Review][Patch] The governance-boundary AST scanner misses two evasion routes it should catch given it's the load-bearing AC5b gate: dynamic `import()`/`require()` calls with a non-literal specifier, and bracket/computed property access (`obj['featureFlags']`) on the feature-flags namespace. Extend detection for both; re-run `governance-boundary:check`/`:test` after to confirm no false positives against the current tree. [scripts/governance-boundary/lib.ts:75,152]
- [x] [Review][Patch] `registry.ts`'s `effective_until`-after-`effective_from` ordering check runs before `effective_from` is defaulted to `dbNow()` when the caller omits it, so a flip with `effective_until` in the past relative to the *resolved* `effective_from` is accepted and silently never becomes in-force. [packages/domain/src/feature-flags/registry.ts:389]
- [x] [Review][Patch] `dead_by` is never validated as a real calendar date end-to-end: the contract layer only checks length (`z.string().min(10).max(10)`, any 10-char string passes despite the doc comment claiming "ISO date"), and the handler's `new Date(...)` + `Number.isNaN` check doesn't catch JS's silent date rollover (e.g. `2027-02-30` silently becomes `2027-03-02`). [packages/contracts/src/feature-flags/feature-flags.ts, apps/api/src/modules/feature-flags/handlers.ts:218]
- [x] [Review][Patch] Flag-subsystem lookup failures are swallowed with zero logging in both wired KYC consumers (bare `catch { return configDefault; }` / comment-only catch) — combined with the already-declared-absent health-signal/rollback mechanism (AC7), a flag-store outage produces no operational signal whatsoever. [apps/api/src/modules/kyc/manual-fallback-seam.ts, apps/api/src/modules/kyc/provider-registry.ts]
- [x] [Review][Patch] Admin flip mutation has no staleness guard: if the inventory list refetches while the flip form is open, submitting silently overwrites a concurrent admin's change with the stale snapshot held in form state (lost-update race). [apps/admin/src/modules/feature-flags/FeatureFlagsPage.tsx:71]
- [x] [Review][Patch] Admin flip form's Cancel button doesn't abort an in-flight mutation — a success/error banner can appear later for a flip the operator believed they cancelled. [apps/admin/src/modules/feature-flags/FeatureFlagsPage.tsx:225]
- [x] [Review][Patch] `capability-bar.ts` never checks `prohibited[].root` values from `governance_boundary.yaml` for path traversal (`..`, absolute paths) before joining them into a filesystem path in the gate's scan step — low exploitability (repo-controlled config) but cheap to close at load time. [packages/domain/src/feature-flags/capability-bar.ts:187]

### Review Findings — Pass 2 (2026-07-31, Group 1: domain + migration + RBAC + gate)

Second adversarial pass over commit `91e66b0`, run blind to Pass 1's findings. Scope: the 35 files / ~4400 lines covering `packages/domain/src/feature-flags/`, migration 0087 + schema + RLS policies, the RBAC deltas, `governance_boundary.yaml` + `scripts/governance-boundary/`, and their tests. The API/contracts, admin console, KYC wiring, and generated/docs artifacts are Groups 2–4 and are NOT covered here.

Three layers (Blind Hunter diff-only, Edge Case Hunter with project access, Acceptance Auditor against the spec + the AI-6-5 load-bearing-invariant checklist) produced 41 raw findings, deduplicated to 26. The Auditor independently re-ran the RLS policy-regression spec (10/10) and the gate suite (24/24), and performed its own revert-sanity plant against a real `packages/domain/src/consent/` file (gate failed with exit 1, then passed on removal) — leg (b) does have genuine semantic coverage against *direct* imports; the findings below concern indirect routes.

**Decision-needed — ALL 7 RESOLVED WITH THE USER (2026-07-31); each became a patch**

Resolutions, in the order the decisions appear below:

1. **Gate leg (b) evasion** → Close the two cheap AST holes (destructured bindings out of a literal-specifier dynamic `import()`/`require()`; `NamespaceExport` re-exports) with revert-sanity controls for both, AND retreat the "structurally impossible" wording in `scripts/governance-boundary/README.md`, `governance_boundary.yaml`, and the story record to what the gate actually guarantees: no direct or single-hop-named flag read inside a governance module. Transitive indirection is documented as out of reach for a per-file scanner rather than silently implied to be covered. Module-graph resolution rejected as disproportionate surface for this story.
2. **`kyc_manual_fallback.fallbackDefault`** → Set to `false`. The flag is named for the CUTOVER, so `fallbackDefault` answers "is the cutover active?" and the safe answer is no: `false` → `enabled:false` → `!false` → manual fallback AVAILABLE, which is what all three prose claims already assert. Rewrite the `FLAG_DEFAULTS` comment and the attested YAML `rationale` to state the trace explicitly instead of conflating the flag boolean with fallback availability. Requires a re-attestation note (it edits attested prose).
3. **Temporal model** → Immediate-effect flips only: reject `effectiveFrom > dbNow` at write time AND add `isNull(supersededByVersion)` to the `flagVersionInForce` predicate. The two fixes are interdependent — excluding superseded rows alone would regress, since scheduling a future flip supersedes the current row the moment it is inserted. Closes the rollback deadlock and the resurrection hole together; the `:480` ordering guard becomes trivially satisfiable. Scheduled flips are dropped (used by nothing, required by no AC) and recorded as a deliberate narrowing. No migration.
4. **AC7 transition map** → Forward ladder, no skips, enforced in `createFlagVersion` against the prior row's state (already read at `:451`): `off→canary|off`; `canary→rollout|canary|rolled_back`; `rollout→full|rollout|rolled_back`; `full→full|rolled_back`; `rolled_back→off|canary|rolled_back`. **Identity transitions legal throughout — that is the cohort-edit path.** Forbidden: `off→rollout`, `off→full`, `canary→full`, `rolled_back→rollout`, `rolled_back→full`, and `rolled_back` as a flag's first-ever version.
5. **Empty cohort on `canary`/`rollout`** → Reject at write time; a staged state must name at least one clause. Makes `canary` structurally different from `full` and removes the two-step author-then-narrow window in which `kyc_manual_fallback` would go hard-mandatory tenant-wide.
6. **Prohibited-root contents** → Add `packages/domain/src/pool/`, `packages/domain/src/claim/`, and `apps/api/src/{middleware,modules/rbac,audit,plugins}/`, each with its own attested `prohibited[]` entry. Verified none of these reference `featureFlags`, so the gate stays green. ⚠ Adding all of `apps/` was rejected — five legitimate consumers live there (`kyc/manual-fallback-seam.ts`, `kyc/provider-registry.ts`, `channel-config/composition.ts`, `modules/feature-flags/*`) and it would red the gate immediately.
7. **Decision 5's "ANDed within a clause"** → Record the narrowing, do not build it. OR-only cohorts are the ratified v1; add an explicit deliberate-narrowing note to the Dev Agent Record plus a `deferred-work.md` entry with a named re-trigger, exactly as D4's cache substrate and D6's auto-rollback were recorded. Conjunction ripples through the JSONB shape, contracts, OpenAPI, the admin form, and the capability-bar hash — its own story.

- [x] [Review][Decision] Gate leg (b) misses at least four indirect routes to a flag read inside a governance module — how far should AC5b's "structurally impossible" be hardened? Confirmed against `scripts/governance-boundary/lib.ts:92-197`: (1) `const { evaluateFlag } = await import('@twt/domain')` — route 1 clears it (literal, innocent specifier), route 2 only visits `ImportDeclaration`/`ExportDeclaration` so a `VariableStatement` binding is never inspected, route 3 sees a bare `CallExpression` not a `PropertyAccessExpression`; (2) `export * as featureFlags from '@twt/domain'` — `NamespaceExport` is not `isNamedExports`, so route 2 skips it; (3) `import * as d from '@twt/domain'; const { featureFlags: ff } = d; ff.evaluateFlag(...)` — an `ObjectBindingPattern` is not a property access; (4) a one-hop shim outside every scanned root (`export { evaluateFlag as decide } from './feature-flags/evaluate.js'`) then `import { decide }` inside the governance module — the scanner resolves no specifiers and follows no module edges. Options: accept as "catches accidental, not adversarial" and say so in the README + YAML (retreating from the "structurally impossible" wording); OR close the cheap AST holes (binding patterns, namespace re-exports) and leave transitive alone; OR add module-graph resolution. Note a per-file scanner cannot make transitive reachability structurally impossible.
- [x] [Review][Decision] `FLAG_DEFAULTS.kyc_manual_fallback.fallbackDefault = true` inverts the fail-safe its own trustee attestation claims. Trace: `evaluate.ts:146` returns `enabled: fallbackDefault` on a malformed clause → `manual-fallback-seam.ts:110` returns `!decision.enabled` = `!true` = `false` = manual fallback hidden = **KYC hard-mandatory**. `governance_boundary.yaml`'s attested rationale says the subsystem "can never silently make KYC mandatory for anyone," and `manual-fallback-seam.ts:19-21` claims the inversion "mak[es] the degraded outcome the permissive one" — both are the opposite of the behaviour. `configDefault` correctly guards the *no-row* path (`manual-fallback-seam.ts:107`), so this bites only a PERSISTED row with an unevaluable clause. Setting `fallbackDefault: false` makes the trace match the stated intent. Requires editing an attested `rationale`, hence a decision, not a patch. [packages/domain/src/feature-flags/registry.ts:80-96, governance_boundary.yaml:63-72]
- [x] [Review][Decision] The effective-window + supersession model has two confirmed holes and one fix has to settle both. (a) **Future-dated version deadlocks rollback:** `registry.ts:480` rejects any flip whose resolved `effectiveFrom` precedes `priorRow.effectiveFrom` (`priorRow` = highest version). A version scheduled with a future `effectiveFrom` — which `validateFlagVersionInput` permits — makes every subsequent flip throw `FlagEffectiveFromOutOfOrderError` until that date arrives, including the audited `rolled_back` flip that Decision 6 names as the *entire* shipped rollback path. The row cannot be amended (append-only trigger) or deleted (no DELETE grant); the only exit is a superuser write. (b) **Superseded version resurrects:** `flagVersionInForce` (`registry.ts:205-252`) filters on window and scope only and never reads `superseded_by_version`, so when a bounded rollback version's `effective_until` passes, the older `effective_until IS NULL` row re-enters force and the flag silently returns to its pre-rollback state.
- [x] [Review][Decision] AC7's legal-transition map was never built — what are the legal transitions? `grep -rn "transition" packages/domain/src/feature-flags/` returns nothing; `registry.ts:360` validates set membership only, and `createFlagVersion` already reads the prior row (`:451`) without comparing its `state`. So `off → full` (skipping canary entirely), `rolled_back → full`, and `rolled_back` on a never-launched flag are all accepted. AC7 states the state set ships "with its legal-transition map"; Completion Note 5 records the five-state machine as the half of AR-64 that *did* ship for real, so the knowing-partial-delivery record does not cover this. The map's content is a product call.
- [x] [Review][Decision] `evaluate.ts:138-140` hard-codes `enabled: true` for `cohort_empty` — the only arm that ignores `fallbackDefault`. `validateFlagVersionInput` explicitly permits an empty clause list, so `canary` and `full` are behaviourally identical until a cohort is authored. The header comment (`:108-110`) documents this as deliberate ("a staged rollout with no cohort is a rollout to all"), but on `kyc_manual_fallback` it means a two-step author-then-narrow flow makes DigiLocker hard-mandatory tenant-wide in the gap. Keep as-is, gate on `fallbackDefault`, or reject an empty cohort at write time for `canary`/`rollout`?
- [x] [Review][Decision] The prohibited-root list omits governance surfaces the gate claims to protect. `governance_boundary.yaml`'s six roots exclude `packages/domain/src/pool/` (freeze row 1, deterministic assignment — cited by the parser's own `FROZEN_BEHAVIOUR_MARKERS`), `packages/domain/src/claim/`, and **all of `apps/`** — where RBAC enforcement on request paths actually lives, which is where prohibition (d) "escalate RBAC" would be violated in practice rather than in the key catalog. Distinct from the Pass-1 deferral (which was about *drift detection* for the list); this is about the list's *current* contents.
- [x] [Review][Decision] Decision 5's "ANDed within a clause" is not expressible. A clause carries exactly one `dimension`, values are ORed (`evaluate.ts:80`), and clauses are first-match, i.e. ORed across. So "district = patna AND role = member" — the canonical staged-rollout cohort — cannot be authored; the only recourse is a `cohort_tag` precomputed outside the audited, replayable flag document. Decision 5 is binding, and the Dev Agent Record lists every other deliberate narrowing (D4's cache, D6's AR-64, AC3's audit context) but not this one. Implement conjunction, or record the narrowing explicitly.

**Patch**

- [x] [Review][Patch] The lookup cache key omits `at` (and the effective RLS scope), so the memoized value is not a function of its key — a historical/replay read and a live read collide for the 5s TTL, and a window that expires mid-TTL serves stale. Converged on independently by all three layers. [packages/domain/src/feature-flags/cache.ts:47-49,74-113]
- [x] [Review][Patch] `evaluateFlag` throws a `TypeError` on a structurally-incomplete cohort document, breaking AC2's "never throws" — `evaluate.ts:77,80` dereference `clause.values` and `:138` `cohortDefinition.clauses` with no existence check. Verified by execution: `{clauses:[{dimension:'district',op:'in'}]}` and `{}` both throw. The certifying test feeds only structurally-complete documents. `cohort_definition` is opaque `jsonb` with no CHECK, and global rows are authored outside `validateFlagVersionInput` by design. [packages/domain/src/feature-flags/evaluate.ts:77,80,138; packages/domain/tests/feature-flags/evaluate.test.ts:176-188]
- [x] [Review][Patch] A `prohibited[].root` that does not exist on disk makes leg (b) pass vacuously — `check.ts:65` returns silently and `:136-138` prints "clean (0 file(s))". No `scannedFiles > 0` floor exists, so if every root were stale the gate would pass having scanned nothing. The test claiming to cover this asserts only `expect(p.root).toMatch(/^(packages|scripts)/)`. [scripts/governance-boundary/check.ts:65,136-141; packages/domain/tests/feature-flags/capability-bar.test.ts:140-145]
- [x] [Review][Patch] The scanner's file collection misses `.mts`/`.cts`/`.mjs`/`.cjs` and every symlink (`Dirent.isFile()`/`isDirectory()` are both false for a link, so it is skipped with no diagnostic). `scripts/` is a prohibited root and prohibition (f) is "a flag must never disable a CI gate". Verified currently zero such files under the six roots — an unguarded region, not a live violation. [scripts/governance-boundary/check.ts:69-77]
- [x] [Review][Patch] The golden hash excludes `rationale` and `prohibition` — the exact prose AC6 makes the substance of the trustee attestation — and no test pins `FLAG_DEFAULTS` at all, which AC2 requires ("pins the seeded capability bar **+ the default flag documents**"). A `fallbackDefault` flip or a rewritten rationale passes green. [packages/domain/tests/feature-flags/capability-bar.test.ts:169-177]
- [x] [Review][Patch] The `FlagVersionConflictError` → 409 seam is asserted by a test that never invokes it: `registry.spec.ts:218-256` raw-inserts a duplicate and asserts `code === '23505'`, i.e. that Postgres enforces its own constraint. `createFlagVersion` is never called a second time, and no test repo-wide asserts `FlagVersionConflictError`. Deleting the mapping `catch` leaves every test green — and `isUniqueViolation` reads `err.cause.code`, the exact shape this repo has been burned by. [packages/domain/tests/integration/feature-flags/registry.spec.ts:218-256; packages/domain/src/feature-flags/registry.ts:341,506-511]
- [x] [Review][Patch] AC5c's error-path `onAccess` — the arm Pass 1 just added — has no regression test. The 7 tests in `cache-audit-outside.test.ts` never make the lookup throw, so the fix is one refactor away from silent reintroduction. [packages/domain/tests/feature-flags/cache-audit-outside.test.ts]
- [x] [Review][Patch] If `onAccess`/`observe` throws it escapes before the `throw err`, replacing the typed backend error and losing its discriminant; neither callback is wrapped despite `FlagLookupOptions.observe` being documented "Never throws into the caller". Separately, the error path hands `onAccess` a fully-shaped decision asserting `enabled: callerDefault` for a decision that never happened, which the caller's own catch may then contradict. [packages/domain/src/feature-flags/cache.ts:32,139-152]
- [x] [Review][Patch] `superseded_by_version` has no referential guard of any kind. 0084's composite self-FK was dropped for *every* row, but the stated justification (MATCH SIMPLE is vacuous when `pariwar_id` is NULL) covers only the global half — for a tenant row all three columns are non-null and the FK would have been fully enforced. The append-only trigger named as "the real backstop" enforces column immutability, a different property, and never inspects `NEW.superseded_by_version`. Any tenant-scoped caller can point it at a nonexistent version, at itself, or backwards. [packages/domain/migrations/0087_feature-flags.sql:41-46,98-122]
- [x] [Review][Patch] A caller-supplied `id` colliding on the primary key is reported as a *version* conflict — `isUniqueViolation` reads only `code === '23505'` and never inspects `constraint`/`detail`, so an idempotency replay receives "version N already exists — re-read the latest version and retry", advice that regenerates the identical 409 forever. [packages/domain/src/feature-flags/registry.ts:341-345,506-511]
- [x] [Review][Patch] `actorWhoFlipped` and `auditId` are optional inputs, nullable columns, unvalidated and un-FK'd, while FR-58C is quoted throughout as "actor + rationale" and `rationale` is required and bounded. No discriminator separates a legitimate seed write from an admin flip that forgot the actor, and the immutability trigger makes it unfixable after the fact. The integration spec never passes `actorWhoFlipped`, so only the un-attributed path is exercised. [packages/domain/src/feature-flags/registry.ts:325-330,499-502]
- [x] [Review][Patch] `loadCapabilityBar()` throws a raw `ENOENT` rather than `CapabilityBarInvalidError` when the YAML is unreachable, and is called on every `createFlagVersion` with no memoization — a blocking `readFileSync` + full parse + full structural validation per flip. `capabilityBarPath()` hard-codes four `..` hops from `import.meta.url`, making a runtime write path depend on repo layout. [packages/domain/src/feature-flags/capability-bar.ts:225,232-234; packages/domain/src/feature-flags/registry.ts:431]
- [x] [Review][Patch] Nothing forbids a persisted row at `version <= 1`. `flagVersionForVersion` short-circuits to the code default before querying (`registry.ts:280`), while `flagVersionInForce` filters on window and scope only and would return such a row — so the `(pariwar_id, flag_key, version)` replay pin resolves to a different document than the one actually in force. Reachable via the service-pool/seed path the migration header contemplates. Add `CHECK (version >= 2)`. [packages/domain/src/feature-flags/registry.ts:280; packages/domain/migrations/0087_feature-flags.sql:64]
- [x] [Review][Patch] `state_trustee` is excluded from `feature_flag.view` with no documented rationale, while the `district_admin` deferral is documented at length in both `permissions.ts` and `roles.ts`. Its `scopeCeiling` is `state` — strictly above `pariwar` — so it is the one role that reads as "and above" in plain English (PRD: "visible to Pariwar Admin role and above") and the one whose absence carries no note. The exclusion is almost certainly correct; the omission of the reason is the finding. [packages/domain/src/rbac/roles.ts; packages/domain/tests/rbac/roles.test.ts]
- [x] [Review][Patch] `listFlagVersions` truncates history at 100 rows with no cursor, `hasMore`, or total, behind AC1's "historical flag states are queryable". Worse, the tenant read mixes global rows into the same 100 ordered by `effectiveFrom desc` across two unrelated version sequences, so a busy global catalog can crowd out a tenant's own override history. [packages/domain/src/feature-flags/store.ts:63-88]
- [x] [Review][Patch] The `countMismatch` branch in `runConformanceLeg` is unreachable in situ — `loadCapabilityBar()` throws on `count !== allow.length` before `checkRegistryConformance` is ever called, so the actionable "bump `count` in the SAME commit" message never prints and the generic "gate ERRORED" does instead. The branch is exercised only by `lib.test.ts` calling it directly. Behaviourally benign; a documented gate leg tested only against itself. [scripts/governance-boundary/check.ts:82,107,156]
- [x] [Review][Patch] The bar's own admission workflow requires "Trustee attestation (recorded in `.decision-log.md`)", but no Story 10.8 / feature-flag / `governance_boundary` entry exists there, and the file is not in the story's File List. Four behaviours were admitted under `adr: ADR-0036`, which Completion Note 6 records as `drafted`, not ratified. The first application of the workflow is missing its own ledger entry. [governance_boundary.yaml:75-87]

**Deferred**

- [x] [Review][Defer] A `23505` inside `createFlagVersion` aborts the CALLER's transaction (it runs on the caller's tx with no `SAVEPOINT`), so `FlagVersionConflictError`'s advice to "re-read the latest version and retry" is impossible on that tx, and any audit line the route writes afterward fails with `25P02` — turning a 409 into a 500 and losing the audit line. This repo has hit the pattern before (raw SAVEPOINT required for retry-on-23505 inside a scope tx). [packages/domain/src/feature-flags/registry.ts:5,506-511] — deferred to the Group 2 (API) review, where the route's actual catch behaviour decides whether it bites.
- [x] [Review][Defer] The write path is DB-clock-authoritative (`select now()`, with an explicit comment about instance skew) while the read path compares a caller-supplied app-clock `at` against those values with no skew allowance and no DB-side `now()` option — two app instances can resolve different versions for the same real instant while a window opens or closes. [packages/domain/src/feature-flags/registry.ts:196-212,455-468] — deferred, design-level with no clean fix at this layer.

**Dismissed as noise (8):** `try { snapshot.set() } catch {}` dead defensive code; POSIX-only path-traversal split; `prohibited[].root: "."` accepted (self-failing, not evasion); `FLAG_DEFAULTS` not `Object.freeze`d (`defaultFlagDocument` deep-copies); overlapping-root double-counting; the guarded-unreachable `throw` in `contextValuesFor`; the `vi.spyOn` ESM-namespace note (would fail loudly, not vacuously); integration-spec count assertions (Auditor re-ran both suites green, 16/16 and 10/10).

### Review Findings — Pass 3 (2026-08-01, Group 2: API surface + wire contracts)

Third adversarial pass, scoped to the 12 files / ~1718 lines of `apps/api/src/modules/feature-flags/`, the new `requireGlobalOrAnyPariwarPermission` composition, and `packages/contracts/src/feature-flags/`. Diffed WORKING TREE vs `origin/main`, so the Pass-2 edits are included. 3 layers → 36 raw findings → 22 after dedup.

**⚠ FIRST: a Pass-2 regression this pass caught and which is now FIXED.** Pass-2's AC7 ladder + staged-cohort rule broke 5 of 12 tests in `apps/api/tests/integration/feature-flags/feature-flags.spec.ts`, and Pass 2 reported the tree green. See the corrected Note 13. Fixed by making `flipBody()` a legal first flip (`canary` + a cohort clause), graduating `canary → rollout` in the history test, and climbing the full ladder in AC8. **12/12 now pass.**

**⚠ SECOND: the Pass-1 DEFERRED transaction-poisoning item is RESOLVED AS INNOCENT.** Two layers traced it independently and one reproduced it live: a genuine concurrent double-flip returns `RACE1 200` / `RACE2 409 feature_flag.version_conflict`. Nothing touches the aborted tx after the throw — `mapCreateFlagVersionError` only rethrows, the compensating audit runs on `deps.servicePool` (a different connection), and `closeScopeTx` issues ROLLBACK because `statusCode >= 400`. The deferred-work entry should be closed as investigated-and-cleared, not carried.

**Decision-needed — ALL 4 RESOLVED WITH THE USER (2026-08-01); each became a patch**

1. **Audit-chain content** → Add `rationale` to the digest (it IS available pre-insert) and switch to `canonicalJsonStringify`, restoring tamper-evidence for the field FR-58C names. The VERSION genuinely cannot be hashed — the intent line commits before the insert under ADR-0030, so the version does not exist yet — so correct that claim in the header, the inline comment, and Completion Note 4(a) rather than pretending. Net: two of three claims become true, the third states the real limit. Widening `AuditEntryInput` with a context field was rejected as a change to a §1.5 primitive shared by every audited mutation in the repo.
2. **Attribution** → Snapshot the display name at action time: a new `actor_display` column (migration 0089), populated from the value already fetched, surfaced on the inventory + version DTOs. This is what [[project_admin_display_name_attribution]] actually specifies, and it makes the existing fail-closed gate purposeful instead of ceremonial. Read-time resolution was rejected: an admin rename would silently rewrite the displayed history of past flips.
3. **Idempotency** → Accept `Idempotency-Key` on both flip routes, reusing Story 1.12's `idempotency_keys` + advisory-lock keyed store (the substrate sibling member-facing creates already ride). Keeps the version history a record of operator intent rather than of network retries. Natural idempotency (no-op on an identical flip) was rejected — it silently swallows a deliberate re-flip an operator may intend as a fresh audit event.
4. **Global-catalog gate scope** → Document, do not narrow. VERIFIED during triage: `loadGlobalActorGrants` is a bare `SELECT … FROM role_grants WHERE user_id = $1` with no liveness filter, and the pariwar arm passes `value: g.pariwarId` derived from the grant under test, so containment can never fail on scope — the predicate reduces to "holds the key in ANY tenant per `role_grants`". ⚠ But the exploit is NOT constructible today: there is no Pariwar deactivation/suspension concept anywhere in the repo and no `role_grants` revocation path at all. So state the property plainly in the DELIBERATE block and name the re-examination trigger — the first story introducing tenant deactivation OR grant revocation must revisit this gate before shipping.

- [x] [Review][Decision] **The §1.5 audit chain does not cover the rationale, and two doc claims assert that it does.** `flipPayloadHash` (`handlers.ts:148-160`) digests eight fields; `rationale` is not among them. `resourceLocator` is `feature_flag/${flagKey}` — no version. Yet the file header states the digest covers "the whole flip input INCLUDING the rationale (so the rationale is tamper-evident)" and that the locator is `feature_flag/<key>/v<version>`, and Completion Note 4(a) repeats both as the reason AC3's literal "audit context" requirement could be waived ("Nothing is lost; it is anchored rather than inlined"). Of AC3's six required context items, the chain carries one (`flag_key`). Two rationales on otherwise-identical flips produce byte-identical audit lines; the rationale is protected only by the row's append-only trigger, not by the hash chain. Options: add `rationale` (+ the resolved version, post-insert) to the digest; OR carry the context in the audit line properly; OR accept the narrower guarantee and rewrite all three prose sites to state it honestly.
- [x] [Review][Decision] **`getDisplayName` is fetched, used only as a null-gate, then discarded** (`handlers.ts:252-255`, `:337-340`). The row stores `actorWhoFlipped: ctx.actorId` and the wire exposes `last_flip_actor` as a raw UUID. Every sibling module that calls `getDisplayName` returns and uses it. So an admin with no display name is hard-blocked from flipping during an incident, while the record the gate protects is written identically either way — and AC4's "inventory renders last flip actor" renders an opaque UUID that stops resolving once the account is renamed or removed. Options: snapshot the display name onto the row/audit (matching [[project_admin_display_name_attribution]]); OR keep the gate and accept UUID-only attribution, documenting why.
- [x] [Review][Decision] **No idempotency on either flip route.** The 409 fires only on genuine concurrency; a SEQUENTIAL replay (client timeout + retry, double-clicked console button, proxy retry) simply creates the next version — two identical versions, two audit lines, a history that misrepresents what the operator did. Sibling member-facing creates in this repo carry `Idempotency-Key`; this privileged write does not, and nothing in the extensive commentary mentions the trade-off.
- [x] [Review][Decision] **`requireGlobalOrAnyPariwarPermission`'s pariwar arm derives the target scope from the grant being tested** (`apps/api/src/modules/rbac/index.ts:~215-217`), so the containment comparison can never fail and the predicate reduces to "does this actor hold the key in ANY tenant". CONTESTED across layers: the Auditor read it as correctly documented and verified the global write stays `super_admin`-only; the Blind Hunter argues the route has no `scopeResolutionHook` and therefore no tenant-membership or tenant-liveness check at all, so a surviving `role_grants` row in a deactivated tenant would still unlock the cross-tenant catalog. Needs a decision on whether that is in-scope for this key.

**Patch**

- [x] [Review][Patch] Map the four unmapped domain errors — `FlagStateTransitionError`, `FlagKeyNotAllowlistedError`, `CapabilityBarInvalidError`, `CapabilityBarUnavailableError` — all currently fall through `mapCreateFlagVersionError` (`handlers.ts:131-144`) and the app error-mapping boundary to `500 internal.error`. `FlagStateTransitionError` is the severe one (an ordinary AC7 governance refusal reported as a server fault, message suppressed); `FlagKeyNotAllowlistedError` is the AC5 runtime backstop rendered unreadable. `FlagVersionDuplicateIdError` is unreachable over HTTP (no route supplies `id`, and the request DTO is `.strict()`) — leave unmapped, note it as intentionally dead.
- [x] [Review][Patch] The global flip runs a multi-statement mutation on a POOL, not a transaction (`handlers.ts:357` passes `deps.serviceDb`, which is `createDb(...).db` — verified). `createFlagVersion` issues SELECT-prior / `select now()` / INSERT / forward-pointer UPDATE; a failure between the last two leaves the prior global row un-superseded forever (append-only trigger blocks repair, no DELETE grant). The per-tenant flip is safe (`request.scopeTx!.tx`). Wrap in `deps.serviceDb.transaction(...)`.
- [x] [Review][Patch] `flipPayloadHash` uses bespoke `JSON.stringify` over a client-supplied nested `cohort_definition` whose key order the server does not control — Task 3's binding rule is `canonicalJsonStringify` (RFC 8785). Two semantically identical flips with different key ordering currently produce different digests.
- [x] [Review][Patch] No test at the HTTP layer produces a 409 — deleting the `FlagVersionConflictError` arm leaves the API suite green. The domain proves 23505 → typed error; the typed error → 409 half is untested anywhere.
- [x] [Review][Patch] OpenAPI contradicts the code on both POST paths: 404 is returned (`NotFoundError`, asserted in the spec) but undeclared, and 400 is documented for "unknown flag key" which is actually a 404. `AdminDisplayNameMissingError` has no documented status on any path. The GET history path gets this right.
- [x] [Review][Patch] Correct three surviving false statements about the global-catalog gate: Completion Note 4(b) and Debug Log #3 both say `/api/v1/global/feature-flags` is `super_admin`-gated, and `handlers.ts:170-172` repeats it INSIDE the doc block justifying the BYPASSRLS read. Pass 1 changed that route to `requireGlobalOrAnyPariwarPermission(FEATURE_FLAG_VIEW_KEY)`. The read is still safe (tier 2 pins `isNull(pariwarId)`) but the stated reason is wrong. Also correct Completion Note 8's "API integration 11/11" — the file has 12 tests.
- [x] [Review][Patch] The AC4 HTTP completeness test asserts only `flags.length > 0` and `toContain('kyc_manual_fallback')`, while its header claims "a flag registered but omitted fails the test". Import `FLAG_KEYS` and assert set equality (the domain-layer spec already does).
- [x] [Review][Patch] `expect(flags.every(f => f.source === 'default'))` couples an inventory-completeness assertion to global DB state — it holds only while no global row exists for ANY flag anywhere, and the next test in the same file creates one. Breaks under `--shuffle`, on a re-run after a crash, or once any flag ships globally on by default. The two assertions above it already cover the property.
- [x] [Review][Patch] The contract still advertises `effective_from` as a schedulable window start (`/** Optional window start; defaults to the DB's now(). */`) after Pass 2 dropped scheduling. Neither the DTO nor either POST description says a future value is rejected — a client reading the published contract will author a scheduled flip and get an unexplained 400.
- [x] [Review][Patch] Neither DELIBERATE bypass block states a re-examination trigger (checklist family 9 requires rationale AND trigger): `requireGlobalOrAnyPariwarPermission` and the two BYPASSRLS `deps.serviceDb` paths.
- [x] [Review][Patch] Record the version-history CURSOR as an explicit deferred-work entry. Pass 2 added `has_more` honestly but the route declares no cursor/offset/limit, so a tenant with >100 interleaved rows is now reliably told it cannot see its own provenance with no mechanism to page. Note the repo's forced-pagination guard cannot catch this — it only detects a top-level array or `{items:[]}`, and these DTOs use `{flags:[]}`/`{versions:[]}`.

**Deferred**

- [x] [Review][Defer] Inventory reads pass `new Date()` (app clock) as `at` while the write path deliberately uses the DB clock, so for the duration of any host-vs-DB skew a successful 200 flip can be followed by an inventory read still showing the old state — silent, no signal. Same family as the Pass-2 clock-skew deferral; fix belongs with it, not piecemeal. [apps/api/src/modules/feature-flags/handlers.ts:182,192]
- [x] [Review][Defer] `actorRole: null` on both flip audit lines though AC3 names the field and `request.scopeGrants` is populated on the per-tenant route. Repo-wide drift (28 of 29 `actorRole:` sites pass null; only `audit-log-sink.ts` populates it) — fixing it here alone would be inconsistent, so it belongs to a repo-wide sweep. [apps/api/src/modules/feature-flags/handlers.ts:270,349]

**Dismissed as noise (8):** the Pass-1 deferred transaction-poisoning concern (resolved live as innocent — see above); `FlagVersionDuplicateIdError` mapping (unreachable over HTTP by construction); `feature_flag.rolled_back` vs the compensating `…_rolled_back` suffix (a real naming overlap but the compensating suffix appends to the action, giving `feature_flag.rolled_back_rolled_back` — distinguishable); POST returning 200 not 201 (consistent with the OpenAPI and with sibling modules); `flag_version` `nonnegative` vs `version` `positive`; bare `z.string()` vs `.uuid()` on id fields; `authenticate()` not asserting intermediate inject statuses; the 5s flag cache being a documented bounded-staleness choice.

### Review Findings — Pass 4 (2026-08-01, Group 3: admin console + KYC consumer wiring)

Fourth adversarial pass: `apps/admin/src/modules/feature-flags/` + route/router/client, `apps/api/src/modules/kyc/*`, and the deliberately-unwired `channel-config/composition.ts`. 11 files / ~1105 lines, diffed WORKING TREE vs `origin/main`. The Edge Case Hunter died mid-run on an API error and was RELAUNCHED with the confirmed findings pre-supplied so it hunted new ground; all three layers ultimately delivered. 3 layers → 39 raw findings → 24 after dedup.

**⚠ THE HEADLINE IS A SECOND REGRESSION FROM MY OWN GROUP-1 PATCHES.** The AC7 ladder and the staged-cohort rule landed without `apps/admin` being in scope, and nothing caught it because **the console has ZERO tests** (against 24 elsewhere in `apps/admin`). Same failure mode as the Pass-2 API regression: the changed layer was verified, its consumers were not.

**Verified positives (recorded so the negatives are read in proportion):** AC8's end-to-end tenant isolation is genuinely well tested — through the SEAM itself under two independently scoped transactions, not just the inventory read; the hard-mandatory copy block really exists and renders (`apps/mobile/app/(signup)/kyc.tsx:326-331`, keyed in both locales); Decision 8 held with no third consumer; `composition.ts` still hardcodes its fail-safe `false`; and no provider-name-in-`values` dodge of Decision 5's boolean predicate.

**Decision-needed — ALL 4 RESOLVED WITH THE USER (2026-08-01); each became a patch**

1. **Console reachability** → Replace the staged-cohort WRITE REJECTION (added in Pass 2) with EVALUATOR semantics: an empty clause list on `canary`/`rollout` resolves to `enabled: false`, not `true`. This fixes the original Pass-2 fail-open more directly than the rejection did — an untargeted canary should serve NOBODY, which is the safe reading of "not yet narrowed" — and it restores the console without a cohort editor, because `full` ignores cohorts entirely, so `off → canary → rollout → full` (the FR-2 cutover path) becomes fully reachable. The console additionally derives its state `<select>` from `LEGAL_FLAG_STATE_TRANSITIONS` so it can never construct an illegal transition. ⚠ NOTE THIS PARTIALLY REVERSES A PASS-2 DECISION: the write-time rejection is removed. The property it protected (canary ≠ full) is preserved by the evaluator change, and better — the old rule made the state unreachable rather than making it safe. Cohort authoring stays deferred to "its own reviewed surface" exactly as the console's comment intended.
2. **`off`/`rolled_back` row vs deployment config** → The ROW wins; behaviour unchanged. Once an operator authors a row for a scope, the flag governs that scope: `off` means "the cutover is not active", so the manual fallback is available. `config.digilocker.manualFallbackEnabled` remains what applies when the flag subsystem says NOTHING (source `null`/`default`) — which is precisely what the seam's docblock already defends. The defect is the console's help text ("The flag is inert; consumers use their own default"), which is the one false claim; it gets corrected. No member is locked out under either reading, and this one keeps the flag able to express per-tenant re-enablement.
3. **The dead provider-selection seam** → Do NOT wire it. The only second registered builder is the FIXTURE provider, and pointing a production provider flip at a test double is worse than an honestly-inert seam. Correct `provider-registry.ts`'s false "NO consumer code changes (that is architectural-freeze row 13 / AR-43 realized)" claim and the `deps.ts:189` comment, and add a `deferred-work.md` entry naming the re-trigger: the story registering a real AR-43 second vendor must set `alternateProviderKey` AND add the enabled-path test. Recorded the same way AR-64's auto-rollback was.
4. **AC5c production observer** → Wire a real observability sink at BOTH consumers: pass `onAccess`/`observe` and emit through `request.log`, the channel the Pass-1 `onError` hook already threads. Makes "the audit fires on a cache HIT exactly as on a miss" observable in production rather than only in `cache-audit-outside.test.ts`, and gives a flag-store outage an operational signal — which AC7's deferred auto-rollback makes MORE important, not less. Real §1.5 audit entries were rejected: a member-path read fires on every KYC status check, and the hash chain is not shaped for high-volume non-mutations.

- [x] [Review][Decision] **The console cannot perform a legal FIRST flip of any flag — its entire purpose.** VERIFIED: all four seeded flags ship `cohortDefinition: { clauses: [] }`; the form offers all five states unfiltered (`FeatureFlagsPage.tsx:27`) and forwards the existing cohort verbatim (`:108-111`). From `off`: `canary` → 400 (staged-cohort rule), `rollout`/`full`/`rolled_back` → 409 (ladder), `off` → succeeds and changes nothing. Genuine design tension: the console's own comment says it deliberately flips STATE only because "editing a cohort predicate is a structured-rule edit that deserves its own reviewed surface" — and my staged-cohort rule makes `canary` unreachable without exactly that surface. Options: (a) add a minimal cohort editor; (b) relax the staged-cohort rule and fix the `cohort_empty` fail-open a different way (e.g. empty cohort on a staged state means NOBODY rather than everyone); (c) keep the rule and have the console derive its options from `LEGAL_FLAG_STATE_TRANSITIONS`, accepting that the first flip must be made via API until a cohort editor ships.
- [x] [Review][Decision] **A persisted `off`/`rolled_back` row RE-ENABLES the manual fallback on a deployment that explicitly disabled it.** `manual-fallback-seam.ts:107` returns `configDefault` only when the source is `null`/`default`; an explicit `off` OVERRIDE has source `override`, so `:110` returns `!false` = `true`. On `KYC_MANUAL_FALLBACK_ENABLED=false` (permitted, `config.ts:368`) publishing `off` — currently the ONLY flip the console can make — reverses the deployment's own config with no operator intent, while the console's help text promises "the flag is inert; consumers use their own default". Is a not-serving row "inert, defer to config" or "not serving, and that is the answer"? The guard if the former: also return `configDefault` when `decision.reason === 'state_off'`.
- [x] [Review][Decision] **AC8's SECOND wired seam is dead in every real configuration.** `provider-registry.ts:69` gates the flag read behind `config.alternateProviderKey !== undefined`, and that option is set NOWHERE — verified: it appears only in `provider-registry.ts` and one unit test, never in `deps.ts` or `config.ts`. So the flag lookup never executes in production. The module header's claim that adding a provider + flipping the flag is "the WHOLE change; NO consumer code changes (that is architectural-freeze row 13 / AR-43 realized)" is false — it also requires a `deps.ts` edit. `deps.ts:189` even says "The fixture stays registered so an FR-58C flip can select it without a code change", which the shipped wiring cannot do. Wire `alternateProviderKey` for real, or correct both claims and record the seam as declared-but-inert.
- [x] [Review][Decision] **AC5c has NO production observer.** `provider-registry.ts:72-79` passes no options at all, so `onAccess`/`observe` never fire; `manual-fallback-seam.ts:103` passes `onAccess` only to capture `source` into a local and discard it. So across both wired consumers, "flag evaluation cannot suppress its surrounding audit logging" has no live sink — the property is proven only by `cache-audit-outside.test.ts`. Separately fragile: the seam's load-bearing tier decision is driven by a variable mutated inside an observability callback, so if `onAccess` is ever made async/batched/sampled the flag subsystem silently goes dark and every decision degrades to config. Wire a real observability sink, or record the property as structurally-correct-but-unobserved.

**Patch**

- [x] [Review][Patch] A provider flip STRANDS every in-flight KYC transaction. `kyc.handlers.ts:173,238` re-resolve the provider live on the callback/confirm legs instead of pinning it; `kyc_transactions.provider` exists for exactly this ("so an FR-58C provider swap needs no ALTER TYPE") and is never read. Provider B receives provider A's OAuth `state`/`code`, `verifyAndPullProfile` throws, the member gets `member_kyc.failure` with no recovery. Triggered by a flip, a window closing, a cohort re-tag, or just the flag cache bucket rolling. Resolve the flag on `initiate` only; use `builderFor(txn.provider)` thereafter.
- [x] [Review][Patch] `alternateProviderKey` is never validated against the registered `builders`, and the throw at `provider-registry.ts:89-91` sits OUTSIDE the try/catch — so a data-only flag flip to an unregistered key 500s every KYC initiate/callback for the whole cohort, with `ctx.onError` never firing. Validate at registry construction, or fall back to the default key when the alternate is unregistered.
- [x] [Review][Patch] `manual-fallback-seam.ts:17-25`'s header still argues from `fallback_default: true` — the value Pass 2 corrected to `false` — and its inference only holds under the OLD value, so it documents the inverse of the code on a member-lockout path. Pass 2 fixed `FLAG_DEFAULTS` and the YAML attestation and missed this third site.
- [x] [Review][Patch] `/status` and the manual gate DO disagree within one request: the gate resolves with the pre-transition `memberState`, then `projectMemberState` moves the member and `buildStatus` re-resolves with the NEW state — so a `member_state` cohort clause can make the very response confirming a successful manual submission report `manualFallbackEnabled: false`. The comment at `kyc.handlers.ts:307-308` claims this cannot happen. Resolve once per request, or thread the pre-transition state.
- [x] [Review][Patch] One malformed flag row blanks the ENTIRE inventory. `CohortDefinition` is `.strict()` with a required `clauses` array, so a row with `{}`/`{clauses:null}`/an empty `values` — shapes the domain explicitly tolerates and says reach production via the seed path — fails `schema.parse` for the whole response, rendering "Could not load the flag inventory." and hiding every other flag. The transparency surface fails closed on one bad row.
- [x] [Review][Patch] The console renders the raw actor UUID (`FeatureFlagsPage.tsx:193`, `:254`) and never reads `last_flip_actor_display` / `actor_display` — the fields migration 0089 exists solely to supply. The contract explicitly says to render null as "not recorded" rather than substituting the UUID; the console renders `'unknown'`. Also: the actor is nested inside the `rationale ? … : '—'` branch, so a row with an actor but no rationale hides the actor entirely.
- [x] [Review][Patch] The console ignores `has_more`, reproducing exactly the silent truncation Pass 2 added the field to prevent — the console IS the "consumer rendering provenance" it was added for.
- [x] [Review][Patch] The console sends no `Idempotency-Key`, leaving uncovered the precise scenario Pass 3 Decision 3 named ("a client timeout + retry, double-clicked console button"). In-page double-click is mitigated by `disabled={flip.isPending}`; a post-timeout retry is not, and the console is the route's only caller.
- [x] [Review][Patch] `cancelledRef` is a single boolean shared across ALL flags and is reset at every submit, and `onSuccess` early-returns before `invalidateQueries`. So (a) a cancelled-but-committed flip never refreshes the table, and (b) cancelling flip A then submitting flip B makes A's late `onSuccess` fire against B's session, attributing A's result to a flip the operator abandoned.
- [x] [Review][Patch] The lost-update guard compares `flag_version` only, but global and override are INDEPENDENT counters both starting at 2 — so a form opened on a `global` entry at v2 does not trip when a `override` v2 is published under it, and submit overwrites the new override's cohort with the stale global snapshot: the exact lost update the guard's comment claims to prevent. Compare `source` as well.
- [x] [Review][Patch] No error-code branching anywhere in the console: every flip failure renders `err.message`, and a 403 on the inventory READ is indistinguishable from a 500 ("Could not load the flag inventory."). `ApiError` carries `status` and `code` and both are discarded; react-query silently retries the 403 three times. The five new codes (`illegal_state_transition`, `not_allowlisted`, `idempotency_in_progress`, and the two 503 capability-bar codes) get no actionable message.
- [x] [Review][Patch] `provider-registry.ts:77` uses `new Date()` rather than `deps.clock()` — the only flag read in the request that does — so it evaluates windows against wall time while everything else uses the injected clock, is non-reproducible under a frozen clock, and re-buckets the cache key every second making the lookup a near-permanent miss. It also passes a NARROWER cohort context (`pariwarId` only) than the manual seam (`pariwarId` + `memberState`), so a `member_state` clause silently never matches on one of the two KYC flags.
- [x] [Review][Patch] Both new unit tests are satisfied by the ERROR path: `kyc-signup.test.ts:74-75` asserts `resolves.toBe(true)` where `configDefault` is `true`, which is byte-for-byte what the deliberate `brokenDb()` case produces; `kyc-provider.test.ts:291` asserts `providerA`, which is also what a successful disabled resolution returns. Neither passes an `onError`/`onAccess` spy — added by this very diff to make exactly this observable. Neither test ever authors a flag row, so the inversion the seam header calls "the whole point" has zero coverage: delete the `!` at `manual-fallback-seam.ts:110` and the suite stays green.
- [x] [Review][Patch] The console has NO test at all, against 24 specs elsewhere in `apps/admin` including the 10.3/10.4/10.5 console precedents AC4 cites. This is why the ladder regression, the UUID rendering, and the `has_more` omission all shipped invisibly. Add a spec covering the flip form's state options, the null-render paths, and the error branches.
- [x] [Review][Patch] The FR-73 Telegram-mirror seam got its `deferred-work.md` entry but NO in-code comment at the live call site (`apps/jobs/src/scheduler/contribution-notify.ts`), which AC8 requires literally ("left returning their fail-safe defaults **with a comment** + a `deferred-work.md` entry"). The cost-optimization seam does this correctly and is the model.
- [x] [Review][Patch] `provider-registry.ts:65-68` credits an AC5c access observation it never registers (it passes no options), and `manual-fallback-seam.ts:101-103`'s `callerDefault` argument is unreachable — an unregistered key yields source `null`/`default`, both of which return `configDefault` two lines earlier, before `decision` is read. Dead argument with a comment that makes it look load-bearing.
- [x] [Review][Patch] Two dead client exports (`listGlobalFeatureFlags`, `flipGlobalFeatureFlag`) have zero callers. Pass 1 loosened the global catalog to `pariwar_admin`+ specifically for AC4's "visible to Pariwar Admin and above" and no admin surface renders it.

**Dismissed as noise (7):** `summariseCohort`'s `OR` label (correct — clauses ARE first-match/disjunctive per Decision 5); UTC date slicing in the console; no `min` on the dead-by input; untrimmed `owner`/`rationale` writes; `role="status"` vs `role="alert"` on the redirect notice; import ordering in `router.tsx`; missing focus management on "Change…".

### Review Findings — Pass 5 (2026-08-01, Group 4: ADR-0036 + governance docs — the FINAL chunk)

Fifth and last adversarial pass: `docs/adr/ADR-0036-feature-flag-tool-selection.md`, `adr-index.md`, `inventory-roster.md` Row 12, `gate-inventory.md`, `.decision-log.md`. 314 lines. Layers adapted to a prose chunk: Blind Hunter (internal consistency + overclaiming, document-only), a claim-TRACER variant of the Edge Case Hunter (every factual assertion verdicted BACKED / STALE / FALSE / UNVERIFIABLE against current code), and the Acceptance Auditor (AC6's vendor half + the seven Item-9 properties). 36 raw findings → 21 after dedup.

**⚠ THE FINDING THAT MATTERS MOST IS ABOUT MY OWN EARLIER PATCH.** Pass 2 retracted the gate's "structurally impossible" guarantee as overstated. That retraction reached `scripts/governance-boundary/README.md` and ONE spot in `governance_boundary.yaml` — and left the claim standing in **four** other places: `scripts/governance-boundary/lib.ts:13` (the gate's OWN SOURCE), `gate-inventory.md:37` (the canonical registry a reader consults to learn what a green run means), `governance_boundary.yaml:131` (the same file, self-contradicting at :45), and by implication `ADR-0036`'s "proves no feature-flag evaluation reaches inside a governance module". Four descriptions of one gate, disagreeing. The YAML self-contradiction was fixed during triage; the rest are patches below. This is the same shape as the Pass-2 API regression and the Pass-4 console regression: **the layer I was looking at got fixed and its siblings did not.**

**Verified positives:** ratification hygiene is genuinely clean — the ADR is `drafted`, `.decision-log.md` item 4 and Row 12's ⚠ note both say so, and nothing anywhere treats it as ratified; "complete in FORM but not yet in AUTHORITY" is the record's own phrase. Tenant isolation, lifecycle accountability, and DPDPA posture are all genuinely DEMONSTRATED against shipped code (the claim-tracer verified no network call exists anywhere in the evaluation path). "No concealed flags" is demonstrated with teeth (`store.ts` iterates `FLAG_KEYS`; a contracts test asserts `.strict()` rejects `hidden`/`internal`/`visibility`/`secret`).

**Decision-needed — ALL 3 RESOLVED WITH THE USER (2026-08-01); each became a patch**

1. **Offline resilience** → Demonstrate it properly; the material exists. Rewrite property 5 to trace the ACTUAL degradation path (cache-optional/correctness-mandatory, the typed per-flag `fallback_default` now correctly `false`, the source-keyed config floor, and the `onError` sink Pass 1 added), and address head-on that the deepest fallback IS a config value — explaining why a config FLOOR consulted when no flag row exists is a different thing from the un-audited config EDIT the ADR's Context condemns.
2. **Row 12** → Follow the Row 15 precedent. Restore the prior `current_status` and append a `<superseded by Story 10.8 — tooling delivered, ratification pending>` annotation, keeping the honest prose. Matches the documented rule that a per-Story author-commit never flips a launch-gate row, so a status-token scan no longer shows a state change no trustee ratified.
3. **The `< 5 ms` claim** → Restate as UN-MEASURED with a re-trigger. State the design reasons for sub-millisecond resolution, state plainly that NFR-FR58C is not measured, and name the AI-4-1/AI-4-2 measured-validation family as where that evidence belongs. Consistent with how this story recorded every other unbuilt thing; measuring was available but belongs with that family rather than bolted onto a review pass.

- [x] [Review][Decision] **Item 9 property 5 (offline resilience) is defined away rather than demonstrated, and its actual fallback is the thing the ADR condemns.** The ADR answers "there is no external provider to be outaged by" — which addresses the ABSENT failure mode, not the present one (what an evaluation does when the Postgres lookup errors or times out). And the deeper fallback is `config.digilocker.manualFallbackEnabled`, an environment/config value — precisely the "config edit, an environment variable… the un-audited, un-inventoried behaviour change the capability bar exists to prevent" that the ADR's own Context condemns. Options: (a) demonstrate the property properly (the seam DOES degrade safely — cache-optional/correctness-mandatory, a typed fallback, and since Pass 1 an `onError` sink — so the material exists); (b) concede the property is only partially met for an in-house selection and record it as a named residual risk.
- [x] [Review][Decision] **Row 12 invents a status token outside the roster's own vocabulary and flips it on author-commit — where the nearest precedent deliberately did not.** `current_status: predicate-foreclosed-pending-trustee-disposition` appears nowhere else; the row's own `closure_criteria` names three onward states and the rubric cites four. Row 15 (Story 7.9 — identical shape: tooling delivered, ratification pending) PRESERVED the prior status and appended a `<superseded …>` annotation, noting explicitly that "a per-Story author-commit `done` never flips a launch-gate row". Row 12's prose is scrupulously honest ("this row is not yet closed"), so this is a FORM violation, not a false claim — but a status-token scan now shows a state change no trustee ratified. Follow the Row 15 precedent, or keep the new token and add it to the rubric?
- [x] [Review][Decision] **The ADR's `< 5 ms` NFR-FR58C claim is UNVERIFIABLE — no benchmark exists.** No `p95`/`performance.now`/benchmark anywhere under the feature-flag tests. Every other property in the ADR cites a named test; this one asserts "comfortably inside". Per [[feedback_verify_before_committing_governance_claims]] and the [[project_measured_validation_framework]] precedent, an unmeasured performance claim in a ratification-bound ADR is one a trustee cannot check. Measure it now (the AI-4-1/AI-4-2 harness exists), or restate it as un-measured with an explicit re-trigger?

**Patch**

- [x] [Review][Patch] Retract "structurally impossible" in the three surviving sites — `scripts/governance-boundary/lib.ts:13`, `gate-inventory.md:37`, and `ADR-0036`'s "proves no… reaches inside" — to the precise claim the README and YAML now carry (DIRECT and single-hop NAMED routes mechanically closed; transitive reachability explicitly NOT covered). Leave `ADR-0036:75` alone: that one is tenant isolation via RLS, which genuinely IS structural.
- [x] [Review][Patch] `gate-inventory.md:37` lists SIX scan roots; there are TWELVE (Pass 2 added `pool`, `claim`, and four `apps/api` enforcement roots). `.github/workflows/ci.yml`'s job comment carries the same stale six. Understating a gate's coverage by half in the canonical registry is the [[project_gate_scope_semantic_coverage]] failure inverted.
- [x] [Review][Patch] `ADR-0036` says "three independent detection routes"; there are FIVE (Pass 2 added namespace re-export and object-binding destructuring, specifically because the original three were each defeated). Neither the ADR nor the inventory records the root-existence failure or the coverage floor — the two teeth most likely to be removed by a future "simplification" precisely because no governance doc names them.
- [x] [Review][Patch] `ADR-0036` presents `provider-registry.ts` as a WIRED consumer in its Decision section, and its failure-mode ledger says "two of three consumer seams are registered but unwired". Both are wrong: FOUR flags are registered, and THREE have no live consumer — `kyc_provider_selection` reads the evaluator but is inert in every deployment (Pass 4 Decision 3). Closure-language collapse: "wired" where "recorded, un-built" is the true state.
- [x] [Review][Patch] `ADR-0036`'s replay-safety property describes `effective_from`/`effective_until` as "the window", i.e. the PRE-Pass-2 resolution rule. The shipped rule is "newest version whose `effective_from <= at`, then check THAT row's window"; an expired head means nothing persisted governs; and scheduled (future-dated) flips are now REJECTED outright. The ADR reads as though pre-announced flips are authorable. This is the property AC6 most needs demonstrated and it is described pre-change.
- [x] [Review][Patch] `ADR-0036`'s determinism property omits that an empty cohort on `canary`/`rollout` now resolves `enabled: false` — the arm that was inverted twice across passes — and its "never throws into a caller's request path" claim is true ONLY because Pass 2 added shape guards, which it does not cite. It was false when written.
- [x] [Review][Patch] `ADR-0036`'s "Failure modes accepted" omits three narrowings recorded everywhere else: OR-only cohorts (no conjunction expressible), dropped scheduling, and the absent version-history cursor. Completion Note 10's own reasoning — "omitting these would read as oversight" — applies harder to the outward-facing record.
- [x] [Review][Patch] `ADR-0036` omits every governance control that postdates it: the AC7 transition ladder (so its "five-state machine" reads as unordered, and it never says a flag at `off` cannot be rolled back), `Idempotency-Key`, migration 0088's six CHECK constraints (including the `version >= 2` its own property 3 now relies on), and migration 0089's `actor_display` + EXTENDED immutability trigger. It names 0087 as the sole store of record.
- [x] [Review][Patch] **The attesting ADR never attests the correction that `.decision-log.md` records as a fix to a FALSE attestation.** All four `allow` entries cite `ADR-0036`, and the bar's workflow makes it the attesting artifact — yet the `kyc_manual_fallback` polarity inversion (`fallbackDefault: true → false`, the member-lockout trace) appears nowhere in it. Property 5 is its natural home.
- [x] [Review][Patch] The freeze-marker heuristic (`capability-bar.ts:64-86`) carries NO marker for freeze row 13 or row 4 — the two rows all four shipped entries actually cite — and is self-described as non-exhaustive. Both the ADR and the gate inventory present this substring filter as freeze-table ENFORCEMENT. Separately, the claim "(e) has no import to scan for" now contradicts the YAML, which labels the `pool` root "(e)" and `claim` "(e/g)".
- [x] [Review][Patch] `adr-index.md`'s slot flip is half-done by the index's own convention: the row changed, but the reconciliation ledger still reads `drafted | 4` (should be 5) and the italic ledger note every prior flip carries is absent. Completion Note 6 claims both files' live state was verified — the verification did not reach the index's own §7 reconciliation.
- [x] [Review][Patch] Row 12's `architecture_source_line: 4817-4828` and "predicate per architecture lines 4823-4828 verbatim" are a FALSE cross-reference — the quoted predicate is at `architecture.md:4852`; 4823-4828 is the complexity/re-baselining paragraph. Pre-existing, but this diff rewrites four sibling fields on the row and leaves the wrong anchor standing, and the ADR leans on the row as its risk-of-not-deciding source.
- [x] [Review][Patch] `ADR-0036` describes the admission workflow in the present tense as satisfied ("Adding a flag-toggleable behaviour requires… trustee attestation recorded in `.decision-log.md`") while `.decision-log.md` records that its FIRST use skipped exactly that, and the retroactive entry is self-authored with "Ratifying trustees: pending". A reader of the ADR alone concludes the bar has never been expanded without trustee sign-off; it is 4-for-4 the other way.
- [x] [Review][Patch] Two demonstration gaps to state rather than leave implied: (a) replay safety — version 1 lives in a code CONSTANT with no window and no append-only trigger, so every never-flipped flag replays against TODAY's constant, which is most flags today; (b) "no concealed flags is structural" closes registry-not-in-DB but leaves the inverse open — a persisted row whose `flag_key` is absent from the code registry is invisible to the inventory by the same design, and no FK can bind a table to a TS constant.
- [x] [Review][Patch] Cross-Cutting #15's canary/graduated-cohort staging and automatic error-spike rollback get no row in the ADR's demonstration table, so a 7/7 score reads as complete coverage of a ~9-property commitment. The auto-rollback absence is honestly recorded elsewhere; the table should say so too.
- [x] [Review][Patch] `gate-inventory.md`'s status reads "✅ Active and enforcing" without naming the runner, directly above a row noting GH Actions suspended. Name `ci.yml`'s job and the `ci-local.sh` line (both verified present).
- [x] [Review][Patch] The ADR and `.decision-log.md` disagree about which gate leg is load-bearing — the ADR calls leg (a) "bookkeeping", the log presents registry ≡ allowlist in both directions as the mechanism enforcing admission. Both are partly right and the wording should reconcile: leg (a) prevents silent DIVERGENCE, leg (b) is the invariant.
- [x] [Review][Patch] Two of the ADR's revisit triggers are unobservable by its own admission ("a measured `< 5 ms` p95 risk", "a measured latency or scale problem") in a document stating no metrics substrate exists and AR-31 is deferred. Failure modes 1 and 3 carry no revisit trigger at all.
- [x] [Review][Patch] Reference trivia: `architecture.md:4094-4098` commits owner + retirement signal but NOT the dead-by date (that is at `:224-225`); `.decision-log.md` cites `prd.md:886-892` where the FR-58C heading is at 884.

**Dismissed as noise (5):** the three-document citation ring (inherent to an unratified ADR, and the record concedes the status plainly); "Substantively discharged" vs "not yet closed" on Row 12 (folded into the Row-12 decision); the `db:generate` warning being absent from the ADR (it belongs in the migration headers, where it is); ADR-0036's number being unclaimed (verified correct); the `.strict()`-rejects-hidden-fields claim (verified genuinely demonstrated).

## Dev Notes

### Read this first: the two traps specific to 10.8

**Trap 1 — the vacuous gate.** It is very easy to ship `governance_boundary.yaml` + a gate that checks "every flag key is listed" and declare AC5 done. That gate is green from the moment it lands and stays green while someone adds a flag read inside the RBAC module. The list-conformance leg is bookkeeping; **the source-scan leg (AC5b) is the invariant**. [[project_gate_scope_semantic_coverage]] is the exact lesson (the AI-5-1 trap: a green scan proves nothing without meaningful semantic coverage of the new surface), and [[feedback_mechanization_split_commitment]] is the companion (decay concentrates in the un-mechanized half). Build leg (b) first, prove it fails on a planted violation, then build leg (a).

**Trap 2 — half the capability bar.** Two artifacts, two meanings, one word. See Decision 2. If you only produce `governance_boundary.yaml`, you have silently dropped the ADR obligation at `epics.md:4423` and left launch-gate Row 12's predicate live.

### Why versioned rows and not a 6th state primitive (Decision 3, restated because it's the biggest structural call)

Five tables in this repo carry a projector-only `current_state` with a DB trigger and a dedicated CI gate. Flags look superficially similar — they even have a five-value state set. But those five exist because an *external* event stream moves the state and a writer might diverge from the projector; the gate polices that divergence. Nothing external moves a flag: an admin authors a version, and the version *is* the state. Story 10.1 already made this exact split correctly — helpdesk *tickets* are event-derived-state, the helpdesk *routing-policy registry* is versioned-immutable-rows. **A flag is the registry, not the ticket.** Adding a sixth state gate here would buy no property and dilute what the five existing gates signal.

### The pure-resolver contract (copy it, don't paraphrase it)

`packages/domain/src/helpdesk/routing.ts:1-16` states the determinism contract this codebase means by "pure resolver," and `packages/niyamavali-engine/src/ladder.ts:11-26` states the companion rule (interpret DATA; never `switch (id)`). Both apply verbatim. The subtle one is the **time split**: `resolveRoute` is time-independent and `computeTicketSlaDueDates` is the separate time-dependent derivation. Your `effective_from`/`effective_until` must live in the version-in-force *lookup*, not inside `evaluateFlag` — otherwise the evaluator takes a clock and replay determinism is gone.

### Audit must survive the cache (AC5c)

`packages/validity-service/src/cache.ts:22-26` (Story 4.8 D5-A) is the structural answer to `epics.md:3522`: only the *compute core* is cached; the access/audit layer sits outside it, so the audit fires on a HIT exactly as on a miss. Build the flag cache the same way. If you find yourself putting the audit inside the memoized function, you have built the thing this AC exists to prevent.

### Audit-action naming

`writeAuditEntry`'s `action` is a free-form dotted lowercase string with **no central registry** (`write.ts:99-102` is the only validation). Use `feature_flag.version_created` / `feature_flag.rolled_back`. Keep it distinct from RBAC permission keys (imperative verbs) and from `packages/events` names (past-tense event types). `rationale` is required by FR-58C (`prd.md:890`) — a bounded non-empty string in the audit context, never free-text PII.

### Migration hygiene

Hand-author `0087`; never `db:generate` (snapshots stop at 0020; drizzle skips by journal `when` → `42P07`, [[project_live_db_test_gotchas]]). Mirror `0084`'s tenant-table body. The nullable-`pariwar_id` global-flag row is the one place you deviate from the standard RLS predicate — comment it, test it, and add the policy to the inventory the `schema-diff` gate reads.

### Contracts hygiene

`packages/contracts/src/feature-flags/README.md` already commits your route shapes and forbids type-shadowing in `apps/api/src/modules/feature-flags/`. Pure-Zod `.strict()`, snake_case wire, no `@twt/domain` source import ([[project_contracts_domain_bundle_boundary]] — a source import leaks `pg`/`node:async_hooks` into the RN Metro bundle; the sync-guard is a TEST-only cross-import). Regen `openapi/v1.yaml` via `emit-openapi.ts`.

### Testing standards summary

- Live-DB: `twt-test-pg` on :5433; the audit writer self-commits and accumulates → **assert membership / `traceId` grouping, not counts** ([[project_live_db_test_gotchas]]).
- The table's **own** RLS pos/neg/withCheck/fail-closed regression spec is mandatory — every one of the ~18 sibling tenant-isolated tables has one, and its absence was the one real gap 10.7's review found.
- Run suspect specs in isolation before blaming them ([[project_ci_local_double_run_pollution]], [[project_known_livedb_test_failures]]).
- Merge gate = `pnpm ci:local` (`--concurrency=4`; integration needs `DATABASE_URL` on :5433) — ADR-0017; a cloud CI run is not a substitute ([[project_ci_actions_suspension_local_mirror]]).
- Domain dynamic `.limit()` → `clampLimit` ([[project_domain_limit_clamp_and_savepoint_retry]]); a named const still trips the gate.
- ESLint per-package ([[project_eslint_config_per_package_cwd]]).

### Project Structure Notes

- **NEW:** `packages/domain/migrations/0087_feature-flags.sql`; `packages/domain/src/schema/feature_flag_versions.ts`; `packages/domain/src/policies/feature-flag-versions-rls.ts`; `packages/domain/src/feature-flags/{types,registry,store,evaluate,capability-bar,errors,index}.ts`; `packages/domain/tests/feature-flags/*`; `packages/domain/tests/integration/rls/feature-flag-versions-policy-regression.spec.ts`; `packages/contracts/src/feature-flags/*.ts` (+ tests); `apps/api/src/modules/feature-flags/*`; `apps/api/tests/integration/feature-flags/*`; `apps/admin/src/modules/feature-flags/*`; `governance_boundary.yaml`; `scripts/governance-boundary/{lib,lib.test,check,README}.ts`; `docs/adr/ADR-0036-feature-flag-tool-selection.md`.
- **EXTEND:** `packages/domain/src/index.ts` (`export * as featureFlags`), `src/schema/index.ts`, `src/policies/index.ts`, `src/ids/index.ts` (a `FeatureFlagVersionId` + smart constructor, the `ReportExportId` shape); `src/rbac/{permissions,roles}.ts` (v26→v27); `migrations/meta/_journal.json` (idx 87); `packages/contracts/src/index.ts` + `scripts/emit-openapi.ts` + `openapi/v1.yaml`; root `package.json` + `scripts/ci-local.sh` + `.github/workflows/ci.yml`; `apps/api/src/{server,deps}.ts` + `route-registry.ts`; `apps/admin/src/{router.tsx,api/client.ts}`; `apps/api/src/modules/kyc/{manual-fallback-seam,provider-registry}.ts`; `_bmad-output/implementation-artifacts/{gate-inventory,deferred-work,sprint-status}.md/.yaml`; `docs/knowledge-transfer/adr-index.md`; `docs/launch-gate-inventory/inventory-roster.md`.
- **NOT touched:** no `packages/events` registration, no projector, no `current_state` column, no DB state trigger, no 6th state gate (Decision 3). No `packages/platform-adapters` SDK (deferred seam). No pg-boss worker. If you touch these, re-read the Scope Boundary.
- **Naming note:** the epics text says "AR-64" / "AR-46" / "Story 1.16a-d" / "Story 4.8"; `architecture.md` uses **no `AR-nn` identifiers at all**. AR-64 = Cross-Cutting Concern #15 (`architecture.md:325-326`) + Deferred Decisions `:200-229`. AR-46 = §3.13 (`:2450-2457`). Story 4.8's cache = §1.10 (`:1047-1086`). Cite both spellings so the trail survives.
- **Typo in the source AC, preserved for traceability:** `epics.md:3520` reads *"an `governance_boundary.yaml`"*. The filename is what matters.

### References

- [Source: epics.md:3501-3522 (Story 10.8)] — the flag column set, the capability bar, the governance-boundary invariant, the `governance_boundary.yaml` CI-test AC, the trustee-attested-PR clause, the audit-at-evaluation clause.
- [Source: epics.md:115 (FR-58C) + epics.md:352 (AR-64) + epics.md:510-543 (Architectural Freeze Boundaries) + epics.md:3369 (Epic-10 demoable closure) + epics.md:4423 (Story 14.7(c))] — the capability-bar-frozen/vendor-open disposition, the 15 frozen rows prohibition (e) protects, and the dual-content ADR obligation.
- [Source: prds/prd-TWT-2026-05-22/prd.md:884-892 (FR-58C)] — gating axes (`pariwar_id`, scope, role, cohort tag); DigiLocker as canonical use case; **audit with actor + rationale**; deterministic + `< 5 ms`; inventory visible to Pariwar Admin and above, no secret flags.
- [Source: sprint-change-proposal-2026-05-27.md:747-809 (Item 9)] — the seven-property vendor capability bar + the decision gate + the Gap Analysis escalation path. Mirrored verbatim into architecture.md:200-229 and :4844-4856.
- [Source: architecture.md:325-326 (Cross-Cutting #15)] — canary → graduated cohorts + automatic rollback on error-rate spike; inventory visible; no secret flags.
- [Source: architecture.md:4094-4098 (Test + flag governance)] — per-flag named owner + expected-retirement signal + dead-by date; quarterly inventory audit.
- [Source: architecture.md:4328, 4402, 4570, 4553] — the committed module paths (`apps/api/src/modules/feature-flags/`, `packages/contracts/src/feature-flags/`, the `platform-adapters` SDK mapping) and the **absent** admin module.
- [Source: architecture.md:1047-1086 (§1.10) + packages/validity-service/src/cache.ts:8-26] — the cache posture: optional/correctness-mandatory, best-effort non-blocking writes, and D5-A (audit OUTSIDE the cached core).
- [Source: packages/domain/src/helpdesk/{registry,routing}.ts + schema/helpdesk_routing_policy_versions.ts + migrations/0084] — the versioned-immutable-rows registry + the pure first-match resolver + the time-split. The structural template.
- [Source: fr-100-non-add.yaml + scripts/schema-diff/{lib,check}.ts] — the YAML-allowlist gate template, the `{ kind, artifact, rationale, adr }` entry shape, and the trustee-attested-ADR admission governance.
- [Source: bank-allowlist.yaml:21-31 + packages/bank-parsers/src/allowlist.ts:78-80] — the documented admission workflow + the `count` cross-check as revert-sanity teeth.
- [Source: scripts/kyc-provider-boundary/] — the existing import-allowlist AST gate; the model for AC5's source-scan leg.
- [Source: packages/domain/src/audit/write.ts] — `writeAuditEntry(servicePool, input)`; the dotted-action regex; sha256 `requestPayloadHash`; self-committing advisory-locked global chain.
- [Source: packages/domain/src/rbac/permissions.ts:293 + roles.ts] — `PERMISSION_CATALOG_VERSION = 26` (verified at HEAD 52e6f7d) + the bump ritual.
- [Source: apps/api/src/modules/kyc/{provider-registry,manual-fallback-seam}.ts + apps/api/src/config.ts:98-99 + apps/api/src/modules/channel-config/composition.ts:149-156 + deferred-work.md:2066] — the three pre-committed consumer seams naming this story.
- [Source: packages/contracts/src/feature-flags/README.md] — the reserved contracts dir; the committed route shapes; the no-type-shadowing rule.
- [Source: docs/knowledge-transfer/adr-index.md:110 + docs/launch-gate-inventory/inventory-roster.md:177-183] — the ADR slot and the launch-gate row this story dispositions.
- [Source: implementation-artifacts/10-7-*.md + 10-6-*.md] — the sibling Epic-10 `[SURFACE]`/`[PRIMITIVE]` shapes, the Scope Boundary convention, and the review lessons (missing own-table RLS regression; a named-const `.limit()` tripping the domain gate).

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (`claude-opus-5`) — bmad-dev-story workflow, 2026-07-31.

### Debug Log References

Five real defects surfaced during development. All were found by tests, not by inspection:

1. **`drizzle 0.45` exposes `nullsNotDistinct()` only on `unique()`, not `uniqueIndex()`.** The first `db:migrate` failed with `uniqueIndex(...).on(...).nullsNotDistinct is not a function`. Switched the declaration to a UNIQUE **constraint** so the schema can actually express the null semantics — a declaration that cannot state them would silently misdescribe the DB. Verified live that without `NULLS NOT DISTINCT` Postgres accepts a duplicate `(NULL,'x',2)` (2 rows), so RLS spec case (g) has real teeth.
2. **`db.execute(sql\`select now()\`)` returns a STRING, not a Date.** `<{ now: Date }>` typechecks and then crashes inside drizzle's timestamp encoder (`value.toISOString is not a function`) — but ONLY on the path where the caller omits `effectiveFrom`, which is why a suite that always passes it would never catch it. Fixed with explicit coercion. ⚠ **`helpdesk/registry.ts:258` has the identical latent bug**; recorded in `deferred-work.md` rather than changed out of scope.
3. **`/api/v1/global/feature-flags` 404'd.** `scopeResolutionHook` resolves the tenant from the `:pariwarId` PATH PARAM; a route without one reads `''` and 404s. Re-gated the cross-tenant catalog on `requireGlobalPermission` (super_admin). See Completion Note 4 — this is a boundary decision, not a workaround.
4. **The `config.digilocker.manualFallbackEnabled` fallback was unreachable.** A registered flag ALWAYS resolves (tier 3 = the code default), so "no version in force" never surfaces as a null and the code default would silently override a deployment whose config had manual disabled — dead configuration that still looks live. The seam now keys the fallback off the resolution **source** (`'default'` ⇒ config governs), preserving every existing deployment's behaviour until a deliberate flip.
5. **`getActiveKycProvider` became async**, breaking three call sites and three unit tests (all updated).

### Completion Notes List

**1 — The gate is the story, and it has verified teeth.** Per Trap 1, leg (b) (the source scan) was built FIRST and proven to fail before leg (a) existed. Both legs were then proven to fail **live, against real files**: planting `domain.featureFlags` in `packages/domain/src/rbac/permissions.ts` failed the gate with **exit 1** and printed prohibition (d) verbatim; adding an orphaned bar entry failed it with **exit 1** on the `count` cross-check. Leg (b) detects via **three independent AST routes** (module specifier; named symbol incl. aliases; `featureFlags.*` property access) because a specifier blacklist alone is defeated by `import * as domain from '@twt/domain'` — that third route is what gives the gate semantic coverage rather than being the AI-5-1 vacuous-gate trap. Scanned roots are read from the bar's own `prohibited` list so scan scope and documented prohibitions cannot drift.

**2 — Both halves of the overloaded "capability bar" shipped** (Trap 2): `governance_boundary.yaml` (the runtime allowlist, with the trustee-attested admission workflow in its header + a `count` cross-check) **and** `ADR-0036` (the vendor-selection bar, demonstrating each of Item 9's seven properties against shipped code in a table). Decision 9 honoured: `beta_ux_patterns` is **deliberately absent** — it appears exactly once in the corpus with no FR/AR/UX backing, and an allowlist entry for an undefined behaviour cannot be gate-checked.

**3 — Decision 4's narrowing, stated precisely** ([[feedback_closure_language_precision]]): Story 4.8's **invariants are ADOPTED** (cache-optional/correctness-mandatory; best-effort non-blocking writes; and critically D5-A — the audit/access layer sits OUTSIDE the cached core). Its **cache substrate is DEFERRED pending measurement** (no `member_validity_cache` twin, no cohort-epoch table, no events_log trigger). Neither is "not addressed". AC5c has real teeth: `cache-audit-outside.test.ts` asserts **10 resolutions produce 10 access observations from 1 DB read** — moving `onAccess` inside the memoization makes that 1 and fails. That failure mode is worth the test: it would look correct in dev and decay silently in production as traffic warms the cache.

**4 — One deviation from the AC's literal wording, and one narrowing, both deliberate.** (a) AC3 asks for flag_key/versions/states/rationale in "the audit context", but `AuditEntryInput` has **no context field** — the §1.5 chain content is a fixed tuple and widening it would change the hash content of every audit line in the system. Same information, anchored not inlined: `resourceLocator` carries the key, `requestPayloadHash` digests the whole input **including the rationale** (making it tamper-evident), and the rationale text lives on the row the line's `auditId` anchors. The flip runs under `withCompensatingAudit` (ADR-0030) so the intent line commits FIRST — a flipped flag with no audit line is unreachable. (b) `/api/v1/global/feature-flags` is **super_admin**-gated. `prd.md:892`'s "visible to Pariwar Admin and above" is satisfied **in full** by the per-Pariwar route, which shows every flag's effective resolution for that tenant including the governing global row; the cross-tenant catalog is the set of defaults applying to every Pariwar at once, which is a trustee concern. No Pariwar Admin loses visibility of anything affecting their tenant.

**5 — AR-64 is a KNOWING partial delivery, recorded not closed** ([[feedback_record_unattested_no_backfill]]). The five-state machine + an audited **manual** `rolled_back` flip ship for real. The **automatic** error-spike detector does not: there is no error-rate metrics substrate in this repo (verified — `cache-observability.ts` and `peer-mesh-metric-registry.ts` are per-subsystem counters, neither emits an error rate; AR-31's vendor is itself a deferred ADR). `resolveFlagHealthSignal()` returns `{available:false, reason}` as a first-class absence. Tied to the **AR-31 observability story** as its re-trigger rather than left as an open intention, and stated plainly in ADR-0036 rather than quietly absorbed into the launch-gate closure.

**6 — Launch-gate Row 12 dispositioned with precise language.** Its predicate had two conjuncts — a rollout beginning AND *tool not selected*; the second is now false, so the predicate **can no longer materialize**. Status moved to `predicate-foreclosed-pending-trustee-disposition`, **not** to closed: ADR-0036 is `drafted`, not `ratified`, and the `adr-index.md` row reads `drafted` to match. Both files' live state was verified before the claim was written ([[feedback_verify_before_committing_governance_claims]]).

**7 — `pnpm ci:local`: 29 of 30 jobs GREEN, including the new `governance-boundary` gate.** The one red job (`integration-tests`) is **pre-existing and was MEASURED, not assumed**: (a) each of the 4 failing specs passes alone on a pristine DB; (b) the whole domain suite passes single-pass on a pristine DB — **201/201 files, 2081 tests**; (c) it passes a second SEQUENTIAL pass on the same dirty DB, ruling out accumulation; (d) **a `git worktree` at baseline `52e6f7d` with `pnpm install` on a pristine DB reproduces the identical four failures with zero Story 10.8 code**. Root cause: `ci:local` runs turbo at `--concurrency=4`, so domain integration specs run in parallel with `apps/api`'s, which own-commit rows under the fixed `PARIWAR_A` UUID into `events_log`/`alerts` — and those four specs assert GLOBAL counts for that shared tenant, violating the repo's own "assert membership, not counts" rule. Recorded in `deferred-work.md` with the fix direction (membership assertions or per-spec random pariwarId — **not** lowering concurrency, which is not the defect).

**8 — Story-10.8-specific test totals:** domain unit 55/55; gate `lib.test.ts` 19/19 (incl. revert-sanity for both legs and each leg-(b) route independently); contracts 12/12; `feature_flag_versions` RLS regression 10/10 (incl. both nullable-`pariwar_id` carve-out legs + the NULLS-NOT-DISTINCT teeth + the append-only trigger); domain integration 16/16; API integration 11/11 (incl. the AC8 DigiLocker end-to-end asserted through the real seam, not just the inventory read).

**9 — Scope Boundary held.** No `packages/events` registration, no projector, no `current_state` column, no DB state trigger, no 6th state gate (Decision 3). No `platform-adapters` SDK. No pg-boss worker. Exactly ONE consumer wired (FR-2 DigiLocker); the other two left at their fail-safe defaults with `deferred-work.md` entries and an updated in-code comment at `composition.ts` noting the flag subsystem now exists but the seam is deliberately unwired.

**10 — Review Pass 2 (2026-08-01): two further DELIBERATE NARROWINGS, recorded here because the list above recorded every other one and omitting these would read as oversight** ([[feedback_closure_language_precision]] — both are *deliberate narrowings*, neither is "not addressed"):

- **Cohort clauses are OR-ONLY; Decision 5's "ANDed within a clause" is not expressible.** A clause carries one `dimension`, its `values` are ORed, and clauses are first-match (also OR). So "district = patna AND role = member" cannot be authored; the workaround is a `cohort_tag` precomputed outside the flag document, which weakens the replay property AC2 guarantees. Not built here because conjunction changes the `cohort_definition` JSONB shape and ripples through contracts, the OpenAPI regen, the admin form, and the capability-bar hash. Full entry + re-trigger in `deferred-work.md`.
- **Scheduled (future-dated) flips are DROPPED, not merely unimplemented.** `createFlagVersion` now rejects `effective_from > now()`. A future-dated version deadlocked the rollback path outright: the order guard compares against the highest-version row, so a version scheduled ahead made every later flip throw until that date arrived — including the audited `rolled_back` flip that Decision 6 names as the *entire* shipped rollback mechanism — and the pending row could be neither amended (0087's append-only trigger) nor deleted (no DELETE grant). No AC commits to scheduling. Full entry + re-trigger in `deferred-work.md`.

**11 — Review Pass 2: the resolution rule changed, and the obvious fix would have been WRONG.** The in-force lookup now selects *the newest version whose `effective_from <= at`, then checks that row's own window*, instead of *the newest row still inside its window*. Both a resurrection bug and a replay guarantee turn on this:
- Under the old rule a superseded version RESURRECTED when the version that superseded it left its window (an open-ended `rollout` reappearing the moment a bounded `rolled_back` window closed — silently reverting to the pre-rollback state).
- The obvious fix, `superseded_by_version IS NULL`, would have killed resurrection **and point-in-time replay together**: every historical row is superseded by definition, so `flagVersionInForce(at = a past instant)` would return the code default instead of the version that actually decided — destroying AC1's "historical flag states are queryable for past evaluations", the entire reason these rows are immutable. Caught because the existing immutability spec asserts exactly that replay.
- An expired head now means *nothing persisted governs* (fall through to the next tier / code default), never *try the previous version*. Both properties are asserted together in `registry.spec.ts`.

**12 — Review Pass 2 verification (measured, not asserted).** `governance-boundary` gate: 30 unit tests (was 24) + a LIVE run over **12 prohibited roots / 153 source files** (was 6 roots / 87). Revert-sanity re-run against the real tree for both NEW AST routes — a planted `const { evaluateFlag } = await import('@twt/domain')` in `packages/domain/src/rbac/` and a planted `export * as featureFlags from '@twt/domain'` in `packages/domain/src/consent/` each failed the gate with exit 1 and the correct route attribution, and the gate returned green on removal. Domain feature-flag unit tests 88 (was 61). Live-DB: `feature_flag_versions` RLS regression 10/10, domain integration 17/17, plus a NEW own-committing two-connection race spec (1/1) that is the first test anywhere to actually execute the 23505 → `FlagVersionConflictError` mapping. Migration 0088 applied to `twt-test-pg`; all six CHECK constraints verified present via `pg_constraint`. Per-package `lint` + `tsc --noEmit` clean on domain / contracts / api / admin. `openapi/v1.yaml` regenerated + determinism-checked (the `has_more` field).

**13 — ⚠ CORRECTION (2026-08-01, issued during the Group 2 review). An earlier version of this note claimed the DB-enabled `ci:local` run reproduced ONLY the four documented pre-existing count-assertion specs. THAT CLAIM WAS FALSE and is retracted.** The Group-1 patches ALSO broke **5 of 12 tests** in `apps/api/tests/integration/feature-flags/feature-flags.spec.ts`. The faulty verification: the red `integration-tests` job was diagnosed by running the DOMAIN package only, and the "29/29 green" figure came from a run with `DATABASE_URL` UNSET — which skips integration tests altogether. The API suite was never executed before the claim was written. Recorded rather than quietly amended ([[feedback_record_unattested_no_backfill]], [[feedback_verify_before_committing_governance_claims]] — this is the second time in this story that a green-gate claim outran its evidence; the first was the 10.7 precedent the AC9 text itself warns about).

**Root cause of the 5 failures — two Group-1 changes plus one pre-existing gap that converts them into 500s:**
- `flipBody()` (`feature-flags.spec.ts:49-57`) hard-codes `state: 'full'`, and `off → full` is now illegal under the AC7 ladder. → `FlagStateTransitionError`.
- The version-history test flips `canary` with `cohort_definition: { clauses: [] }`, now rejected by the staged-cohort requirement. → `FlagVersionInvalidError` → 400, so zero rows are ever created and the immutability assertions never run.
- `FlagStateTransitionError` has **no arm** in `mapCreateFlagVersionError` (`handlers.ts:131-144`) and none in the app error-mapping boundary, so an ordinary, correctly-authorized governance refusal surfaces as **500 `internal.error`** instead of a 4xx. That is why the failures read `expected 500 to be 200` rather than a clean status mismatch. Same gap applies to `FlagKeyNotAllowlistedError`, `CapabilityBarInvalidError`, and `CapabilityBarUnavailableError` — all tracked as Group 2 findings.

**Accurate state after the fix (see Note 14):** the DB-enabled run's remaining failures are the four pre-existing domain count-assertion specs only — a claim that is now backed by having actually run both packages.

**14 — Review Pass 3 verification (both packages run explicitly, after Note 13's correction).** `pnpm ci:local` with `DATABASE_URL` set: **28 of 30 jobs green**, and the two red jobs now reproduce ONLY the four documented pre-existing domain count-assertion specs (`cross-pariwar-leak` ×2, `rls/policy-regression` events_log, `pool/active-contribution-read`) — verified by listing every failing spec BY NAME, not by inference. `apps/api` runs **100 files / 769 tests, 0 failures**. Domain feature-flag + RBAC suites: 11 files / 244 tests. Contracts: 45 files / 728 tests. Per-package `lint` + `tsc --noEmit` clean on domain / contracts / api / admin. `openapi/v1.yaml` regenerated + determinism-checked. Migrations 0088 and 0089 applied to `twt-test-pg`; the `actor_display` column and the EXTENDED immutability trigger both verified present via `pg_constraint` / `pg_proc`.

**A fifth flake was found and fixed during this run, in this story's own code.** `packages/domain/tests/integration/feature-flags/registry.spec.ts`'s AC4 test asserted `every(e => e.source === 'default')` — coupled to the state of the whole shared database, so it broke intermittently when `apps/api`'s E2E suite own-committed its global `wa_cost_optimization` row in a parallel turbo worker. Same defect class as the four pre-existing specs, and the exact twin of the `apps/api` assertion the Pass-3 Blind Hunter flagged. Both now assert the real property (this tenant has no other OVERRIDE) instead of a global-database property.

**One test was written, MEASURED AS FLAKY, and replaced rather than shipped.** The first attempt at the missing 409-seam coverage fired two concurrent HTTP flips; it failed 1 run in 3, because two injects serialize often enough that no collision occurs. A version collision cannot be forced deterministically over HTTP at all — `createFlagVersion` claims `max(version) + 1`, so seeding a row just raises the max. Replaced with `apps/api/tests/unit/feature-flag-error-mapping.test.ts`: a deterministic unit test pinning ALL SEVEN typed-error → HTTP-status arms plus the unmasked pass-through, with an exhaustiveness case that fails if a future domain error ships without a mapping. The end-to-end 23505 → typed-error half stays proven live by the domain's two-connection race spec. Together they close the seam without a flaky test in the middle.

**15 — Review Pass 3 scope.** Groups 3 and 4 remain UNREVIEWED: the admin console + KYC consumer wiring (`apps/admin/**`, `kyc/*`, `channel-config/composition.ts`), and the generated/governance docs (`openapi/v1.yaml` as a document, `ADR-0036`, `adr-index.md`, `inventory-roster.md`, `gate-inventory.md`). AC6's vendor-ADR half and AC8's consumer half have therefore never been adversarially reviewed.

**16 — Review Pass 4 verification (every affected package run explicitly).** `lint` + `tsc --noEmit` clean on **domain / contracts / api / admin / jobs**. Full suites: `apps/api` **772 passed / 1 skipped**, `apps/admin` **189 passed** (25 files — including the console's FIRST spec, 13 cases), `apps/jobs` **211 passed**, `@twt/contracts` **730 passed**, `@twt/domain` **2113 passed** with only the four documented pre-existing count-assertion specs failing. `openapi/v1.yaml` regenerated + determinism-checked. Migration 0089 applied; the `actor_display` column and the EXTENDED immutability trigger verified present via `information_schema` / `pg_proc`.

`ci:local` with `DATABASE_URL`: all 28 gates green (including `governance-boundary`). Beyond the four known domain specs, **five `@twt/validity-service` specs failed under `--concurrency=4` and pass 80/80 in isolation** — the p95 benchmark harnesses and cache-timing specs, which are load-sensitive by construction. Verified by running the package standalone rather than assumed; the same oversubscription family already recorded for `@twt/niyamavali-engine`. No feature-flag, KYC, admin, or contracts spec fails in either mode.

**Three defects were found BY the Pass-4 patches rather than by the review layers** — each one a case where a test only started meaning something once it was made non-vacuous:
- **The flag cache made the KYC seam tests order-dependent.** The module-level snapshot is keyed by `(flag_key, pariwar_id, ~1s bucket)`, so every case after the first read the previous case's cached decision and never touched its own mock. Invisible while the tests all asserted the same value; exposed the moment two cases differed only in what the store returned. Fixed with `clearFlagCache()` between cases AND between assertions within a case.
- **`kyc_transactions.provider` never matched the registered builder key.** The E2E seeded `'digilocker'` while the suite registers only `fixture`. Invisible while the callback ignored the recorded provider; surfaced immediately once it PINNED to it. The mismatch is now impossible to ship silently: an unregistered provider raises a typed `KycProviderUnavailableError` mapped to `409 kyc.provider_unavailable` ("start KYC again") rather than an anonymous 500 — because only the originating provider can verify its own OAuth state, so falling back would reproduce the stranding the pinning prevents.
- **Read-tolerance was put in the wrong layer and OpenAPI said so.** Making the response schema `.catch()`-tolerant so one malformed row could not blank the inventory broke `contracts:emit-openapi` outright — `ZodCatch` has no OpenAPI representation. That was the right problem and the wrong layer: a published contract should describe what the API emits, not admit it sometimes emits garbage. Moved to a `safeCohort` projection in `apps/api`, so the response is ALWAYS contract-valid and the wire shape stays honest.

**17 — Review Pass 5 verification + the close of the four-group review.** Gate green (30 unit tests + a live run over 12 roots); `microcopy` gate green; `lint` + `tsc --noEmit` clean on domain / api / admin / contracts. `adr-index.md`'s breakdown now RECONCILES to its stated total (113 + 5 + 0 + 29 + 1 = 148) — it did not before, because the Story-10.8 flip edited the row and left the ledger stale. Row 12 now mirrors the Row 15 precedent exactly (prior status preserved, supersession annotation appended). The retracted "structurally impossible" claim survives nowhere as an assertion; the one remaining instance is tenant isolation via RLS `FORCE`, which genuinely IS structural and is correctly left alone.

**⚠ THE PATTERN THIS REVIEW SHOULD BE REMEMBERED FOR.** Three times across five passes, a correct fix was applied to the layer under the cursor and NOT swept across its siblings:
1. **Pass 2 → Pass 4:** the AC7 ladder + staged-cohort rule landed in the domain and broke `apps/api`'s E2E (5 of 12), reported green because `ci:local` ran without `DATABASE_URL`.
2. **Pass 2 → Pass 4:** the same rules broke the ADMIN CONSOLE so completely it could not perform a legal first flip of any flag — undetected because the console had zero tests.
3. **Pass 2 → Pass 5:** the "structurally impossible" retraction reached the README and one spot in the YAML, leaving the claim in the gate's OWN SOURCE, the gate inventory, the CI job comment, and the ADR.
Each was found by the NEXT group's review rather than by the pass that caused it. The generalisable lesson is not "run more tests" but **"a claim or rule that appears in N places must be changed in N places, and the sweep is part of the patch, not a follow-up"** — for prose exactly as much as for code.

**18 — Review scope, stated plainly for whoever reads this next.** All four groups are now reviewed: Group 1 (domain + migration + RBAC + gate), Group 2 (API + contracts), Group 3 (admin console + KYC wiring), Group 4 (ADR + governance docs). Across five passes: **193 raw findings → 118 after dedup → 18 decisions resolved with the user + 74 patches applied + 4 deferred + 22 dismissed.** Two migrations were added by review (0088 CHECK constraints, 0089 `actor_display` + extended trigger). What remains OPEN and is NOT closed by this review: ADR-0036 is `drafted`, not ratified; the capability bar's four seed entries are author-committed with trustee ratification pending; NFR-FR58C's `< 5 ms` is un-measured; AR-64's automatic rollback is knowingly unbuilt; and the four `deferred-work.md` items (cohort conjunction, scheduling, version-history cursor, `actorRole`) carry named re-triggers.

### File List

**Review Pass 2 (2026-08-01) — NEW:** `packages/domain/migrations/0088_feature-flag-version-checks.sql` (six CHECK constraints; a SEPARATE migration because 0087 is already applied and drizzle skips by journal `when`, so an in-place edit would never run); `packages/domain/tests/integration/feature-flags/flag-flip-concurrency.spec.ts` (own-committing two-connection race).

**Review Pass 2 — MODIFIED:** `packages/domain/src/feature-flags/{registry,evaluate,cache,capability-bar,store,errors}.ts`; `packages/domain/src/rbac/roles.ts` (state_trustee rationale); `packages/domain/migrations/meta/_journal.json` (idx 88); `scripts/governance-boundary/{lib,check,lib.test,README}.ts`; `governance_boundary.yaml` (6 new prohibited roots + the corrected `kyc_manual_fallback` rationale + honest leg-(b) guarantee wording); `packages/contracts/src/feature-flags/feature-flags.ts` (`has_more`); `openapi/v1.yaml`; `apps/api/src/modules/feature-flags/handlers.ts`; `packages/domain/tests/feature-flags/{registry,evaluate,cache-audit-outside,capability-bar}.test.ts`; `packages/domain/tests/integration/feature-flags/registry.spec.ts`; `.decision-log.md` (Decision 2026-08-01-069 — the capability bar's first attestation record); `_bmad-output/implementation-artifacts/deferred-work.md`.

**NEW — domain primitive**
- `packages/domain/migrations/0087_feature-flags.sql`
- `packages/domain/src/schema/feature_flag_versions.ts`
- `packages/domain/src/policies/feature-flag-versions-rls.ts`
- `packages/domain/src/feature-flags/{types,registry,store,evaluate,capability-bar,cache,health-signal,errors,index}.ts`

**NEW — the gate + its capability bar**
- `governance_boundary.yaml`
- `scripts/governance-boundary/{lib,lib.test,check,README}.ts` (+ `README.md`)

**NEW — contracts / API / admin**
- `packages/contracts/src/feature-flags/{feature-flags,index}.ts`
- `apps/api/src/modules/feature-flags/{handlers,routes,index}.ts`
- `apps/admin/src/modules/feature-flags/FeatureFlagsPage.tsx`
- `apps/admin/src/routes/FeatureFlagsRoute.tsx`

**NEW — tests**
- `packages/domain/tests/feature-flags/{evaluate,registry,capability-bar,cache-audit-outside}.test.ts`
- `packages/domain/tests/integration/feature-flags/registry.spec.ts`
- `packages/domain/tests/integration/rls/feature-flag-versions-policy-regression.spec.ts`
- `packages/contracts/tests/feature-flags.test.ts`
- `apps/api/tests/integration/feature-flags/feature-flags.spec.ts`

**NEW — governance docs**
- `docs/adr/ADR-0036-feature-flag-tool-selection.md`

**MODIFIED — domain**
- `packages/domain/src/index.ts` (`export * as featureFlags`), `src/schema/index.ts`, `src/policies/index.ts`, `src/ids/index.ts` (`FeatureFlagVersionId`)
- `packages/domain/src/rbac/{permissions,roles}.ts` (catalog v26→v27; `feature_flag.view` + `feature_flag.flip`)
- `packages/domain/migrations/meta/_journal.json` (idx 87), `packages/domain/package.json` (`yaml` dep)
- `packages/domain/tests/rbac/{permissions,roles}.test.ts`

**MODIFIED — contracts / API / admin**
- `packages/contracts/src/index.ts`, `packages/contracts/scripts/emit-openapi.ts`, `openapi/v1.yaml`
- `apps/api/src/server.ts`
- `apps/api/src/modules/kyc/{manual-fallback-seam,provider-registry,kyc.handlers}.ts` (the wired FR-2 consumer)
- `apps/api/src/modules/channel-config/composition.ts` (comment only — the seam stays unwired by design)
- `apps/api/tests/unit/{kyc-signup,kyc-provider}.test.ts`
- `apps/admin/src/{router.tsx,api/client.ts}`

**MODIFIED — CI + artifacts**
- `package.json` (`governance-boundary:check` / `:test`), `scripts/ci-local.sh`, `.github/workflows/ci.yml`
- `_bmad-output/implementation-artifacts/{gate-inventory.md,deferred-work.md,sprint-status.yaml}`
- `docs/knowledge-transfer/adr-index.md`, `docs/launch-gate-inventory/inventory-roster.md`

### Change Log

| Date | Change |
|---|---|
| 2026-07-31 | Story 10.8 implemented. Feature-flag `[PRIMITIVE]`: `feature_flag_versions` (immutable versioned rows, nullable-`pariwar_id` global tier with an asymmetric RLS carve-out, `NULLS NOT DISTINCT` guard, append-only trigger); a three-tier registry (override ≻ global ≻ code default); a PURE first-match cohort evaluator that never throws; the Story-4.8-posture cache with the audit OUTSIDE the cached core; RBAC catalog v26→v27 (`feature_flag.view` + the narrower `feature_flag.flip`); contracts + OpenAPI; the admin API + inventory console; and the FR-2 DigiLocker consumer wired end-to-end. |
| 2026-07-31 | **NEW repo-global CI gate `governance-boundary`** (Category A) + `governance_boundary.yaml`: registry≡allowlist conformance with a `count` cross-check, and the load-bearing AST source scan (three detection routes) proving no flag evaluation reaches inside a governance module. Both legs proven to FAIL on live planted violations. Registered in `package.json`, `ci-local.sh`, `ci.yml`, and `gate-inventory.md`. |
| 2026-07-31 | **ADR-0036** authored (in-house Postgres-backed selection; all seven Item-9 properties demonstrated against shipped code); `adr-index.md` slot flipped `slot-reserved-pre-write` → `drafted`; launch-gate Row 12 dispositioned `predicate-foreclosed-pending-trustee-disposition` (NOT closed — ADR is `drafted`, not `ratified`). |
| 2026-07-31 | AR-64 automatic error-spike rollback recorded as a KNOWING partial delivery (`FlagHealthSignal` → `{available:false}`, AR-31 re-trigger); the two unwired consumer seams + the deferred client SDK + the boolean-predicate provider-selection limit recorded in `deferred-work.md`. Two pre-existing defects discovered and recorded rather than silently fixed: the `helpdesk/registry.ts` `now()`-is-a-string latent crash, and the baseline-verified 4-spec `ci:local` concurrency failure. |
